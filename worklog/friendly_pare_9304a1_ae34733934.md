
## [2026-07-26 21:26] ? 2bb509e9
- DID: K2: fixed storyboard scene-picker root cause (named scenes all rank 0 -> picker silently kept prev scene; gave lesson1 unique rank 305 in D1; storyboard_editor_v2 no-silent-fallback; slideshow_server bad ?scene= -> honest 400; pushed 2f69079 + 85c296e). That exposed the REAL remaining bug: lesson1 board left side (spine/spots/lines) is blank because it is a NAMED PROJECT with scene_id='lesson1', 0 script_lines, reels have line_hash=None (hash in birth_line_hash). Storyboard only builds spine from script_lines + finds reels via scene_id LIKE 'scNN%'.
- STATE: AGREED WITH K1 (K1 owns data, K2 owns storyboard server 8790, do NOT add script_lines). Build named-project spine on SERVER side from manifest: C:/Users/maxre/Nextcloud/ai_images/kazarian_episode/sound/lesson1_production/lines_20260726/manifest.json (34 spots; each has order=spot# 1..34, line_hash, text, duration, tag). Bind reel to spot by reel.birth_line_hash==manifest.line_hash. Canonical reels spots1-10=jobs3403-3412 done, 11-34=3417-3440 held/no video yet. IGNORE dupes 3413-3416 (LOCAL/s2v re-renders of spot5). Spot order = jobs.label 'lesson1_reel_spotNN'.
- NEXT: Teach slideshow_server: when scene rank maps to a named project (scenes.name not scNN), synth /api/script_lines from manifest (idx=order,text,line_hash) and bind reels in /api/reel_membership by birth_line_hash. Client untouched. Restart 8790, verify lesson1 spine renders + PLAY works spots 1-10.

## [2026-07-26 21:37] ? 2bb509e9
- DID: K2: DONE + pushed (814960f) - storyboard 8790 now renders NAMED PROJECTS (lesson1): server helper _named_project(rank) reads KAZARIAN_ROOT/sound/<name>_production/lines_*/manifest.json; /api/script_lines + /api/reel_membership serve the 34-spot spine, reels bind by birth_line_hash==manifest line_hash. Storyboard left side + spots + tiles CONFIRMED back by Max. Earlier scene-picker root fix pushed 2f69079+85c296e. NO script_lines added (K1 rule).
- STATE: REMAINING BUG: the PLAY button opens /mixboard?autoplay=1&start_lh=... and mixboard.html says 'No data loaded. Click Refresh first.' The MIXBOARD player is a SEPARATE UI that loads its own data keyed by NUMERIC scene + script_lines and does NOT understand named projects (scene_id='lesson1', rank 305). So it finds nothing for lesson1. Same design gap as the storyboard had, different consumer.
- NEXT: Teach mixboard (mixboard.html + whatever slideshow_server endpoints it calls: likely /api/approved_images already fine, but its spine/line load + player membership) to use the SAME _named_project manifest spine when the scene is a named project. Trace mixboard.html data-load (grep 'No data loaded' + its loadAll) and its start_lh player path; bind reels by birth_line_hash. Reels spots1-10=jobs3403-3412 done (playable), 11-34 held/no video. Verify PLAY plays spot1 reel + continues.

## [2026-07-26 23:18] ? 2bb509e9
- DID: Fixed MOMA named-project system (scene lesson1, no script_lines): 3 defects in slideshow_server _named_project spine resolver. (1) idx 1-based vs numeric 0-based -> every reel skipped on PLAY. (2) reel_membership filtered output_status=done which lesson1 lacks -> empty. (3) scene_lines_manifest still D1-only -> assembler got nothing. Now all 4 spine endpoints route through the ONE resolver, same shape+0-based idx as numeric scenes.
- STATE: 2 commits pushed to master. Verified: scene=305 -> 34 lines 0-based, 39 reels mapped, 34/34 audio; assemble produced 123s/25.8MB MP4 (10 lipsie + 24 blank). Scene 10 unchanged.
- NEXT: 24 blanks = K1 held reels 3417-3440 (empty output_file) not yet rendered - K1 data side. Ask Max to click PLAY + review the MP4.
