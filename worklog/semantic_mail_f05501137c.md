
## [2026-06-17 20:48] ? ????????
- DID: Max APPROVED full-archive vision (phase 2). Current body-only text index keeps running now. PHASE 2 = redownload via Google Takeout (complete archive incl attachments) + build extraction pipeline: save raw archive, save attachments per-email, EXTRACT TEXT from PDFs/DOCs and fold into semantic index, SAVE IMAGES unindexed. Re-indexing already-done emails is fine per Max. Heavy archive -> teal16 (Centauri D:) if Pine C: tight.
- STATE: Phase1 body-index RUNNING (chunked, ~20+ months done). Phase2 NOT started - it's next, after/alongside current index. Need to design pipeline + kick off Takeout when ready.
- NEXT: Keep monitoring phase1 index. When ready to start phase2: kick off Takeout export (max.rempel2@gmail.com), plan extraction pipeline, decide teal16 path. Confirm message-id matching strategy (Gmail API id vs mbox Message-ID) before merging attachment text into existing vectors.
