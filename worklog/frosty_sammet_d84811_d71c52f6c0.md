
## [2026-06-13 14:22] B80 d89120c0
- DID: I am B80 (team b), lane = implement po_token + cookies/login to beat YouTube bot-wall. B8 runs the Sol fill57 worker (~31/57, wide 6-16min gaps, wall cleared overnight). DONE: bgutil po_token PROVIDER is LIVE on Sol as Docker container 'bgutil-pot' on :4416 (metadata-only minting, no downloads). Worker venv ~/song_timing/venv = yt-dlp env, UNTOUCHED. Sol ALSO already runs local Invidious (B9 built it) at http://192.168.1.113:3000 (LAN, companion-enabled, but it's on Sol=home IP so does NOT hide IP, just funnels).
- STATE: Provider up but NOT yet wired into yt-dlp. B8 coordination rule: metadata-token minting is FINE, but any real test DOWNLOAD against YouTube on the shared home WAN IP - PING B8 FIRST so they pause the Sol worker. Cookies MUST use a THROWAWAY Google acct, never Max's main Tamza-channel acct (adviser+me agree: account-ban risk). Waiting on Max to answer WHICH account + log into Firefox.
- NEXT: 1) Install pot plugin into a SEPARATE test venv on Sol (NOT B8's venv) + verify 'yt-dlp -v' shows the PO Token provider loads & reaches :4416 - metadata-only, no YT download. 2) For cookies: once Max logs into throwaway YT acct in Firefox on Pine, export Netscape cookies.txt (browser ext 'Get cookies.txt LOCALLY' or yt-dlp --cookies-from-browser firefox on Pine), scp to Sol. 3) Real end-to-end test download: ping B8 to pause Sol first. yt-dlp call lives in fill57_groq.py line ~92 (venv/bin/yt-dlp), args incl --extractor-args youtube:player_client=tv_simply,default. NEVER touch Max's main login.
- LESSON: Over-monitoring kills: I killed+restarted a HEALTHY worker twice mis-reading a normal 16-min gap as a stall. With wide gaps, long quiet IS normal - peek .part/file growth before ever restarting.

## [2026-06-13 14:32] B80 d89120c0
- DID: BOTH halves now STAGED on Sol. (1) po_token provider = Docker 'bgutil-pot' on :4416, live. (2) Cookies from Max's THROWAWAY acct maxrempel35@gmail.com exported from Pine Firefox (375 cookies, 25 youtube) -> staged at /home/maxre/song_timing/yt_cookies_maxrempel35_20260613.txt on Sol + backed up to zSyncMain/ssh/yt_cookies_maxrempel35_20260613.txt.
- STATE: Provider + cookies staged but NOT yet wired into yt-dlp or tested. Testing = a real YouTube download on the shared home WAN IP = MUST ping B8 to pause the Sol fill57 worker FIRST (B8 rule). B8 still running ~31/57 on wide gaps.
- NEXT: 1) Ping B8 on joint board to coordinate a pause window for a single test download. 2) Test: ~/song_timing/venv/bin/yt-dlp with --cookies ~/song_timing/yt_cookies_maxrempel35_20260613.txt (and/or pot plugin via :4416) on ONE video, confirm no bot-wall. 3) pot plugin still needs pip install into a venv: ~/song_timing/venv/bin/pip install bgutil-ytdlp-pot-provider (coordinate w/ B8 since shared venv). NEVER use Max's main acct - maxrempel35 is the throwaway he approved.

## [2026-06-13 14:42] B80 d89120c0
- DID: B80: solved YouTube bot-wall bypass via po_token. Installed+verified bgutil po_token plugin into Sol worker venv (~/song_timing/venv); tv_simply client now auto-attaches a gvs po_token from the Docker provider (bgutil-pot :4416, restart=unless-stopped) and resolves 720p (136+251) cookieless, no Deno.
- STATE: Worker armed with po_token. Cookie path DEAD on Sol (Deno segfaults exit139; cookies force web client needing JS sig-solve). po_token is the durable bypass, no login needed. Cookies staged as unused insurance at ~/song_timing/yt_cookies_maxrempel35_20260613.txt. Isolated test venv at ~/b80_cookietest. B8 worker untouched except additive plugin.
- NEXT: Watch B8's next pulls for po_token effect when volume wall returns. Nothing urgent - B8 has 0 wall now.
- LESSON: Recent yt-dlp (2026.06) needs an external JS runtime (Deno) for web-client sig/n-challenge; Deno segfaults on Sol. tv_simply client skips JS but rejects cookies - so po_token (not cookies) is the working anti-botwall path on Sol.

## [2026-06-13 15:11] B80 d89120c0
- DID: b80: shipped + documented the po_token bot-wall bypass. Method doc committed+pushed (tools/tamza_hucolo_backup/potoken_botwall_bypass_method_v01_tomemex.md). Plugin live in b8 worker venv; provider Docker bgutil-pot :4416. Sol rebooted mid-session and the provider AUTO-RESTARTED on boot (restart=unless-stopped) - real-world proof it survives reboots. b9 dropped the dead public-Invidious idea and adopted tv_simply+po_token; b8+b9 now run throttled-parallel (Max cleared 2 YT users/home-IP as normal).
- STATE: DONE: token bypass implemented, verified end-to-end (real 720p bytes), documented, shared with b8+b9. Identity corrected to b80. Cookie/login path shelved (Deno segfaults on Sol). Nothing pending for b80.
- NEXT: Idle/monitor. Optional: arm Centauri with same provider for b9's bulk pull if Max says go. Watch for first real wall event to confirm token still attaches under volume.
- LESSON: Docker --restart unless-stopped genuinely survives a host reboot - validated when Sol bounced and bgutil-pot came back without intervention.
