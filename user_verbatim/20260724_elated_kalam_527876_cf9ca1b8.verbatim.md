# VERBATIM user (Max) log - session cf9ca1b8-29a9-4749-80bc-2804908c42bc
# cwd: C:\moma\.claude\worktrees\elated-kalam-527876
# Every line Max typed, saved verbatim, so it survives compaction.
# Recover specs / investigate trouble by reading this file.


## [2026-07-24 22:49:56] turn 1
So super weirdly the context which is loaded is very large.    Obviously, since the context was loaded in your memory, you know everything which is there. Look for junk there. Global 2 is only about 40k tokens, so I don't get it where from 100k tokens came from. Originally, until now it was 70k and now it doubled. So there is some junk. All junk is loaded in your context.

## [2026-07-24 22:52:04] turn 2
TLDR: it's not you, it's a known Claude Code regression
Others hit exactly this. The system prompt grew about 70,000 tokens across two versions (2.1.91 and 2.1.96) â€” jumps of 40K then 30K. Same symptom: fresh session already huge, unusable without constant compacting.
Suspected causes, all on Anthropic's side, none of it your files:

* The skill listing injected every turn
* Expanded safety/injection-defense text
* New tool descriptions â€” the browser suite, computer-use, Skill tool
* Plugin and subagent metadata

The issue was closed as "not planned" with no maintainer explanation and no fix.
So your ~150K fresh start is roughly: 70K of new Anthropic overhead plus your ~65K of rules. Your rules were never the thing that doubled.
Two real levers left. Trim your own rule stack, or cut the tool suites you rarely use â€” chrome, computer-use, playwright are the fat ones and they can't be deferred. Bumping compaction to 300K is sensible but only masks it.
Want me to check whether a newer version fixed it, or start measuring your rule files?
Sources: [claude-code issue #45188](https://github.com/anthropics/claude-code/issues/45188), [Systima: token overhead](https://systima.ai/blog/claude-code-vs-opencode-token-overhead)

## [2026-07-24 22:54:09] turn 3
No, actually, Global 2 is intended to be loaded automatically, and if it says otherwise, that's a stupid addition which was stupid.
