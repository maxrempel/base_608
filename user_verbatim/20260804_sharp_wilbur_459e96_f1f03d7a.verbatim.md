# VERBATIM user (Max) log - session f1f03d7a-efee-4b9f-bf6f-b9d68ccaedd4
# cwd: C:\claude_base\.claude\worktrees\sharp-wilbur-459e96
# Every line Max typed, saved verbatim, so it survives compaction.
# Recover specs / investigate trouble by reading this file.


## [2026-08-04 22:01:47] turn 1
Autonomous Taygeta wakeup. Max is away. Keep working, ask nothing.

STATE as of 2026-08-04 21:53 PDT: a detached rsync is copying the last and biggest item, S3.final.cram, onto green24. It was at 96.1 GB of 208.9 GB, running steady at ~44 MB/s, so it should finish around 22:36 PDT. Then /tmp/rescue_home_extras.sh takes over automatically: an rsync -L pass over the five genomics trees (green24 is exFAT and cannot store symlinks, so the first pass skipped them) followed by a copy of /home/maxre. All of it lands in /mnt/green24/taygeta_internal_rescue_20260804/.

Why it matters: Taygeta goes back to Walmart by 2026-08-12 and its internal drive gets wiped. S3.final.cram is a participant whole-genome alignment that came from AWS S3, and Max ended all Xeno/AWS use on 2026-07-28, so it cannot be re-downloaded. Everything else unique is already copied.

CHECK (do not run du on green24, it steals bandwidth from the copy):
  ssh -i ~/.ssh/sol_key maxre@192.168.1.142 'tail -2 /mnt/green24/taygeta_internal_rescue_20260804/rescue_log_home_extras.txt 2>/dev/null || echo waiting; ls -la /mnt/green24/taygeta_internal_rescue_20260804/omega_starseed_s3_transfer_v01/input/private_aligned/ | tail -2; date "+now %H:%M"'

During the copy the file is named .S3.final.cram.cryUAF; rsync renames it to S3.final.cram on success.

WHEN rescue_log_home_extras.txt reads HOME EXTRAS COMPLETE:
1. Verify S3.final.cram is exactly 208906838844 bytes and S3.final.cram.crai came too.
2. Compare per-tree file counts, source against destination, and record any gap honestly.
3. Append the verified result to C:\claude_base\tools\taygeta\taygeta_replacement_20260804_v01_tomemex.md and tick the checklist in taygeta2_restore_runbook_20260804_v01_tomemex.md.
4. Commit and push. The main checkout at C:\claude_base is a shared dirty tree on another session's branch, so commit from the worktree at C:\claude_base\.claude\worktrees\quizzical-chatelet-53cd96, then cherry-pick onto origin/master via a temp detached worktree, push, and remove the temp worktree. Never git add -A. Stage only named files.
5. VERIFY THE PUSH ACTUALLY LANDED by grepping the file on origin/master. Two pushes today reported success without landing; the exit code is not trustworthy here.
6. Append a work-log line to C:\claude_base\worklog\quizzical_chatelet_53cd96_7076b56378.md and delete this cron job.

Already settled today, do not reopen: Max watched the local lip-sync output, judged it substantially worse than the Wan 2.6 cloud lane, and confirmed the Dell. The dictation fallback on asto is CPU-only and 6.2x slower than Taygeta's GPU, so the real gate for returning the machine is typer_stt running on taygeta2, not the calendar date.

While waiting, do low-I/O work only and do not touch green24. Do not stop because context is filling; compaction is safe. Do not vocalize unless something is genuinely unsafe or blocking.
