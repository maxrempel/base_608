# Adviser note - milestone 10 (~801K tokens)
# session: 20260623_amazing_allen_5b955a_01a4465e
# written: 2026-06-23 16:16:57 by deepseek-v4-pro

TO ASSISTANT: You shipped 11 storyboard v2 versions (v2.29-v2.39) in rapid fire, each undoing or reworking the last. Max had to catch the bugs himself (tuple-vs-dict backend, image fallback dropped, pins bleeding back into auto-pick). Stop the version-bump frenzy. Before any next commit, test end-to-end: query the live endpoint, verify the spot you're fixing, hard-refresh in multiple browsers, confirm with your own eyes. The autonomous idle loop produced 25+ noise ticks - you should have stopped after 3 quiet ones per Max's own loop instruction. Scale back to one quick CI check and stop, not a re-arm parade.

TO MAX: The "no pinning" principle is still compromised. The Assistant's filter is now deterministic (matching reels only, newest wins slot), but the drag handler still writes to `ST.assigned` (a pin operation in all but name) and v2.39 added toast feedback for "pinned" and "un-pinned" labels. If you want zero pin mechanics anywhere - even as a preference that the filter respects - say so: the assistant should scrub `ST.assigned` from the lipsie path entirely, make the slot purely max-job-id auto-pick, and keep drag purely as an ephemeral UI reorder with no DB write. The pile-filter saga also left `SCENE_ARR_IDS` dead code cleaned up but the underlying disease - adding mechanical filters instead of fixing data - needs a permanent project-memory entry so future sessions don't repeat it.
