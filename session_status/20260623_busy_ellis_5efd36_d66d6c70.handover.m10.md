# Scribe handover - milestone 10 (~807K tokens)
# session: 20260623_busy_ellis_5efd36_d66d6c70
# cwd: C:\moma\.claude\worktrees\busy-ellis-5efd36
# written: 2026-06-23 15:43:55 by deepseek-v4-pro

# Handover for sc10 Reel Production (D21 session)

## GOAL (in Max's words)
Build the entire 33-line sc10 scene (Anna?Ishtab) as merged "reels" - one reel per conversational beat (spot). Each reel must start from a **different background image** (no duplicates). Reels must **auto?match their spot** by exact whole?text membership (no manual pins). Fix the L23 (Ishtab monologue) reel to have **varied, open, natural two?hand gestures** - one per phrase - while preventing the model from rendering any literal objects (a blue Earth planet appeared). Do it with a minimal prompt that describes every gesture and explicitly bans extra images.

## DECISIONS + WHY
- **Spot?per?beat structure:** 11 spots covering all 33 lines. Long monologues (lines 8, 23) remain single?line spots because the audio alone nearly fills the 15s clip cap.
- **Describe?both?first + speak?order:** Beats speaker?swap on merged reels. The winning pattern: "On the left: a young woman with red hair... On the right: an elder woman..." then "Left speaks first, then Right answers."
- **Neutral hands / no stillness words:** "Completely still" froze hands; "arms moving" made them robotic. The fix: relaxed, natural mentions, no stillness commands, and picking stills where hands are already relaxed.
- **No smile/grin words:** Those trigger laughing or constant smiling. Tone words like "composed," "calm," "serious" work better.
- **Walking beats require a walking?pose still:** A static standing still gives floating/glide motion. The corridor walk stills (e.g. `sc_walk2_c2_pan_right`, `B1_corridor_walk`) have a mid?stride pose that animates as real walking.
- **Object hallucination:** Naming "globe," "planet," "sphere" in the prompt spawns a literal object. To keep the globe **gesture** without the object, describe the hand motion ("both hands sweep a wide circle") and use `negative_prompt` + an explicit ban on any floating object.
- **Auto?match spine (Max's mandate):** Manual pinning is prohibited. Reels must carry canonical merge hashes; the storyboard will match them to spots by exact whole?text equality. The sass `merges.json` for current sc10 spots is missing - this is a team blocker (D30/D31/D24fixer).
- **4?digit numbers:** Max can't read them - I'll avoid them in this handover.

## CURRENT STATE
- All sc10 spots have candidate reels made from distinct background stills. The spine currently shows many of them, but some recent reels are invisible because D31's strict membership filter hid them. A spine restoration by D30 is pending (D21 handed over the exact spot?to?reel mapping).
- The **L23 reel** (Ishtab's monologue, on still `sc_walk2_c2_orig_B_v01`) is being iterated for natural hand gestures. The current fired version is **job 2924** - a minimal prompt with four distinct hand gestures per phrase, a ban on extra images, and a negative prompt listing "floating globe, planet, Earth, sphere, orb..." It just finished rendering and is awaiting Max's verdict.
- The session is at **~96% context** - a fresh window is strongly recommended to avoid mis?steps.

## EXACT NEXT STEP
1. **Wait for Max's verdict on job 2924 (L23 gesture reel).** He will say whether the gestures are varied and no planet appears.
2. If he wants another iteration:
   - Keep the same still: `sc_walk2_c2_orig_B_v01.png` (source_job 482, line_hash ed148de2073a1b)
   - Use the minimal template: describe both characters + their positions, then for each phrase a short distinct two?hand gesture description, then the exact line text (quoted, Right spokesperson). Add: "NO objects or extra images appear anywhere - only their hands move." Use `lipsync_params` with `negative_prompt` listing all planet/sphere words.
   - Fire via `fire_job` (NOT `fire_merge_lipsie` - that endpoint requires merges.json). Do **not** pin; Max will view it directly.
3. If 2924 is approved, move on to any remaining spots Max wants re?done (e.g., line 32 maybe needs its own reel). But the immediate priority is the L23 fix.
4. **Long?term:** Coordinate with D30recoder/D31/D24fixer to get sass `merges.json` generated for the current sc10 spots so the auto?match spine system can work. Until then, reels can be made and reviewed via the liper but won't land in the spine automatically.

## OPEN QUESTIONS (Awaiting Max)
- Does reel 2924 have varied, natural gestures with NO planet artifact?
- Which spots still need distinct reels or re?staging? (e.g., line 32 independent reel?)
- Who will produce the sass `merges.json` for sc10's current spots? (The auto?match spine is blocked until this exists.)

## KEY PATHS & IDs
- **Liper (view reels):** `http://localhost:8779/lipser?ids=<job_id>`
- **L23 audio:** line_hash `ed148de2073a1b`, line text: "They are not ready. The Alliance operates one program per planet. One representative body speaking for the whole planet. They must form a coalition and accept our offer of contact."
- **L23 source still:** `sc_walk2_c2_orig_B_v01.png` (job 482)
- **Latest L23 reel:** job 2924
- **Worker:** `combo_wan26au_worker.py` in `C:\moma\sc10\combo_runner\code` (polls D1, uses DashScope wan2.6?i2v?flash)
- **DB client:** `D1Client` in `moma_db.py`; query with `d1.query_sql(...)`
- **Batch comments:** `python batches.py comments N` to get Max's latest verdicts
- **Handover/log:** `python C:/claude_base/compaction_kb/scripts/worklog.py log "..."` - checkpoint already saved for all recent work.

## GOTCHAS
- Never use "globe," "planet," "sphere," "earth" in the prompt (even to describe a gesture). Use motion words only.
- **No manual pins** - do not call the auto?pin code or write to `line_current_clip`. The system must match automatically.
- The wan26au worker can die; check with `cat data/wan26au_worker_pid.txt` and `ps` (or `Get-CimInstance` in PowerShell). A dead worker freezes the queue - restart MOMA or relaunch the worker.
- If you need to fire a merged reel, do not use `fire_merge_lipsie` (it requires merges.json which doesn't exist). Use `fire_job` directly, merging audio with `audio_resolver.resolve_per_line_audio` and creating a manifest.
- Context is near limit - spin up a fresh session if you need to do significant work. The new session can read this handover and the worklog to resume instantly.
