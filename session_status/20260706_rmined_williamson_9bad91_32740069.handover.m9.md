# Scribe handover - milestone 9 (~679K tokens)
# session: 20260706_rmined_williamson_9bad91_32740069
# cwd: C:\claude_base\.claude\worktrees\determined-williamson-9bad91
# written: 2026-07-06 22:32:48 by deepseek-v4-pro

# HANDOVER - P2 NPA PROJECT (Session X12B)
**From the Scribe, for a cold post?compaction session. Start here.**

---

## GOAL (max's own words)
Max wants a **plain catalogue of the non?parental alleles (NPAs)** across all 602 1000?Genomes trios, genome?wide.  
Secondary (and now his real focus): **measure the ancient?vs?modern drift of the new NPA alleles, split by NPA category** (both?parents?absent, father?only, mother?only, etc.).  
Further downstream he wants to:  
- find **reproducible hotspots** present in a small % of the population (and eventually overlay them on an autism?gene set, still to be named),  
- find **haplotypes where NPAs cluster**, grouped by family and by genomic location.

He is **not** asking for significance tests of subgroups or ancestry?corrected "is it aliens" proofs right now. He explicitly said earlier filtering that collapsed 108?000 candidates to 45 was "hiding the signal." He wants the full distribution, carefully calibrated, not clean?cut negatives.

---

## DECISIONS MADE + WHY
1. **No aggressive filtering or funnel**  
   After Max's sharp feedback, the team agreed that filters like "gnomAD common" and "singleton?only" were circular against his hypothesis (5?% of people might carry the alien edit ? it would appear in gnomAD).  
   ? Now keep all NPAs; any culling must be calibrated against noise, not subtractive.

2. **Ancient?vs?modern drift by category (aggregate, not per?child)**  
   Max sees the drift statistic as "novel and interesting." The raw 90?% modern is **tautological** (the NPA allele is almost always the non?reference base, and reference bias inflates "modern").  
   ? We must report the **bias?controlled stratum** (`ref = DER`, where the reference base is the derived allele, so the bias works the other way) **split by NPA category**. The key is whether categories *differ*, and whether the DOCHAN (both?parents?absent) class shows a particular drift.

3. **Box is kept, not terminated**  
   Max wants the EC2 box ("Xeno") stopped but preserved (~$5?/month disk cost).  
   ? The box is `i-00c83a0af889f8bf1` (us?east?1), key at `C:/Users/maxre/Nextcloud/zSyncMain/ssh/xg1_aws.pem`. The AMI clone is `ami-0f4a94c1d017a0942`.

4. **All significance/ancestry?subgroup machinery is parked**  
   The earlier directional tail?test and scrambled?parent permutations were interesting but not asked for right now. They are documented in the handoff but should not be revived unless Max redirects.

---

## CURRENT STATE

### Done & synced to Max
- **Genome?wide NPA scan** finished (all 23 chromosomes, 108?606 raw misland regions, all 602 trios).  
  ? Raw data locally under: `C:/claude_base/projects/XG1/kenefick/paper_repro/genome_out/`  
  ? Also in S3 bucket `s3://xg1-genome-out-207399218415/genome_out/`
- **Trio Mendelian validation** complete (all 602 trios genuine, no fakes removed).
- **NPA type histogram** (novel?both 68?%, single?parent 32?%, LOH ~0?%).
- **222?hotspot Manhattan plot** shown to Max (but that was before the "stop filtering" pivot).
- **Machine image (AMI)** available.

### Running right now (on AWS Box)
- **Category?drift jobs on chr20, chr21, chr22**  
  They are **streaming** the VCF from S3 and computing, for each NPA category ? strand (ref=ANC / ref=DER), the counts of ancient and modern alleles.  
  The box is running at IP likely `44.223.76.117` (check `/tmp/boxip.txt` or `aws ec2 describe?instances ...`). PIDs were 5232/5235/5238 (but may have finished). They write output to `~/catdrift_chr{20,21,22}.out`. To check if still running:  
  `pgrep -f catdrift.py`  
  The scripts are `~/catdrift.py`; the fix was to iterate `for rec in vf:` (the earlier `vf.fetch()` failed on stdin).  
  **These results have NOT been reported to Max yet** - they are the critical immediate deliverable.

### Not yet built
- **Plain NPA catalogue** (per?SNP and per?region, with population and recurrence columns). Needs a script over the local `genome_out/`. All data is local, no box needed.

---

## EXACT NEXT STEP (for the cold session)

1. **Collect the category?drift table**  
   - SSH to the box (key `C:/Users/maxre/Nextcloud/zSyncMain/ssh/xg1_aws.pem`, user `ec2-user`; IP may have changed - re?query `aws ec2 describe?instances ...` or read `/tmp/boxip.txt`).  
   - Ensure your current public IP is allowed in the security group `sg-0651e79bde1c34bd0` (the old IP `66.75.225.131` was added).  
   - Check if jobs are still running: `pgrep -f catdrift.py`. If yes, wait; if zero, they are done.  
   - Concatenate the three output files: `cat ~/catdrift_chr20.out ~/catdrift_chr21.out ~/catdrift_chr22.out`.  
   - Merge them locally: sum ancient/modern counts per **category ? stratum** (both_hom/both_het/not_from_father/not_from_mother/other ? refanc/refder).  
   - Create `outputs/real/catdrift_merged.tsv` and **immediately report to Max** the clean table, highlighting the **ref=DER** stratum across categories. (The raw/ref=ANC numbers are inflated by reference bias.)

2. **Stop the box** after collecting the table (to save cost):
   `aws ec2 stop-instances --instance-ids i-00c83a0af889f8bf1 --region us-east-1`

3. **Build the NPA catalogue (local, no box)**  
   - Write a script that reads `genome_out/chr*/calls/*.tsv` and `genome_out/chr*/regions/*.tsv`.  
   - For each SNP call, add: population (join `masks/g1k.ped` column 7 by child ID), recurrence count (number of distinct children with an NPA at the exact chrom:pos).  
   - Output two files:  
     `outputs/real/NPA_catalog_perSNP.tsv`  
     `outputs/real/NPA_catalog_perRegion.tsv` (with region span).  
   - Do not dump all rows into the session; just report file paths, row counts, and the top 20 most?recurrent sites.

4. **Ask Max** for the specific gene?set to overlay on hotspots (autism/SFARI or other), then proceed with the hotspot?gene mapping and haplotype?clustering (if he still wants it).

5. **All results must be committed and pushed** (explicit paths, never `git add -A` again).

---

## OPEN QUESTIONS AWAITING MAX
- **Which gene set** for the hot?spots ? phenotype overlay? (autism/SFARI? something else?)  
- Does he want the per?child outlier children (the ones with unusually many mislands) investigated as potential individual hybrids, or should that line stay abandoned? (He earlier seemed to drop it.)

---

## KEY PATHS, IDs, COMMANDS

### Project root
`C:/claude_base/projects/XG1/kenefick/paper_repro/`

### Critical data
- Genome?wide per?trio calls: `genome_out/` (local)  
- 222 hotspots (before the pivot): `outputs/real/payload_candidates_v3_moderate_band.tsv`  
- Trio validation: `validation/val_chr21.tsv`, `val_chr22.tsv`  
- Type histogram results: `outputs/real/npa_types_chr21.tsv`  
- Reopened/calibrated findings: `outputs/real/P2_REOPENED_calibrated_findings_v01.md`  
- Per?child reversion table from chr22 (archaic_annotate run): `outputs/real/arch22_resid.per_child.tsv`  
- Overarching methodology: `npa_detection_methodology_v01_tomemex.md`, `P2_methodology_additions_v01.md`

### AWS
- Box instance ID: `i-00c83a0af889f8bf1` (region `us-east-1`)  
- AMI clone: `ami-0f4a94c1d017a0942`  
- Security group: `sg-0651e79bde1c34bd0`  
- Key: `C:/Users/maxre/Nextcloud/zSyncMain/ssh/xg1_aws.pem`  
- Bucket for results: `s3://xg1-genome-out-207399218415/`  
- To re?start box: `aws ec2 start-instances ...`  
- To stop box: `aws ec2 stop-instances ...`

### The Handoff File
`C:/claude_base/session_status/20260706_X12B_P2_reopened_handoff.md`  
Contains Max's full agenda, box details, exact commands for the category?drift collection, and the history up to this point. A fresh session **must** read it.

---

## GOTCHAS + DEAD ENDS RULED OUT

- **git add -A disaster**: the session once committed a 1?GB log file; now always use explicit paths.  
- **VCF remote random?seek corruption**: cannot repeatedly `vcf.fetch(region)` on a remote .vcf; the handle degrades. The working pattern is **single?pass streaming**: `aws s3 cp <url> - | (script)`. The category?drift scripts already use this.  
- **Tautological "modern" drift**: When an NPA allele is the non?reference base, calling it "derived" is tautological. The **only** trustworthy stratum is **ref = DER** (the reference base itself is derived). Always report that stratum separately.  
- **IP rotation**: The box gets a new public IP every time it starts. Re?check and re?add your current IP to the SG before SSH.  
- **The `catdrift.py` fix**: It originally used `vf.fetch()` but was changed to `for rec in vf:` because `fetch()` needs a seekable file. The current scripts on the box should have that fix.  
- **Do not resurrect the ancestry?subgroup/filter?heavy pipeline** unless Max explicitly asks. The team now works on catalogue + drift.  
- **Context was high at session end (~80 %)**. The above handoff and the separate `session_status` file assume a fresh session can resume seamlessly.
