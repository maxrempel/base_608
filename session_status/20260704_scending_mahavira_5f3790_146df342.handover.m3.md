# Scribe handover - milestone 3 (~277K tokens)
# session: 20260704_scending_mahavira_5f3790_146df342
# cwd: C:\moma\.claude\worktrees\condescending-mahavira-5f3790
# written: 2026-07-04 21:23:23 by deepseek-v4-pro

# HANDOVER - SESSION D52, MOMA housekeeping + sc11 spot2 minimal-edit batch

## GOAL (in Max's own words)

1. **Housekeeping (root-cause investigation):** "Why would worker die? That's super interesting. Let's investigate that, stop the production, let's figure out what killed the workers."
2. **Production (sc11 spot2 - arrangement 2):** Take s3031, replace chairs with white Vienna chairs, replace podstakannik-type cups with white cups - keep everything else the same. "Minimal change, no feeding of the result, just minimal change in text and remove podstakannik from the portrait and replace it with cups and make Vienna chairs white, but everything else keep the same as in original."
3. **New angle prompt (interrupted, the CURRENT pending task):** Spot 2, change the angle - background p1188, Werner left, Derek middle, Anna right, symmetrically. Then corrected: MUCH closer, table smaller, no second table/chair, facing each other looking at flowers. Then evolved: ?????? ???????? ? ????? ?? ?????????? (????? 11?, ???? 2?, ???? 5?, ?????? 8?), ???? ???? ????????. In Russian.

## DECISIONS MADE + WHY

### Worker-death root cause (FOUND + FIXED)
- **Root cause:** combo_worker.py's `while True:` poll loop crashed on transient Cloudflare D1 network errors (WinError 10054 connection reset, read timeout) because `moma_db._request` had no retry and the worker loop had `try/finally` with NO `except`.
- **Elegant two-layer fix (applied + pushed to master):**
  1. `moma_db.py` `_request`: added retry-with-backoff (0.5s, 2s, 5s, 10s) for transient errors (URLError, TimeoutError, OSError). HTTPError explicitly excluded from retry. Added `import time`.
  2. `combo_worker.py` `main()`: wrapped the loop body in try/except ? log error + sleep 10s + continue instead of exit.
- **Reasoning:** protect ALL callers at once (layer 1) + belt-and-suspenders worker survival (layer 2). Matches existing anti-silent-death pattern (crashlogs).
- **Verification:** commit pushed, main checkout confirmed on fix, all 3 servers + 6 workers restarted and verified live with new code.

### Minimal-edit rule (HARD LESSON from Max's feedback)
- **What went wrong first attempt:** Assistant fed s3031 as primary ref #1 ? produced derivatives (v???) that stared at the camera, lost Werner's likeness, had "selfie" mood. Max: "It's a derivative. Fucking, fucking derivative."
- **The rule Max established:** "You should recreate it from scratch and add the result only as a reference, but NOT as the first one - as the second or third. The original input should remain the same. Otherwise it produces a derivative."
- **Second attempt (correct):** Used original 10-ref formula from s3031 production, added s3031 only as a secondary ref, re-described from scratch with ONLY the two textual swaps (white Vienna chairs + white cups + remove podstakannik). Result: v51/v52/v53 - all three correct, Werner likeness intact, nobody at the camera, mood preserved.
- **Saved to memory:** `C:\Users\maxre\.claude\projects\C--moma\memory\feedback_minimal_edit_not_derivative.md` + indexed in MEMORY.md.

## CURRENT STATE

- **Worker fix:** DONE, committed, pushed, live. Workers now survive network blips.
- **sc11 spot2 minimal batch (v51/v52/v53):** DONE, approved by Max ("much better, still lost a little but that's OK. Done."). Jobs 3112-3114. Arrangement 2. v53 picked best.
- **Closeup Russian prompt batch (v?? - "??????? ??????, ?????? ?????" batch):** Fired AND rendered (poll completed). Results described as "wasn't bad but too far, needed closer." The Russian-language correction was written and the fire script `_d52_fire_sc11_arr02_spot2_closeup_ru.py` was created.
- **Four-person clock-position Russian prompt:** WRITTEN but INTERRUPTED before firing. The last action was: assistant started writing the prompt for the 4-person shot (????? 11?, ???? 2?, ???? 5?, ?????? 8?, bigger table, ????, Russian language). `[Request interrupted by user]` - the prompt was likely half-composed, never fired.

## EXACT NEXT STEP

**Fire the four-person clock-position shot for sc11 spot2 (arrangement 2) in Russian:**
- Four people around a ???? table, positioned on a clock face: Derek at 11 o'clock, Anna at 2 o'clock, Ishtab at 5 o'clock, Werner at 8 o'clock.
- Table slightly bigger ("????-???? ?????? ????").
- Background: p1188 (Max specified it).
- In Russian language prompt.
- Use the MINIMAL-EDIT principle: original portrait refs for faces, do NOT feed any previous result as primary ref.
- Method: `fire_job()` with `arrangement_id=8` (or whatever arrangement 2's ID is - verify: arrangement 8 was sc11-arr01; sc11-arr02 may be arrangement 9 or different).
- **Verify arrangement_id for sc11 spot2 arrangement 2 before firing.**

## OPEN QUESTIONS

- **What is the arrangement_id for sc11 spot2 (arrangement 2)?** Max called this "spot number two" - it may be a different arrangement than the sc11-arr01 (which was arrangement_id=8, scene_id_db=3). Need to query the DB for arrangement 2 of scene 11. The last batch (v51-v53) was fired to `arrangement_id=?` - check the fire script `_d52_fire_sc11_arr02_spot2_minimal.py` to extract the correct ID.
- **Did the Russian closeup batch (the "??????? ??????, ?????? ?????" correction) actually fire?** The script `_d52_fire_sc11_arr02_spot2_closeup_ru.py` was written; it appears it was about to fire but interrupted. Need to check whether jobs were created from it.
- **Where is the full Russian prompt text saved?** The interruption occurred mid-write - need to check `_d52_fire_sc11_arr02_spot2_closeup_ru.py` for whatever was written.

## KEY PATHS, IDs, NAMES

### Files modified (worker fix)
- `C:\moma\sc10\combo_runner\code\moma_db.py` - lines 53-66, added retry-with-backoff + `import time`
- `C:\moma\sc10\combo_runner\code\combo_worker.py` - lines 530-567, wrapped loop body in try/except
- Commit: `Fix recurring worker deaths: survive transient Cloudflare D1 network errors` (on master)

### Fire scripts
- `C:\moma\sc10\combo_runner\code\_d52_fire_sc11_arr02_spot2_minimal.py` - the SUCCESSFUL minimal-edit batch (3134-3136? no, 3112-3114 ? v51/v52/v53). **READ THIS to extract arrangement_id, refs, and prompt template.**
- `C:\moma\sc10\combo_runner\code\_d52_fire_sc11_arr02_spot2_closeup_ru.py` - the Russian closeup correction. **READ THIS for the prompt text and to see if jobs were created.**
- `C:\moma\sc10\combo_runner\code\_d52_fire_sc11_arr01_handwave.py` - the eariler 13-ref handwave batch (not spot2, different arrangement)

### Memory/lessons
- `C:\Users\maxre\.claude\projects\C--moma\memory\feedback_minimal_edit_not_derivative.md` - THE CRITICAL LESSON about not feeding results as primary refs
- `C:\Users\maxre\.claude\projects\C--moma\memory\MEMORY.md` - master memory index

### Key IDs
- Scene 11 arrangement 2 ("spot number two") - exact arrangement_id TBD
- s3031 - the "ideal" image used as secondary ref for spot2
- p1188 - background image specified by Max for the new angle
- Jobs 3112-3114 ? v51/v52/v53 (the approved batch)
- Ishtab portrait refs: needed for the 4-person shot (she was not in the 3-person batch)

### Servers/ports
- Dashboard: `http://localhost:8779`
- Mixboard: 8790
- Prompter: 8791
- Workers: combo_worker.py (image + others), all restarted

## GOTCHAS + DEAD ENDS

- **DO NOT feed any result image as ref #1** - that produces camera-staring, likeness-losing derivatives. Keep original portrait refs as primary faces. Add a result image only as ref 2 or 3 if needed.
- **DO NOT reuse/iterate on generated images** - always re-describe from scratch with the same original refs, only changing the text for the specific tweak.
- **Prompter didn't auto-start** with the batch restart script - if restarting stack, manually start prompter_server.py if it's down.
- **The 4-person clock prompt MUST include Ishtab's portrait refs** - the 3-person batch (v51-v53) only had Anna/Werner/Derek. Ishtab's face and body portraits need to be added back.
- **Worker deaths are NO LONGER a live problem** - if a worker appears to die, the network-retry fix should handle the blip; the process itself should stay alive. The crashlogs are in `data/crashlogs/combo_worker.stderr.log`, the stderr is captured via `moma_restart.py` spawn(), and the error-on-blip was definitively caught there.
- **Max is working in Russian** at this point - the pending prompt should be composed in Russian, maintaining the same technical rigor (10 refs, clock-face positioning, no derivative feeding).
