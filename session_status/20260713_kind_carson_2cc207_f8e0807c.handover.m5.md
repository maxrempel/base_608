# Scribe handover - milestone 5 (~402K tokens)
# session: 20260713_kind_carson_2cc207_f8e0807c
# cwd: C:\claude_base\.claude\worktrees\kind-carson-2cc207
# written: 2026-07-13 12:45:47 by deepseek-v4-pro

This handover covers an extended session updating the Starseed Genetics website (starseedgenetics.com), publishing two blog posts on maxrempel.com, and building a native signup form tied to a single contacts database. The user's instructions evolved, but the final state is a clean, single?pitch site and a merged contact list.

---

### GOAL (in Max's own words)
- Update Starseed Genetics site to reflect the new approach: whole?genome sequencing via sequencing.com (?$379), not 23andMe/genotyping; accept all family configurations and children of any age with parental consent; collection is "brushing inside the cheek"; participants send photos of labelled tubes.
- Remove ALL references to long?read sequencing, the $6,000 fundraising campaign, PacBio, 23andMe, and old genotyping tiers - one clean pitch.
- Live running project?age counter and auto?rolling date on the donation update.
- Publish two blog posts on maxrempel.com exactly as dictated, with only necessary factual corrections.
- Replace the Google?Form?based signup flow with a native on?site form that writes directly into ONE contacts database, and migrate any missing historical entries into that database.

---

### DECISIONS + WHY
1. **Single source of truth** - The whole site lives in `C:\claude_base\sites\starseed-site\src\worker.js` (a Cloudflare Worker, bilingual EN/RU). Edits here ? `bash deploy.sh` ? live.  
   *Why*: it's the only code that serves starseedgenetics.com; modifying anything else would be futile.

2. **Backup before every change** - Each live?edit cycle started by copying the current worker to `C:\claude_base\backups\cf_workers\starseed-site\history`.  
   *Why*: quick fallback if a deploy broke something.

3. **Native signup form over Google Forms** - Instead of editing existing Google Forms, a simple HTML form was embedded directly on the site, posting to a new `/api/join` endpoint.  
   *Why*: Google Forms sometimes demand a login and bounce visitors off?site; a native form looks like the site, avoids that friction, and writes instantly to the D1 contacts database.

4. **One D1 database for all contacts** - The existing `starseed-genetics-contacts` D1 database (table `contacts`) is now the single store. A `source` column distinguishes origin (website/historical import).  
   *Why*: user explicitly wanted "one database, the previous properly migrated".

5. **Migration: newsletter subscribers merged** - After diffing the two Google Form experiencer sheets against the DB, all those people were already present. The separate newsletter subscriber sheet had 7 genuinely new email?only contacts; these were inserted as `type: 'newsletter'`, `source: 'legacy_subscriber_list'`.  
   *Why*: completes the merge without duplicates.

6. **Blog posts via D1 insert** - maxrempel.com's blog is backed by D1 table `blog_posts`. Two rows were inserted with HTML content. No worker deploy needed.  
   *Why*: the documented blog API for that site is D1; instant live, no risk of breaking worker code.

7. **Content purges** - "Long-read", "$6,000", "PacBio", "23andMe", "adult child", "$400", "$99", the entire 23andMe forwarding page, and the old fundraising narrative were all removed. The Donate page lost the progress bar and now just describes donating to buy kits for families in need.  
   *Why*: user's explicit instruction: "Forget the long reads. We are just one pitch now."

8. **Pricing precision** - Started with "about $400", then user recalled ~$388, then found the real price $379 for 30? WGS + swab kit on sequencing.com. Updated all four occurrences (EN+RU).  
   *Why*: accuracy; user wanted the real current price.

9. **Headline and framing** - "Check for traces of alien genetic manipulation in your DNA" replaces the old $6K call. Buttons read "Submit your DNA data" (not "Send us your DNA" - we accept sequence data, not physical DNA).  
   *Why*: clarifies the ask and removes confusion.

---

### CURRENT STATE
- **starseedgenetics.com**: live, clean, one?pitch. All pages (home, register, consent, updates, donate, links) updated and verified in both English and Russian.  
  - Home: live age counter (from July 2024), "about twenty families, sixty people".  
  - Subscribe page: native newsletter signup (name, email).  
  - Register page: two native forms - "Get in line to be sequenced" (full details) and "Just keep me posted" (newsletter).  
  - API endpoint `/api/join` writes directly to D1 contacts database. Honeypot spam protection.  
  - `/forwarding` route removed - returns 404.  
  - No remaining Google Form links on the site.  
- **maxrempel.com**: two blog posts live:  
  - `/blog/starseed-genetics-project-update-2026-07` (verbatim)  
  - `/blog/you-are-an-alien-hybrid-and-we-can-look` (with one factual correction: photo of labelled tube, not box code).  
- **Contacts database** (`starseed-genetics-contacts`): 46 contacts, all sources merged.  
  - Table `contacts` has columns: id, name, email, location, participants, funding, mode, notes, type, status, created_at, source.  
  - 7 legacy newsletter subscribers added; all historic experiencer form respondents already present.  
- **Worker source** at `C:\claude_base\sites\starseed-site\src\worker.js` includes the native form logic, CSS, and POST handler. A new D1 binding `CONTACTS_DB` was added to `wrangler.toml` (pointing to the same D1 database id).  
- **Sync method doc** updated (`C:\claude_base\tools\xg1_starseed_forms\xg1_starseed_forms_method_v01_tomemex.md`) to note the native form is now the primary input, with periodic manual checks of old Google Sheets recommended.  
- **Git**: all changes committed and merged to master (branch `claude1/gracious-turing-72ae8e`). Current worktree is `kind-carson-2cc207` (old `admiring-diffie-13ddd6` recycled).

---

### EXACT NEXT STEP
The session ended with Claude asking: **"Want me to fully delete those old Google Forms, or leave them as a dead backstop?"**  
The user has not yet answered. So the immediate next action is to **wait for that decision**, then either:
- Delete the Google Forms (if user wants them gone), or
- Do nothing (they're harmless and still capture form data if anyone finds the old link; but the site no longer points to them).

---

### OPEN QUESTIONS (awaiting user)
1. Should the old Google Forms be deleted entirely, or kept as a fallback?
2. The site now says "about twenty families, sixty people" everywhere - this matches the handover figure. Is this final, or does the user want another number?
3. The handover mentioned "thorough review of the thing" - the native form and merge addressed the major friction, but other improvements (e.g., adding clearer visual CTAs, an FAQ page, or a more engaging design) were not yet requested. The user may still have review feedback.

---

### KEY PATHS, IDS, NAMES
- **Worker source**: `C:\claude_base\sites\starseed-site\src\worker.js`
- **Deploy**: `C:\claude_base\sites\starseed-site\deploy.sh`
- **Backup dir**: `C:\claude_base\backups\cf_workers\starseed-site\history\`
- **D1 database**: `starseed-genetics-contacts` (ID `065179a3-d586-4ce3-a34a-3cca9f477224`), bound as `CONTACTS_DB` in the worker.
- **Blog D1**: `maxrempel-site-db` (for maxrempel.com blog posts).
- **Cloudflare account**: accessed via MCP server `fee7c39e-4816-4a04-b41f-7067182da1c3`.
- **Notion handover**: "Starseed handover number 1" (page ID from Notion search).
- **Google Drive MCP**: `62ad6c43-6d9d-4a95-89d5-afe68b9798fd` for reading old forms.
- **Forms tool**: `C:\claude_base\tools\xg1_starseed_forms\`
- **Migration reference**: `xg1_starseed_forms_method_v01_tomemex.md`

---

### GOTCHAS & DEAD ENDS
- **Worktree path changed**. This handover's new worktree is `kind-carson-2cc207`; the old `admiring-diffie-13ddd6` is gone. Any file reads must use the current absolute paths.
- **WebFetch caches** - used `curl` with random cache?busters to verify changes; sometimes needed 3 tries for all edge nodes.
- **D1 query MCP** can insert/read but requires exact JSON quoting for escapes. The native form?handling code must `await` the DB and handle errors gracefully.
- **Worker code is monolithic** - all pages, styles, and API logic in one file. Avoid large?scale refactoring unless explicitly requested.
- **Honeypot trap** - a hidden field `botcheck` is used; if filled, the submission is silently discarded. Works well.
- **The old 23andMe forwarding page** was deleted entirely; route returns 404. No lingering link.
- **Native form testing** - two test rows were submitted, verified in DB as id 42 and 43, then deleted. DB is clean (44 real rows after, but 46 after migration - note the discrepancy: earlier count was 39, plus 7 migrated = 46; after test deletes it's 44? Actually final count 46 includes the newsletter adds, no test rows so 46 correct).  
- **Blog posts** - directly inserted into D1; no worker editing. If the user wants them on starseedgenetics.com as well, that would require embedding a blog feed or adding static content to the worker. Not yet requested.
