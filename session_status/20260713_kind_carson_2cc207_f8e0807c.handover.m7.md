# Scribe handover - milestone 7 (~527K tokens)
# session: 20260713_kind_carson_2cc207_f8e0807c
# cwd: C:\claude_base\.claude\worktrees\kind-carson-2cc207
# written: 2026-07-13 14:14:16 by deepseek-v4-pro

# Starseed Genetics Site Overhaul & Native Signup - Handover

**Session Goal (in Max's own words):**
Update the Starseed Genetics site to reflect the switch from 23andMe to sequencing.com whole-genome sequencing at ~$379, accept all family configurations and any-age kids, use "brushing inside the cheek" verbatim, lead with the invitation to get sequenced (not a fundraiser), hide old 23andMe helper, and later build a native signup/registration/questionnaire system writing into one database, with email alerts on every submission.

## Decisions Made & Why

1. **Single pitch only** - everything about long-read sequencing, PacBio, $6,000 fundraiser, 23andMe genotyping, and Dante/Novogene tiers was removed. The entire site now only promotes the sequencing.com whole-genome kit at 30x coverage. *Reason:* Max explicitly said "one pitch now ... no 23andMe, no long reads, no bullshit."

2. **Price set to $379** - after checking the live sequencing.com page, $379 is the actual cost for the 30x WGS + swab kit, so all mentions were updated to "about $379 per person."

3. **Collection wording** - "brushing inside the cheek" (verbatim) replaces any other phrasing. Participants send a photo of the labeled tube with its sample code (not the box code). *Reason:* direct instruction from Max.

4. **Family age/configurations** - all configurations: individual, one parent + child, both parents + child, larger families. Kids of any age with parental consent. The old "adult child, 18+" requirement is gone.

5. **Hero headline changed** - to "Check for traces of alien genetic manipulation in your DNA." And we accept DNA sequence data (not physical DNA), button says "Submit your DNA data."

6. **Live counters** - a real-time auto-updating "project age" (started July 1, 2024) shown with decimals, and the Donate page's fundraising update date auto-rolls to today's date.

7. **$6K fundraiser removed from homepage** - the sidebar goal bar became a simple "Submit your DNA data" card, and the donate page still accepts donations but without a publicly stated target.

8. **23andMe forwarding page retired** - removed from all links, that URL now returns "Page not found."

9. **Native signup forms (Google Forms replaced)** - short registration (name, email, location, who participates, funding choice), newsletter/updates, and the full detailed questionnaire. All submit directly into the Cloudflare D1 `starseed-genetics-contacts` database (table `contacts`) via POST endpoints on the worker. *Reason:* avoid Google's forced login, off-site bounce, and unify contacts into one database.

10. **Four action cards on home** - beautiful 2?2 grid: Register & pay yourself, Register & join waitlist, Full questionnaire, Get updates. The first two pre-select the funding option on the form.

11. **Email alerts on every signup** - a secured feed endpoint (`/api/signup-feed`) on the worker, a Python poller running every 5 minutes on Centauri (always-on Windows machine) that sends Max an email with a short DeepSeek summary plus full details. Questionnaire submissions are included. *Reason:* Max wanted updates; we reused his own mail tool and DeepSeek key, avoiding new services.

12. **Contacts database merged** - pre?existing Google Form responses and newsletter subscribers were reconciled; all 46 real people now live in one D1 table (`contacts`), tagged with source ("website", "google-form", etc.).

13. **Blog posts published on maxrempel.com** - two posts inserted into the D1 `BLOG_DB` (maxrempel-site) via Cloudflare API, not by editing the worker.

## Current State

- **starseedgenetics.com** - completely updated, all old multi?pitch content purged. Four?card home, native registration and questionnaire, bilingual (EN/RU). All changes committed and merged to master in `claude_base`.
- **maxrempel.com/blog** - two posts live:
  - "Starseed Genetics Project Update" (slug: `starseed-genetics-project-update-2026-07`)
  - "You Are an Alien Hybrid, and We Can Look" (slug: `you-are-an-alien-hybrid-and-we-can-look`)
  They are consistent with the current site messaging (no outdated $99/23andMe/$6K contradictions).
- **Signup notifier** - Python script at `C:\claude_base\tools\starseed_signup_notifier\notify.py`, running as a scheduled task on Centauri (`StarseedSignupNotifier`) every 5 minutes. State file `starseed_signup_notifier_state.json` in Nextcloud ssh folder. It reads the feed endpoint via HTTPS with a secret key, formats DeepSeek summary, and emails `max@dnaresonance.org` using `mxmail_v01.send_email` (your own mail system). Works.
- **Database** - `starseed-genetics-contacts` D1, `contacts` table has columns: id, name, email, phone, location, participants, funding, message, subscribe, role, source, questionnaire_answers, created_at. Current 46 real contacts.
- **Worker bindings** - `wrangler.toml` now includes a `d1_databases` binding for the contacts DB. No additional bindings needed for signup; secrets `SIGNUP_FEED_KEY` set on the worker for the feed.

## Exact Next Step

**There is no immediate next step** - the session concluded successfully; Max said "It looks great, thank you very much." The requested "full questionnaire native" was implemented and tested.

If a future session continues, possible follow?ups to ask Max about:

- Translate the native questionnaire into Russian (currently only English).
- Add immediate Telegram notification in addition to email (can reuse the Telegram critical alarms bot).
- Decide whether to delete the old Google Forms entirely (they are still accessible via direct links but not linked anywhere; Max previously said "don't be stupid" about deletion).
- Monitor that new signups actually trigger the email alerts (everything tested clean, but no real submissions have come through yet).

## Open Questions Awaiting Max

1. **Russian translation of questionnaire** - currently long form is English only.
2. **Telegram instant ping** - Max initially left the channel vague; we implemented email. He might want Telegram as well.
3. **Deletion of legacy Google Forms** - not urgent, Max was against deletion; but they exist and could be confused if someone finds a direct link.
4. **Family numbers in old dated sections** - the fundraising story on Donate page's historical note still says "10 families"; Max's handover says ~20 families, 60 people. We updated all other occurrences but left that section as historical snapshot. May want to bump.

## Key File Paths and Identifiers

- **Starseed site source**: `C:\claude_base\sites\starseed-site\src\worker.js` and `wrangler.toml`
- **Deploy script**: `C:\claude_base\sites\starseed-site\deploy.sh` (uses `CLOUDFLARE_API_TOKEN` and account ID; token is deploy?level, cannot read D1 live, but worker uses OAuth inside Cloudflare)
- **Worker name**: `starseed-site` (custom domains attached)
- **Contacts D1 database**: name `starseed-genetics-contacts` (UUID `18b8acfd-5688-4ef5-808d-23780fad0661`), production binding used by the worker
- **Signup feed key file**: `C:\Users\maxre\Nextcloud\zSyncMain\ssh\starseed_signup_feed_key.txt`
- **Notifier script**: `C:\claude_base\tools\starseed_signup_notifier\notify.py`
- **Notifier scheduled task**: on Centauri (`192.168.1.176`), task name `StarseedSignupNotifier`, triggers every 5 min, runs `C:\Users\maxre\AppData\Local\Python\bin\pythonw.exe` (hidden)
- **Max's email**: `max@dnaresonance.org`
- **DeepSeek key location**: read from `C:/Users/maxre/Nextcloud/zSyncMain/ssh/deepseek_api_key_20251112.txt` (in the Python script)
- **Blog DB**: `maxrempel-site` D1, table `blog_posts`; publishing done via `claude` Cloudflare API client (using OAuth token from MCP), using `insert` SQL
- **Backups**: live worker backed up in `C:\claude_base\backups\cf_workers\starseed-site\history\` before changes

## Gotchas & Dead Ends

- **Cloudflare edge caching** - during verification, some endpoints (especially the apex `starseedgenetics.com/`) served stale content for several minutes. Workaround: add a random query parameter (`?cb=$RANDOM`) and retry; eventually the cache purges.
- **scp to Centauri fails** - the server's SSH banner (post?quantum key exchange message) broke `scp`'s binary protocol. Solution: encode file to base64 locally, pipe through `ssh`, and decode with PowerShell on Centauri.
- **Git auth on Centauri** - Centauri wasn't logged into GitHub; couldn't `git pull`. File transfer via PowerShell was used instead. The notifier script now lives locally on Centauri and will need manual update if changed.
- **Google Form edit** - never attempted; we replaced forms entirely with native ones, avoiding Google's quirks.
- **Price $388 vs $379** - we first set to $388 (Max's memory), then to real $379 after live check. All occurrences updated.
- **Long?read/PacBio removal** - we had to touch meta description, home main column, home rail, Participate text, Updates page, Donate page, and delete the forwarding page function; a thorough grep ensured no leftovers.
- **Worker `POST` not reaching handler** - first test returned 303 but no row inserted because Cloudflare edge hadn't propagated the new worker. Waited and retested; subsequent tests succeeded.
- **Honeypot bot protection** - a hidden field in the signup form (`honey`) discards submissions that fill it; tested and working.
- **Questionnaire answers stored as text** - the native form packs answers into a single `questionnaire_answers` column as lines like "Q: ... ? A: ..."; no structured JSON, but easy to read.

## Summary for a Cold Session

All requested changes to Starseed Genetics are live. The site has a clean, single?pitch design with native registration and newsletter forms writing into the unified contacts DB. A real?time email alert system (with DeepSeek summaries) is running on Centauri. Two blog posts are published on maxrempel.com. No immediate action required; wait for Max's next direction. If reviving a development task, work from the `worker.js` source in `sites/starseed-site/` and remember the D1 binding is already configured. The notifier token and DeepSeek key are in Max's Nextcloud ssh folder.
