# Scribe handover - milestone 6 (~93K tokens)
# session: 20260609_vigorous_gould_3d960c_02725b78
# cwd: C:\moma\.claude\worktrees\vigorous-gould-3d960c
# written: 2026-06-09 15:59:44 by claude-opus-4-8

# HANDOVER

## GOAL (in Max's words)
"Broll is inserted as a title slide, hurray, it is 8 sec, but in player (mb player) it plays only for 2-4 seconds. Something is weird. Most likely there is a time when it starts playing invisibly while the mplayer thinks and then i see only a tail of the 8 second title. Please fix."

Max has switched the current assignment. The active task is **debugging a playback-duration problem** with b-roll-as-title-slide clips. Everything below about the broader b-roll/music design discussion is now **background only** - it is not the current task.

## CURRENT ASSIGNMENT (the live one)
A b-roll has been successfully inserted as a title slide. Its intended/declared duration is **8 seconds**. When played back in the player ("mb player" / "mplayer" - Max's shorthand for the project's media player), it only displays for **2-4 seconds** instead of the full 8.

Max's own hypothesis (worth testing first): the clip **starts playing invisibly** during a startup/loading delay - the player "thinks" (buffers/initializes) while the clip's playhead is already advancing - so by the time the frame becomes visible, several seconds have elapsed and Max only sees the **tail end** of the 8-second slide.

In plain terms: the clock likely starts before the picture does. The fix probably involves making the visible-start and the timeline-start coincide - e.g. not advancing the playhead until the player is actually rendering/ready, or preloading the title slide so there is no "thinking" gap.

## EXACT NEXT STEP
1. Locate the player code (Max calls it "mb player" / "mplayer") and find where a title-slide / b-roll clip's duration is set and where playback begins.
2. Confirm the slide's declared duration really is 8s in the data/spine, so the bug is in playback not authoring.
3. Investigate the startup gap: check whether the playhead/timer starts before the slide is loaded and visible (buffering, async load, render warm-up). This matches Max's "plays invisibly then shows only the tail" theory - verify it before fixing.
4. Fix so the full 8 seconds are visible: ensure the duration timer starts only once the frame is actually displayed, or preload the slide so there is no invisible head.

## CURRENT STATE
- No tool calls or investigation have happened yet this session - the player code has not yet been opened or read.
- The b-roll insertion as a title slide is reported by Max as **working** ("hurray"). The defect is purely in playback duration.
- Working directory is a git worktree: `C:\moma\.claude\worktrees\vigorous-gould-3d960c`.

## KEY PATHS / IDS / NAMES (to discover)
- The project is "moma."
- The player is referred to as "mb player" / "mplayer" - exact module/file name not yet identified; find it first.
- Core concepts in this codebase: **spine** (ordered timeline of lines), **libretto** (Notion-based source of lines, with `line_hash` per line), **lipsie** (the normal media slot content / lip-synced clip), **sass** (the TTS component), and **media slots** that hold either a lipsie or a clip.
- A title slide / b-roll occupies a media slot like a normal line but holds a clip instead of a lipsie.

## BACKGROUND - earlier b-roll / music design discussion (NOT the current task)
This was the conversation before Max changed the assignment. Preserve it in case Max returns to it.

**B-roll insertion design (agreed direction):**
- A b-roll is treated as a "line with no spoken text." It gets a libretto entry (empty or parenthetical text), receives a `line_hash`, and flows into the spine like any other line. Its media slot holds a clip instead of a lipsie. No new ordering machinery needed - b-rolls are just **silent lines**.
- **Open decision (was awaiting Max):** how to mark a line as "b-roll, no voice" so **sass** doesn't try to TTS it. Proposed solution: a tag in the libretto such as `[BROLL]` that the parser recognizes and skips.

**Music overlay (Max was anxious - "Panic", "way beyond moma design"):**
- Reframe given to reduce panic: music is **not** a per-line or per-spine concept. It is a **separate layer riding on top of the finished spine timeline** - one long track with in/out times, independent of line boundaries.
- Conclusion: music does **not** belong in moma's line model; it's a **final-assembly overlay**, a different layer, not a redesign of moma.
- This was deferred - Max chose to do neither and pivoted to the playback bug instead.

## OPEN QUESTIONS (deferred, not blocking current task)
- How exactly to tag a b-roll line so sass skips TTS (the `[BROLL]` tag idea was proposed but not confirmed by Max).
- Music overlay implementation was discussed only at the conceptual level; no implementation decided.

## GOTCHAS
- Do not get pulled back into the b-roll/music design brainstorm - Max explicitly said "Changing assignment." The only live task is the **8s-slide-plays-for-2-4s** bug.
- Trust Max's diagnostic instinct (invisible head / showing only the tail) as the leading hypothesis, but verify the declared duration is actually 8s before assuming it's a playback-timing issue.
