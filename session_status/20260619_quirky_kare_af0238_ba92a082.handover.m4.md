# Scribe handover - milestone 4 (~365K tokens)
# session: 20260619_quirky_kare_af0238_ba92a082
# cwd: C:\moma\.claude\worktrees\quirky-kare-af0238
# written: 2026-06-19 12:07:24 by deepseek-v4-pro

# HANDOVER - DNAvibe meeting transcript pipeline

## GOAL (Max's words, final prompt)

> "Make a weekly script to download the thing automatically. Say, on Sat, or any idle time but not immediately after the computer goes awake. Give it an hour of work, and then the first idle to download. I guess it can be an autonomous job? Whatever is easy."

In plainer English: Automate pulling fresh Read AI meeting transcripts (DNAvibe weekly huddles) roughly once a week, at idle/low-impact times, without firing right when the machine wakes up.

---

## DECISIONS MADE + WHY

1. **Read AI has a public API (OAuth 2.1) + official MCP server at `https://api.read.ai/mcp`** - no scraping needed. We wired it into Claude Code and confirmed it works. The endpoint is NO trailing slash (with slash = 404).

2. **Account identity for transcripts: max.rempel2@gmail.com, authenticated via Zoom login (max@tamza.com).** This is the Read AI account that actually owns the meetings. Max's dnavibe email (max@dnavibe.com) is forwarded into max.rempel2, and logging into Read with max@dnavibe (Microsoft) gave "request access" - Tony/Perry's workspace, not Max's. The Zoom identity is the one that lists the meetings.

3. **Transcripts go to Nextcloud, not Notion.** Max explicitly wants a single canonical DNAvibe hub in Nextcloud and doesn't want to pollute Notion with raw transcripts. Folder: `C:\Users\maxre\Nextcloud\dnavibe\meeting_transcripts\`.

4. **The Notion DNA Vibe Meetings DB still exists** (under DNA Vibe Essentials) - it has today's row (Perry hosting, 15 attendees) as a template/placeholder. Transcripts go to disk; Notion DB is for meeting metadata/notes, not raw text dumps.

5. **Personal non-DNAvibe meetings get separated** into `_personal_nondnavibe\` subfolder so the hub stays clean.

6. **8 transcripts in Max's account today, not 19.** Tony shared ~19 weekly recaps to Max's email, but only 8 landed in his actual Read AI account. The other ~14 live in the dnavibe Read workspace (gated behind admin approval). Today's June-19 meeting wasn't in the account yet - still processing, so a re-pull is needed.

---

## CURRENT STATE

### What's DONE:

- **Read AI MCP** wired and authenticated in this worktree (`quirky-kare-af0238`) under `C:\moma` project key. Tools: `list_meetings`, `get_meeting_by_id`. OAuth verified working (Zoom login flow).
- **8 transcripts pulled and saved** to `C:\Users\maxre\Nextcloud\dnavibe\meeting_transcripts\`:
  - 5 DNAvibe: 2025-08-08 Weekly Team Huddle, 2025-09-08 Instagram Team Training, 2026-05-15 Weekly Team Huddle, 2026-05-22 Weekly Team Huddle, 2026-06-12 Weekly Team Huddle
  - 3 personal (in `_personal_nondnavibe\`): two audio-test Zooms + RedValleyRetrievers call
  - Each file is a clean `.md` with summary, topics, action items, key questions, and full speaker-labeled transcript.
- **`README_tomemex.md`** in that folder documents the source, tools, and how to refresh.
- **Two reusable Python tools** committed to `claude_base`:
  - `tools/readai_transcripts/convert_meeting.py` - takes a saved Read AI JSON result and writes a clean `.md` transcript
  - `tools/readai_transcripts/extract_from_session.py` - scans a Claude Code session JSONL, finds all `get_meeting_by_id` results, converts each
- **Notion DNA Vibe Meetings DB** created (under DNA Vibe Essentials) with today's row ready.

### What's in FLIGHT (the automation Max just asked for):

- A weekly script/task to pull new transcripts automatically. Not built yet.
- Requirements from Max: run ~weekly (Saturday), only at idle time, wait ~1 hour after machine wake, don't run immediately on wake. Autonomous preferred, but "whatever is easy."

### Related side-work DONE in this session:

- **Playwright MCP** now configured for persistent profile with Bitwarden + Grammarly extensions (launch flags in `pw_mcp_config.json`, extensions extracted to `C:\claude_base\playwright_profile\extensions\`). Verified both load correctly in a headful Chromium.
- Config patched across multiple Claude Code project keys in `.claude.json` to ensure the extensions load on next restart.

---

## EXACT NEXT STEP

**Design and build a weekly autonomous job that:**
1. Uses the Read AI MCP to list meetings and download any new transcripts not already on disk.
2. Saves them to `C:\Users\maxre\Nextcloud\dnavibe\meeting_transcripts\` (using the existing `convert_meeting.py` logic).
3. Can be called from a script (not a full Claude Code session) - OR - called via a lightweight Claude Code invocation on a schedule.
4. Respects timing constraints: not immediately on wake, wait ~1 hour of uptime, then run at first idle opportunity within a window (e.g., Saturday).

**Key question to resolve first:** Does the Read AI MCP OAuth token persist across sessions/restarts? If tokens expire after 10 minutes (Read's stated TTL), automation needs either a long-lived refresh token (Read's OAuth scopes include `offline_access`, so this may work) or a re-auth flow. If re-auth is needed, the "autonomous" part gets harder - might need a scheduled Claude Code session that does the auth as part of the script.

---

## OPEN QUESTIONS AWAITING MAX

1. **"Whatever is easy" - which scheduler?** Windows Task Scheduler? A cron-like approach via WSL? A Claude Code agent that self-schedules? Need Max's preference.
2. **OAuth token persistence:** Does the readai MCP's OAuth refresh token survive across Claude Code restarts? If not, the automation needs to handle re-auth (which means Playwright + Zoom login, which means Max's interactive presence - or Bitwarden auto-fill, now that BW is wired in).
3. **"Idle time" detection:** How should the script detect idle? Windows idle timer? CPU load threshold? Max didn't specify detection method.
4. **Scope:** Pull *only* new meetings since last run? Or re-sync everything (in case missing transcripts populate later, like June 19)? Leaning toward delta-only (new since last pull).
5. **The missing ~14 transcripts in Tony's workspace:** Should we pursue dnavibe workspace admin access to get the full set, or is the current 8-meeting sync enough?

---

## KEY PATHS, IDs, COMMANDS

| What | Path/Value |
|---|---|
| Transcript folder (Nextcloud) | `C:\Users\maxre\Nextcloud\dnavibe\meeting_transcripts\` |
| Read AI MCP endpoint | `https://api.read.ai/mcp` (NO trailing slash!) |
| Read AI account | max.rempel2@gmail.com (auth via "Sign in with Zoom" ? max@tamza.com) |
| Converter script | `C:\claude_base\tools\readai_transcripts\convert_meeting.py` |
| Extractor script | `C:\claude_base\tools\readai_transcripts\extract_from_session.py` |
| Playwright profile dir | `C:\claude_base\playwright_profile\` |
| Playwright config | `C:\claude_base\playwright_profile\pw_mcp_config.json` |
| Bitwarden ext path | `C:\claude_base\playwright_profile\extensions\bitwarden\` |
| Grammarly ext path | `C:\claude_base\playwright_profile\extensions\grammarly\` |
| Claude Code config | `C:\Users\maxre\.claude.json` |
| Worktree key for MCPs | `C:\moma\.claude\worktrees\quirky-kare-af0238` (plus `C:\moma` root key) |
| Notion DB | "DNA Vibe Meetings" under "DNA Vibe Essentials" |
| Claude base repo | `C:\claude_base` (already committed + pushed) |

**Useful commands:**
- List meetings via MCP: `mcp__readai__list_meetings`
- Get one meeting: `mcp__readai__get_meeting_by_id` with `meeting_id` param
- Convert saved JSON to MD: `python C:\claude_base\tools\readai_transcripts\convert_meeting.py <json_file> <output_dir>`
- Search local files: `C:\claude_base\tools\es\es.exe -n 200 "dnavibe"`

---

## GOTCHAS & DEAD ENDS ALREADY RULED OUT

1. **Read AI endpoint trailing slash = 404.** Must use `https://api.read.ai/mcp` exactly. Slash version was registered first and broke - fixed by removing and re-adding the MCP.

2. **max@dnavibe.com (Microsoft login) = "request access"** - dead end for API. The dnavibe Read workspace is a *different* workspace owned by Tony/Perry, not Max. Max's actual Read account is max.rempel2@gmail.com via Zoom auth.

3. **Gmail search via Playwright for transcripts was ruled out** - Max correctly pointed out that he has a Gmail connector. But even with the connector, the "View report" links in Tony's emails go to Read AI (login-walled). The API/MCP route is the correct one.

4. **No existing Nextcloud transcript folder** - we created `dnavibe/meeting_transcripts/` fresh. The existing `202603_dnavibe_*` Nextcloud folders are clip-pilot specific, not meeting transcripts.

5. **Read API token TTL is 10 minutes** - but the OAuth scopes include `offline_access`, which means refresh tokens should exist. Whether the MCP client actually refreshes them is unverified. If it doesn't, automation breaks without re-auth.

6. **Today's meeting (June 19) not yet in Read AI** - transcripts take time to process after the meeting ends. A re-pull later will catch it.

7. **`claude mcp add` with `--transport http` via CLI may place the MCP under a different project key than the current worktree.** We had to manually patch `.claude.json` to copy the readai + Playwright config entries to the `quirky-kare-af0238` worktree key. Something to watch if re-running setup commands.
