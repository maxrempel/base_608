# Scribe handover - milestone 2 (~154K tokens)
# session: 20260625_uspicious_noether_c28301_154694b6
# cwd: C:\moma\.claude\worktrees\suspicious-noether-c28301
# written: 2026-06-25 15:45:47 by deepseek-v4-pro

# HANDOVER - D38: Fix Broken Thumbnails on Combo Runner Screenshot

## GOAL (in Max's words)
> Can you fix the broken thumbnails on the screenshot? I think there are lots of images missing.

## DECISIONS + WHY

1. **Investigated the API, not the frontend first.** The tiles rendered with label badges ("Pn") but no image, meaning the src URL was failing. Probing `/api/jobs` and `/api/plates` on `localhost:8779` was the right starting point because thumb URLs are built server-side from `legacy_plate_id`.

2. **Root cause: `legacy_plate_id=NULL` in D1.** 56 plate rows in the D1 database had no `legacy_plate_id`. The API returned `plate_id=null`, so the frontend constructed badge text "Pnull" and thumb URL `/thumb_by_plate/null`, which 404'd. This matched the screenshot symptom exactly - bare tile skeletons with only the "Pn" badge visible.

3. **Fix: backfill `legacy_plate_id` in D1, not code.** The problem was data, not logic. Assigned fresh sequential IDs 1114-1169 (immediately above the prior max of 1113). No code change was needed - the existing thumb-resolution logic works fine when it has a real ID. Pure D1 data fix.

4. **Did not commit.** Other sessions' WIP was already in the worktree (`suspicious-noether-c28301`); manually committing a D1-only change would have tangled things. The fix is live in D1 and survives restarts.

## CURRENT STATE

- **Done:** 56 plate rows backfilled with `legacy_plate_id` values 1114-1169.
- **Done:** Temp probe scripts and JSON dumps cleaned up (`_d38_probe.py`, `_d38_backfill_lpid.py`, `*_dump.json`).
- **In flight:** Nothing. The immediate visual breakage is fixed. Refreshing the Imager UI should show restored thumbnails.
- **Leak still open:** Whatever code path creates `role='plate'` rows (likely in `fire_job` or the plate-creation flow) is still inserting rows without minting `legacy_plate_id`. New plates will break again unless this is plugged.

## EXACT NEXT STEP

If Max confirms the thumbnails now look correct on refresh: **hunt down the leak in the plate-creation path** that is inserting `role='plate'` rows without setting `legacy_plate_id`. The suspect locations are in `runner_core.js` (the `fire_job` / plate-creation logic) and possibly `combo_gui.py` (the Python-side plate handling). The fix would ensure every new plate row gets a valid `legacy_plate_id` at insert time, so this never recurs.

## OPEN QUESTIONS (awaiting Max)

- **"Want me to hunt that down next?"** - The assistant explicitly asked whether to fix the root-cause leak in the plate-creation code path. No answer yet.
- Does the Imager UI need a hard refresh or cache clear to pick up the fixed thumb URLs?

## KEY PATHS / IDS / COMMANDS

| What | Value |
|---|---|
| Worktree | `C:\moma\.claude\worktrees\suspicious-noether-c28301` |
| App code | `C:\moma\sc10\combo_runner\code\combo_gui.py` |
| Job runner | `C:\moma\sc10\combo_runner\code\runner_core.js` |
| DB module | `moma_db` ? `D1Client` |
| API base | `http://localhost:8779` |
| API endpoints | `/api/jobs`, `/api/plates` |
| Thumb route pattern | `/thumb_by_plate/<legacy_plate_id>` |
| Prior max `legacy_plate_id` | 1113 |
| New IDs assigned | 1114-1169 (56 plates) |
| Affected rows count | 56 |
| D38 check-in | Via `bcast.py whoami D38` |

## GOTCHAS & DEAD ENDS

- **Do NOT look for a frontend bug.** The broken images were entirely a backend data problem - null `legacy_plate_id` producing 404 thumb URLs. Any attempt to fix this in CSS/JS/HTML would be a dead end.
- **The "Pnull" badge is the diagnostic signature.** If you see "Pnull" on a tile in the Imager, a plate row is missing its `legacy_plate_id` in D1. That's the canary.
- **Other sessions' WIP is in this worktree.** The assistant deliberately avoided committing because the tree had uncommitted changes from other sessions. Any future commits here should check `git status` first.
- **The probe used `curl` piped to Python one-liners** to inspect the live API without touching running processes. This pattern (dump JSON to file, then parse with `json.load`) is reliable and non-destructive - use it again if similar API debugging is needed.
- **`moma_db.D1Client` was used for the backfill.** The exact method for executing SQL wasn't transcribed but is available in that module.
