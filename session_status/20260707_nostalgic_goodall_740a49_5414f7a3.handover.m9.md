# Scribe handover - milestone 9 (~697K tokens)
# session: 20260707_nostalgic_goodall_740a49_5414f7a3
# cwd: C:\claude_base\.claude\worktrees\nostalgic-goodall-740a49
# written: 2026-07-07 18:21:23 by deepseek-v4-pro

# HANDOVER - X12B, P2 "NPA", population-wide de-novo drift direction

---

## GOAL (in Max's words)

**"The whole-population drift: where does it shift?"** Not the 5% subgroup hypothesis, not foreign DNA chunks (Track A / P3), not regional clusters of edits (separate sub-projects). This lane is: **aggregate all real de-novo changes across all 602 trios and measure whether the population is being pushed in a systematic direction, every generation.** The direction is unknown - it might be toward ancestral/primate, toward novel bases, or nowhere.

The method Max landed on: measure the **mutation spectrum** (which exact letter?letter changes occur, in their neighbor context) and compare it to the **known published human de-novo spectrum** from large trio studies. If our spectrum matches the textbook baseline ? ordinary drift, nothing special. If a specific change type is in systematic excess ? candidate "something is steering," and the excess names the direction concretely.

Max also raised (and has not yet decided between):
- **Hotspots**: filter to recurrent identical changes in ?5 unrelated families, classified by exact change (e.g., A?G vs A?C), to find real targeted spots.
- **Hotspots + gene-set overlay**: Max mentioned "autistic genetics" but did not name a specific gene set - he was thinking of SFARI/autism genes, but this is unconfirmed.

---

## DECISIONS MADE + WHY

### 1. The previous primate-ancestry analysis is dead (Max killed it)
Max called the whole ancient-vs-modern drift measurement "bullshit." The honest reason it failed:
- Most "non-parental alleles" in ordinary families are genotyping artifacts (dropouts, mis-calls), not real de-novo mutations.
- My analysis was per-position and artifact-dominated. No per-letter "direction" survives that noise.
- Even the corrected version (catdrift3.py, scrambled-parent null, all 23 chromosomes) that showed NPAs land on the ancestral base ~13% of the time (below chance) is now considered a measure of sequencing-artifact statistics, not biology.
- **The key missing step: we never filtered for genuine de-novo events using quality scores (genotype confidence, read depth, parental absence verified by reads).**

### 2. The population-wide approach replaces the per-position approach
Instead of labeling every single surprise letter ancient/modern (noise-dominated), the new plan is:
- Filter to real de-novo changes first.
- Aggregate the mutation spectrum - all types of letter changes in their trinucleotide context.
- Compare to the known human germline de-novo spectrum (from publications like J?nsson et al. 2017, or gnomAD).
- This captures ALL directions (including the "3rd and 4th base" jumps Max correctly pointed out my binary ancient/modern missed), not just ancestral-vs-derived.

### 3. A/B/C framing
Max clarified that the alien-trace subtypes are split across sub-projects:
- **(A) Foreign DNA chunks** ? different sub-project (P3/OMEGA, Kristen's lane).
- **(B) Clusters of edits in one region** (Max's 16kb run) ? different sub-project.
- **(C-like) Population-wide fingerprint/drift** ? **our lane (X12B, P2).**

### 4. The 5% hybrid hypothesis is parked for this lane
Max explicitly said: "we don't have to stick to 5%. What if the whole population is manipulated to shift somewhere?" So the analysis is on ALL 602 trios, not a subset.

### 5. Box strategy: stopped, kept, $5/mo
The AWS box (i-00c83a0af889f8bf1, c7i.8xlarge, us-east-1) is STOPPED, not terminated. It has the 1000-Genomes VCFs accessible in-region via S3, the ancestral FASTA files, the pedigree, and catdrift3.py. It should only be restarted when a concrete compute job is ready.

---

## CURRENT STATE

### Delivered and committed
- **NPA catalog** - per-SNP: `outputs/real/NPA_catalog_perSNP.tsv` (902,249 NPA calls, 140,849 distinct sites, 602 trios, with recurrence column). Per-region: `NPA_catalog_perRegion.tsv` (108,606 mislands). Population column needs filling from the pedigree (currently "?").
- **Primate-drift report** - `outputs/real/P2_primate_drift_bycategory_genomewide_v01.md` (genome-wide, by category, proper null - but Max has rejected its interpretation). Raw data in `outputs/real/gd3/` (per-chromosome .out files from catdrift3.py).
- **Goal doc** - `outputs/real/GOAL_primate_ancestry_of_NPAs_v01_tomemex.md` (now stale - documents the approach Max rejected).
- **Handoff** - `session_status/20260706_X12B_P2_reopened_handoff.md` (contains full recipe for the primate-ancestry job, box commands, etc. - now historical reference).
- **NPA type histogram + validation** - all 602 trios passed Mendelian trio validation on chr21+chr22. Type breakdown: both-hom, both-het, not-from-father, not-from-mother, other.
- **222 hotspot recurrence map** - from X11B, committed. Recurrence sits in hyper-variable genotyping-artifact gene families (HLA, OR, TAS2R, keratin).

### What is NOT done (the actual next work)
- **No real de-novo filter has been applied.** Every NPA in the catalog is "child allele not in parents" at the genotype level - no quality-score or read-depth verification. This is the critical missing step.
- **No mutation spectrum has been computed.**
- **No comparison to known human de-novo spectrum exists.**
- **Population column in the catalog is unfilled** (pedigree join is trivial, one-liner).

### Box state
- **Stopped.** i-00c83a0af889f8bf1, us-east-1. IP rotates on start/stop; current IP unknown (needs `aws ec2 describe-instances` query after start). SG sg-0651e79bde1c34bd0 needs the new IP added before SSH.
- SSH key: `C:/Users/maxre/Nextcloud/zSyncMain/ssh/xg1_aws.pem`
- On the box: `~/catdrift3.py`, `~/xg1/anc/homo_sapiens_ancestor_GRCh38/` (ancestral FASTA per chromosome), `~/xg1/g1k.ped`, `~/gd3/` (per-chrom .out files, already scp'd local).
- Box has no local VCFs - it streams them from S3 in-region (`s3://1000genomes/1000G_2504_high_coverage/working/20201028_3202_raw_GT_with_annot/`).

---

## EXACT NEXT STEP

**Pilot: one chromosome, apply real de-novo quality filter, compute the mutation spectrum, compare to published human baseline.** No big claims - just see if the signal is measurable and what it looks like.

Concrete sequence:
1. **Define the de-novo quality filter.** This means: child genotype called with confidence (GQ ? threshold), good read depth, AND both parents have zero reads supporting the child's allele (not just called homozygous - actually absent in the reads). The VCF FORMAT fields (GQ, DP, AD) carry this. On the box, streaming one chromosome, extracting these fields per trio.
2. **Apply it to one chromosome** (chr22 is smallest and fastest) on the box. Count how many NPAs survive the filter.
3. **Compute the mutation spectrum** for the survivors: count each exact change type (ref?alt) in its trinucleotide context.
4. **Fetch the known human germline de-novo spectrum** from published data (J?nsson et al. 2017 Nature, or the gnomAD de-novo resource). Compare: chi-square or Poisson excess per change type.
5. **Report to Max honestly**: survival count, top excess change types (if any), and whether the spectrum matches the known baseline or deviates.
6. **If the signal is measurable and interesting:** scale to all chromosomes (resumable parallel streaming, same pattern as catdrift3.py). If it's too sparse on chr22: discuss with Max whether to combine chromosomes or refine the filter threshold.

---

## OPEN QUESTIONS STILL AWAITING MAX

1. **What gene set for hotspot overlay?** Max mentioned "autistic genetics" / SFARI but never confirmed. The NPA catalog is built with recurrence; overlaying genes is a quick follow-up once the gene set is named.
2. **Haplotype clustering:** Max asked for clustering NPAs by family and by location. Not started. Needs decision on method (shared-haplotype detection in regions with recurrent NPAs).
3. **Experiencer genomes:** Max noted the real ~5% hybrid test needs experiencer trios, not 1000G controls. No experiencer data is available yet - this is a future dependency, not currently actionable.
4. **The "where are we moving" direction:** Max said he doesn't know - the answer has to come from the data. The mutation spectrum approach is my proposal; Max has not yet confirmed it as the method. He said "let's brainstorm" and "do you have better ideas?" - so this is still open for his steer.

---

## KEY FILE PATHS

All under `C:/claude_base/projects/XG1/kenefick/paper_repro/`:

| File | Description |
|---|---|
| `outputs/real/NPA_catalog_perSNP.tsv` | 902k NPA calls, 140k sites, with recurrence |
| `outputs/real/NPA_catalog_perRegion.tsv` | 108k NPA regions |
| `outputs/real/P2_primate_drift_bycategory_genomewide_v01.md` | Completed primate-ancestry report (Max rejected) |
| `outputs/real/gd3/` | Raw per-chromosome .out files from catdrift3.py |
| `outputs/real/GOAL_primate_ancestry_of_NPAs_v01_tomemex.md` | Stale goal doc |
| `scripts/npa_detector.py` | Core NPA detector (60-SNP windows, segdup mask) |
| `scripts/trio_validate.py` | Per-trio Mendelian validation |
| `scripts/npa_typecount.py` | NPA type classification |
| `genome_out/` | Local per-chromosome calls + regions (also on S3) |
| `masks/g1k.ped` | Pedigree (602 complete trios) |
| `outputs/real/payload_candidates_v3_moderate_band.tsv` | X11B's 222 candidate hotspots |

Handoff: `C:/claude_base/session_status/20260706_X12B_P2_reopened_handoff.md`

---

## GOTCHAS AND DEAD ENDS

1. **Do NOT repeat the per-position ancient-vs-modern analysis.** It's artifact-dominated, Max called it bullshit, and the interpretation over-reached. The catdrift3.py code and gd3/ data are preserved but should not be re-run or extended.

2. **Do NOT re-run the 222-hotspot aggregator pipeline.** It's done, and the result (recurrence sits in hyper-variable genotyping-artifact gene families) is committed. No new signal will come from re-filtering those same 222.

3. **Do NOT restart the ancestors/permutations subgroup machinery.** Max explicitly told me three times to stop the per-child significance and null-permutation tests. That whole approach is parked.

4. **The reference-allele tautology trap.** Any "direction" analysis that labels an allele as ancient/modern based solely on the reference base will be dominated by the fact that NPA alleles are almost always the non-reference (ALT) base. The proper fix is: (a) filter to real de-novo events first, and (b) use the mutation spectrum (which has a known biological baseline) rather than inventing a null from the data.

5. **Box IP rotates on stop/start.** Always query the current IP (`aws ec2 describe-instances`) and add it to SG sg-0651e79bde1c34bd0 before SSH. Box auto-stops on a timer - do not leave it running unattended.

6. **Never `git add -A`.** Session nearly had a 1GB disaster. Explicit paths only.

7. **The nospiral token** bypasses the suicide-prevention hook at `C:/claude_base/tools/suicide_prevention/block_death_spiral.py`. Use it for any SSH/box command.

8. **Do not wake teammates.** Max said to conserve the weekly API limit and work solo. X11B and X12F are separate lanes; their board posts are informative but do not require action from us.

9. **The 1000G genotype VCFs are `*recalibrated_variants.vcf.gz`**, NOT the sites-only `*.annotated.vcf.gz`. The S3 path pattern is: `s3://1000genomes/1000G_2504_high_coverage/working/20201028_3202_raw_GT_with_annot/20201028_CCDG_14151_B01_GRM_WGS_2020-08-05_chrN.recalibrated_variants.vcf.gz`.

10. **Single-parent NPA categories** (not_from_father, not_from_mother) do not carry an allele absent from both parents - they're a different signal (one parent has it, child doesn't match that parent). Only both-hom and both-het are genuine "neither parent" events.
