# Scribe handover - milestone 4 (~301K tokens)
# session: 20260705_nice_maxwell_b61025_66b4058f
# cwd: C:\claude_base\.claude\worktrees\nice-maxwell-b61025
# written: 2026-07-05 23:05:30 by deepseek-v4-pro

# HANDOVER - D06C (in-progress investigation of storyboard image disappearance)

## GOAL (in Max's own words)

> "When ellipsis is made, produced, the image just disappears. It is just gone. The image should be bumped from the primary spine to the secondary spine, but it shouldn't be gone. Like, when ellipsis is produced, when the reel is produced, the image should be bumped to the second spine and never deleted, never junked. Right now it just disappears, which is super idiotic. You should look for, not just fix, not patch it, but look for underlying trouble. It's a very wrong behavior, so somewhere there should be a huge problem which needs to be investigated and discovered what it is. You cannot just patch it. The goal is not to patch it, but to discover what's the cause of that stupidity."

## DECISIONS MADE AND WHY

- **No code was touched for this bug.** Only diagnosis was done. Reason: user explicitly said "look for underlying trouble ... not patch it"; and the file is currently being edited by other sessions (D02A/D03B) for spot re-splitting, so editing now would cause collisions.
- **Investigated root cause, not surface rendering.** Traced data flow, not just UI behavior. Verified that images are NOT deleted or junked - they survive in the database as approved jobs with hide_tile=0.
- **Identified the actual problem as a spot-identity stability issue.** The storyboard uses the exact set of line numbers merged into a spot as its "address" (e.g., `"2,3,4,5,6,7,8,9"`). When spot merges/splits happen (and they are happening right now due to other sessions' work), the addresses change. Images placed under old addresses become orphaned: still in the DB but never rendered, appearing as if deleted.
- **Provided Max a choice of fix strategy (not yet decided):** (a) redesign spot identity to be stable and migrate old addresses; (b) write a one-time recovery script to re-union the currently stranded images; or (c) both. No response from Max before session ended.

## CURRENT STATE - WHAT IS DONE, WHAT IS IN FLIGHT

- **Diagnosis complete and delivered to Max.** The exact mechanism is understood and explained.
- **Four specific stranded images in Scene 11 were identified:** J3031, J3114, J3160, J3256. They exist and are approved; they are invisible on the storyboard because they sit under the old spot_order key `"2,3,4,5,6,7,8,9"` while the current board has split that into two spots (`"2,3,4,5,6,7"` and `"8,9"`). Neither key matches the old key, so neither spot renders them.
- **Dynamic environment:** other sessions D02A and D03B are actively re-splitting scenes, so more orphanings may occur. This needs to be fixed before it gets worse.
- **Storyboard itself is otherwise healthy:** earlier in this session D06C fixed two separate bugs:
  1. Imager filter pollution (server-side plate scoping in combo_gui.py + runner_core.js) - already committed and pushed.
  2. Drag-off-spine behavior: now silently returns images to pile, only prompts junk for lipsies - committed and pushed as v2.60.
- **The file at stake for the current bug:** `storyboard_editor_v2.html` (currently v2.60, D06C's last change). It contains the `runAutoPlacement` logic and spot rendering, but not the spot key definition; that's in shared helper functions (groupLines, spotKeyOf, etc.).
- **Server endpoints involved:** `/api/reel_membership`, `/api/approved_images`, `/api/arrangements`, `/api/spot_order` (all on port 8790). The spot_order is stored in D1; data models are unchanged.

## EXACT NEXT STEP

**Wait for Max to reply** to the diagnosis. He has not yet chosen which action to take:
- He may say "yes, do the stable-identity fix now" - which would require coordinating a window with D02A/D03B (they are editing the same file) and implementing a stable spot ID plus migration logic.
- Or he may say "just recover the 4 images for now and do the real fix later" - which would be a small script to reassign those images to current spot keys.
- Or something else.

Until Max responds, **do nothing** to the storyboard code.

## OPEN QUESTIONS STILL AWAITING THE USER

- Which fix approach: stable identity + migration, or one-time recovery now and full fix later?
- If stable identity: how should a spot be identified? First line hash? Explicit spot_id? The user's mental model of "primary" and "secondary spine" should be preserved.

## KEY FILE PATHS, IDs, NAMES

- **Storyboard UI:** `C:/moma/sc10/sound_assembly/code/storyboard_editor_v2.html` (v2.60, contains pile-drop fix and spot rendering)
- **Storyboard server:** `C:/moma/sc10/sound_assembly/code/slideshow_server.py` (handles D1 endpoints for spot_order, etc.)
- **Data endpoints (D1 via HTTP):**
  - `http://localhost:8790/api/approved_images`
  - `http://localhost:8790/api/reel_membership?scene=11`
  - `http://localhost:8790/api/arrangements`
  - `http://localhost:8790/api/spot_order?arr_id=...`
- **D06C's branch/check-in:** Identified as D06C on the bulletin board. Used `C:/claude_base/branch_bulletin/bcast.py` for coordination.
- **Stranded image job IDs:** J3031, J3114, J3160, J3256 (all approved, hide_tile=0, exist in DB).
- **Previous imager fix (irrelevant to current bug but part of session):** `C:/moma/sc10/combo_runner/code/combo_gui.py` and `runner_core.js` - committed as `b9c5aa3`.

## GOTCHAS AND DEAD ENDS ALREADY RULED OUT

- **Do NOT assume images are deleted/junked.** They are not. Verified by querying live DB: all four source images exist and are approved. The bug is purely in the spot-address instability.
- **Do NOT assume the render pipeline is broken.** `runAutoPlacement` correctly writes the image into spot_order when a reel lands; the image is bumped to position 2. It's just written under the **current** spot key at that moment. When the spot later changes (due to re?splitting), that key becomes stale and the image is orphaned.
- **The "bump to second spine" logic is not broken** - it works. The problem is that later grouping changes break the association.
- **These orphaned spot_order keys pile up silently** - there is no cleanup or garbage collection.
- **Other sessions are actively re?splitting spots in Scene 11 right now**, so more images will go missing if the root cause is not fixed soon.
- **Avoid editing `storyboard_editor_v2.html` without coordinating with D02A/D03B** (they announced their editing on the board). D06C posted updates to the board; check there for any new coordination messages.
- **The earlier imager fix is completely separate:** it scoped `/api/plates` to arrangement on port 8779; does not affect the storyboard on 8790.

-

**Status:** Diagnosis presented. Awaiting direction from Max.
