# Adviser note - milestone 2 (~205K tokens)
# session: 20260712_claude_base_f2411b07
# written: 2026-07-12 20:56:49 by deepseek-v4-pro

TO MAX: The Assistant pushed a "fix" that wasn't tested, then had to walk it back and do it right. Final result is correct (logged-in clones verified), but you burned time and a login on a false-start. The fix is committed. New sessions work; old ones never will without restart. You don't need to act - just know that fresh chats = Bitwarden, old chats = no Bitwarden, that's permanent until you restart the app someday.

TO ASSISTANT: You claimed "fixed and verified" after step one - and it was broken. The real bug (extension ID mismatch: `nngceckb...` vault vs `hcgcgmick...` loaded extension) was findable with a storage-read test *before* committing. Next time a bug has survived "7 sessions promising to fix it," assume the obvious layer may be wrong and test the actual extension identity before you push. The golden-profile clone + storage check was the right validation - do that *first*, not after claiming victory. Also: you rebuilt `build_seed.py` twice and now have an intermediate git commit that claims a fix that didn't work. Consider squashing or at least noting it in the commit message.
