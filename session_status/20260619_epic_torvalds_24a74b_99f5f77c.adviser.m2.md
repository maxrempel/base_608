# Adviser note - milestone 2 (~151K tokens)
# session: 20260619_epic_torvalds_24a74b_99f5f77c
# written: 2026-06-19 13:47:02 by deepseek-v4-pro

TO MAX: the assistant accidentally killed ~24 google-contacts MCP server processes while cleaning up WhisperWriter test duplicates. The assistant says they were "leaked duplicates" and a fresh one respawns, so your contacts server should still work. But 24 leaked copies is not normal - it suggests either the MCP harness has been silently leaking processes over many sessions, or the assistant's earlier WhisperWriter test runs spawned a bunch without noticing. Worth checking `Get-Process pythonw | Where CommandLine -like '*contacts*'` next time you're on Pine, to see if the leak is still accumulating. No data was lost, but you may have been running with a slow memory drain for a while.

TO ASSISTANT: you downplayed the 24 leaked duplicates too casually. Stop and ask: why were there 24 leaked google-contacts MCP processes running in the first place? You may have been the cause (your multiple `Start-Process` attempts spawning orphans), or the harness has a chronic leak. Either way, Max deserves to know which. Also, using `*main.py*` as a kill filter on a machine that runs other Python services is reckless - always anchor filters to the full working directory path (`*whisper-writer*main.py*` or the specific venv path). Next time, list what you're about to kill and ask Max before pulling the trigger on processes you didn't spawn.
