# Scribe handover - milestone 3 (~226K tokens)
# session: 20260715_angry_moser_2d773b_2163fd6d
# cwd: C:\claude_base\.claude\worktrees\angry-moser-2d773b
# written: 2026-07-15 11:08:26 by deepseek-v4-pro

# HANDOVER: Nadali Project - Anna presenter images and audio ready, video lipsync next

## GOAL (Max's own words)
> "We are making a video presentation on hybrid children... making an image of Anna in a kitchen room... chest up portrait, chest up as a podcast presenter, but she is actually standing... beautiful greens and a window with a view of Earth from the orbit."

> "Next, locate the sound files for that project"

Max brought 28 pre?split WAV files - Anna's narration of his 16?minute UEI talk on alien hybrids - and expects them integrated into the Nadali project immediately, without stalling.

## DECISIONS MADE + WHY
1. **Anna's face / room**: canonical Anna (`anna_approved_headshot_white_cloak_20260330.png`) + approved meeting room (`station_con_a1_ls.png` front view). Chosen because those are the user?blessed locked references.
2. **Presenter framing**: chest?up, slight up?angle (she stands behind a podium?like position). Used the `/edits` endpoint with a composite strip (Anna + room) to keep exact identity.
3. **Camera pan/zoom set**: fed the winning v2 image back as base and generated 6 more: pan left (yaw left, center stays), pan right, zoom in, zoom out, plus two combos. Made via `/edits` so face and room stay locked.
4. **MoMA registration (the big fix)**: initial images were orphan PNGs in Nextcloud outside the MoMA tree. Max caught this. Fixed by:
   - moving all 9 PNGs into `kazarian_episode/nadali/output/`
   - creating `fire_nadali_anna.py` (modeled on `fire_mediakit_portrait.py`) that creates scene `nadali` (id 9) + arrangement, registers each image via `fire_job` as D1 `image` jobs 3290?3298
   - committed and pushed to moma repo
5. **Project type**: non?movie `nadali` scene (like media_kit), not a numbered film scene. Max hasn't objected to this choice.
6. **Audio handling**: the 28 WAV files were already split when Max pointed to them - assistant did **not** split them. Copied from `Videos/max talks/uei_max_talk_20260711/anna_audio` into `kazarian_episode/sound/nadali_production/` to keep the original as backup.

## CURRENT STATE
- **Images**: 9 registered Anna presenter stills (3 base + 6 camera moves). Visible in the MoMA imager under scene "nadali".
- **Audio**: 28 WAV clips (intro a/b/c, im01?im16, conclusion a/b/c) now inside the MoMA sound tree at `kazarian_episode/sound/nadali_production/`, along with `manifest.json` and `manifest.tsv`. Total voice duration ~3:55.
- **No audio jobs registered in D1** - the clips are file?only, not yet wired to the nadali scene.
- **No video lipsync clips generated** - no WAN calls, no video tasks fired.
- **Assistant's last utterance**: proposed "wire the per?clip audio into the nadali scene and start pairing narration to Anna shots", but did not act on it. The user's prior "Why in hell are you stuck?" makes it clear he wants action, not proposals.

## EXACT NEXT STEP (begin immediately, no further questions)
1. **Register the audio clips as proper D1 audio jobs** under the nadali scene. Either extend `fire_nadali_anna.py` or write a new `fire_nadali_audio.py` that:
   - reads the manifest
   - creates `prompts` rows (kind=`audio`, each clip's text as prompt)
   - calls `fire_job` for each clip, pointing `output_file` to the WAV paths relative to the kazarian root
   - marks them `done` (since files exist)
   This makes the imager aware of the audio and tie it to the scene.
2. **Pair narration clips with Anna images** to prepare for video generation. Simplest start: pair each `imNN` narration with one Anna still (use the best v2 still for all, or cycle through the camera?pan/zoom set for visual variety). The intro/conclusion clips can pair with the front?on v2.
3. **Generate video lipsync clips** using the WAN image?to?video endpoint documented in Notion page `30f0316f-5560-8195-a3d9-c7c2149b76de`. Read that page for exact API/parameters. Expected pipeline: for each pair (Anna image + audio WAV), call WAN to produce a talking?head clip. Register each resulting video as a D1 video job under the same arrangements.
4. **Build the final presentation view** (likely an arrangement "nadali-presentation" with all video clips in order) so Max can review in the imager.

**Do not ask the user which image to use or whether to start - pick a reasonable default (v2 for all or the whole camera set distributed) and begin.** Max will course?correct if he dislikes the selection.

## OPEN QUESTIONS (for reference only, do not ask Max)
- Should the WAN output be 16 separate short clips or a single concatenated video? (Assume separate clips matching the audio segments; MoMA tooling handles arrangement?level playback.)
- Is any audio format conversion needed? WAN might require MP3; have `ffmpeg` ready in the MCP environment.
- Does the user want Anna's cloned voice used, or is he happy with the already?recorded WAVs (which are Max's own voice? The manifest shows `speaker: "Anna"` but the actual audio might be Max reading the lines - assumption is these are the final voice track and no TTS is needed.)

## KEY PATHS / IDs
| What | Path / ID |
|---|---|
| MomA code root | `c:\moma\sc10\combo_runner\code\` |
| Registration entry point | `fire_nadali_anna.py` (images only; in that dir) |
| D1 scene "nadali" | `id = 9` |
| Image jobs | `jobs.id` 3290-3298 |
| Approved Anna headshot | `C:\Users\maxre\Nextcloud\ai_images\kazarian_episode\characters\anna\anna_approved_headshot_white_cloak_20260330.png` |
| Approved room front | `C:\Users\maxre\Nextcloud\ai_images\kazarian_episode\interiors\station_landscape_remake_20260628_v01\station_con_a1_ls.png` |
| Nadali images (registered) | `C:\Users\maxre\Nextcloud\ai_images\kazarian_episode\nadali\output\[anna_meetingroom_presenter_v1.png ... camera_zoom_out.png]` |
| Nadali audio (placed) | `C:\Users\maxre\Nextcloud\ai_images\kazarian_episode\sound\nadali_production\` (28 WAVs) |
| Original audio backup | `C:\Users\maxre\Videos\max talks\uei_max_talk_20260711\anna_audio\` |
| Imager GUI | `http://localhost:8779` (scene "nadali" listed) |
| Notion: Video Clip Prompt Format | `30f0316f-5560-8195-a3d9-c7c2149b76de` (WAN I2V endpoint) |
| Notion: Image generation master | `3090316f-5560-81ed-b2ca-ce7caceea2af` |
| MCP python tool (for video generation) | whichever is available out of pine?python, vega?python, sirius?python |
| API key for OpenAI (image gen) | `C:\Users\maxre\Nextcloud\zSyncMain\ssh\openai_api_key_20260216.txt` (used only for images; video may require different key) |

## GOTCHAS / DEAD ENDS
- **Do NOT generate images as orphan files**. Always use `fire_job` + D1 registration so they appear in the imager. The first batch had to be re?registered because of this exact mistake.
- **Approved ? any old generated still**. The first room reference came from `combo_output_stills` (unapproved worker output); canonical room is the approved set under `station_landscape_remake_20260628_v01`.
- **No live audio pre?exists beyond the 28 WAVs just placed**. All previous audio is for numbered movie scenes only.
- **The user did NOT split the audio - the files came pre?split**, contrary to Max's initial suspicion. This was clarified.
- **Max is in a "do it, don't ask" mood**. Any delay or redundant question will be met with frustration. The handover should communicate that the next session must begin executing the video pipeline immediately.
- **The image?gen skill is Rachel?project?specific** (paths, cast). The assistant already forked it to Nadali?only paths. Continue to reuse the code patterns (requests to OpenAI `/v1/images/edits`
