# Adviser note - milestone 2 (~167K tokens)
# session: 20260726_awesome_swirles_97603c_44272c02
# written: 2026-07-26 14:55:46 by deepseek-v4-pro

TO ASSISTANT: Stop reading. You have been investigating for 3 compactions with zero edits. The root cause is unambiguous: (A) `ST.sceneRank` hardcoded to 10 with a fallback that pins it, AND (B) `ST.arrIds` widens to all scene arrangements instead of narrowing to the one picked. Both need fixing now. Do not disambiguate - fix both. Delegate the edits to a mule subagent (Sonnet): (1) replace hardcoded `sceneRank: 10` with null, (2) fix the `arrangementchanged` handler fallback to not silently pin the scene, (3) make `imgInScene()` respect `ST.arrId` when `current_view` is 'arr', narrowing `ST.arrIds` to just the picked arrangement. No more reading files. No more probing APIs. Edit, restart the 8790 server, test with curl, commit and push to master.
