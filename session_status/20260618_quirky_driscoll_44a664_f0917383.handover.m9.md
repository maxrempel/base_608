# Scribe handover - milestone 9 (~677K tokens)
# session: 20260618_quirky_driscoll_44a664_f0917383
# cwd: C:\moma\.claude\worktrees\quirky-driscoll-44a664
# written: 2026-06-18 16:30:03 by deepseek-v4-pro

# HANDOVER - D21/D23 ? sc10 Merged Multiline Lipsies + System Update

## GOAL (Max's words)
Rebuild sc10 so each "arrangement" (a conversational beat of ~4 lines) is ONE merged multiline lipsie - not the old per-line clips. "One lipsie, multiperson." Then update the system (storyboard, assembly) so it shows and plays these merged lipsies as the spine.

## WHAT GOT DONE

### sc10 rearranged into 11 merged lipsies (all 33 lines covered)
The entire scene was rebuilt as merged multiline clips, each spanning 1-7 script lines (two are singles because lines 8 and 23 are 13-14s monologues that can't share a 15s clip).

**Final approved set (all in the DB, all `approved` per Max):**

| Arrangement | Lines | Job ID | Location/Still |
|---|---|---|---|
| arr01 | 0-3 (greeting) | 2774 | `sc01_meet_twoshot` (hall) |
| arr02 | 4-5 | 2775 | `sc01_meet_twoshot` (hall) |
| arr03 | 6-7 | 2810 | Corridor walk (`B1_corridor_walk`) |
| arr04 | 8 (Anna mono) | 2812 | Corridor walk |
| arr05 | 9 (Ishtab) | 2811 | Corridor walk |
| arr06 | 10-16 | 2805 | Window (`sc05_window_twoshot`) |
| arr07 | 17-22 | 2806 | Window |
| arr08 | 23 (Ishtab mono) | 2807 | Window |
| arr09 | 24-27 | 2808 | Alcove (frame from spine lipsie) |
| arr10 | 28-29 | 2794 | Doorway (frame from spine) |
| arr11 | 30-32 | 2795 | Doorway |

### The prompt recipe that cracked it
After ~30 fires fighting speaker swaps, penguin-nodding, laughter, frozen hands, and camera-gaze:
- **Describe both characters + positions FIRST** (names, hair, robes, left/right) - then the lines in quotes. Bare "Left/Right" labels are ignored by wan2.6.
- **Explicit speak-order** ("the red-haired woman on the left speaks first, then the elder on the right answers") - this is what finally killed the speaker swap.
- **No smile/grin words anywhere** ("minimal grins" still triggered laughing).
- **"Only the one speaking moves her lips; the listener stays composed and still"** for standing/serious beats - but for **walking** beats, use **neutral hand phrasing** (don't direct arms - "frozen" and "robotic" were both over-directing; neutral phrasing works best).
- **"In profile, eyes on each other, never the camera."**
- **Auto 1s silence pads** where they fit (only the two long monologues can't take them - they're at the 15s cap).

### System updated to support merged lipsies (all pushed to master)
1. **Renderer (v09):** `render_mixboard_video_v01.py` - consecutive lines pinned to the same lipsie collapse into ONE segment (since each merged clip carries its own baked audio). Assembly video renders correctly: 33 lines ? 11 segments, 2:24 total. Output at `G:\My Drive\00Main2026\00_rehearsals\mixboard_assembly_scene10_20260618_081035.mp4`.

2. **Storyboard grouped view (v45-v50):**
   - Grouped rows: consecutive lines sharing one merged lipsie show as ONE row, with all speaker lines stacked in the left column and a "MERGED ?N" badge.
   - "Whole Scene" toggle: button next to Refresh that drops the per-arrangement filter so you see all 11 merged rows. **Persists across refreshes** (v50 bugfix).
   - 2ND SPINE filter: now shows only takes with the same `line_hash` as the spine pick (same sound combination), not per-line fragments.
   - Lip-rate stars: gold ? badge on BOTH the spine slot tile and the 2ND-SPINE tiles (from `lip_rating` field). Stars refresh live within ~30s.

3. **Canonical merge IDs:** All 57 merged takes relabeled with a frozen span-ID (`start birth-line-hash ? end birth-line-hash`). Takes of the same line-span now share one ID ? they **gather in the 2ND SPINE** for side-by-side star comparison. Edits to wording won't scatter them. Future merges auto-use this ID.

4. **Old spine backed up:** `G:\My Drive\00Main2026\sc10_spine_backups\sc10_spine_backup_20260618_080616.json` (32 per-line picks, fully restorable).

5. **Spine populated:** All 33 lines pinned to their 11 merged lipsies (`line_current_clip` written directly, not via the assign API - to avoid corrupting the synthetic hashes).

### Rules saved to memory (`C:\Users\maxre\.claude\projects\C--moma\memory\`)
- `feedback_variation_not_verbatim.md` - don't re-fire Max's exact prompt as one of the variations.
- `feedback_dont_block_poll.md` - don't post a message and block; fire and stay responsive.
- `feedback_no_grin_smile_words.md` - never put "grin"/"smile" in prompts (triggers laughter).
- `feedback_distinct_still_per_lipsie.md` - each lipsie must start from a *different* starting image, not one still reused per location.
- `project_merge_canonical_id.md` - merged-lipsie IDs = start?end birth-line-hash (frozen, edit-proof).
- `MEMORY.md` updated with all the above.

## CURRENT STATE
- **sc10 is fully approved** - all 11 arrangements have an approved merged lipsie.
- **The system works end-to-end:** storyboard shows them grouped, assembly plays them in order, 2ND SPINE gathers alternate takes, stars display.
- **Git:** all commits pushed to master. Nothing unpushed of mine.
- **Punch-list for the NEXT round** (saved, NOT redoing now):
  1. **Distinct still per lipsie** - the big one: reused one still per location; each arrangement needs a different starting image from the per-line spine pool.
  2. **arr11 (2795):** room is behind them - they should turn toward it; portraits drifted from canon.
  3. **arr07 (2806):** Anna's delivery more relaxed, analytical, warm.
  4. **Minor:** arr02 last-half-second smile; arr06 small artistic interpretations.

## EXACT NEXT STEP
Max's explicit ask at the end of the session:

> *"next, task, in sb in pile - first part up to J840 is good, but after that - stupid sessions labeled unrelated images with SC01 - any idea how intelligently filter them out - totally unrelated to the scene images"*

Translation: In the storyboard's candidate pool ("Available" / pile), the images up to job ID 840 are correctly sc10-related, but after that, past sessions wrongly labeled non-sc10 images as scene SC01. Those irrelevant images are cluttering the pool and need to be filtered out.

**The task is:** find a way to filter the storyboard's candidate pool so only images actually belonging to scene 10 appear, excluding the mislabeled SC01 images that have no real connection to the scene.

## OPEN QUESTIONS
- Max hasn't specified the filtering criteria beyond "up to J840 is good, after that it's mislabeled." Need to investigate what's different about those mislabeled images (wrong scene tag? wrong folder? wrong metadata?) to design the filter.

## KEY PATHS
- **Storyboard:** `http://localhost:8790/storyboard` (use "Whole Scene: ON" toggle to see all 11 merged rows)
- **Storyboard code:** `C:\moma\sc10\sound_assembly\code\storyboard_editor.html` (v50)
- **Renderer:** `C:\moma\sc10\sound_assembly\code\render_mixboard_video_v01.py` (v09)
- **Slideshow server:** `C:\moma\sc10\sound_assembly\code\slideshow_server_v01.py` (port 8790)
- **DB client:** `C:\moma\sc10\combo_runner\code\moma_db.py` (D1Client class)
- **Spine backup:** `G:\My Drive\00Main2026\sc10_spine_backups\sc10_spine_backup_20260618_080616.json`
- **Assembly video:** `G:\My Drive\00Main2026\00_rehearsals\mixboard_assembly_scene10_20260618_081035.mp4`
- **Scratch scripts archived:** `C:\moma\sc10\combo_runner\code\_d2x_scratch_archive\`
- **Stills directory:** `C:\Users\maxre\Nextcloud\ai_images\kazarian_episode\scenes\scene10_images\`

## GOTCHAS / DEAD ENDS
- **Never use "Left:" / "Right:" labels alone** - wan2.6 ignores them. Must describe both characters by appearance + position first, then the lines in quotes, with explicit speak-order.
- **Never use grin/smile/giggle words** - triggers random laughter.
- **"Frozen"/"still" listener breaks walking shots** - hands look unnatural. Neutral phrasing only; don't direct arms.
- **15s clip cap is hard** - two long monologues (lines 8, 23) cannot share a clip. This is a model limit, not a choice.
- **Merged lipsies carry synthetic sound-hashes** - they won't match per-line `line_hash`, so the assign API would corrupt them. Always write `line_current_clip` directly for pinned merged picks.
- **The storyboard has a per-arrangement filter on by default** - that was the root cause of Max not seeing the merged rows. Must click "Whole Scene: ON" to see all 11.
- **Browser cache:** the storyboard auto-refreshes every 30s and detects rating changes, but hard-refresh is sometimes needed for code changes. Max's tab was stale and needed a refresh to see pinned changes.
- **The `approved_images` endpoint needed `lip_rating`/`fit_rating` added** - these weren't being served, so stars couldn't render. That's fixed in the server code.
- **"Move to spine" on merged items was silently broken** - it re-pinned the synthetic hash instead of the real lines. Fixed in v50.
- **Assembly video exists** but is draft quality. A higher-quality render can be triggered anytime.
