# Scribe handover - milestone 4 (~301K tokens)
# session: 20260705_scending_mahavira_5f3790_146df342
# cwd: C:\moma\.claude\worktrees\condescending-mahavira-5f3790
# written: 2026-07-05 00:03:34 by deepseek-v4-pro

## HANDOVER - D52, sc11-arr02 (spot2) + worker resilience fix

### GOAL (in Max's words)
- **Overall for spot2 (arrangement 2, sc11):** Take the scene starting from s3031, replace the chairs with white Vienna chairs and replace the podstakannik-type cups with white cups, "as in the previous images." Create multiple camera angles; the latest angle is over Anna's shoulder, with **Ishtab, Werner, Derek left-to-right** (Anna in foreground), using background **p1189**.
- **Worker fix (completed):** "housekeeping alarm, why would worker die? ... figure out what killed the workers" - root cause was unhandled transient Cloudflare D1 network errors crashing the image worker.

### DECISIONS + WHY
1. **Worker resilience fix (moma_db.py + combo_worker.py):**  
   - Root cause: `_request` in `moma_db.py` had a single `urlopen` with no retry; worker loop had `try/finally` without `except`. Any transient network blip (WinError 10054, read timeout) killed the worker process.  
   - Fix: added retry-with-backoff (0.5s, 2s, 5s, 10s) in `_request` for `URLError` / `TimeoutError` / `OSError`, but NOT for `HTTPError`. Added `try/except` around the worker's `while True:` loop body to log, sleep 5s, and continue.  
   - **Why:** a single elegant fix in the DB client protects all callers; the loop wrapper is a safety net for any remaining unexpected DB errors.  
   - Applied, committed & pushed. All workers restarted and running with the new code.

2. **No derivative rule (CRITICAL learned lesson):**  
   - Max explicitly forbade feeding a generated image as the primary ref. Doing so produces "idiotic derivatives" - faces deteriorate, characters stare at the camera, mood breaks.  
   - **Principle adopted:** always build from original portrait/scene refs (the same 10-ref formula) and only change the prompt text. If an emerging result must be used as a reference, add it as ref 2 or 3, never ref 1, but the session ultimately used **minimal text-only edits** - no result feedback - with great success.  
   - Stored as permanent memory: `feedback_minimal_edit_not_derivative.md`, and indexed in `MEMORY.md`.

3. **sc11-arr02 spot2 canon frame locked:**  
   - After many iterations (including close-ups, clock-face positions, Russian prompts), Max called **s3135 (job 3135, output v62, over Ishtab's left shoulder)** "?????? ?????????, ???????? ?? ? ??????."  
   - Set `role='canon'` (already approved). Recorded in staging bible.

4. **Anna-POV shot approach:**  
   - Same 10-ref formula (size ref, room ref, portrait refs for faces/bodies).  
   - Background swapped to p1189 (`station_cv_ne_ls.png`) per Max's instruction. Prompt describes the room exactly as the ref shows, so the model doesn't redraw it.  
   - The people order explicitly written: left-to-right **Ishtab, Werner, Derek**, with Anna's shoulder in the foreground.  
   - Three different variations were fired (same settings, small randomness) to give choice.

### CURRENT STATE
- **Worker fix:** Live, all 3 servers + 6 workers operational. No further deaths expected from transient network blips.
- **sc11-arr02 spot2 canon frame:** s3135 (v62) - over Ishtab's shoulder
