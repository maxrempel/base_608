# Scribe handover - milestone 7 (~113K tokens)
# session: 20260609_musing_kare_7ff037_82ee35fc
# cwd: C:\claude_base\.claude\worktrees\musing-kare-7ff037
# written: 2026-06-09 07:42:36 by claude-opus-4-8

# HANDOVER - Watcher System Build (Max's project)

## GOAL (in Max's words)
Max is "building an automated system" that will run on its own - "You will not be always watching over." He wants two watchers built and implemented:

1. A **numbering watcher** - watches session IDs and team assignments, talks to the team to help fix numbering collisions and assignment problems. **It must never bother Max.**
2. A **safety watcher** - acts as Max's "safety manager while I am away." It judges damage ? probability, translates everything into "Max's English," and pings Max via Telegram *only* when he personally needs to return to his laptop and stop a process.

Max's most recent instruction promotes the assistant to **manager + programmer simultaneously**. The team is partly awake. **C1 has memory and can advise.** Everyone reports to the assistant now. So the next move is to actually coordinate the team and build both watchers - not to keep drafting text.

## DECISIONS + WHY
- **Two separate watchers, not one.** Max corrected an earlier conflation: the ~2000-char charter that was drafted is for the *safety* watcher only. The existing `watcher.py` is the *numbering/coordination* watcher.
- **Numbering watcher loses all ability to ping Max.** It only talks to the team. (Assistant flagged this as an assumption Max could halt; Max effectively confirmed by saying it "should not bother me.")
- **Safety watcher specs (Max's explicit orders):**
  - **Proper context** - kept via a *rolling per-project "situation digest"* (read prior digest + only new events since last run, then rewrite the digest forward; bounded size, nothing forgotten). This avoids context bloat / compaction cliff.
  - **Model: "Opus 4.8 low"** - assistant interpreted this as Opus 4.8 kept cheap/minimal (small budget, no deep thinking). *Worth confirming this interpretation is right.*
  - **One per team** (one safety watcher per team/project).
  - **Reports to Max via Telegram**, only to say "come back to the laptop and stop the process."
- **Why Max wants the translation step:** current alerts are "too voluminous, frequent and technical" - he can't read/comprehend them. The watcher must re-analyze before alerting: estimate damage, estimate probability, translate into plain language.
- **Tone of the charter must be non-deterministic.** Max explicitly rejected absolute words ("never" = minimize, etc.). Instructions should be logical and explanatory, assuming the watcher is smart - not rigid rules.

## CURRENT STATE
- The **safety watcher charter text is DONE and approved** ("Perfect"). Final version includes: think-first framing; damage ? probability re-analysis; "Max's English" register (general scientist, advanced Windows user level, cares that databases/data/computers stay alive, no minutia); softened non-deterministic language; explicit **Telegram** mention; guidance that pings be easy to read, optimal length, most important info loaded upfront with probability and extent of damage explained; watcher has authority to talk to workers and prioritize conflict & safety; pings reserved for "come stop the process."
- **Neither watcher is built/implemented yet.** Today's `watcher.py` is only the numbering one.
- Files already read this session: `watcher.py`, the design report `bcast_guard_watcher_report_20260608_v01_tomemex.md`, and `bcast.py`.
- Session was renamed to **Max** by mistake, then **reverted back to c5** on the board. (Confusion: Max meant "rename *me* to Max *in the text*, third person," not the session ID.)

## EXACT NEXT STEP
Act as manager+programmer. Wake/coordinate the partly-awake team, consult **C1 (has memory)** for advice, and **implement both watchers**:
1. Numbering watcher: ensure it only talks to team, strip any Max-paging ability.
2. Safety watcher: new build - one per team, Opus 4.8 (low/cheap), rolling per-project digest as context, Telegram pings only for "return and stop," driven by the approved charter.

## OPEN QUESTIONS (awaiting Max / to confirm)
- Is "Opus 4.8 low" correctly read as Opus 4.8 with minimal/cheap budget?
- Exact division of labor among the partly-awake team vs. C1's advisory role - who builds what.

## KEY PATHS / IDS / NAMES
- Working dir: `C:\claude_base\.claude\worktrees\musing-kare-7ff037`
- `C:\claude_base\branch_bulletin\watcher.py` (existing = numbering watcher)
- `C:\claude_base\branch_bulletin\bcast.py`
- `C:\claude_base\branch_bulletin\bcast_guard_watcher_report_20260608_v01_tomemex.md` (design report)
- Scheduled task: `bcast_watcher`, runs every 10 min on host **Pine**
- Session ID: **c5** (this is "Max"); **C1** = has memory, advisor
- Existing watcher uses: a cheap Python duplicate-ID sweep (no AI) + an Opus judgment pass that currently writes raw jargon to a Telegram critical-alarm bot. A small bookkeeping/state file persists timestamps & alert keys between runs (no growing context).

## GOTCHAS / DEAD ENDS
- **Max hates editorializing.** When he asks for his text back, return it verbatim/cleaned - do not add commentary or "improve" his meaning. He called this out repeatedly and sharply.
- "Rename me to Max" meant *in the text, third person* - NOT the board session ID. Don't rename the session.
- Avoid deterministic/absolute language in any watcher instructions (no "never/always" as hard rules).
- The safety watcher's digest is only as good as what workers write to the board/work-log - if a team works silently the watcher is half-blind; workers must keep narrating.
- The existing watcher carries **no running AI context by design** - don't "fix" that for the numbering watcher; the rolling digest is a *new* mechanism specific to the safety watcher.
