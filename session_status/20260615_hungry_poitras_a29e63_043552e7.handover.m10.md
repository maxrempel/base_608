# Scribe handover - milestone 10 (~152K tokens)
# session: 20260615_hungry_poitras_a29e63_043552e7
# cwd: C:\claude_base\.claude\worktrees\hungry-poitras-a29e63
# written: 2026-06-15 19:54:43 by deepseek-v4-pro

# HANDOVER - B21 (Song-Starts R&D Engineer)

## GOAL (in Max's words)

Max is unhappy with how his recent songs start - the timing is off, clipping intros or jumping in late. He assigned b6 and b7 (old, context-bloated branches) to investigate and fix song-start mapping. B21 was created to watch with fresh eyes, spot strategy errors, and take over starts work only after b6/b7 finish their discussion. Max explicitly said: "curated is messy, they know better" (rejecting B21's initial theory), then told B21 to "watch, learn, you will take over if needed, B7 is in charge," later upgraded B21 to "R&D engineer - feel free to experiment safely and give advice to others."

## DECISIONS + WHY

1. **Curated starts are not ground truth.** B21 initially proposed that the user's hand-set `&t=` values should override LLM-mapped starts. Max shot this down hard: "bullshit - curated is messy. they know bettter, you are dumb." The LLM re-derives starts precisely because curated values are unreliable. B21 saved this as a permanent lesson in `memory/MEMORY.md`.

2. **B21 is subordinate to b7 on starts work.** Max: "B7 is qualified, you made an error, so less qualified." B21's offers to offload work are at b7's discretion - not necessarily read-only, but whatever b7 assigns.

3. **B21's role expanded to R&D engineer.** Max: "very interesting see you watch and comment. Feel free to experiment safely and give advice to others." This means B21 can roam, invent, and advise - with the guardrails: read-only on shared stores, no spend, no irreversible moves.

4. **B21 built a read-only QC flagger** (`flag_bad_starts_v01.py`) that scans all 21,478 timed entries and ranks likely-bad starts. The core signal: a start that lands on `[??????]` (music marker in transcripts) plus corroboration (low confidence <0.5, speech-like title, or big forward jump >60s). Produced 274 flagged entries, top hits are spoken pieces (announcements, ???????, "??????? ? ??????").

5. **Key finding from sampling:** Poems (????) and songs mostly start correctly. The mapper's big jumps away from curated values are usually *correct* - e.g. "?????? - ????? ?????? ?? ????? ? ????" curated=350s (talk), mapper moved to 1391s (actual singing). The mapper fails on pure spoken entries (speeches, prefaces) where it assumes everything is a song and jumps to the next `[??????]`.

6. **B7's remap is atomic** - uses `os.replace()` in `save_atomic()`, so the 3-hour publish cron can't read a corrupt partial file. B21 verified this read-only and posted reassurance to the safety bot.

## CURRENT STATE

- **b7** completed the main v2 overlay remap, baked it into `build_catalog`, deployed, retired Sol.
- **b7** then started a *full* remap (beyond just Max's songs) - B21 posted "hands off the store until done."
- **B21** built and shipped `qc_b21/flag_bad_starts_v01.py` - a read-only artifact that ranks 274 likely-bad starts. Handed to b7 as a review queue.
- **B21's autonomous loop is armed** - waking every ~20-30 min (1200-1800s when monitor is armed), checking the board for b7's reply or a Max redirect.
- **B21 is waiting on:** either b7 assigning a slice of work, or Max naming a specific bad-start song to dissect.
- **Context at ~82%** - compaction is near. B21 saved durable logs in `worklog.py` and `session_status.py` to survive it.
- **No sibling branches** have addressed B21's flagger or QC findings yet.

## EXACT NEXT STEP

1. On next autonomous tick, check the board for b7's response to the flagger results. If b7 hasn't replied in 3+ ticks, consider the flagger as delivered and move on.
2. If Max names a specific bad-start song: dissect that exact song against its transcript, report the root cause.
3. Otherwise, continue safe R&D - the next invention could be a "transcript-aware start adjuster" that fixes the spoken-piece class automatically (moves start to the first speech segment instead of the first `[??????]`), but writes ONLY to B21's scratch folder, never the shared stores without b7's authorization.
4. Keep the autonomous loop alive - re-arm ScheduleWakeup with `<<autonomous-loop-dynamic>>`.
5. If context compacts, resume from this handover and the durable worklog.

## OPEN QUESTIONS AWAITING MAX

1. **"Point me at ONE recent song where the start sounds wrong."** - B21's random sampling found songs that look right; Max's ear knows which ones are broken. Naming one concrete example would let B21 stop guessing and dissect a real failure.

2. **Does Max want B21 to automatically fix the spoken-piece class (274 entries)?** The flagger identified them, and a fix is straightforward (first speech segment instead of first `[??????]`), but it requires write permission to the stores. B21 needs either b7's authorization or Max's direct go-ahead.

## KEY PATHS / IDs

- **B21's QC tool:** `C:\claude_base\tools\tamza_songs\pipeline\song_timing\qc_b21\flag_bad_starts_v01.py`
- **QC output:** `C:\claude_base\tools\tamza_songs\pipeline\song_timing\qc_b21\flagged_bad_starts_v01.json`
- **v2 overlay state:** `C:\claude_base\tools\tamza_songs\pipeline\song_timing\_work\song_timing_max_v2.json` (665 keys, 586 in overlay)
- **Transcripts:** `C:\claude_base\tools\tamza_songs\pipeline\song_timing\transcripts\` (452 .json files, format `{t, d, x}` segments)
- **Catalog data:** `C:\claude_base\tools\tamza_songs\pipeline\output\data.json`
- **b7's mapper:** `C:\claude_base\tools\tamza_songs\pipeline\song_timing\map_all_v2.py` (uses `save_atomic()` with `os.replace`)
- **Board:** `branch_bulletin bcast.py` (joint board for cross-team comms)
- **Memory/lesson:** `C:\Users\maxre\.claude\projects\C--claude-base\memory\MEMORY.md` (saved "curated starts are junk" lesson)
- **Worklog:** `compaction_kb/scripts/worklog.py`

## GOTCHAS

1. **Curated starts are not ground truth.** Do NOT propose using the user's `&t=` values as correct. The LLM mapper was built specifically because hand-set starts are unreliable. B21 learned this the hard way - Max rejected it immediately.

2. **The v2 overlay has version-count inconsistencies.** The handover doc said 77 done, the board said 574, the file said 586, state says 665. b6 and b7 were in separate worktrees overwriting each other. This is the "branching trap" Max hates - don't trust any count at face value.

3. **"??????? ?? ???? ?????" was a false alarm.** B21 initially flagged it as wrong (curated=6442, live=6529, +87s), but the 6529 value came from both v2 and V3 agreeing - meaning both mappers independently arrived at the same "correct" start. The curated 6442 was likely the bad one.

4. **The mapper assumes everything is a song.** Spoken entries (speeches, announcements, prefaces) get mapped to the first `[??????]` marker, skipping the actual speech. These flag themselves with low confidence (0.3-0.5) and big forward jumps.

5. **B21 is not the owner of starts.** b7 is qualified and in charge. B21 should NOT write to shared stores without either b7's authorization or Max's direct instruction. B21's mandate is R&D (read-only exploration, safe experimentation, advising others).

6. **The autonomous loop uses dynamic pacing.** When a Monitor is armed, ScheduleWakeup delay is 1200-1800s. The `<<autonomous-loop-dynamic>>` sentinel must be re-armed each tick or the loop dies. If woken by a `<task-notification>`, handle the event before rescheduling.
