# Scribe handover - milestone 2 (~152K tokens)
# session: 20260704_eautiful_bhaskara_0b8970_08e6521f
# cwd: C:\claude_base\.claude\worktrees\beautiful-bhaskara-0b8970
# written: 2026-07-04 14:58:44 by deepseek-v4-pro

# X1D - HANDOVER: Sequencing.com Inversion Artifact Demo

---

## GOAL (in Max's words)

Log in to Kristin's sequencing.com account with Playwright, find ~5 examples of the "inversions" she quoted in her email, cross-check them against our data, and pick the single cleanest one that demonstrates the browser's inversion call is an artifact. The end deliverable is one sample shown in-browser that proves the inversion is wrong - ideally one she herself cited. This becomes a concrete illustration of why "their browser just sucks."

---

## DECISIONS + WHY

1. **Read the email first.** Kristen's thread is the authoritative source for which inversions she flagged. We found it - she cites ~1500+ homozygous ("II") inversions shared with her son (she says biologically impossible; humans average 40-50). She also flagged a Y-chromosome indel at position 10810652 (insertion GTTCCA, rs2081743753) as "starting one position later" - a classic coordinate/annotation artifact signature. These are our targets.

2. **Used existing Bitwarden session.** The `bw_session.txt` token was live, so we read the sequencing.com login without prompting for the master password. Faster, no user interruption needed.

3. **Used persistent Playwright profile.** Kristen's sequencing.com session was already active in the browser profile - no manual login was needed after navigating to the sign-in page. We proceeded directly.

4. **Went to Genome Explorer app** (ID `886b5b3a-e2f6-4b93-be08-53a382c6838a`). This is where the variant table lives. The dashboard alone doesn't show the raw per-position "II" / "2 copies" data Kristin referenced.

5. **Targeting raw-data view, not annotated-data view.** The annotated view shows ~5M rows of annotated variants. Kristen's per-position "inversion" / "II" view lives in the raw/unannotated data mode. We were in the process of switching to that when the user interrupted.

6. **Switched to JS evaluate over snapshot for dropdown reading.** The snapshots were huge (massive variant tables); to save context tokens, we used `browser_evaluate` to read dropdown options via DOM queries.

---

## CURRENT STATE

- **Playwright browser is open and logged in** to Kristin's sequencing.com account.
- **Browser is on the Genome Explorer page**, viewing the variant table.
- **We were mid-action:** clicking the "Data with Annotations" dropdown (or a similar data-mode toggle) to switch from annotated-variant view to raw-data view.
- **No inversion examples have been located or screen-captured yet.**
- **No cross-check against our local data has been performed yet.**
- **The Playwright release timer is armed** (9-minute safety net). The session holder should dismiss/re-arm it if continuing to use the browser.

---

## EXACT NEXT STEP

1. **Complete the view switch to raw/unannotated data** in the Genome Explorer. The dropdown/toggle was clicked but the UI may need a second click or a selection from the options that appeared. Evaluate the current DOM state to see what's visible.

2. **Once in raw-data mode, locate entries with genotype "II" or "2 copies"** in the variant table. These are the "inversions" Kristin is talking about. Prioritize:
   - The Y-chromosome indel she explicitly cited: position **10810652**, insertion **GTTCCA**, rsID **rs2081743753**.
   - Any other "II" entries on the Y chromosome (homozygous calls on a haploid chromosome are an immediate giveaway of an artifact).

3. **Capture ~5 examples** (screenshots or data exports) of these "II" inversion entries from the browser.

4. **Cross-check each example against our local data** (the real variant calls). For each one, note whether the inversion is genuine or an artifact.

5. **Select the single best example** - the one that most clearly shows the browser is wrong. Kristin's own Y-chromosome example (position 10810652) is the prime candidate because a homozygous call on a haploid chromosome is a self-evident artifact.

6. **Document the proof** - ideally a side-by-side: what the sequencing.com browser shows vs. what the real data says.

---

## OPEN QUESTIONS (awaiting Max)

- **None yet.** The task direction is clear. The only thing that might need Max is confirmation of which example he prefers, but the instructions already say to pick the best one ourselves.

---

## KEY PATHS / IDs / COMMANDS

| What | Value |
|------|-------|
| Sequencing.com login email | From Bitwarden: `sequencing` search (extracted via `bw list items --search sequenci`) |
| Bitwarden session file | `C:/Users/maxre/Nextcloud/zSyncMain/ssh/bw_session.txt` |
| Genome Explorer app ID | `886b5b3a-e2f6-4b93-be08-53a382c6838a` |
| Genome Explorer URL | `https://sequencing.com/dna-apps/genome-explorer/886b5b3a-e2f6-4b93-be08-53a382c6838a` |
| Sign-in URL | `https://sequencing.com/sign-in` |
| Kristen's cited Y indel | Pos 10810652, insertion GTTCCA, rs2081743753 |
| Email thread subject | Contains "1500+ inversions" / "II" discussion |
| mcp__playwright__browser_evaluate | Use `ref` param (not `target`) for element clicks |

---

## GOTCHAS

- **Playwright click target param is `ref`, not `target`.** We hit this already - don't repeat the mistake.
- **Snapshots are enormous** (5M+ row variant tables). Prefer `browser_evaluate` with targeted JS queries over `browser_snapshot` to avoid burning context.
- **The browser session is persistent.** No need to re-login unless the session expires. If you close the browser, you'll need to sign in again (creds in Bitwarden).
- **"II" = homozygous inversion call** in sequencing.com's notation. On haploid chromosomes (Y, MT in males), any "II" call is impossible - this is the smoking gun we're looking for.
- **Do NOT modify anything** in Kristin's account. Read-only browsing only.
- **Arm the Playwright release timer** if you'll hold the browser lock for more than a few minutes between actions. Call `mcp__playwright__browser_close` when done.
