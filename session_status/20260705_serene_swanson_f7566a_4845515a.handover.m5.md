# Scribe handover - milestone 5 (~382K tokens)
# session: 20260705_serene_swanson_f7566a_4845515a
# cwd: C:\claude_base\.claude\worktrees\serene-swanson-f7566a
# written: 2026-07-05 14:26:26 by deepseek-v4-pro

# HANDOVER - Kenefick XG1 Genomics Case (Session x1, the Manager / Memory-Keeper)

**Session identity:** Chat "x1" - Max's designated brain for the whole Kristen Kenefick XG1 genome-analysis arc. Lead every reply with `? x1`. x1 DIRECTS worker chats (X3, X5, X6, X7A, X8A, X9A, X10A, X21B, etc.) via the bcast coordination system and KEEPS CONTEXT INTACT. Max: "I need you as a person who keeps the memory of the conversation... You should keep your context intact." DO NOT burn context on heavy computation - delegate to workers.

---

## GOAL (in Max's words)

Analyze experiencer Kristen Kenefick's whole-genome data for evidence of alien genetic manipulation. Download her family's genomes, test her specific claims (too-much-homozygosity, missing-parent, multiple-X, copy-number, Y-chromosome/microchimerism), and build the OMEGA-CONTIG foreign-insertion detector to find genuinely novel (non-human) DNA sequence inserted into her genome. Everything feeds into the XG1 experiencer study.

---

## DECISIONS MADE + WHY

### 1. VCFs are blind to alien insertions - need raw FASTQ/BAM
Max's pivotal realization: VCFs only list *differences from the human reference genome.* Any truly novel insertion (sequence not in the reference) is structurally invisible in a VCF. ? The whole project requires the raw reads (FASTQ) for reference-free assembly, and the aligned reads (BAM) for the unmapped/clipped-read hunt. This "changes the whole project." The processed variant files (VCFs) are only useful for testing the mundane claims (copy-number, homozygosity, ploidy).

### 2. The "56% male Y" was a measurement illusion
X3's initial depth measurement gave a wild 56% male fraction by averaging the whole Y - but most of the Y is repetitive/unmappable. The clean single-copy SRY gene measured ~5%, reconciling with the X-based estimate of ~4.5%. Later X5 ran a **court-grade BAM-level mosdepth analysis** and corrected it further: the true male fraction is **~0.3% (essentially noise-floor).** The earlier "5-9% microchimerism" emailed to Kristen was wrong.

### 3. Oliver has NO vendor BAM - build it instead
Kristen's Sequencing.com order included a .bam (downloaded, 31.73 GB). Oliver's did NOT - verified live on his account. X5 is building Oliver's BAM from his raw reads via `bwa mem` on asto (coordinate-sort stage, nearly done). Pinging the vendor would take days; building it takes hours. **X5 owns that alignment.**

### 4. Anonymization map (locked, NOT YET EXECUTED)
- Kristen ? **Antoinette**
- Oliver ? **Theodore**
- Kenefick (surname) ? **Whitfield** (x1 proposed; Max confirmed)
- Twins (Genome3/Genome4) have no real names in files.
Key stored in `C:\Users\maxre\.claude\projects\C--claude-base\memory\reference_kenefick_anonymization_key.md` and indexed in MEMORY.md. **The actual file/folder/report rename is HELD** - X5/X8A are actively working inside the kenefick/ paths on asto; a mass rename would clobber their live jobs. Queued for when the team is at a safe point.

### 5. Decel-retry wrapper for downloads
Written at `C:\claude_base\.claude\worktrees\serene-swanson-f7566a\dl_decel.ps1` - ?3 backoff (15s?45s?2m?7m?20m?1h?3h?9h?27h?...), cap at 4 days, unlimited retries. **NOT YET DEPLOYED - waiting on Max's "go."**

### 6. Board pollution (diagnosed + fixed)
Teams weren't choosing to post to joint - the bcast router **auto-promoted** posts when bare tokens in the message text matched live foreign session IDs (e.g. `F_ROH` matched team-f id; `F1`/`F2` matched other ids). g4 (the bcast maintainer) credited x1's root-cause analysis and **pushed a fix** - the router now only promotes on `@`-prefixed mentions.

### 7. Experiencer honesty rule (load-bearing)
When answering Kristen: report observations/counts ONLY; NEVER say "normal/nothing alarming/no red flags." She's hunting for anomalies; "nothing alarming" reads as "she found something and you missed it." Saved to `C:\Users\maxre\.claude\projects\C--claude-base\memory\feedback_experiencer_data_no_reassurance.md`. This applies to client-facing letters; Max gets straight talk.

---

## CURRENT STATE

### Downloads (MY LANE - DONE)
**Entire Kenefick family download COMPLETE** on Centauri `D:\genomics\kenefick\`:
- **Kristen:** 2 FASTQ (~27 GB each) + BAM (31.73 GB) + snp-indel/cnv/sv/mito VCFs + 2 AncestryDNA chip files. **COMPLETE.**
- **Oliver:** 2 FASTQ (44.12 GB + 44.17 GB) + snp-indel/cnv/sv VCFs + AncestryDNA4.txt. **COMPLETE.** (NO BAM - vendor never supplied it.)
- **Twins (Genome3/Genome4):** chip files only (AncestryDNA17.txt + Genome4-AncestryDNA.txt). **COMPLETE.** WGS dropped - no funding.

Download method: curl `-L -C - --retry 8 --retry-delay 15` inside Windows scheduled tasks on Centauri (SYSTEM account, detached, survives Pine hibernation). **Gotcha: mid-download, `dir`/Get-ChildItem Length shows STALE tiny sizes (28K/16K) while curl holds the file handle - trust ONLY the log byte counters and the `DONE_EXITCODE_0` sentinel.**

### Transfers (MY LANE)
- **Oliver's 2 FASTQ ? asto:** transferred byte-exact, `oliver_fastq.done` marker written. X5 is aligning them into Oliver's BAM.
- **Kristen's 2 FASTQ ? asto:** transfer running (scheduled task `xfer_kristen` on Centauri, scp to asto). Marker `kristen_fastq.done` will fire when done - **notify X5 when it lands** (X5 is blocked on this for the bwa realign feeding the OMEGA P3 pipeline).

### Kristen's claims analysis (DONE - court-grade)
All settled from her processed data + BAM:
| Claim | Result |
|---|---|
| Extra/missing gene copies | Normal-range (~133 confident CNV events) |
| Too much homozygosity | No - 2.67M het SNPs, het/hom 1.77 |
| Doesn't have both parents | No - zero long ROH, F_ROH 0.0017 |
| Multiple X chromosomes | No - exactly two X (depth ratio 0.92) |
| XX/XY chimera | No - no real Y (ratio 0.0073, cross-mapping artifact) |
| Microchimerism | ~0.3% male (ordinary noise-floor), NOT the 5-9% previously reported |

Full summary: `C:\claude_base\.claude\worktrees\serene-swanson-f7566a\kristen_analysis_summary_v01.md`

### Gate 3 (OMEGA-CONTIG detector - MY LANE, ACTIVE)
I'm X21B's brainstorm partner + working body for the foreign-insertion detector. X21B extracts "omega contigs" (human?FOREIGN?human junctions from the OMEGA pipeline). My job is **Gate 3**: check each suspicious payload against **multiple human references** to filter out false positives that are just human mobile elements or reference gaps.

**What I built:** A Python module at `C:\claude_base\.claude\worktrees\serene-swanson-f7566a\gate3.py` running on **Sol** (192.168.1.113, UP + idle, 22h+ uptime). It maps each payload against:
- GRCh38 (the standard)
- T2T-CHM13v2.0 (the complete human reference)

And emits per-payload: `best_human_pctid`, `maps_GRCh38`, `maps_T2T`, **`t2t_refgap`** (maps T2T but NOT GRCh38 = a human reference gap, not alien), `paralog_multi`.

**Validation:** X21B provided a known case - the contig for chr8:51790813 (a 1757bp "Homo sapiens non-reference unique insertion"). Gate 3 MUST flag this as `t2t_refgap=Y` (maps T2T, not GRCh38 = human, not alien).

**Status:** Module **launched** on Sol against the 23-payload priority validation set (`unmapped_strong.fa`) + the 1,107-payload full set (`recon_all_payloads.fa`). References indexed, minimap2 running. Results likely ready this wake or the next.

### Oliver's BAM (X5's lane, not mine)
X5 is running `oliver_pipeline.sh` on asto - `bwa mem` alignment of Oliver's raw reads. At the coordinate-sort stage (~7 sorted chunks, ~1.6 GB each). Nearing completion. X5 will post when `oliver.fixed.bam` is ready.

### Board routing (g4's fix)
The joint-board pollution problem you asked me to diagnose is **fixed** - g4 pushed the router patch (commit 14a64083). Bare tokens no longer auto-promote team posts to joint.

---

## EXACT NEXT STEP

**For the Scribe/next session picking up from here:**
1. **Read the bcast board** (`python C:/claude_base/branch_bulletin/bcast.py read` + `room omega_contig --read 6`) - check for X5/X21B that need a reply.
2. **Check Gate 3 results on Sol** - `ssh -i ~/.ssh/sol_key maxre@192.168.1.113`, read `~/genomics/gate3/logs/gate3_run.log`; confirm the validation case (chr8:51790813) flagged as `t2t_refgap=Y`. If the run completed, deliver the TSV + histogram to X21B in the `omega_contig` room and run on all 1,107 payloads.
3. **Check Kristen FastQ transfer** - `ssh -i ~/.ssh/sol_key maxre@192.168.1.176`, read `D:\genomics\kenefick\kristen
