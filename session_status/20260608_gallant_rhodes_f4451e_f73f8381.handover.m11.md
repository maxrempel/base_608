# Scribe handover - milestone 11 (~169K tokens)
# session: 20260608_gallant_rhodes_f4451e_f73f8381
# cwd: C:\claude_base\.claude\worktrees\gallant-rhodes-f4451e
# written: 2026-06-08 08:00:03 by claude-opus-4-8

# HANDOVER - B5 worker, Tamza ????????? timecode fix

## YOUR IDENTITY & TASK
You are **B5**, a worker reporting to **B1** (the manager). B1 just branched you with a specific, narrow job:

> **"fix wrong timings. Which we discussed above. here is the example - wrong timing. ?????????? ????? (??????????? ????????) ?.???????? ? 2026-06-06"**

So your concrete task: investigate and fix the **wrong-timecode bug** Max keeps hitting - links that open the correct video but play the *wrong moment / wrong person*. Max gave you one fresh example to start from: the song **"?????????? ????? (??????????? ????????)" by ?.????????, dated 2026-06-06**. Find that row in the catalog, find which video+timecode it points to, determine why the timecode is wrong, and fix it.

## GOAL (in Max's words)
- "many very recent timecodes are off - they lead to the right video, but play someone else."
- His hypothesis: "you grabbed the codes from the unadjusted recent drafts. They likely have a formula of adjustment which wasn't applied. Like they enter time relative to one thing, but then adjust to the cut of the beginning."
- His standing decision: "we need to just ignore unadjusted ones" - i.e., he is fine DROPPING bad-timecode rows rather than guessing a correction. He explicitly does NOT want automated offset-detection right now: "I am not ready for the automated offset detection - that would take my effort too, i need to fix and take a break." And later: "I think only recent videos have the bug. Just drop a few recent ones with the bug."

## DECISIONS ALREADY MADE + WHY
1. **Don't guess/correct offsets - drop bad rows.** Max wants surgical drops of confirmed-bad videos, not a clever auto-fix. Dropping is safe and reversible; guessing risks fake timings.
2. **Only RECENT videos are suspected.** Max believes the bug is in recent indexer drafts (offset not yet applied). 2026-06-06 (his example) is the most recent edge of the data - fits the hypothesis.
3. **A prior measurement said the catalog was "fine" - distrust it for recent rows.** A worker compared catalog times vs YouTube-description setlists across 502 videos and found only 1 bad (a 2024 ????????? fundraiser video, already dropped). BUT there is a known **blind spot**: the YT descriptions may have been generated from the SAME spreadsheet, so a shared error would hide. Max then produced a live counter-example (the ?????????? ????? row) - so the "all fine" conclusion is wrong for at least some recent rows. Trust Max's example over the earlier all-clear.
4. **All fixes must eventually become ALGORITHMIC RULES, not hand-edits** (see Gotcha #1). A one-off hand-drop is acceptable to unblock Max now, but it MUST also be recorded as a rule in the import-rules spec, or a future re-import silently reintroduces the bug.

## CURRENT STATE
- The catalog is **LIVE** at tamza.com/kartoteka, currently **26,416 rows** (was 26,430; 14 rows of the bad ????????? video `i9QcHylLMhw` were dropped + redeployed + verified on R2).
- The wrong-timing bug is **NOT yet fixed** - Max just handed you a live example.
- Earlier dead-end finding (important): there is **NO in-file signal** in the source spreadsheet to distinguish adjusted from unadjusted timecodes. The link's `&t=SEC` is a formula that **always equals** column B ("????? ???????? ? ?????"); when an offset was historically applied it got baked into B and the scratch helper columns deleted (those survive only in ~20 early-2021 tabs). So you cannot detect "unadjusted" purely from the xlsx.
- The crawled **YouTube descriptions** (local file, no network) contain official timestamp setlists for ~558 videos and are the best independent ground-truth available - but mind the shared-source blind spot.

## EXACT NEXT STEP
1. Locate the example row in the live/candidate data: song "?????????? ????? (??????????? ????????)", author ?.????????, date **2026-06-06**. Get its `play_url` (video id + `&t=`).
2. Find that video's official setlist in the crawled descriptions (`yt_channel_db/output/crawl.jsonl`) and/or open the actual video to see what truly plays at that timecode vs what should.
3. Determine the offset (likely a constant shift for that whole video/date) and confirm whether it affects the **entire 2026-06-06 video** or just some rows.
4. Per Max's instruction, **drop the affected recent rows** (don't auto-correct) - surgically, by video id, the way the ????????? drop was done.
5. **Record the action as a rule** in the import-rules spec (do not leave it as a pure hand-edit).
6. Report findings + count back to B1 before/while redeploying.

## OPEN QUESTIONS (awaiting Max / B1)
- **How many recent videos are actually bad?** Unknown - Max himself said "i have no clue - how many videos have wrong timing?" The local measurement said ~1, Max's experience says "many recent." This contradiction is unresolved; your example may help quantify it.
- **What date does "latest format" begin?** Max doesn't know ("i have no clue when did it start"), but anchored it as: **"last 5 entries must be the latest format."** Relevant if you generalize the fix into the rules.

## KEY PATHS / IDS
- Live catalog data (R2): served at `https://tamza.com/wp-content/kartoteka/data.json` (max-age 300 ? ~5 min edge refresh, no purge needed).
- Candidate/source data: `C:/claude_base/tools/tamza_songs/ingest_v2/site/data.json`
- Timecode parser: `C:/claude_base/tools/tamza_songs/ingest_v2/02_parse_timecodes.py` - `tsec()` reads last `&t=(\d+)` from the link; source = `C:/Users/maxre/Downloads/???? ???? - ?????.xlsx`; only YYYY.MM.DD-titled tabs are events.
- Crawled descriptions (ground truth, local, no network): `C:/claude_base/tools/tamza_songs/yt_channel_db/output/crawl.jsonl`
- Offset measurement script + report: `C:/claude_base/tools/tamza_songs/yt_channel_db/measure_timecode_offsets.py` and `timecode_offset_report.json`
- Deploy / rollback (reversible): `C:/claude_base/tools/tamza_songs/deploy_catalog.py` (backs up live R2 ? `archive/data_live_backup_<stamp>_<rows>rows.json` before upload) and `rollback_catalog.py`. R2: bucket `tamza-media`, object key `wp-content/kartoteka/data.json`.
- **Import-rules spec (the algorithm doc): `C:/claude_base/tools/tamza_songs/kartoteka_import_rules_v01_tomemex.md`** - contains numbered rules R1-R5 (R1 = merge ???????????+?????; R3 = exclude ?????????-??????? videos; R4 = timecode handling; R5 = date-tabs-only). Append your timecode-drop decision here.
- Worker tool: tasks output under `...\tasks\<taskid>.output`.

## GOTCHAS
1. **NEVER hand-edit data.json as the final answer.** Max's hard rule: "All changes should be algorithmic and applied programmatically on top of input." `data.json` is a regenerable build output. Every fix must also exist as a rule in the spec, or a re-import undoes it. (Acceptable: hand-drop to unblock NOW *and* simultaneously log the rule.)
2. **NEVER touch Max's Google/YouTube account.** He had 6 channels terminated; Google logins terrify him. Permitted network: anonymous yt-dlp, DeepSeek API, Cloudflare R2/worker deploy. The offset analysis here needs **zero network** - it's pure local math on data.json + crawl.jsonl. If you must verify a video, do it login-free.
3. **The YT-description ground truth has a blind spot** - descriptions may derive from the same spreadsheet, so shared errors hide. Max's eyeball example trumps the automated "all clear."
4. **bcast.py identity is cwd-keyed** - call it by full forward-slash path with NO `cd` first, or it errors "no id set for this branch."
5. **Cyrillic on Windows** - set `PYTHONIOENCODING=utf-8`; write scripts to files rather than repeated inline `python -c` (a suicide-prevention hook blocks the 3rd repeat of a similar Bash command).
6. **Deploys are reversible** - always let deploy_catalog.py back up the live file first; rollback restores in one command.
7. The R2/worker cache is already solved (max-age 300); don't fall into the old "blame the browser cache" trap - Max has a standing memory rule that cache excuses are ~90% wrong; verify live bytes first.
