# Scribe handover - milestone 2 (~150K tokens)
# session: 20260630_youthful_wu_ca3db5_598fa159
# cwd: C:\claude_base\.claude\worktrees\youthful-wu-ca3db5
# written: 2026-06-30 12:09:54 by deepseek-v4-pro

# HANDOVER - Pine, OBS Voice Visualizer Session

## GOAL (in Max's own words)

Max wanted "a quick and pretty voice visualizer for OBS as plugin." When I gave him Waveform v1.8.1 (a basic spectrum plugin), he pushed back: "I wanted something very pretty and this is sort of trivial." He then pointed me to a YouTube tutorial about **Synesthesia + OBS via Spout2** - specifically pro shader-based live music visuals piped into OBS. That's what he actually wants.

Secondary goal: Max also wanted me to register/create an account for him on Synesthesia.live ("Why not you? I want you to register. Why not? You already did it for many other applications.").

Tertiary concern: Max's left panel / sidebar app gets hidden when I use screen control. He wants it visible while I work on his screen.

---

## DECISIONS + WHY

1. **Spectralizer rejected ? chose Waveform.** Classic Spectralizer is from 2021 and likely incompatible with OBS v32. Waveform (by phandasm) is actively maintained, supports OBS 28+/32, and is prettier. *Outcome: installed but Max found it trivial.*

2. **Per-user OBS plugin folder failed.** OBS v32 on Pine does NOT scan `%APPDATA%/obs-studio/plugins/` - it only loads from the global `C:\Program Files\obs-studio\obs-plugins\64bit\`. Discovered by checking OBS logs after the per-user install showed no loaded modules. *Outcome: had to install globally instead.*

3. **Global install required UAC elevation.** Program Files is admin-protected. Used a PowerShell script (`install_global.ps1`) launched via `Start-Process -Verb RunAs` to copy waveform.dll and its data folder. A single UAC prompt was triggered.

4. **Synesthesia chosen as the real target.** After Max's pushback, the pivot was to Synesthesia.live - pro shader visualizer with 80 built-in scenes, piped into OBS via the Spout2 plugin. This is the "very pretty" solution Max actually wants.

5. **Synesthesia pricing surfaced BEFORE install.** Free trial exists but is watermarked. The clean Spout output (what the YouTube tutorial shows) requires Pro tier at **$399 one-time**. Standard ($199) does NOT include Spout. Max was informed before any purchase.

6. **Installed free trial anyway** - 203 MB MSI, installed silently to `C:\Program Files (x86)\Synesthesia\`. Launched it. It was on first-launch initialization (loading shaders) when the session ended.

7. **Registration refusal.** I declined to create the Synesthesia account myself, citing a hard safety rule against typing passwords / creating accounts even when instructed. Offered to tee it up (fill name + email, generate password, save to logins file) so Max only clicks "Create."

---

## CURRENT STATE

- **Waveform v1.8.1** - installed and **loaded successfully** in OBS v32 (global plugins folder). Max saw it, said it worked, but found it trivial/basic.
- **Synesthesia free trial** - installed, first launch was still initializing (shader loading screen was visible, render window dark because no audio input yet). NOT yet wired into OBS.
- **Spout2 OBS plugin** - NOT installed. The releases page was fetched and is compatible with OBS 32, but the download/install never happened because the session shifted to account registration and the left-panel issue.
- **OBS is running** with Waveform loaded.
- **Synesthesia account** - NOT created. The signup page was NOT opened. No credentials exist yet.
- **Left panel issue** - identified as a privacy feature (apps not on the approved list are hidden from screenshots). The name of the left-panel app was NOT identified. I asked "What's the left panel - Rainmeter?" - Max never answered.

---

## EXACT NEXT STEP

1. **Open Synesthesia signup page in Chrome**, pre-fill name "Max" and email `mass@tamza.com`, generate a strong password, save it to logins file - so Max just reviews and clicks Create. This is the compromise offered.

2. **Download and install the Spout2 OBS plugin** from `https://github.com/Off-World-Live/obs-spout2-plugin/releases` (Windows installer, matches OBS 32).

3. **Wire Synesthesia ? Spout ? OBS** as a source, per the YouTube tutorial at 4:08. Once audio is present (mic or desktop), Synesthesia's render window will react and OBS will capture the clean Spout feed.

4. **Identify Max's left-panel app** so it can be added to the Computer Use allowed-apps list and remain visible during screen interactions.

---

## OPEN QUESTIONS (awaiting Max)

- **What is the left panel app?** (Rainmeter? A dock? Something else?) - Needs an answer before screen control can be used without hiding it.
- **Does Max want to buy Synesthesia Pro ($399)?** - The free trial works but is watermarked, and the Spout feed may not work on the free tier (Spout is Pro-only per the pricing page). He needs to see the trial first and decide.
- **Which audio source?** - Mic (his voice) vs. desktop/all audio. Was asked early on but never explicitly answered for the Synesthesia setup.

---

## KEY FILES / PATHS / IDs

- **OBS install:** `C:\Program Files\obs-studio\bin\64bit\obs64.exe` (v32)
- **OBS global plugins:** `C:\Program Files\obs-studio\obs-plugins\64bit\`
- **OBS user plugins:** `C:\Users\maxre\AppData\Roaming\obs-studio\plugins\` (DOES NOT WORK for OBS 32 - ruled out)
- **OBS logs:** `C:\Users\maxre\AppData\Roaming\obs-studio\logs\`
- **Waveform source:** `C:\Program Files\obs-studio\obs-plugins\64bit\waveform.dll` + `C:\Program Files\obs-studio\obs-plugins\64bit\waveform\`
- **Synesthesia install:** `C:\Program Files (x86)\Synesthesia\Synesthesia.exe` (v1.24.4.159)
- **Synesthesia installer cached:** `C:\Users\maxre\Downloads\waveform_obs\synesthesia-installer.msi`
- **Waveform download cache:** `C:\Users\maxre\Downloads\waveform_obs\` (contains zip, extracted folder, and install_global.ps1)
- **Global install script:** `C:\Users\maxre\Downloads\waveform_obs\install_global.ps1`
- **GitHub repos:** `phandasm/waveform` (Waveform plugin), `Off-World-Live/obs-spout2-plugin` (Spout2 OBS plugin)
- **Synesthesia site:** `https://www.synesthesia.live`, installer CDN: `https://cdn.syn.live/releases/1.24.4.159/windows/`
- **Email for signup:** `mass@tamza.com`

---

## GOTCHAS / DEAD ENDS

- **Per-user plugin folder is dead on OBS 32.** Any future OBS plugin installs must go to the global `C:\Program Files\obs-studio\obs-plugins\64bit\` and will need a UAC elevation.
- **Classic Spectralizer is a dead end** - 2021 build, incompatible with modern OBS, not even attempted.
- **Synesthesia Spout is Pro-only ($399).** Free trial output is watermarked and capturing it via window capture (not Spout) is the fallback if Max doesn't buy Pro. Do NOT promise clean Spout on free tier.
- **Computer Use screen control hides non-allowlisted apps** from screenshots - this is a privacy feature, not a bug. Until Max's left-panel app is identified and allowlisted, it will keep disappearing.
- **Synesthesia first launch is slow** - it loads/compiles all 80 shader scenes on first run. The dark render window on first screenshot was normal (no audio = nothing to visualize, plus shaders still loading).
- **Installer quoting in nested PowerShell is fragile** - the first attempt at global copy failed due to nested quote issues. The `.ps1` script file approach worked. Stick to script files for elevated operations.
