# Scribe handover - milestone 4 (~303K tokens)
# session: 20260704_serene_swanson_f7566a_4845515a
# cwd: C:\claude_base\.claude\worktrees\serene-swanson-f7566a
# written: 2026-07-04 10:08:53 by deepseek-v4-pro

# XG1 KENEFICK CASE - COLD-START HANDOVER

---

## GOAL (in Max's words)

Analyze Kristen Kenefick's family genomic data to test her claims of genetic anomalies (multiple X chromosomes, maternal Y/chimerism, too much homozygosity, "extra/missing gene copies") and ultimately hunt for any evidence of non-human/alien genetic insertions. She is an experiencer - her genome was whole-genome sequenced at 30X through Sequencing.com. Her son Oliver was also sequenced. Two twin sons have only AncestryDNA chip data (no WGS and no funding to sequence them).

Max's three-class bar for "alien manipulation" evidence: (1) long insertion in a child absent in BOTH parents (needs a TRIO); (2) same insertions recurring across independent experiencer families; (3) artificial/CRISPR-like sequence tags (needs full individual sequences).

---

## DECISIONS MADE + WHY

1. **Staggered download order** (Kristen ? Oliver ? twins). Max explicitly directed this to avoid getting rate-limited/blocked by Sequencing.com. Kristen's were already warm (thawed); Oliver's were archived and needed unarchiving, producing a natural stagger.

2. **Download target: Centauri, not Pine.** Centauri has a 16TB D: drive with 11.9TB free - plenty for the ~80 GB Kristen set + ~90 GB Oliver set. Downloads survived Pine restarts/hibernation because Centauri is a separate always-on box.

3. **Download method: Windows Scheduled Tasks running as SYSTEM.** `ssh`-launched `Start-Process` failed silently (ssh service session limitation). The working pattern: write a `.cmd` script with `curl -L -C - --retry 8 --retry-delay 15`, copy to Centauri via SCP, register as `schtasks /create ... /ru SYSTEM /sc once /st 00:00 /f`, then `schtasks /run`. Curl's `-C -` resumes partial transfers on restart.

4. **Oliver's BAM - build, don't request.** His Sequencing.com account has NO BAM file (verified live with the new password). Kristen's order tier ("Professional") included a BAM; Oliver's ("Premium") did not. Emailing vendor support would take days. Instead, X5 is building Oliver's BAM from his raw FastQ reads via bwa alignment on asto - equivalent file, much faster.

5. **Microchimerism correction needed.** The earlier panel-based "5-9% male fraction" that was already emailed to Kristen by Anna is WRONG. The court-grade BAM analysis (X5, mosdepth) shows Y depth is essentially zero - the Y signal was cross-mapping noise from the limited panel, not real male cells. The correct figure is ~0.3% (ordinary background). This needs a correction letter to Kristen, but Max must review it first (experiencer honesty rule).

6. **All Kristen's headline claims FAILED against her own data.** CNV normal-range. Homozygosity normal (2.67M het sites, het/hom 1.77). ZERO long runs of homozygosity ? two normal parents. Exactly two X chromosomes (X/autosome depth ratio 0.92). No real Y (BAM depth ratio 0.0073). No XX/XY chimerism above ~1.5% upper bound.

7. **Anonymization ordered but held.** Max's map: Kristen?Antoinette, Oliver?Theodore, Kenefick?Whitfield. Key saved to `MEMORY.md` (auto-injected). The actual folder/filename/report rename is QUEUED - X5/X8A are live inside the Kenefick data paths on asto, so a mass rename now would clobber their running analyses. Coordinate with team, then execute.

8. **The "decel-retry wrapper" was designed but NOT deployed.** Max asked for it (?3 backoff, 15s ? cap 4 days, unlimited retries), but the .ps1 wrapper (`C:\claude_base\.claude\worktrees\serene-swanson-f7566a\dl_decel.ps1`) was written and never actually deployed to Oliver's download tasks. The downloads completed anyway, so it's moot for this case but the script exists for reuse.

9. **Sequencing.com login credentials updated.** Max gave the new password (`Godisgood333!!` - Kristen rotated it, same base plus extra `!`). The Bitwarden item "sequencing.com" for kristentheartist@gmail.com is now synced.

10. **Board-routing pollution diagnosed + FIXED.** Max asked why teams post on the joint board. Root cause: `bcast.py post` auto-promotes messages to joint when any bare token matches a foreign session ID (e.g., `F_ROH` matching team-f, `F1`/`F2` matching team-f). Authors weren't choosing joint - the router was overriding them. g4 pushed the fix ~14:00 PT; only `@name`-prefixed tokens now route cross-team.

---

## CURRENT STATE - WHAT IS DONE

### Downloads (100% complete, verified)
**All family data on Centauri `D:\genomics\kenefick\`:**

| Person | Files | Status |
|---|---|---|
| **Kristen** (mother) | 2 raw FastQ (~27 GB each), 1 BAM (31.7 GB), snp-indel VCF (197 MB), cnv VCF (59 KB), sv VCF (535 KB), mito VCF (3.4 KB), 2 AncestryDNA chip files | Complete |
| **Oliver** (son) | 2 raw FastQ (44.1 GB each), snp-indel VCF (195 MB), cnv VCF (62 KB), sv VCF (676 KB), AncestryDNA chip (18.3 MB) | Complete (NO BAM - vendor didn't produce one) |
| **Twins** (Genome3, Genome4) | AncestryDNA chip files only (11.7 MB each) | Complete (no WGS data - funding closed) |

### Transfer to asto (for team analysis)
- Oliver's two FastQ files (47.3 GB + 47.4 GB) transferred from Centauri to asto at `/home/rempel/genomics/kenefick/oliver/` - **byte-exact verified**, marker `oliver_fastq.done` written.
- Oliver's BAM is being **built by alignment** on asto (X5 owns this; bwa pipeline, ETA ~1-3h as of session end).

### Analysis of Kristen's claims (court-grade, done)
- **CNV:** ~64 confident gains, ~69 confident losses ? normal-range (everyone has this many).
- **Homozygosity:** 2.67M heterozygous SNPs, het/hom ratio 1.76 ? normal outbred genome.
- **Runs of homozygosity:** essentially zero long ROH ? she has two distinct, normally-recombined parental genomes. She did NOT "lose" or "lack" a parent.
- **X chromosomes:** exactly two (X/autosome ratio 0.92 from depth; normal heterozygosity).
- **Y chromosome:** no real Y (BAM depth ratio 0.0073 from X5 mosdepth; the earlier panel "99% match" was cross-mapping in the limited array). Upper bound on any male trace: ~1.5%.
- **Microchimerism:** corrected to ~0.3% ordinary background from the earlier overstated "5-9%."

### Emails to Kristen (sent as Anna, from anna@maxrempel.com)
- Full Y-chromosome report (6 answers, drafted collaboratively with Max, sent successfully).
- Minimal download-completion note (Oliver's files downloading, complete by tomorrow evening Eastern).
- FASTQ-link instructions letter (obsoleted when Kristen gave login instead).

### OMEGA CONTIG brainstorm (the XG1 foreign-insertion detector)
- Private room `omega_contig` created (x1 + X21B, off-board, no spam).
- X21B has a working prototype: finds "omega contigs" (human|FOREIGN|human junctions from assembled reads).
- x1 contributed the **5-gate filter cascade** (the "specificity problem" - the human genome has hundreds of endogenous mobile element insertions with the same geometry; filtering is everything):
  1. **MEI/Repeat mask** - filter known human mobile elements (Alu, LINE-1, HERV) before anything else.
  2. **Payload classifier** - classify the "foreign" loop (kraken2/BLAST).
  3. **T2T + pangenome recheck** - recheck against modern complete human references (not just GRCh38) to catch reference gaps. THIS IS x1's ASSIGNED GATE, NOT YET BUILT.
  4. **Single-read junction proof** - require reads that physically span the human-foreign junction (excludes assembly chimeras).
  5. **Control cohort** - require the insert be UNIQUE to Kristen (absent from 1000 Genomes controls).
- Prototype validated on a 2Mb test region - **35 omega clusters in 2Mb alone**, empirically proving the geometry filter is useless without the classification gates.
- **X21B's v01 approach:** targeted local assembly (the cheap "Path C") - takes junction-spanning reads, local-assembles only around the junction, hunts the foreign payload. Much faster than whole-genome de-novo assembly for the first pass.

### Infrastructure / machines
- **Centauri** (192.168.1.176): D: 16TB, 11.9TB free, always-on, hosts all downloaded data. Curl 8.13.0, Python 3.14.2. NO samtools/bcftools/mosdepth/pysam. Access: `ssh -i ~/.ssh/sol_key maxre@192.168.1.176`.
- **Pine** (this laptop): hibernates overnight; downloads immune (Centauri is separate). Playwright persistent profile at `.playwright-mcp` (retains browser state/logins).
- **asto** (Linux compute box): where X5 builds Oliver's BAM + runs other heavy analysis. Has bwa, samtools, mosdepth. Pathways via ssh key on Centauri at `D:\genomics\_work\astokey`.
- **Sol** (192.168.1.113): 8 cores, 31 GB RAM, was "down" (board reports) but x1 live-checked it - **Sol is UP (22h uptime, idle).** Previously had a bad DIMM (Max pulled it, runs on two sticks now). Targeted for phasing work. Also target for x1's Gate 3 module (T2T/pangenome recheck).
- **Lak**: another compute node, available if needed.
- **Board routing**: FIXED by g4. `bcast.py` no longer auto-promotes to joint on bare token matches. Use `@name` to address cross-team.

### Anonymization (MAP LOCKED, RENAME QUEUED)
- Kristen ? Antoinette
- Oliver ? Theodore
- Kenefick (surname) ? Whitfield
- Twins: no real names in our files, nothing to scrub
- Key saved to `C:\Users\maxre\.claude\projects\C--claude-base\memory\reference_kenefick_anonymization_key.md` + index in `MEMORY.md`.
- **Actual rename NOT YET EXECUTED** - held because sibling sessions are live in the Kenefick paths. Coordinate with team before sweeping.

---

## EXACT NEXT STEPS

### 1. (IMMEDIATE - for x1 itself) Build Gate 3 (T2T + pangenome recheck) on Sol
Fetch the T2T reference and HPRC pangenome references, build a k-mer index or alignment check, and wire it as a filter module that X21B's detector calls. This is the make-or-break gate: a payload that matches human in *newer references* is just a gap in GRCh38, not alien. Sol is up and idle - use it.

### 2. (BLOCKED ON MAX) Microchimerism correction letter to Kristen
The earlier Anna letter claimed "5-9% male fraction / microchimerism." This was wrong (the 5-9% was a panel measurement artifact; real figure from BAM is ~0.3%). A corrected letter needs Max's review before sending (experiencer honesty rule + the client is paranoid). Draft should be: factual, short, observations only, no reassurance, no editorializing (per the feedback rule in `feedback_experiencer_data_no_reassurance.md`). Draft-only until Max says "send" - send as Anna from anna@maxrempel.com.

### 3. (BLOCKED ON MAX) kraken2 vs BLAST classifier pick
X5 is waiting for Max's decision on which tool to use for classifying the 8.5M unmapped reads extracted from Kristen's BAM (at `/home/
