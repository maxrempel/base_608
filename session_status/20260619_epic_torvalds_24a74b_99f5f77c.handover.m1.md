# Scribe handover - milestone 1 (~111K tokens)
# session: 20260619_epic_torvalds_24a74b_99f5f77c
# cwd: C:\moma\.claude\worktrees\epic-torvalds-24a74b
# written: 2026-06-19 13:26:12 by deepseek-v4-pro

# HANDOVER - Max's Whisper dictation keyboard setup

## GOAL (in Max's own words)
> "search online for the best kebyoard for windows based on user feedback, so the keyboard is using Openai whisper to transcribe my voice."

Refined filter explicitly by Max:
**Windows app + system-wide dictation + your own OpenAI API key + text pasted into the active cursor.**

## DECISIONS MADE AND WHY

Max evaluated the first round of suggestions (WhisperTyping, Wispr Flow, OpenWhispr) and then supplied his own corrected ranking with reasoning. The weighted criteria are:

1. **Must be Windows-native.**
2. Must do **system-wide dictation** (not just a single app - type into any text field).
3. Must accept **the user's own OpenAI API key** (bring-your-own-key).
4. Must **paste transcribed text directly at the cursor** in whatever app is active.
5. Should support the `whisper-1` model (the model that already handles Max's accent well).

The decision:

- **WhisperWriter is the #1 choice** - "best match." It's a small background app that uses a hotkey, records, sends to OpenAI API, and writes into the active window. It explicitly allows `whisper-1`, custom API endpoint `https://api.openai.com/v1`, and hold?to?record mode.
- **SpeechPulse is #2** - a more polished commercial Windows alternative with a 30-day trial, support for Whisper APIs, and good stability per Reddit feedback. Recommended if the GitHub/Python setup of WhisperWriter causes frustration.
- **OpenWhispr is #3, but overbuilt** - it's a full voice/agent/notepad system, not just a simple Whisper?to?text keyboard. It does support BYOK and global hotkeys, but adds complexity Max doesn't need.

Ancillary decision: **stick with `whisper-1`** - OpenAI now offers `gpt-4o-transcribe` and `gpt-4o-mini-transcribe`, but the user's known good accent performance is with Whisper. Do not switch to random "AI dictation" engines.

Max's own practical recommendation to himself: "try WhisperWriter first; if setup annoys you, try SpeechPulse."

## CURRENT STATE

The conversation is in the **middle of Max refining the answer**. The last user prompt contains Max's corrected ranking and recommendation. I (Claude assistant in this session) have not yet replied to that message; the session ends immediately after Max's ranking. No further tools have been called, no installation steps initiated, no configuration started.

So the raw session state is:

- Max has decided on the two main candidates (WhisperWriter / SpeechPulse).
- The assistant has not acknowledged or acted on that ranking.

## EXACT NEXT STEP

**Acknowledge Max's corrected ranking and offer the next concrete action.**

The assistant should:

1. Confirm that Max's ranking matches all criteria and is a great shortlist.
2. Explicitly note that WhisperWriter is the first thing to try.
3. Offer to help with the setup of WhisperWriter right now: fetch the GitHub repo, check dependencies (Python, API key), or open a browser to the repository.
4. Alternatively, if Max just wanted the search, the assistant can ask "Do you want me to walk through the WhisperWriter installation steps, or would you like to explore SpeechPulse first?"

The handover for a cold session: resume from the point of Max's corrected ranking by replying supportively and prompting action.

## OPEN QUESTIONS

No open questions are awaiting Max at this point. Max has not asked anything beyond the search. The session has no pending yes/no queries from the assistant.

## KEY PATHS, IDs, AND NAMES

- **WhisperWriter**  
  GitHub: `https://github.com/savbell/whisper-writer`  
  (Razvan's repo, MIT license, Python project)

- **SpeechPulse**  
  Mentioned via Reddit review:  
  `https://www.reddit.com/r/windowsapps/comments/1dn46wx/speechpulse_a_windows_app_for_dictation_and_file/`  
  (30-day trial, paid app)

- **OpenWhispr**  
  GitHub: `https://github.com/OpenWhispr/openwhispr`  
  (Discarded for this use case as overbuilt)

- **OpenAI API reference**  
  Speech-to-text guide: `https://developers.openai.com/api/docs/guides/speech-to-text`  
  Models: `whisper-1`, `gpt-4o-transcribe`, `gpt-4o-mini-transcribe`

- **Noted, but out of scope**  
  WhisperTyping (paid, no clear BYOK confirmation)  
  Wispr Flow (supports phone, not solely Windows keyboard replacement)

## GOTCHAS AND DEAD ENDS ALREADY RULED OUT

- The initial assistant search surfaced **WhisperTyping, Wispr Flow, and OpenWhispr**. Those have been **superseded**:  
  - WhisperTyping is not confirmed to support user's own API key in the way Max wants.  
  - Wispr Flow is more of a cross-device dictation tool, not a simple "keyboard" app that pastes into the active cursor; it doesn't strictly meet the "Windows app + system?wide dictation + own API key" filter.  
  - OpenWhispr is still viable, but Max sees it as overbuilt - it adds notebooks, agents, etc.

- The refined search now clearly focuses on **WhisperWriter vs. SpeechPulse**.

- Setup annoyance is a known risk: WhisperWriter is a GitHub Python project; if the environment or hotkey daemon are too fiddly, the fallback is SpeechPulse.

- Model selection: stick with `whisper-1` - don't blindly switch to `gpt-4o-transcribe` or `gpt-4o-mini-transcribe` unless there's a proven benefit for Max's accent.
