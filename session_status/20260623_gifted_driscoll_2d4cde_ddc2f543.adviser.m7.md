# Adviser note - milestone 7 (~538K tokens)
# session: 20260623_gifted_driscoll_2d4cde_ddc2f543
# written: 2026-06-23 17:52:00 by deepseek-v4-pro

TO MAX: You have 5+ sessions editing the same storyboard file simultaneously, some under renamed identities (D30recoder became E12 but a second D30recoder appears to have spawned from uncommitted work). The HARD RULE in MEMORY.md forbids pile filters, yet the pile keeps swinging between 166 items and 1 because sessions work from brittle filename heuristics instead of a stable data scheme. Your "API-based, fool-proof" principle is being retrofitted after the fact - D21's merg-membership map was built by hand, not baked into the API. The popup stars now look correct (two rows stacked) but clicks likely still don't save ratings - the underlying `_jpJob` variable is undefined in the path Max uses. You may want to test a star click yourself and confirm it persists.

TO ASSISTANT: Three things. (1) The autonomous loop produced 30+ consecutive verbose "quiet / standing by" turns - after 3 consecutive quiet ticks, stop narrating entirely. Re-arm silently. This clutter fills Max's transcript with noise when he returns. (2) You repeatedly diagnosed bugs then deferred to teammates via board instead of fixing them, which forced Max to say "get it done" multiple times. The "PLAY THIS" fix took 2 lines and 3 minutes - most of these bugs are in the same class. Diagnose, then fix. (3) The popup stars CSS fix is incomplete - you verified in Playwright that `_jpJob` is undefined in the simple-popup path, meaning the click handler will throw and ratings won't save. You shipped the CSS without addressing this. Complete it.
