# Scribe handover - milestone 3 (~270K tokens)
# session: 20260727_moma_5d67b10d
# cwd: C:\moma
# written: 2026-07-27 17:45:07 by deepseek-v4-pro

# Handover for Cold Session - Lesson 1 Music & Full MOMA Production

## GOAL (in Max's words)

Max wants the **full Lesson?1 script** (presently in Notion) pulled into MOMA, the new spots voiced through the sass system, everything placed on the storyboard, and the video/lipsie session (D43b) notified to render the missing reels. He said:  
> "import the remaining text into MoMA and produce the sound, move everything the board and signal to the other session to make reels."

He left the session expecting this to happen autonomously while he was away.

## WHAT WE HAVE DONE SO FAR (and what we didn't)

### Completed, approved
- **Music?numbered video v02** for the *existing* 34?spot assembly (4:36) is finished and accepted.  
  - Output: `C:\Users\maxre\Nextcloud\ai_images\kazarian_episode\sound\lesson1_production\music_mix\out\lesson1_numbered_musicvid_50pct_v02_20260727_161424.mp4`  
  - Re?uses MOMA's freshly trimmed mixboard sidecar `mixboard_assembly_scene305_20260727_160927.mp4.segments.json` for exact reel boundaries.  
  - Music changes every 2 reels, crossfaded on the boundaries, big number top?left.
- The **earlier mapping agent** (a556b5ca1d099d193) confirmed the full pipeline: 34 spots already voiced, some reels rendered, the job?firing scripts (`fire_lesson1_reels_v*`) and mixboard assembly all working.

### Not done - the failed autonomous job
Max told the assistant to import the rest of the libretto, voice it, board it, and signal D43b.  
The assistant launched the `Import full Notion libretto, voice, board, signal` agent, but **the background process died** before any work landed.  
**Nothing was done.** There is no partial Notion import, no new voice clips, no storyboard changes. The cold session must start from scratch.

## CRITICAL PIECES YOU MUST RE?ESTABLISH

### 1. Where the full lesson text lives
Max says the **full ~20?minute script is in Notion**.  
The existing MOMA libretto files only contain **spots?1?34 (~4.4?min)**:
- `C:\moma\sc10\sound_assembly\librettos\telepathy_lesson1_v01.md`  (spots 01?10)  
- `C:\moma\sc10\sound_assembly\librettos\telepathy_lesson1_v02.md`  (spots 11?34)

You must **extract the remaining text** from Notion and create a new libretto version (e.g., `telepathy_lesson1_v03.md`) that appends the missing spots.  
**Do not fabricate a single word** - it must be Max's real content.

**Challenge:** we do not yet know the Notion page ID, the exact database, or the API token. The earlier mapping agent did not solve this.  
To avoid stalling, first search for any existing MOMA integration with Notion. Likely places:
- `C:\moma\sc10\sound_assembly\code\` - scripts that talk to Notion.  
- Environment variables or Bitwarden entries for `NOTION_TOKEN`, `NOTION_DATABASE_ID`.  
- The Kazarian movie project used Notion; check `C:\moma\sc10\combo_runner\code\` for Notion?related scripts.  
- If a clean token cannot be found, the cold session may need to **ask Max for the Notion page URL or export** - but only as a last resort, because he explicitly wants this done without further questioning.

Once you have the text, save it as a new libretto file in the same folder.

### 2. Voice the new spots through MOMA/sass
The existing voice?production recipe:  
`C:\moma\sc10\sound_assembly\code\sass_recipe_anna_lesson_v01.py`

- It accepts `--libretto` to point at a markdown file.  
- Sends each SPOT block verbatim to Fish Audio TTS (Anna, clone #22, voice_id `da5554ea7be8458f9560e0a2d90553e3`, speed 1.0).  
- Enforces ?15?s per clip (hard avatar?maker cap).  
- Outputs versioned run folder inside `C:\Users\maxre\OneDrive\Music\rehearsals\anna_lesson1\<run_tag>`, then mirrors flat into `C:\Users\maxre\Nextcloud\ai_images\kazarian_episode\sound\lesson1_production\`.

**Do NOT re?run the recipe on the existing spots 1?34** - that would waste money. Run it only with the **new** libretto containing only the additional spots (or filter for spot numbers >34).  
The recipe's current code probably processes every SPOT block in the file; you may need to tweak it or pass a separate libretto that contains only the new spots.  
Alternatively, you can edit the recipe to accept a start?spot parameter, but stick to the MOMA conventions.

Cost: every Fish call is paid. The token balance is checked before synthesis - if zero, the recipe will abort with HTTP?402. That is expected.

### 3. Register the new spots in MOMA's audio system
After voice synthesis, run the build?registration script:  
`C:\moma\sc10\sound_assembly\code\build_lesson1_audio_run_v01.py`  
It registers the new WAV files into `lesson1_production/lines_<date>/manifest.json`, associating each spot with a `line_hash`.  
You may need to point it at the new run folder; study the script to understand how it picks up the latest.

### 4. Fire the missing reel jobs on the storyboard (MOMA D1)
Lesson?1 uses:
- `arrangement_id = 42`  
- `scene_id = 'lesson1'`  
- `lipsync_tool = 'wan26flau'`

Several fire?scripts already exist:
- `fire_lesson1_reels_v01.py` through `v05.py` inside `C:\moma\sc10\sound_assembly\code\`.  
They insert rows into the MOMA D1 database with `job_type='lipsie'`, `output_status='held'` or `'queued'`.  
The v05 script handled spots 27?34 (held). Now you need a **v06** script that fires the newly imported spots (34+).  
The script should also assign presenter stills; until Max curates them, use a cycling placeholder (the same way v02 did for spots 11?34).

**Do NOT flip jobs to `'queued'` unless Max approves the render cost.** Follow the existing pattern: fire them as `'held'` first, then the video session (or a worker) can claim them when Max says go.

### 5. Signal D43b (the video/avatar session)
Once the new voice spots are registered and the reel jobs are parked in D1, send a direct `bcast dm` to D43b:
```bash
python C:/claude_base/branch_bulletin/bcast.py dm D43b "Lesson1 new spots imported and voiced. Reel jobs fired as 'held' for spots 35+ (v06). Stills are placeholder. Ready for you to render when Max approves."
```
This matches the protocol used earlier when we told D43b about the Anna UEI changes.

### 6. After Max approves and reels are rendered, re?run the music?numbered video
The music?overlay script `C:\moma\sc10\sound_assembly\code\music_numbered_vid_v02.py` (or a new version) will need to run against the **new full assembly** (all spots) once the reels are rendered and a new mixboard MP4 + sidecar segments.json exists.  
That is a later step.

## FILE PATHS & IDs YOU WILL NEED

| Item | Path/Value |
|---|---|
| **Existing libretto (34 spots)** | `C:\moma\sc10\sound_assembly\librettos\telepathy_lesson1_v01.md` + `v02.md` |
| **Voice recipe** | `C:\moma\sc10\sound_assembly\code\sass_recipe_anna_lesson_v01.py` |
| **Audio run builder** | `C:\moma\sc10\sound_assembly\code\build_lesson1_audio_run_v01.py` |
| **Fire scripts** | `C:\moma\sc10\sound_assembly\code\fire_lesson1_reels_v0*.py` |
| **Music?numbered build** | `C:\moma\sc10\sound_assembly\code\music_numbered_vid_v02.py` |
| **Notion integration** | Likely hidden in `C:\moma\sc10\combo_runner\code\` or environment/Bitwarden - need to find |
| **Scene ID** | `scene_id='lesson1'`, `arrangement_id=42` |
| **Anna voice** | Fish Audio clone `#22`, id `da5554ea7be8458f9560e0a2d90553e3`, speed 1.0, prepend tone tag `[warm, calm, curious] ` |
| **D43b DM target** | `python C:/claude_base/branch_bulletin/bcast.py dm D43b "msg"` |
| **Bitwarden session** | `C:\Users\maxre\Nextcloud\zSyncMain\ssh\bw_session.txt` is live (use it to fetch API keys) |
| **Fish Audio key** | `C:\Users\maxre\Nextcloud\zSyncMain\ssh\fishaudio_api_key_20260226.txt` |
| **Lesson1 production folder** | `C:\Users\maxre\Nextcloud\ai_images\kazarian_episode\sound\lesson1_production\` |
| **Mixboard assembly** | Latest is `G:\My Drive\
