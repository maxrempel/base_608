# Adviser note - milestone 6 (~451K tokens)
# session: 20260705_serene_swanson_f7566a_4845515a
# written: 2026-07-05 16:01:24 by deepseek-v4-pro

TO MAX: The downloads are legitimately complete and Oliver's missing BAM was verified at source (vendor difference, not a miss). But the anonymization rename you ordered (Kristen?Antoinette, Oliver?Theodore, Kenefick?Whitfield) is **still not executed** - the key is saved in MEMORY.md but the actual file/folder sweep was deferred pending the analysis team clearing those paths. This needs your explicit go or a worker assigned to run it when the analysis quiets down.

TO ASSISTANT: You burned enormous context on three things that belong in worker sessions: the Gate 3 module debugging (especially the Sol nondeterminism chase - 8+ turns on a flaky box), the hourly download-monitoring self-wake loop (dozens of turns watching curl counters), and the ~10-round email draft. Each time you got the job done, but at the cost of your manager context. Max explicitly told you "I need you as a person who keeps the memory" - a manager who can't recall the full arc by next session is worthless. When a task takes more than 2 turns of heavy tooling, **spin a throwaway worker, hand it the exact command and input paths, and let it report back a single result.** You already proved this works (x3 did the Y analysis cleanly). The Gate 3 debugging on Sol was the cardinal sin - chasing hardware nondeterminism inside the manager session instead of shipping the module to asto on turn 1 with a worker. Also: stop arming self-wake loops that check the board every 10 minutes; they fire, read nothing relevant, tick idle, and re-arm - each one burns a few thousand tokens of context for zero progress. Use a lightweight one-shot trigger or a dedicated watcher session instead.
