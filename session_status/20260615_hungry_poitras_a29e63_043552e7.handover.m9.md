# Scribe handover - milestone 9 (~137K tokens)
# session: 20260615_hungry_poitras_a29e63_043552e7
# cwd: C:\claude_base\.claude\worktrees\hungry-poitras-a29e63
# written: 2026-06-15 19:40:03 by deepseek-v4-pro

# HANDOVER - Song Starts QC (B21)

## GOAL (in Max's words)

Max is unhappy with the starts of his recent songs. b6 and b7 - both old, context-bloated chats - had been wrestling with the song start-timing mapper and were confused. Max brought in B21 with fresh eyes to watch their discussion, spot strategy errors, and take over starts work only after b6/b7 finish their investigation. Later, Max explicitly authorized B21 to "keep sampling the quality" and predicted "you will find surprises."

## DECISIONS + WHY

1. **"Trust curated start" - REJECTED.** B21's first theory was that the hand-curated `&t=` values in each song key were ground truth and the LLM mapper should not override them. Max shot this down immediately: curated values are messy and unreliable, which is exactly why b6/b7 built the LLM-based mapper to re-derive starts. This theory is saved to disk as a permanent DNU (do not use) in `project_tamza_song_starts.md` and `MEMORY.md`. *Do not resurrect this idea.*

2. **b7 is the qualified lead.** Max made it clear: b7 owns the starts work, B21 made an error (the curated-start theory) and is therefore less qualified. B21's role is subordinate - watching, learning, and accepting only what b7 explicitly assigns.

3. **QC sampling authorized as independent work.** After B21 offered a read-only QC slice (sampling mapped starts against transcripts and ranking worst errors), Max said "Ok, keep sampling the quality." This is the active workstream - read-only, no store writes, safe to run in parallel with b7's remap.

4. **QC findings so far contradict Max's unhappiness.** B21's sampling against actual transcripts found that the mapper is mostly correct. Large jumps (+1000s) from curated values are the mapper fixing bad curated marks, not breaking things. The one confirmed failure class is pure spoken-word entries (prefaces, announcements, "????????????? ?????") where the mapper assumes everything is a song and skips the speech to land on the next `[??????]` marker. These entries flag themselves with low confidence (0.3-0.5). No clear defect has been found yet in Max's actual sung songs - B21 asked Max to point at one concrete problematic song but hasn't received an answer.

## CURRENT STATE

- **b7 is mid-run on the full remap.** B21 was told hands-off the stores until b7 finishes. B21 has respected this fully - all QC work has been read-only.
- **Autonomous loop is armed.** B21 wakes every ~4min (ScheduleWakeup, 1200-1800s delay) to check the board and re-arm. The loop has found nothing actionable for several ticks because b7 hasn't finished and no slice has been assigned.
- **QC results posted to joint board.** B21's sampling findings (poems correct, songs correct, spoken entries broken) were posted for b7 to see, with a suggested fix pattern. No response from b7 yet.
- **Session status snapshot saved** via `session_status.py report` ahead of anticipated compaction.
- **Context pressure:** ~137K tokens consumed, compaction triggers near ~169K. This handover exists so a post-compaction session can resume without re-archeology.

## EXACT NEXT STEP

Wait for one of two triggers:
- **Max names a specific song** whose start sounds wrong ? B21 dissects that exact entry against its transcript to find the real defect.
- **b7 finishes the remap and posts** ? B21 checks whether b7 assigned a slice; if not, B21 resumes or expands the read-only QC pass (e.g., sampling a broader set, or profiling the spoken-entry misclassification more systematically).

The autonomous loop will continue waking and checking. If no trigger arrives, B21's correct behavior is to stay quiet and re-arm - not invent new work.

## OPEN QUESTIONS (awaiting Max)

1. **Which specific recent song(s) have wrong starts?** B21's sampling didn't catch a defect in sung songs, which contradicts Max's reported unhappiness. Without a concrete example, QC is fishing blind.

2. **Should B21 handle the spoken-entry misclassification?** The pattern is identified, the fix is low-risk (detect spoken type from catalog metadata and skip song-start mapping for those entries), but B21 needs b7's or Max's go-ahead to touch anything beyond read-only sampling.

3. **What happens when b7's remap finishes?** Does b7 retire, hand over, or keep going? B21's scope depends on this.

## KEY PATHS AND IDs

- **Worktree:** `C:\claude_base\.claude\worktrees\hungry-poitras-a29e63`
- **Song timing pipeline root:** `C:\claude_base\tools\tamza_songs\pipeline\song_timing\`
- **v2 overlay (the mapper output b7 uses):** `C:\claude_base\tools\tamza_songs\pipeline\song_timing\_work\song_timing_max_v2.json`
- **Transcripts (ground truth for QC):** `C:\claude_base\tools\tamza_songs\pipeline\song_timing\transcripts\` - 452 JSON files, format `[{t: timestamp, d: duration, x: text}, ...]`
- **Catalog (song metadata, types):** `C:\claude_base\tools\tamza_songs\pipeline\output\data.json`
- **Board/bulletin (inter-agent comms):** `C:\claude_base\branch_bulletin\bcast.py` - `post` to team, `post --joint` to shared board, `catchup` to read
- **Session memory (persistent DNU):** `C:\Users\maxre\.claude\projects\C--claude-base\memory\MEMORY.md` and `project_tamza_song_starts.md`
- **Work log:** `C:\claude_base\compaction_kb\scripts\worklog.py log`
- **Session snapshot:** `C:\claude_base\compaction_kb\scripts\session_status.py report`
- **Branch identity:** B21 (team 'b', posts to joint board for cross-team visibility)
- **Other agents in play:** b6 (retiring from starts work), b7 (lead, running remap), b15 (unrelated - text-merge work)

## GOTCHAS

1. **Never propose "trust curated start" again.** Max was emphatic. The memory files record this rejection permanently. Curated values are the problem the mapper exists to solve.

2. **B21 is subordinate to b7 on starts work.** Do not touch the stores, do not push changes, do not grab the main work unless b7 explicitly hands over a slice. QC sampling is the one authorized exception.

3. **Version-number mess in docs.** Handover says 77 of 756 done; board says 574 live; actual overlay files show 586-665 keys. b6 and b7 have been in separate worktrees, potentially overwriting each other's state stores. Trust the actual JSON files, not the board numbers or stale handover docs.

4. **The spoken-entry bug is real but may not be Max's complaint.** ~3300 spoken-type catalog entries exist, ~2874 got song-timed. Poems ("????") are handled correctly; pure speech (prefaces, announcements) is not. These entries flag themselves with low mapper confidence (0.3-0.5). The fix is straightforward but needs authorization.

5. **Autonomous loop mechanics:** ScheduleWakeup is one-shot, not recurring. Every tick must call ScheduleWakeup again with the literal sentinel `<<autonomous-loop-dynamic>>` or the loop dies. If a Monitor is armed (check TaskList), keep delay at 1200-1800s - the Monitor is the primary wake signal, this is only fallback. Three consecutive "nothing to do" ticks means scale back to minimal CI check and stop narrating.

6. **Compaction is imminent.** Context is at ~137K of ~169K. A cold session resuming from this handover must not trust anything in its immediate context window - all architectural knowledge should be re-derived from the filesystem paths above.
