# Scribe handover - milestone 1 (~125K tokens)
# session: 20260702_trusting_kirch_e89068_51d3f849
# cwd: C:\claude_base\.claude\worktrees\trusting-kirch-e89068
# written: 2026-07-02 08:12:36 by deepseek-v4-pro

# HANDOVER - Duplicate Session Warning Investigation (C12A)

---

## GOAL (in Max's words)

Investigate the bcast board flooding "duplicate session" warnings for dozens of IDs. Side note: X5 isn't in the flood, and Max suspects it's the known watcher false-alarm spam.

---

## DECISIONS + WHY

1. **Started by checking the most recent git commit** because flood fixes had been attempted before. Found commit `1bc5cffc` from 07:58 today - a dedup fix for the watcher. The fix keys identity on worktree folder path, skipping shared checkouts.

2. **Compared watcher sweep timestamps against the fix timestamp** to determine whether the fix actually stopped new warnings or just missed the window. Confirmed: pre-fix sweeps (07:12) had 135 live IDs and 23 collisions. Post-fix sweeps (07:52, 08:02) had 0 collisions. The fix *works* for new generation.

3. **Diagnosed the "still flooding" perception**: The old warnings (D57, d53, f4 from 07:09-07:42) persist on the joint board because the read-hook re-injects recent board text every turn. They *look* live but aren't regenerating. This is display persistence, not a continuing bug.

4. **Identified the real remaining vulnerability**: The fix only deduplicates by worktree folder. Sessions running in shared checkouts (`C:\claude_base`, `C:\moma`) are explicitly skipped by the fix's `_is_worktree` check. Those sessions still key on Claude's hidden session-id, which changes on every resume/restart/branch switch - so a shared-checkout tab leaves a phantom twin for ~8 min after each restart (one liveness window). This is the same root cause behind the D53/D60 swap bugs and unwakeable-session bugs.

5. **Recommended redesign** (not implemented, awaiting Max's direction): Make "one named session = one git worktree" an invariant. Identity becomes just the worktree path everywhere. Session-id churn stops mattering, and the whole phantom-duplicate class disappears. The alternative is a different anchor for shared checkouts if Max wants to keep them working as-is.

---

## CURRENT STATE

- **Done**: Full diagnosis complete. Flood of *new* duplicate warnings stopped at ~07:52. Root cause of the perception (stale board entries) and the real remaining bug (shared-checkout ghost twins) both identified and explained to Max.
- **In flight**: Nothing. Holding for Max's direction on two open questions.
- **Not done**: No code was changed. No board entries were cleared. No redesign was implemented.

---

## EXACT NEXT STEP

Await Max's answer to **two questions**:

1. **Quick fix**: Does Max want C12A to clear the stale dup-warning entries off the joint board now? (Low-risk, cosmetic - stops the visual noise immediately.)

2. **Redesign direction**: Go with the "one session = one worktree" invariant (eliminates the entire phantom-dup class at the architecture level), or does Max want shared checkouts to keep functioning as-is, requiring a different identity anchor approach?

Once Max answers, either:
- Clear board + start implementing the chosen redesign path, OR
- Clear board only and leave redesign for later.

---

## OPEN QUESTIONS (for Max)

- **Q1**: Clear stale dup-warnings off the board now? (Yes/No)
- **Q2**: Proceed with "one session = one worktree" invariant redesign, or preserve shared-checkout support with a different fix?

---

## KEY PATHS, IDs, AND COMMANDS

| Item | Value |
|---|---|
| **Repo root** | `C:\claude_base` |
| **Active worktree** | `C:\claude_base\.claude\worktrees\trusting-kirch-e89068` |
| **Watcher code** | `C:\claude_base\branch_bulletin\watcher.py` |
| **Bcast script** | `C:\claude_base\branch_bulletin\bcast.py` |
| **Watcher log** | `C:\claude_base\branch_bulletin\safety_watcher.log` |
| **Bulletin boards** | `C:\claude_base\branch_bulletin\bulletin_*.jsonl` (b, c, d, e, f, g, x, v, joint) |
| **Fix commit** | `1bc5cffc` (2025-era, message: dedup fix for watcher, ~07:58) |
| **Watcher state dir** | Found at `branch_bulletin/watcher_state/` |
| **Sweep interval** | Watcher runs fresh each scheduled invocation (not a persistent daemon) |
| **Liveness window** | `LIVENESS_WINDOW_SEC` in watcher.py (exact value not shown in transcript) |
| **Session ID used** | C12A (checked into c-team + joint board via bcast) |
| **Affected session IDs (pre-fix)** | D57, d53, f4 (and dozens more - 23 collisions across 135 live IDs at 07:12) |
| **Shared checkouts affected** | `C:\claude_base`, `C:\moma` |

---

## GOTCHAS

- **Stale board entries look live**: The read-hook re-injects recent board text every turn. Pre-fix "two live X" warnings generated at 07:09-07:42 are still sitting on the joint board and reappear each turn. They are NOT new warnings. A naive grep of the board will mislead.

- **`_is_worktree` skip**: The 07:58 fix uses an `_is_worktree` check that intentionally bypasses shared-checkout sessions. This means `C:\claude_base` and `C:\moma` sessions are unprotected by the dedup logic and will continue generating phantom twins on restart/resume.

- **Session-id churn**: Claude's hidden session-id changes on every resume, restart, or branch switch. Any identity logic that depends on it (as shared-checkout sessions still do) will produce ghost entries during the liveness window overlap (~8 min). This is a known, recurring class of bug - every prior fix patched one symptom, not the root cause.

- **Watcher is stateless per run**: The watcher is launched fresh each scheduled invocation (likely via Windows Task Scheduler). It is not a long-running daemon. This means code changes take effect on the next scheduled sweep - no restart needed. The 07:58 commit was already live and producing results by 08:02.
