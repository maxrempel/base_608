# Scribe handover - milestone 2 (~217K tokens)
# session: 20260715_angry_moser_2d773b_2163fd6d
# cwd: C:\claude_base\.claude\worktrees\angry-moser-2d773b
# written: 2026-07-15 10:00:28 by deepseek-v4-pro

# HANDOVER: Nadali Project - MoMA Image Generation + Registration

---

## GOAL (Max's own words)

Starting a new project in MoMA called **"Nadali"** - a separate video presentation about hybrid children. First image: **Anna standing chest-up as a podcast presenter** in the meeting room with beautiful greens behind her and a window showing Earth from orbit. Anna is actually standing (the standing posture subtly changes perspective even though you only see chest-up).

---

## DECISIONS MADE + WHY

### 1. Canonical Anna reference
Used the **approved headshot**: `anna_approved_headshot_white_cloak_20260330.png` - redhead, freckles, green eyes, cream linen hooded robe. This is the locked canonical face, not a random generation.

### 2. Approved room reference (corrected mid-task)
Claude initially grabbed a **non-approved** raw worker output (`bfl_v06_room_anna_tight.png` from `combo_output_stills`) - Max stopped him cold.

After checking `canonical_status.py`, the correct approved room plate is:
- `station_con_a1_ls.png` (straight-front room view)
- From the approved set: `station_landscape_remake_20260628_v01`
- Ornate white walls, big arched Earth-view window, lush greens, warm floor
- The set has ~10 approved angle positions; only the straight-front one was used

**Lesson burned in:** "Approved" means Max personally blessed it as the canonical locked reference. Never guess from filenames - always check `canonical_status.py` output for what is actually approved.

### 3. Image generation method
Used the **gpt-image-1.5 /edits endpoint** with a **composite reference strip**:
- Anna's approved headshot + the approved room plate combined into one horizontal PIL image
- Model: `gpt-image-1.5` (not gpt-image-1, not dall-e-3 - this is the only model preserving identity)
- Output: `C:\Users\maxre\Nextcloud\ai_images\nadali\anna_meetingroom\` (originally - later moved)
- Quality: low for iteration, except v3 which used high

### 4. Camera variations (6 more from v2 base)
Fed v2 back as reference to lock face/lighting/room, then generated:
- Camera pan left, pan right (yaw, staying centered)
- Zoom in, zoom out
- Two combos (pan+zoom)

### 5. The big mistake: images were orphaned outside MoMA
Claude generated 9 PNGs into a plain Nextcloud folder with **no D1 registration**, no scene, no arrangement - invisible to the MoMA imager. Max caught it: "You should be working through MoMA properly... I don't see it."

### 6. Proper MoMA registration (the fix)
Modeled on `fire_mediakit_portrait.py` (the template for non-movie projects):
- Created scene `nadali` (id 9) and arrangement `nadali-anna-presenter-20260715`
- Moved all 9 PNGs into `kazarian_episode/nadali/output/` (imager serves from kazarian root)
- Registered each image via `fire_job()` as proper D1 `image` jobs (IDs 3290-3298)
- Wrote reusable entry point: `fire_nadali_anna.py`
- No API money re-spent - files already existed, just registered in-place
- Committed and pushed to MoMA repo

### 7. Nadali is a non-movie project
Built like `media_kit` / `essay_nonhier`, not a numbered movie scene. This is Claude's judgment - Max hasn't confirmed if he wants it that way or as a real numbered scene.

---

## CURRENT STATE

**Images:** 9 Anna presenter stills properly registered in MoMA, viewable via imager at localhost:8779 under the "nadali" view dropdown.

**Sound:** Nothing exists yet. The entire MoMA sound tree is keyed to numbered movie scenes (scene09-scene80) and fed from Notion script pages via Fish TTS. No "nadali" sound folder exists.

**Files generated:**
| Variant | Description |
|---------|-------------|
| v1, v2, v3 | Base presenter shots (straight-on, chest-up, standing) |
| v2_pan_left, v2_pan_right | Camera yaw variants |
| v2_zoom_in, v2_zoom_out | Zoom variants |
| v2_pan_left_zoom_in, v2_pan_right_zoom_out | Combo variants |

All 9 in: `C:\Users\maxre\Nextcloud\ai_images\kazarian_episode\nadali\output\`

---

## EXACT NEXT STEP

**Wait for Max to bring the sound files.** Claude's last message asked what kind of audio and where from - no answer yet. Then:
1. Place audio in a new `kazarian_episode/sound/nadali_production/` folder
2. Wire them to the nadali scene so they appear in MoMA's sound tools
3. If it's a script to be voiced by Anna: run it through Fish TTS (Anna's cloned voice already exists - see `Anna_*_test_20260323` audio set) + forced-align to get per-line MP3s

---

## OPEN QUESTIONS (awaiting Max)

1. **What kind of sound files?** Recorded voiceover by a human, or a script to be spoken in Anna's cloned voice via Fish TTS?
2. **Where are they coming from?** A folder, email, recording app?
3. **Should Nadali be a non-movie project** (like media_kit, as Claude built it) or a real numbered scene in the film's spine?

---

## KEY PATHS + IDs

| Item | Path / ID |
|------|-----------|
| MoMA root | `C:\moma\` |
| Scene ID | `nadali` (id 9) |
| Arrangement | `nadali-anna-presenter-20260715` |
| D1 job IDs | 3290-3298 |
| Images (registered) | `C:\Users\maxre\Nextcloud\ai_images\kazarian_episode\nadali\output\` |
| Registration script | `C:\moma\sc10\combo_runner\code\fire_nadali_anna.py` |
| Canonical Anna headshot | `...\kazarian_episode\characters\anna\approved portraits\anna_approved_headshot_white_cloak_20260330.png` |
| Approved room plate | `...\kazarian_episode\interiors\station_landscape_remake_20260628_v01\station_con_a1_ls.png` |
| Approved room set (all angles) | `station_landscape_remake_20260628_v01\` (10 positions: station_con_a1 through a4, station_cv variants, etc.) |
| `canonical_status.py` | `C:\moma\sc10\combo_runner\code\canonical_status.py` |
| Image skill (Rachel-specific, forked for Nadali) | `C:\Users\maxre\AppData\Roaming\Claude\local-agent-mode-sessions\skills-plugin\...\imagegen` |
| API key | `C:\Users\maxre\Nextcloud\zSyncMain\ssh\openai_api_key_20260216.txt` |
| Sound assembly root | `kazarian_episode/sound/` (per-scene folders: `sceneNN_production/`) |
| Anna's cloned voice ref | `Anna_*_test_20260323` audio files |

---

## GOTCHAS + DEAD ENDS RULED OUT

1. **Never pull images from `combo_output_stills` or raw archive folders** - those are unapproved worker outputs. Only `canonical_status.py` output or explicitly "approved" folders count as canonical.

2. **Never generate images as orphan PNGs outside MoMA** - always register via `fire_job()` in D1 so they appear in the imager. The `fire_nadali_anna.py` script now exists as the reusable entry point for future Nadali images. Pattern: create scene ? create arrangement ? insert prompts row ? fire_job ? update output_file.

3. **Imager serves from `KAZARIAN_ROOT`** (the `kazarian_episode` folder) - images must live under that tree or the imager can't resolve paths.

4. **Never parallelize the image API** - run sequentially. The background-task timeout for v3 was a network read-timeout, not a content-moderation block.

5. **gpt-image-1.5 is the only working model** for identity preservation. `/edits` endpoint with `files=` and `data=` (multipart form), NOT `json=`.

6. **The image skill file is Rachel-project-specific** - its default paths/cast/STYLE all reference `rachel_story_images`. Claude forked only what Nadali needed (output to fresh folder, Anna as sole reference). Nothing from Rachel was touched.

7. **No Nadali Notion script page exists yet** - if Anna voiceover is needed, a Notion script may need to be created first to feed Fish TTS + forced-align.
