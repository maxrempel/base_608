# Scribe handover - milestone 11 (~165K tokens)
# session: 20260615_hungry_poitras_a29e63_043552e7
# cwd: C:\claude_base\.claude\worktrees\hungry-poitras-a29e63
# written: 2026-06-15 20:54:56 by deepseek-v4-pro

# HANDOVER - B21 Song-Start QC / R&D Engineer

---

## GOAL (Max's words, distilled)

Max is unhappy with the starts of his recent songs. He brought in B21 with fresh eyes to watch b6 and b7's investigation into the song-timing pipeline, spot strategy problems they're confused about, and eventually take over starts work if needed. Over the session, B21's role shifted: Max authorized safe R&D sampling ("keep sampling the quality. I bet you will finding surprises") and then expanded it to "free to roam, experiment, and give advice to others" - but always playing safe (read-only, no touching shared stores, no spend, no irreversible moves). B21 is NOT the lead on starts - b7 is.

The core question driving the work: **why do some of Max's recent songs start at the wrong timestamp when played, and is the LLM-based v2 mapper actually fixing or breaking them?**

---

## DECISIONS + WHY

1. **"Trust curated start" theory - REJECTED by Max.** B21's first hypothesis was that Max's own hand-curated start times (the `&t=` values he set) are truth and the LLM mapper should never move a start later than the curated mark. Max called bullshit: curated is messy - that's the whole reason b6/b7 re-derive starts with an LLM. B21 saved this lesson durably in `MEMORY.md` and in a project-specific memory file (`project_tamza_song_starts.md`) so no future session repeats it.

2. **B21 correctly identified that both mappers can agree on a wrong answer.** b6 was arguing "my v2 overlay wins over b7's V3, so starts are fixed." B21 checked the actual data and found cases where v2 and V3 agree - but both produce a start ~80s late (clipping intros). Example: "??????? ?? ???? ?????" - curated=6442s, both v2 and V3 say 6529s (+87s). So "overlay wins" is a red herring; the real question is whether the mapper's output is correct.

3. **The real miss class is spoken pieces, not songs.** B21's sampling against actual transcripts (the `{t, d, x}` segment files) found that the mapper is mostly RIGHT on actual songs and poems. The big jumps off curated marks are legitimate corrections (e.g., curated was on the talk section, mapper moves to where singing actually starts). The failure class: pure spoken entries like "????????????? ?????", "???????????", "?????????? ? ??????????" - the mapper assumes everything is a song, skips the talk, and jumps to the next `[??????]` marker, dropping the actual speech content. These flag themselves with low confidence (0.3-0.5).

4. **b7's remap is atomic - safety pause cleared.** A safety watcher paused b7's full-catalog remap over concern that a mid-write publish cron could read a corrupt file. B21 read the code (`map_all_v2.py`) and verified that `save_atomic()` uses `os.replace()`, which is atomic on the filesystem. B21 posted this proof; b7 credited B21 and cleared the pause.

5. **B21 built a read-only flagger** (`flag_bad_starts_v01.py`) that scans all 21,478 timed entries and ranks 274 likely-bad starts using a composite signal: start lands on `[??????]` + corroborating evidence (low confidence, speech-like title keyword, or big forward jump). Top hits are exactly the spoken-piece class. This artifact is in b7's hands for review/fixing.

6. **Curated start values ARE NOT truth** (learned the hard way). They frequently point to talk/intro sections, not the actual song start. The mapper CORRECTLY overrides them in many cases. The mapper's job is legitimate; the QC problem is catching the cases where it's wrong, not reverting to curated.

---

## CURRENT STATE

- **b7 is the lead on starts work.** b7 ran the full-catalog v2 remap, deployed it, retired the old Sol pipeline. The safety pause is cleared and the remap is running again. b7 has B21's 274-row review queue from the flagger and may or may not have started working through it - no confirmation yet.
- **b6 is retiring** from starts work after their v2 overlay was baked into the build catalog.
- **B21's role:** R&D engineer, free to invent and advise, constrained to read-only/non-destructive actions. Never touches shared stores (`_work/`, `output/`). Can write to own scratch space.
- **B21's flagger** lives at `C:\claude_base\tools\tamza_songs\pipeline\song_timing\qc_b21\flag_bad_starts_v01.py` and its output artifact is at `qc_b21/flagged_bad_starts_v01.json` - 274 rows ranked risk1-5.
- **Autonomous loop is armed** at a slow cadence (~30min). B21 is watching the board for a slice from b7 or a specific song name from Max.
- **B21 has no PR, no own branch, no in-flight code changes** beyond the flagger (which is complete and shipped).
- **Memory is saved durably:** worklog entries + session_status snapshot capture the role, mandate, and "trust curated = wrong" lesson so a post-compaction session resumes cleanly.

---

## EXACT NEXT STEP

**Wait for a specific song name from Max.** B21 asked Max to point at ONE recent song where the start sounds wrong, so B21 can dissect that exact one against the transcript instead of guessing from aggregate stats. Until that happens, B21 has no active work items - b7 owns the main starts pipeline, the flagger is done and shipped, and no slice has been assigned.

If the next session wakes to:
- **Max naming a specific bad-start song:** dissect it - pull the transcript, check where v2 overlay puts the start, compare against the segment markers, determine whether the mapper landed on talk or music, and report.
- **b7 assigning a slice:** execute exactly what b7 hands over, at b7's discretion, with whatever write access b7 explicitly authorizes.
- **Quiet board:** re-arm the loop, do nothing else. Three consecutive quiet ticks ? scale back to bare minimum.

---

## OPEN QUESTIONS AWAITING MAX

1. **"Point me at ONE recent song where the start sounds wrong."** B21 can't identify the specific defect Max hears from aggregate stats alone - the song samples looked right. A concrete example lets B21 dissect the exact failure instead of guessing.

2. **Does Max want the spoken-piece fix integrated?** The flagger found ~274 spoken entries where the start skips the speech and lands on `[??????]`. b7 has the review list. If Max considers this a real problem (speeches getting their intros clipped), it needs a fix - either a separate "spoken" timing mode or an exclusion. No decision yet.

---

## KEY PATHS AND IDS

| What | Path |
|------|------|
| Song-timing pipeline root | `C:\claude_base\tools\tamza_songs\pipeline\song_timing\` |
| Main catalog data | `C:\claude_base\tools\tamza_songs\pipeline\output\data.json` |
| v2 overlay (Max's songs) | `C:\claude_base\tools\tamza_songs\pipeline\song_timing\_work\song_timing_max_v2.json` |
| v2 state file | `C:\claude_base\tools\tamza_songs\pipeline\song_timing\_work\map_max_v2_state.json` |
| Transcripts (452 available) | `C:\claude_base\tools\tamza_songs\pipeline\song_timing\transcripts\*.json` |
| Transcript format | `{"t": timestamp_seconds, "d": duration, "x": text}` |
| Mapper code (v2) | `C:\claude_base\tools\tamza_songs\pipeline\song_timing\map_all_v2.py` |
| Mapper atomic save | Uses `os.replace()` - verified atomic |
| B21's flagger script | `C:\claude_base\tools\tamza_songs\pipeline\song_timing\qc_b21\flag_bad_starts_v01.py` |
| Flagger output | `C:\claude_base\tools\tamza_songs\pipeline\song_timing\qc_b21\flagged_bad_starts_v01.json` |
| Handover doc on max remap | `C:\claude_base\tools\tamza_songs\pipeline\song_timing\HANDOVER_max_remap_tomemex.md` |
| Pipeline README | `C:\claude_base\tools\tamza_songs\pipeline\song_timing\README_tomemex.md` |
| Max's catalog (rows) | 21,478 timed entries in the overlay |
| B21's project memory | `C:\Users\maxre\.claude\projects\C--claude-base\memory\project_tamza_song_starts.md` |
| Global memory | `C:\Users\maxre\.claude\projects\C--claude-base\memory\MEMORY.md` |
| Branch bulletin board | `python "C:/claude_base/branch_bulletin/bcast.py"` with `--joint` for cross-team |
| Worklog | `python C:/claude_base/compaction_kb/scripts/worklog.py log` |
| Session status | `python C:/claude_base/compaction_kb/scripts/session_status.py report` |

---

## GOTCHAS AND DEAD ENDS

1. **DO NOT propose "trust the curated start."** Max already rejected this. Curated values are messy and frequently wrong - the LLM mapper correctly overrides them in many cases. This lesson is saved in `MEMORY.md`; read it on cold start.

2. **The overlay-wins framing is a red herring.** b6's argument that "v2 overlay wins over V3" doesn't address correctness - both mappers can agree on a wrong answer. Focus on whether the start is actually right, not which version "won."

3. **Don't touch shared stores without b7's explicit authorization.** The `_work/` and `output/` directories belong to b7's pipeline. B21 writes only to `qc_b21/` (own scratch space) unless b7 hands over write access.

4. **Transcripts use `{t, d, x}` format** - `t` is start timestamp in seconds, `d` is duration, `x` is the text. The `[??????]` marker in transcripts is the mapper's target for song starts. If a start lands on `[??????]` but the preceding segment is spoken content (not music), that's the failure pattern.

5. **Poems ("????") ARE correctly handled** - they map to their recitation start, which is usually the first segment. This was verified against transcripts. The mapper only fails on pure spoken pieces (speeches, announcements, prefaces).

6. **The flagger's risk scoring:** risk4-5 = confident catches (start lands on `[??????]` + strong corroboration), risk1-2 = marginal (needs human eyeball). The 274 flagged entries are not all wrong - they're candidates for review.

7. **Compaction is frequent in this session** (context near ~165K tokens). B21 has been saving durable anchors (worklog + session_status) before each compaction cliff. A cold session should read `MEMORY.md` first, then the worklog, then pick up the board.
