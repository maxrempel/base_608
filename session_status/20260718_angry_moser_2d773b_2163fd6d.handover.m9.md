# Scribe handover - milestone 9 (~687K tokens)
# session: 20260718_angry_moser_2d773b_2163fd6d
# cwd: C:\claude_base\.claude\worktrees\angry-moser-2d773b
# written: 2026-07-18 16:20:54 by deepseek-v4-pro

# HANDOVER: Nadali Project - Anna UEI Talk Video Assembly

---

## GOAL (Max's own framing)

A **video presentation on hybrid children** - Anna (the AI character) narrating interstitials between sections of **Max Rempel's UEI launch talk** (alien DNA in the human genome). The talk is ~16 minutes; Anna introduces Max, provides 16 chapter-break intermissions, and gives a conclusion. The finished video is ~21 minutes, assembled from Max's hi-res v04 talk footage + 34 Anna talking-head reels, with site/email lower-thirds and title/credits.

---

## DECISIONS MADE + WHY

### 1. Nadali as a named MoMA project (not a numbered movie scene)
- **Why:** Nadali is a standalone presentation, not part of the film's spine. Registered as scene `nadali` (id 9) in D1, modeled on `media_kit`/`essay_nonhier` patterns.
- **Key consequence:** MoMA's audio/reel lanes were hardcoded to numbered scenes (scene09, scene10...). I taught both `paths.py` (`scene_production_dir`) and `audio_resolver.py` to accept **named** scenes - small, general changes that preserve numbered-scene behavior. This was necessary; without it, reels couldn't resolve audio.

### 2. Anna presenter images - generated via imagegen skill, then retroactively registered in MoMA
- **Why it was wrong:** The imagegen skill bypasses MoMA's `fire_job` ? worker ? D1 pipeline. Images were orphan PNGs in Nextcloud with no database rows.
- **Correction:** Moved files into the kazarian tree, created `fire_nadali_anna.py` (modeled on `fire_mediakit_portrait.py`) to register all 9 stills as proper D1 `image` jobs (3290-3298).
- **Reprimand recorded:** `memory/feedback_never_circumvent_moma.md` - "never circumvent MoMA without explicit permission" is now a standing rule.

### 3. The imagegen skill has been permanently disabled
- Max said "just delete it or comment it so it cannot be used." I renamed `SKILL.md` to `SKILL.md.DISABLED_20260716` in the anthropic-skills plugin store. It can't be invoked. Left a `DISABLED_README.txt` explaining why. Max said "I'm not planning to come back to the old ways" - the disable stands.

### 4. Reel engine guardrail (wan22 silent-clip hijack bug)
- **Problem:** MoMA's `jobs.engine` column defaults to `wan22`, so lipsie jobs fired without an explicit engine were race-eligible for the **deprecated wan22 silent-clip worker**. Multiple reels (3299, 3302) got stolen and came out as silent clips.
- **Fix:** Added guardrail in `moma_db.py`'s `fire_job()` - a `lipsie` job can never carry `engine='wan22'`; it gets `engine='wan26flau'` instead. This is a **general MoMA fix**, not Nadali-specific.

### 5. Prompt style - MoMA's "documentary scene" anchor
- **Problem:** My reel prompts (English, ad-hoc) didn't use MoMA's house style - the Russian "????????? ?????????????? ?????" anchor that keeps wan2.6 realistic and un-glamorized.
- **Status:** Acknowledged but not yet fixed. Max said the latest video was "OK" so I kept the current style. The 10 re-rendered reels for D42b used the same prompts as before (consistency over correctness).
- **Fix waiting:** Re-render all 34 reels using an actual MoMA sc11 reel prompt template as the base.

### 6. Assembly anti-drift method (concat filter)
- **First try (wrong):** Plain ffmpeg concat with per-segment loudnorm - audio and video lengths diverged by 0.02-0.06s per segment, accumulating to ~2s drift by video end.
- **Fix:** Concat filter (`concat=n:v=1:a=1`) - Max's own `assemble_max2_v01.py` uses this. One continuous timeline, no independent drift. Verified: final audio and video equal length (0.085s delta, constant).

### 7. trimmer2 - sentence-aware cut-point finder
- **Problem:** My first assembly's 16 chapter cuts were hand-placed via text-matching; 7 of 16 landed mid-sentence.
- **Fix:** Built **trimmer2** (`C:\claude_base\tools\trimmer2\`, committed + documented with `_tomemex`). It reads Deepgram's sentence segmentation and snaps every cut to a true sentence start. Verified all 16 boundaries. Named per Max's request (pronounceable: "trimmer two").

### 8. MoMA silence-trim reused (0.3s before / 0.2s after)
- Source: `combo_lipsync_worker.py`'s `auto_trim_lipsie()` - silencedetect at ?45dB, 0.05s min-silence, pads configurable. I used Max's specified 0.3/0.2 pads (the module default is 0.15/0.35). No wheel reinvented.

### 9. D42b line update - Anna audio regenerated (28 ? 34 clips)
- **What changed:** im07 and im08 rewritten (1?2 clips each), conclusion expanded (3?7 clips). 24 lines unchanged.
- **Approach:** Reused 24 existing reels, re-rendered only 10 new/changed reels (3328-3337). Rebuilt assembly as v06 (21:09).

### 10. Site/email lower-thirds - approved, NOT YET IMPLEMENTED
- **Design:** Centered pill at bottom, translucent dark-navy with thin gold border, white Calibri Bold, no https. Reusable transparent PNGs.
- **Timing:** starseedgenetics.com - during Anna's conclusion line 33 ("Everything is at Starseed Genetics dot com"). anna@maxrempel.com - during line 34 ("anna at max rempel dot com").
- Mockups reviewed and approved. Implementation was **interrupted** before executing.

### 11. Title and credits - partially approved
- **Title:** Still open. Max rejected "Alien Hybrids in Human DNA" as "totally idiotic" (category error: hybrids are people, DNA is a molecule). My last proposal: **"Alien DNA in the Human Genome"** - not yet approved.
- **Subtitle:** APPROVED - "A Max Rempel talk ? July 11, 2026"
- **Credit:** APPROVED - "by Max Rempel & Claude Opus 4.8"

### 12. Retake cleanup - NOT YET IMPLEMENTED
- Two time-asides to cut: host at ~13:00 ("We got about a minute left") and Max at ~14:50 ("I have like half or two more seconds maybe").
- ~12+ known retakes (half-word corrections) where Max stumbles and restarts. Many more half-word retakes suspected - Max says "you are still missing a lot of blemishes where I pronounce half of the word."
- **Correct tool:** `retake_cleaner` using **Deepgram nova-3 verbatim** (with `filler_words=true`), NOT smart_format. Max explicitly warned: smart_format smooths partial words. Whisper was ruled out earlier ("bad for capturing fractions of words"). The right config needs re-running to catch the half-word retakes.
- Protocol: dense keyframes, lossless stream-copy (no re-render of the talk source).

### 13. Host intro (Nadalee)
- **Source:** `v03_clean.mp4` - Nadalee Hill introducing Max at 0:00-12.69s ("...inside of our genome. Thank you so much Max, welcome in."). Preceded by Anna's 3 intro clips.
- v04 (hi-res, the main source) was trimmed to start at Max's own opening - the host intro was cut. I pull it from v03.

### 14. Temporal sharing via maxrempel.com/temp4
- Existing pattern: R2 bucket `maxrempel-papers`, generic `/tempN` worker route with Range support. Added `video/mp4` mime type (was falling to octet-stream).
- 14-day auto-delete via Cloudflare R2 lifecycle on `temp4/`.
- Video and player page uploaded, link emailed to max.rempel2@gmail.com.

---

## CURRENT STATE

### Done and working:
- **9 approved Anna presenter images** - registered as MoMA D1 jobs 3290-3298, showing in the imager under `nadali` view.
- **34 Anna audio clips** - per-line run under `kazarian_episode/sound/nadali_production/` with sass-format manifest and line_hashes.
- **34 lipsync reels** - all rendered by MoMA's wan26au worker, `lipsie`/`wan26flau` jobs, in the lipser (localhost:8779, `nadali` view). Jobs 3299-3337.
- **Full video v06** - assembled at `C:\Users\maxre\Videos\max talks\uei_max_talk_20260711\nadali_uei_full_video_v06.mp4` (21:09, 1920?1080, AAC stereo). Sentence-snapped cuts, drift-free, host intro included, MoMA silence-trim applied.
- **Live at** https://maxrempel.com/temp4 (auto-deletes ~July 30).
- **Engine guardrail** - lipsie jobs can't be stolen by wan22 lane anymore.
- **trimmer2** - built, committed, documented.
- **Imagegen skill** - permanently disabled.
- **Nadali project memory** - recorded at `C:\Users\maxre\.claude\projects\C--claude-base\memory\project_nadali.md` and indexed in `MEMORY.md`.

### In flight (NOTHING rendering right now):
- The final assembly rebuild was **interrupted** before executing the retake cuts + overlay + title/credits implementation. No background task running.

### Needs work:
- Retake cleanup script (re-transcribe with Deepgram nova-3 verbatim, cut all stumbles/half-words)
- Bake site/email lower-thirds onto the video
- Add title card and credit
- Rebuild final assembly
- Re-upload to temp4

---

## EXACT NEXT STEP

Read the durable plan at:
```
C:\Users\maxre\Videos\max talks\uei_max_talk_20260711\NADALI_FINAL_PLAN.md
```

Then execute in this order:

1. **Get the title approved.** My last proposal was "Alien DNA in the Human Genome" - ask Max if that works, or offer alternatives. Do NOT proceed without title confirmation.

2. **Re-transcribe the talk** using the proper fine-mapper: `retake_cleaner` with **Deepgram nova-3 verbatim** (`smart_format=false`, `filler_words=true`). This catches the half-word retakes that the earlier pass missed. The Deepgram API key is at `C:\Users\maxre\Nextcloud\zSyncMain\ssh\deepgram_key_20250624.txt` (verify path from `transcribe_max2.py`).

3. **Cut all retakes + the two time-asides** on the talk source using retake_cleaner's lossless protocol (dense keyframes, stream-copy). The talk source is:
   ```
   C:\Users\maxre\Videos\max talks\uei_max_talk_20260711\Max Rempel talk UEI launch 20260711 v04_hires_clean.mp4
   ```
   Two time-asides to excise:
   - ~13:00 host/timekeeper: "We got about a minute left. Sorry, I didn't tell you. I'm close, I'm close. Okay."
   - ~14:50 Max: "I have like half or two more seconds maybe -" (cut the aside, keep the sentence restart)

4. **Rebuild the assembly** using the existing `assemble_nadali_video_v04.py` pattern (updated for the cleaned talk + 34-reel map). Key specs: 1920?1080 @ 25fps, concat filter for drift-free join, MoMA silence-trim on Anna clips, host intro from v03, sentence-snapped chapter boundaries computed by trimmer2.

5. **Add approved overlays:**
   - Lower-thirds (transparent PNGs at `overlay_mockup/`): starseedgenetics.com (fade in/out over conclusion line 33), anna@maxrempel.com (over line 34).
   - Title card (intro): **TITLE TBD** + subtitle "A Max Rempel talk ? July 11, 2026"
   - End credit: "by Max Rempel & Claude Opus 4.8"

6. **Re-upload to maxrempel.com/temp4** (delete old v06, update the page, cache-bust).

---

## OPEN QUESTIONS (awaiting Max)

1. **Title.** Last proposed: "Alien DNA in the Human Genome." Max said "titles should be descriptive and straight" and "read the text." Has not approved or counter-proposed.

2. **Reel look.** The reels still use non-MoMA prompt style (English, no "documentary scene" anchor). Max said the latest video was "OK" - not sure if he wants the full 34-reel re-render now or after everything else is done.

3. **Trim the two "minute left" asides?** I listed them in the plan but Max hasn't explicitly confirmed the cut list. Present the exact timestamps for approval.

4. **Should the retake cleanup re-render the talk source?** The retake_cleaner protocol does lossless stream-copy with keyframe cuts, but if cuts land on non-keyframes, there will be a brief re-encode at those splice points. Confirm this protocol is acceptable.

---

## KEY PATHS, FILES, IDs

### MoMA code (committed changes):
| File | What changed |
|------|-------------|
| `C:\moma\sc10\combo_runner\code\paths.py` | `scene_production_dir` accepts named scenes (line ~168-230) |
| `C:\moma\sc10\combo_runner\code\audio_resolver.py` | Resolver falls back to named scene tag |
| `C:\moma\sc10\combo_runner\code\moma_db.py` | `fire_job` guardrail: lipsie can't carry engine='wan22' |
| `C:\moma\sc10\combo_runner\code\fire_nadali_anna.py` | Register Anna stills as D1 image jobs |
| `C:\moma\sc10\combo_runner\code\build_nadali_audio_run.py` | Build sass-format per-line audio manifest |
| `C:\moma\sc10\combo_runner\code\fire_nadali_reels.py` | Fire lipsie jobs through MoMA (batches 1-5) |

### Content files:
| What | Where |
|------|-------|
| Anna stills (9 PNGs) | `C:\Users\maxre\Nextcloud\ai_images\kazarian_episode\nadali\output\
