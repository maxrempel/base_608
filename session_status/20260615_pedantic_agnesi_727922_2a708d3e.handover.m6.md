# Scribe handover - milestone 6 (~91K tokens)
# session: 20260615_pedantic_agnesi_727922_2a708d3e
# cwd: C:\claude_base\.claude\worktrees\pedantic-agnesi-727922
# written: 2026-06-15 09:42:15 by deepseek-v4-pro

# SESSION HANDOVER - B14 Indexer (Kartoteka)

---

## GOAL (in Max's words)

Max wants B14 to **index timecodes automatically**. The workflow:

1. Use **"ds4flash"** (DeepSeek for flash identification) to read concert transcripts
2. Search online for song name, authors
3. Flag when the song is authored by the performer (self-authored)
4. If the song is already in the catalog, combine/merge it
5. **Fuzzy full-text matching** against all songs in the catalog - Max believes this will "manyfold" increase identification because even with poor transcription quality, full text of lyrics/refrains will match, surfacing "tons of songs"
6. For performer names that transcription can't catch reliably: build a face-recognition game ("like bingo - with pleasant sounds!") for the human timecoder team to match faces to profiles for problematic clips

The project is the **Kartoteka (?????????)** - a song catalog on tamza.com. Siblings b7-b12 are building the player and voting interface.

---

## DECISIONS MADE + WHY

### Registration and orientation (completed)
- B14 registered on the branch bulletin board via `bcast.py whoami b14` and `catchup` - confirmed no standing orders waiting
- Oriented by reading the import rules doc (`kartoteka_import_rules_v01_tomemex.md`) and surveying the existing pipeline

### What exists (discovered, not decided)
- **Song-timing pipeline** at `C:/claude_base/tools/tamza_songs/pipeline/song_timing/` contains `map_max_v2`, a flash mapper, and `fill57`
- **Catalog of ~26k rows** with title, author, performer, timecode fields - built from the human timecoders' spreadsheet
- The catalog currently stores **titles, not lyrics** - this is the gap the fuzzy full-text idea targets
- The pipeline already reads Deepgram transcripts and maps song start positions in concert videos

### The fork proposal (presented to Max, awaiting resolution)
The agent identified two possible starting points and asked Max which bottleneck to crack first:

- **(A)** Auto-timecode brand-new videos (transcribe ? fuzzy-match against known songs ? emit timecodes). Argued as the highest payoff for the fuzzy matching idea.
- **(B)** Enrich the existing catalog (fill authors, mark self-authored, dedupe) without adding new videos.

### Name resolution
- "ds4flash" did not match any filename. Max clarified in the last (truncated) message: **"ds is deepseek"** - meaning DeepSeek is the LLM to use for the flash identification/mapping step, replacing or augmenting whatever LLM the existing flash mapper currently uses.

---

## CURRENT STATE

- B14 has read the project orientation, surveyed the file tree, and understood the pipeline
- No code has been written yet
- A decision fork has been proposed to Max
- Max's last response was truncated at "ds is deepseek" - likely additional direction was cut off

### Files read/explored:
- `C:/claude_base/tools/tamza_songs/kartoteka_import_rules_v01_tomemex.md` - the import rules document
- `C:/claude_base/tools/tamza_songs/pipeline/song_timing/` - the existing timing pipeline directory
- `C:/claude_base/tools/tamza_songs/pipeline/scripts/` - pipeline scripts

### Key existing tools identified:
- `map_max_v2` - maps songs to transcript segments
- The flash mapper - does LLM-based song identification from transcript chunks
- `fill57` - likely fills in metadata fields
- Deepgram - transcription engine already integrated

---

## EXACT NEXT STEP

**Wait for Max's full response to the fork question (A vs B).** The truncated "ds is deepseek" was likely the start of a longer answer that also addresses which path to take first. Re-ask or wait for Max to clarify:

1. Which fork: (A) new-video timecoding or (B) catalog enrichment?
2. Any specifics on how DeepSeek should be integrated (API endpoint, model name, prompts, rate limits)?

Once the fork is resolved, the first build step is to hook DeepSeek into the flash identification layer (either extending the existing flash mapper or building a parallel path) and implement the fuzzy full-text matcher against the ~26k catalog entries.

---

## OPEN QUESTIONS AWAITING THE USER

1. **Fork resolution**: (A) auto-timecode new videos first, or (B) enrich the existing catalog first?
2. **DeepSeek integration details**: API key location, specific model (DeepSeek-V3? R1?), any rate-limit or cost constraints, expected prompt format?
3. **Lyrics/full-text corpus**: Does a corpus of full song lyrics exist already for the ~26k catalog entries, or does that need to be sourced/fetched (e.g., web scraping) as part of this task?
4. **Rest of truncated message**: Max's last message was cut off - what else was said after "ds is deepseek"?

---

## KEY PATHS AND IDS

| Item | Path/Value |
|------|------------|
| Worktree | `C:\claude_base\.claude\worktrees\pedantic-agnesi-727922` |
| Branch board | `C:/claude_base/branch_bulletin/bcast.py` |
| Search tool | `C:/claude_base/tools/es/es.exe` |
| Kartoteka rules | `C:/claude_base/tools/tamza_songs/kartoteka_import_rules_v01_tomemex.md` |
| Song-timing pipeline | `C:/claude_base/tools/tamza_songs/pipeline/song_timing/` |
| Pipeline scripts | `C:/claude_base/tools/tamza_songs/pipeline/scripts/` |
| Catalog size | ~26,000 rows |
| Catalog source | Human timecoders' spreadsheet |
| Sibling agents | b7-b12 (player/voting UI) |
| This agent's id | b14 |

---

## GOTCHAS AND DEAD ENDS ALREADY RULED OUT

- **"ds4flash" is not a filename** - searching for it by name or content returned nothing. It's a conceptual label. "ds" = DeepSeek, "4" = for, "flash" = the flash identification step. Do not look for a tool called ds4flash.
- **The catalog has titles, not lyrics** - this is the core gap. Fuzzy matching on titles alone won't give the "manyfold" increase Max predicts. To get that boost, full lyric text (or at least refrains/choruses) must be added to the catalog or matched against an external lyrics database. This is an implicit sub-task no matter which fork is chosen.
- **The existing flash mapper already uses an LLM** - the nature of the "ds4flash" request is likely to swap in DeepSeek as that LLM (or add it as an option), not to build the flash mapper from scratch.
- **Transcripts come from Deepgram** - this is already operational. Whisper was NOT mentioned; do not introduce Whisper unless Max asks.
