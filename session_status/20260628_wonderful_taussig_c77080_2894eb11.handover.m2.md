# Scribe handover - milestone 2 (~165K tokens)
# session: 20260628_wonderful_taussig_c77080_2894eb11
# cwd: C:\moma\.claude\worktrees\wonderful-taussig-c77080
# written: 2026-06-28 23:15:00 by deepseek-v4-pro

# HANDOVER - MoMA Scene 11 Input Images

---

## GOAL (Max's words)

"Start designing the input images for Lipsys for scene 11." Dictated as "SYN 0 / SYN 11" then corrected: that was a dictation slip - the target is **scene 11** (the Service Desk and Crisis Briefing), not something called SYN.

The underlying ask: produce the per-line still images that feed into the reel pipeline (wan26flau talking clips: still + audio ? animated clip).

---

## DECISIONS MADE + WHY

1. **Read everything before acting.** Max insisted: "Do your homework. Read the scene, read the whole instructions, understand how the system is set up." So instead of jumping to image generation, D52 audited the entire state of scene 11 across D1 database, Notion, and the combo_runner config files.

2. **Identified the prerequisite chain.** A reel's input image is composed from canonical reference plates (locked faces + locked background). Scene 11 has none of its own plates. So the real work isn't "make 80 stills" - it's a dependency chain:
   - Lock Werner's face/body (new character)
   - Lock Derek's face/body (new character)
   - Lock the background plate (Art Nouveau service desk room)
   - THEN compose per-line stills

3. **Faces-first instinct.** D52 recommended locking the two new character faces first, since every shot needs them. But explicitly deferred to Max's call.

4. **Cleaned up probe scripts** (`_d52_scene11_probe.py` deleted after use).

---

## CURRENT STATE

### Scene 11 identity
- **D1 id:** 3
- **Title:** "Service Desk and Crisis Briefing"
- **Renamed from:** sc12
- **Characters:** Anna, Ishtab, Werner ("Gunther" once in dialogue), Derek
- **Setting:** Art Nouveau room - plants, Vienna chairs, tea table, tall windows showing Earth in blackness
- **Structure:** ~80 dialogue lines across beats 4, 6, 7, 8, 9, 10, 11 + 2 B-rolls. Beat 8 is the big set-piece ("twelve men who don't want smaller chairs / king of the mountain")
- **Notion page:** `3430316f-5560-8150-bc4f-de12b00ce5ac` - full script retrieved and read, but **not parsed into D1**

### What exists (database)
- **Script lines in D1: ZERO.** Only sc9 (22) and sc10 (126) are parsed. Scene 11's script lives only in Notion.
- **Images/reels rendered: ZERO.** One stale empty placeholder clip exists (probably a test artifact).
- **Canonical faces:** Anna ? and Ishtab ? are locked and carry over from prior scenes. **Werner ? - name only, no face plate, no body, no canonical. Derek ? - same.**
- **Background plate: NONE.** Scene note mentions the room is glimpsed through a doorway in sc10-arr05 - possible seed reference.
- **Jobs tagged sc11: ONE** - an empty placeholder, not a real render.

### What was read/consulted
- `C:/moma/sc10/combo_runner/code/moma_db.py` - the D1Client interface (`query_sql` method)
- `C:/moma/sc10/sound_assembly/code/config/scenes.json` - scene-to-Notion-page mapping
- Full Notion page content for scene 11 (via notion-fetch MCP)
- D1 tables: `scenes`, `script_lines`, `images`, `jobs`

### How the reel system works (per D52's research)
- A reel = one still image + that line's audio ? wan26flau generates the talking clip
- Still images are prompt-composed from canonical reference plates (background + each character's locked face/body)
- Multi-turn reels follow the Gesturing Protocol (numbered Left/Right speaker positions, etc.)
- Faces must be locked canonicals or they drift shot-to-shot

---

## EXACT NEXT STEP

**Awaiting Max's call on which blocker to tackle first:**

- **Option A:** Design and lock Werner's face/body, then Derek's face/body
- **Option B:** Design and lock the Art Nouveau service desk room background
- **Option C:** Both in parallel (but D52 recommended Option A first)

Once faces and background are locked, the workflow is: parse scene 11 script lines from Notion into D1 ? compose per-line still prompts ? fire gpt-image-2 ? feed into the reel pipeline.

---

## OPEN QUESTIONS (for Max)

1. **Faces or background first?** D52's instinct is faces (since every shot needs them), but Max may have a reference or priority that changes the order.
2. **Werner/Derek design constraints?** No character briefs were found in D1 or the script beyond names and dialogue - any existing design notes, actor references, or art direction for these two?
3. **Background seed reference?** Should D52 pull the sc10-arr05 frame where the service desk room is glimpsed through a doorway as a starting point?
4. **Script parse timing?** Parse scene 11 Notion ? D1 script_lines now (unblocks everything), or after faces/BG are locked?

---

## KEY PATHS / IDs

| What | Path / ID |
|---|---|
| D1 client code | `C:/moma/sc10/combo_runner/code/moma_db.py` |
| Query method | `D1Client.query_sql(sql)` |
| Scene-to-Notion config | `C:/moma/sc10/sound_assembly/code/config/scenes.json` |
| Scene 11 Notion page | `3430316f-5560-8150-bc4f-de12b00ce5ac` |
| Scene 11 D1 scene id | 3 |
| Scene 11 rank | 11 |
| Known canonical faces | Anna, Ishtab (locked - carry over) |
| Needs canonical faces | Werner, Derek |

---

## GOTCHAS

- **SYN is a dictation artifact.** Max said "SYN 0 / SYN 11," corrected to "scene 11." Do not search for "SYN" or "synopsis" - they don't exist in this context. D52 wasted ~2 turns chasing "SYN" before the correction came.
- **No script lines in the database.** Grepping D1 for scene 11 lines returns nothing. The script exists ONLY in Notion. Any per-line work requires parsing it into the `script_lines` table first.
- **Two characters are blank slates.** Werner and Derek have zero canonical assets. Generating any still with them before locking faces will produce inconsistent results line-to-line and waste GPU time.
- **Token budget note from Max.** Max flagged that ~140K tokens were consumed "doing nothing." D52 explained: the auto-loaded instruction set (global CLAUDE.md, global2.md, max_profile, memos, MEMORY.md hook, fleetcomm/bcast hook dumps) preloads ~120K-133K before the first command. Actual work tokens were ~6K. Max may want to trim auto-loads to reduce session overhead.
- **Hook attention.** The hooks flagged D52's repeated `python -c` one-liners as a pattern; D52 corrected by writing and running a single probe script instead of firing multiple ad-hoc commands. Future work should batch queries.
