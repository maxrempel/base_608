# Scribe handover - milestone 2 (~178K tokens)
# session: 20260726_nifty_faraday_2308eb_387e881d
# cwd: C:\claude_base\.claude\worktrees\nifty-faraday-2308eb
# written: 2026-07-26 12:32:34 by deepseek-v4-pro

# HANDOVER: Tayscribe Reverted to Classical, Typer State as of Context End

**Max's words (summary of goal):**
"I want typer dictation with good RU/EN balance. We built a fully-local STT on Taygeta (Tayscribe) and tried it on F8, then promoted it to Numpad+. But the transcription quality is terrible. Revert to classical."

**What actually happened and why:**
- Tayscribe was deployed on Taygeta (192.168.1.142): faster-whisper large-v3, CPU-only due to a GPU driver/library mismatch (`NVML 595.84`). No reboot was done - the GPU remains unusable without one. CPU large-v3 is slow (5-12 seconds) and Max found the quality unacceptable (exact errors unclear - he just said "terrible").
- Max tested Tayscribe on F8 briefly, then asked to move it to the main Numpad+ key. He did a short Russian test; the assistant saw it working verbatim from the server logs, but Max probably experienced repeated slowness and mismatches when dictating longer/under real use. He called it "terrible" and said "revert to classical".
- The assistant immediately reverted Numpad+ back to the cloud whisper-1 clamp (the old classical system) and parked Tayscribe back on F8 as a test key, not deleted. Num6 remains as a cloud backup.
- The shift-no-send bug was not diagnosed; no repro was provided.
- The clipboard history pollution (Win+V) from player2 read-aloud/OCR keys was noted but not fixed - it's unrelated to dictation; the dictation code already excludes its text from history.

**Current state:**
- **Numpad+ (main key)**: uses the classical cloud whisper-1 clamp (OpenAI-compatible provider). Fully functional, fast, old behaviour. Max is now testing this and presumably satisfied.
- **F8 (test key)**: Tayscribe, local large-v3 on Taygeta CPU. Available but Max told us to abandon it for now. It may be revived if the GPU is fixed and quality improves.
- **Num6**: cloud clamp backup.
- **Other keys**: untouched (Num0/Insert large-v3 asto RU, Num4 large-v3 local Pine, etc.).
- **Taygeta STT service**: still running (systemd `typer-stt.service`), large-v3 CPU, reachable on port 8123. Not being used by any active key now except F8, which Max likely won't use after the "revert".
- **Shift bug**: unresolved, suspected due to player2 OCR/voice keyboard hooks. Next step requires Max to do one deliberate failing shift-stop while the log catches it.
- **Clipboard pollution**: player2's Ctrl+C for read-aloud/OCR copies text to clipboard and leaves it in Win+V history. Typer's own paste excludes itself. This is a known annoyance but not fixed.

**Exact next step (as of the transcript end):**
Max is actively testing the + key to confirm the classical quality is back. The assistant told him to "dictate on + and tell me the classical quality is good again." The next cold-session step is to **wait for Max's confirmation on the + key** - once he says it's good, the immediate dictation issue is resolved. After that, the two open troubles are:

1. Clipboard history mess from player2 (read-aloud/OCR) - Max was aware and said he'd consider it. Ready to fix if he says "go".
2. Shift no-send bug - still needs a deliberate repro, then log inspection.

If Max later wants to revisit Tayscribe, the prerequisite is a Taygeta reboot to fix the GPU driver, then switching the STT service to `--device cuda`. But as of now, Max has explicitly abandoned Tayscribe for the main key; F8 is only a leftover test key.

**Open questions awaiting Max:**
- What exactly was "terrible" about Tayscribe? Slowness only, or transcription errors? (The assistant assumed slowness was the main issue, but Max said "transcription quality" - it might be that the CPU large-v3 is making mistakes that his real use showed.)
- Whether to pursue Tayscribe again after GPU reboot, or stay permanently on the classical cloud clamp.
- Whether to fix the clipboard pollution now.
- Whether to attempt the shift bug reproduction.

**Key file paths and identifiers:**
- Typer main script: `C:\claude_base\tools\typer\typer_e25c.py`
- Launcher (source of truth for key mappings): `C:\claude_base\tools\typer\start_typer_all.bat`
- Key-map documentation: `C:\Users\maxre\Nextcloud\claude_md_synced\global2.md` (TYPER section, ~line 562)
- Taygeta STT server: `/home/maxre/typer_stt/typer_stt_server.py`, systemd unit `typer-stt.service` (port 8123, CPU)
- Idle-safe restart script: `restart_typer_safe.py`
- SSH to Taygeta: `ssh -i ~/.ssh/sol_key maxre@192.168.1.142`, sudo password `T2w3e4r5t6y=` in shared_logins (must never be printed)
- Commits referenced: `5cfb29a9` (Tayscribe deploy), later revert commit (not numbered explicitly but done on master, named files only)

**Gotchas and dead ends already ruled out:**
- **GPU on Taygeta is unusable without reboot** - confirmed by `nvidia-smi` error. CPU-only large-v3 is too slow.
- **F8 already occupied by player2 OCR** - Max chose to leave OCR on F9 and put Tayscribe on F8, so no collision.
- **Remote URL passthrough bug** - `_remote_transcribe` was clipping `auto` to `ru`. Fixed by adding `"auto"` to the passthrough set so the server could detect language.
- **Shift bug not a code regression** - the latch logic hasn't changed in weeks; issue is likely from new keyboard hooks (player2).
- **Do NOT blind-fix shift** - need Max's deliberate repro first.
- **Never kill typer mid-dictation** - always use `restart_typer_safe.py`.

**For the next session:** The most likely scenario is Max reports + key works perfectly and the immediate crisis is over. Then you can ask if he wants to clean up the clipboard history trash. Keep Tayscribe on F8 but don't bring it up unless he does; it's parked.
