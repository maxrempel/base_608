# Scribe handover - milestone 1 (~96K tokens)
# session: 20260625_ecstatic_volhard_50c7c6_2e632416
# cwd: C:\claude_base\.claude\worktrees\ecstatic-volhard-50c7c6
# written: 2026-06-25 14:11:34 by deepseek-v4-pro

# Handover: Duplicate Hotkey Tray Icons

## GOAL (in Max's words)
*"Why do I have two hotkeys in my tray, like icons, tray icons on the taskbar? There should be only one hotkey icon. Now I have two and they are of different kind. One is like version 1, another version 2. Investigate and see how it can be fixed. I think it doesn't hurt, but it just doesn't look pretty to have two of them."*

## DECISIONS + WHY
- Investigation revealed two separate Python processes both running `typer.py --key rctrl --lang ru`, each from a different virtual environment.
- The old one (PID **9692**) is launched by a leftover `WhisperWriter.lnk` shortcut in the Windows Startup folder, using the *old* whisper-writer venv's Python.
- The current one (PID **20884**) is launched correctly by `start_typer.bat`, the canonical launcher.
- No fix has been applied yet - awaiting user go-ahead.

## CURRENT STATE
- **Duplication confirmed**: two tray icons, same hotkey (`rctrl`), same language (`ru`), two different interpreter paths.
- **Root cause identified**: stale Startup shortcut from a previous WhisperWriter install that auto-starts a competing copy.
- **No changes made** - both processes still running, shortcut still in place.

## EXACT NEXT STEP
1. Wait for Max to confirm he wants the fix applied.
2. On confirmation:
   - Delete (or rename to `.disabled`) the shortcut at:  
     `C:\Users\maxre\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup\WhisperWriter.lnk`
   - Kill the old process: **PID 9692** (leave PID 20884 alone).
3. After next login, only one tray icon will appear.

## OPEN QUESTIONS
- **Awaiting user decision**: does Max want the deletion and process kill executed now? (Claude explicitly asked this at end of session.)

## KEY PATHS / IDs
- **Stale startup shortcut to delete/disable**:  
  `C:\Users\maxre\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup\WhisperWriter.lnk`
- **Old process (kill this)**: PID `9692` - `typer.py --key rctrl --lang ru` (old venv)
- **Current process (keep this)**: PID `20884` - `typer.py --key rctrl --lang ru` (correct venv)
- **Canonical launcher**: `start_typer.bat`

## GOTCHAS / DEAD ENDS
- Both processes use the same arguments (`--key rctrl --lang ru`), so visually distinguishing them required looking at the Python executable path and parent process - not just the command line.
- Simply killing PID 9692 without removing the Startup shortcut means it will return on next login.
- The old typer code may differ from current (`"version 1" vs "version 2"` appearance noted by Max), confirming they come from different installs.
