# Scribe handover - milestone 2 (~163K tokens)
# session: 20260701_naughty_khayyam_59d9f2_77644c24
# cwd: C:\claude_base\.claude\worktrees\naughty-khayyam-59d9f2
# written: 2026-07-01 12:21:08 by deepseek-v4-pro

# HANDOVER - Max + Noeticus Interview Cleanup (2026-06-30)

---

## GOAL (in Max's words)

Clean up a ~2-hour two-speaker interview recording. The pipeline: level both speaker tracks across time, loudness-normalize them to match, silence each speaker's mic while the other is talking (cross-talk gate - **not yet built**), then edit the transcript to remove retakes and filler. The retake rule is strict: **always keep the LAST take, never any earlier take.** The final output should be cut via stream-copy (no re-render) using dense keyframes every 0.5s.

Max explicitly said: *"Don't merge them until I approve."* The two speaker tracks stay separate until he signs off on balance.

---

## DECISIONS MADE + WHY

| Decision | Reasoning |
|---|---|
| **Track mapping**: T1=mix (ignored), **T2=Noeticus** (quiet mic, -41 dB), **T3=Max** (loud mic, -21 dB) | Confirmed analytically (subtraction T1?(T2+T3) ? near-silent at -54 dB) AND by Max's ear audition. The quiet mic is Noeticus (Claude Opus 4.8 in a browser). |
| **Leveling = two operations**: `dynaudnorm` for across-time evening THEN `loudnorm` to -16 LUFS | dynaudnorm smooths mic-distance drift; loudnorm fixes the absolute volume so both speakers sit equal. Chosen because part 2 is 85 min - drift is real. |
| **Target loudness = -16 LUFS** | Standard podcast/streaming level. Both speakers verified matched within 0.2 dB after processing. |
| **Keyframe interval = 0.5s** (every 15 frames at 30fps) | Max chose this: *"We are not interrupting each other, so that would be perfect... enough to do the cutting without re-rendering."* This is the one intentional re-encode - all later cuts will be stream-copy. |
| **Per-speaker transcription** (separate Deepgram nova-3 runs per leveled track) | Avoids diarization errors. Each track has exactly one speaker, so attribution is perfect. Merged conversation files (`part1_conversation.txt`) are human-readable; JSON files with timestamps are the machine input for cutting. |
| **Language = English** | Detected via 60s probe at 0.99 confidence. |

---

## CURRENT STATE

### ? Done

1. **Audio leveled + normalized** - 4 files in `01_leveled/`:
   - `part1_Max_leveled.mp3`, `part1_Noeticus_leveled.mp3`
   - `part2_Max_leveled.mp3`, `part2_Noeticus_leveled.mp3`
   - All at -16 LUFS. **Max accepted this step.**

2. **Dense keyframe master** - encoding launched (`make_kf_master.sh`), both parts. At last check, two ffmpeg processes were alive and grinding (~2370s CPU each). Outputs go to `03_kf_master/` as MKV files with H.264 video (gop=15, 0.5s keyframes) + both leveled audio tracks as separate streams.

3. **Transcripts generated** - via Deepgram nova-3 in `02_transcript/`:
   - Per-speaker JSON: `part1_Max.json`, `part1_Noeticus.json`, `part2_Max.json`, `part2_Noeticus.json`
   - Merged human-readable: `part1_conversation.txt`, `part2_conversation.txt`
   - **Retakes already visible in the transcript** - e.g. Part 1 Turn 1 is all pre-interview direction ("start again", "don't reference human colony"); Turn 5 has an explicit abandoned take ("I forgot to hold the record button... previous answer should be deleted, now starting again").

4. **Project git repo initialized** - commit `b8f0a65` at `C:\Users\maxre\Videos\podcast_cleanup\max_interview_20260630\` with README, code, transcripts, logs. Heavy media is git-ignored.

### ? In Flight

- **KF master encode** - running in the background (two ffmpeg processes). Output files not yet finalized when last checked. Part 2 is 85 min of 1080p; this takes time.

### ? Not Yet Started

- **Cross-talk gate** - silencing Noeticus's mic during Max's speaking turns and vice versa. The diarization data (timestamps of who speaks when) exists from the per-speaker transcripts, so the gate logic is straightforward. Max explicitly wants this: *"when one person is speaking, then another one should be silenced."*
- **Transcript retake editing** - this is what the branch will do.
- **Final stream-copy cut** - depends on the branch's keep/cut list and the cross-talk gate.

---

## EXACT NEXT STEP

The branch is about to be spun to **edit the transcripts**. The branch's job (as written in the README):

1. Read `02_transcript/part1_conversation.txt` and `part2_conversation.txt`
2. Identify retakes, filler, false starts, and pre-interview direction
3. Produce a **keep/cut list** - timestamps of segments to keep
4. **MUST keep the LAST take** of any repeated line. Never the middle one. *"It's always the last one, never medium."*

I (the supervisor) will review the branch's cut list against the keep-last-take rule, then build the cross-talk gate and execute the final stream-copy cut.

The branch should start by reading `README_tomemex.md` in the project folder for full context.

---

## OPEN QUESTIONS (awaiting Max)

- **Cross-talk gate approach**: Fade or hard mute? Max hasn't specified. Fading (e.g. 50ms) is less jarring.
- **Final output format**: MP4? MKV? Single mixed track or keep both mics as separate channels?
- **Any content the branch should definitely KEEP or definitely CUT** beyond what the keep-last-take rule captures? (Max may have editorial preferences about topics to include/exclude.)

---

## KEY PATHS + IDs

| What | Path |
|---|---|
| **Project root** | `C:\Users\maxre\Videos\podcast_cleanup\max_interview_20260630\` |
| **Source - Part 1** | `C:\Users\maxre\Videos\2026-06-30 15-19-01.mkv` (25 min) |
| **Source - Part 2** | `C:\Users\maxre\Videos\2026-06-30 15-52-35.mkv` (85 min) |
| **Leveled audio** | `.../max_interview_20260630/01_leveled/` (4 files) |
| **Transcripts** | `.../max_interview_20260630/02_transcript/` |
| **KF master (encoding)** | `.../max_interview_20260630/03_kf_master/` |
| **Code** | `.../max_interview_20260630/code/` (level_normalize.py, make_kf_master.sh, transcribe_speakers.py) |
| **Pipeline docs** | `C:\claude_base\tools\retake_cleaner\retake_cleaner_v06_tomemex.md`, `C:\claude_base\tools\podcast_cleanup\podcast_cleanup_method_v01_tomemex.md` |
| **Deepgram key** | `C:\Users\maxre\Nextcloud\zSyncMain\ssh\deepgram_key_20260515.txt` |
| **Git commit** | `b8f0a65` |

---

## GOTCHAS + DEAD ENDS

- **The other 2026-06-30 files in Videos are false starts** (1-70 seconds). Ignore them. Only the two MKVs listed above are the real recordings.
- **Noeticus's mic was ~20 dB quieter** than Max's. Leveling it up to -16 LUFS will raise background hiss during Max's turns. That's exactly what the cross-talk gate must fix - it's expected, not a bug in the leveling.
- **Do NOT merge tracks** until Max approves the balance. He wants to hear them separately first (already done for part 1 via the full-length samples in `samples/`).
- **Retake rule is absolute**: keep only the LAST take. The transcript already shows clear retakes (Turn 1 and Turn 5 in part 1). If the branch proposes keeping any earlier take, that's an error to flag.
- **The KF master encode must finish** before any stream-copy cutting can happen. Check that the output files in `03_kf_master/` exist and are complete before proceeding to the final cut step.
- **Per-speaker transcript attribution is perfect** - no diarization guessing needed because each track has exactly one speaker. The merged conversation file was built by sorting timestamps from the two JSON files and interleaving. Don't second-guess the speaker labels.
