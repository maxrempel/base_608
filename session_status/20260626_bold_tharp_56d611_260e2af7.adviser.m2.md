# Adviser note - milestone 2 (~156K tokens)
# session: 20260626_bold_tharp_56d611_260e2af7
# written: 2026-06-26 15:57:33 by deepseek-v4-pro

TO MAX: The Assistant pushed directly to master (C:/claude_base, not a worktree) while the checkout had dozens of uncommitted files from other sessions sitting in it. The commit itself was scoped to one file and is correct - but the shared-main-checkout pattern is fragile. If some other session runs `git add .` or `git commit -a`, everyone's half-done work gets baked in. You may want to enforce a rule that autonomous pushes happen only from worktrees, not the shared master checkout.

TO ASSISTANT: You started `wake_listener.py` in the background - that process likely blocks (it's a listen-loop). Verify it's not hogging a port or hanging your session. Second: the main checkout is a minefield - dozens of dirty files from other sessions. Your single-file `git add` was correct this time, but never let a blanket add or commit -a happen there. Worktree-only for edits that touch git.
