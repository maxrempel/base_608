# Scribe handover - milestone 2 (~163K tokens)
# session: 20260625_crazy_ptolemy_294abf_c644d998
# cwd: C:\moma\.claude\worktrees\crazy-ptolemy-294abf
# written: 2026-06-25 16:30:48 by deepseek-v4-pro

## HANDOVER - Scribe notes

### GOAL (Max's words)
"Make four variations of that image [sc_window_orig_B_v01.png], make them turn towards the planet, more straight, more royal, change lighting. Use proper inputs, not variations, don't use derivatives, start from canonical inputs."

The image shows Anna and Ishtab in a corridor with a window, looking out at a planet. The aim was to fire 4 distinct render jobs varying only the lighting while keeping posture (both turned toward window, upright/regal, hands clasped) and all character/facility details locked to the canonical 5-reference plate set.

### DECISIONS + WHY
- **Base image identification:** The original image is `sc_window_orig_B_v01.png`. It maps to arrangement 2 (`bg_corridor_window_v01` BG + Anna face/body + Ishtab face/body). This arrangement was used as the template.
- **Canonical inputs only:** No derivatives. The 5 refs are registry-resolved plates (BG, Anna face, Anna body, Ishtab face, Ishtab body). Arrangement 2 already holds those references, so the script re-used it verbatim.
- **Prompt structure:** Prompt is a long template identical for all 4 jobs except the `<LIGHTING>` placeholder. Lighting variants: cool planetshine (J2939), warm cabin rim + cool window fill (J2940), soft diffuse low contrast (J2941), hard key from above-right (J2942).
- **Fire method:** Wrote a throwaway Python script `fire_window_planet_variations.py` that creates 4 jobs in the D1 database with status `queued`. The script inserted the prompt into the `output_prompt` column (mistake - see gotchas).
- **Bug fixes:**
  1. Worker was looping on job 2751 (empty `input_prompt`, status `queued`). Set its `input_status` to `done` to skip it.
  2. The new jobs had prompt only in `output_prompt`. Worker reads `input_prompt`. Copied `output_prompt` ? `input_prompt` for jobs 2939?2942.
  3. No restart was necessary - the worker picked up the unblocked queue automatically.
- **Checked-in as D-75:** No impact on task, just identity.

### CURRENT STATE
- 4 jobs (IDs 2939, 2940, 2941, 2942) are queued/rendering. J2939 started building strips ~20 seconds after the fix. Likely all 4 are now complete or will be within minutes.
- The picks-link for viewing all 4: `http://localhost:8779/imager?ids=2939,2940,2941,2942&title=window%20planet%204var%20royal%20turn-to-planet%20lighting%20sweep`
- The `combo_worker` is running correctly now, no stuck jobs observed.
- The `fire_window_planet_variations.py` script still has the bug (writing to `output_prompt`): **not yet fixed**.

### EXACT NEXT STEP
1. **Verify all 4 jobs completed** - check D1:  
   `SELECT id, output_file, output_status FROM jobs WHERE id IN (2939,2940,2941,2942);`  
   Or inspect the log: `tail -20 <data_dir>/combo.log`.
2. **Present the final images** to Max via the imager link (already shared, but may need to refresh or confirm rendering done).
3. **Fix the fire script**: edit `fire_window_planet_variations.py` (or a permanent version) to write prompts into `input_prompt`, not `output_prompt`, so future fires work without manual copy.
4. **Clean up** any temporary changes (like job 2751 status) if needed.

### OPEN QUESTIONS
- Max said "Maybe I need to restart something. I will restart the thing." The assistant fixed the queue without Max restarting. Unknown what "the thing" was; maybe the worker. Monitor to ensure no further stalls.
- The user hasn't seen the rendered images yet - wait for Max's feedback before making further tweaks or comparisons.

### KEY PATHS / IDS / COMMANDS
- **D1 client**: `C:\moma\sc10\combo_runner\code\moma_db.py` (class `D1Client`)
- **Job IDs**: 2939, 2940, 2941, 2942
- **Stuck job**: 2751 (now `input_status='done'`)
- **Data directory (logs, PID files)**: `C:/Users/maxre/Nextcloud/ai_images/kazarian_episode/scenes/scene10_images/combo_runner/data/`
- **log file**: `combo.log` inside that data dir
- **Output stills dir**: `C:/Users/maxre/Nextcloud/ai_images/kazarian_episode/scenes/scene10_images/combo_runner/output_stills/`
- **Fire script**: `C:\moma\sc10\combo_runner\code\fire_window_planet_variations.py`
- **Arrangement used**: ID 2, `bg_corridor_window_v01` with Anna+Ishtab plates
- **Imager link**: `http://localhost:8779/imager?ids=2939,2940,2941,2942&title=window%20planet%204var%20royal%20turn-to-planet%20lighting%20sweep`
- **Worker process**: Python `combo_worker.py` (no PID file observed, but tasklist showed multiple Python processes)

### GOTCHAS
- **Database column mismatch**: The `jobs` table has `input_prompt` (the prompt the worker reads) and `output_prompt` (maybe used for logging). My script mistakenly populated `output_prompt`, leaving `input_prompt` null. Always use `input_prompt` when firing jobs.
- **Stuck queue**: If any job has `input_status='queued'` and empty/missing `input_prompt`, the worker will re-pick it indefinitely (looping "SKIP - empty prompt"), blocking everything behind it. Fix: set its `input_status` to `done` or `error`.
- **No restart normally needed**: The worker picks up new jobs as soon as they're queued, unless it's stuck in a loop or failed silently. Check log tail before restarting.
- **The fire script is temporary**-if reused, fix the column name and maybe add error handling for empty prompts.
