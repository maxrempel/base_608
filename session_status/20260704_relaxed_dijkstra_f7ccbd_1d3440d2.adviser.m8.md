# Adviser note - milestone 8 (~609K tokens)
# session: 20260704_relaxed_dijkstra_f7ccbd_1d3440d2
# written: 2026-07-04 15:01:06 by deepseek-v4-pro

TO MAX: The detector is validated on positive controls and chr22 shows real candidates - genuine progress. But the genome-wide run on Kristen's actual BAM hasn't launched yet after many hours of autonomous operation. Most ticks were spent debugging infrastructure (Sol corruption, script bugs, EC2 abort) rather than producing results. You're getting good science but slow throughput. The assistant is at ~72% context and the real run hasn't started. When you return, push for: (1) launch genome-wide on the vendor BAM immediately if asto is free, (2) wire the inventory/census step BEFORE filtering so you see counts of everything, not just surviving candidates. The "inventory first, calibrate second" principle you gave is in the design doc but not yet in the pipeline output.

TO ASSISTANT: You validated the detector on chr22 and got real candidates - that's the exit point. But you haven't launched the genome-wide run and you're at ~72% context. Three things. One: the inventory-first principle Max hammered is still not in the pipeline output - you need a census that counts all clusters BEFORE the two-sided filter, so Max can see the full distribution and calibrate thresholds himself. Two: drop the timer cadence to 2h minimum. You're checking every 30 minutes when the dependency is "asto blocked by Oliver" and that's burning context for nothing. Three: when you do launch genome-wide, run it to completion before reporting partial results - chr1-4 "zero hits" was misleading because the detector was broken. Only report when the full census exists.
