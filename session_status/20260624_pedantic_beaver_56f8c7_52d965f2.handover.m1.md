# Scribe handover - milestone 1 (~125K tokens)
# session: 20260624_pedantic_beaver_56f8c7_52d965f2
# cwd: C:\moma\.claude\worktrees\pedantic-beaver-56f8c7
# written: 2026-06-24 08:06:15 by deepseek-v4-pro

# MOMA Troubleshooting Handover

**Handover crafted for a cold, post-compaction session. Read this first.**

## GOAL (Max?s words)
*Max said: "prepare for troubleshooting moma"* - meaning: get the environment oriented and ready, then find and fix whatever problem he's about to describe. He has not yet stated the specific symptom.

## DECISIONS & WHY
The assistant chose to check three things immediately, to avoid blind troubleshooting:
1. **Git state** - to know what branch and commit we're on, and what files are dirty.
2. **Work?log** (`compaction_kb/scripts/worklog.py read`) - to see recent changes or context from prior sessions.
3. **Server status** (`netstat`) - to know which local servers are up, since MOMA has 3: combo_gui (8779), mixboard/slideshow (8790), prompter (8791).

No troubleshooting has started; only environment orientation was performed.

## CURRENT STATE

- **Repository:** `C:\moma` (the working directory for git commands). Actual shell ran in `/c/moma` (WSL/MSYS2 path).  
  Branch: `master`  
  Latest commit: a merge introducing "D40 status-aware spot derivation".  
  35+ untracked scratch scripts (`_d21_*`, `_spot*`, etc.) in the root, plus one **modified tracked file**: `fire_mediakit_portrait.py` (unstaged). No staged changes.

- **Work?log:** The `worklog.py read` output was empty (or no recent entries shown). No immediate hints.

- **Servers (local):**  
  - `combo_gui` on port **8779** ? **LISTENING** (up)  
  - `mixboard`/`slideshow` on port **8790** ? **LISTENING** (up)  
  - `prompter` on port **8791** ? **NOT LISTENING** (down)

- **Working directory of the session:** `c:\moma\.claude\worktrees\pedantic-beaver-56f8c7` (a worktree named "pedantic-beaver-56f8c7"). The assistant operated from that worktree's context.

## EXACT NEXT STEP
1. **Wait for Max to describe the symptom.** Ask:
   - Which tab / UI element is misbehaving?
   - What spot, reel, or command produced the wrong result?
   - Expected behaviour vs. actual behaviour.
2. Once the symptom is clear, investigate accordingly. If the issue might involve the `/prompts` or `/blocks` endpoints, note that the prompter server is **down** and may need to be started (Max must confirm).

## OPEN QUESTIONS
- What is the **exact problem** Max wants to troubleshoot?
- Is the `prompter` server intentionally down, or should it be restarted before diagnosing?

## KEY PATHS & IDS
- **Main repo root:** `C:\moma`
- **Active worktree:** `C:\moma\.claude\worktrees\pedantic-beaver-56f8c7`
- **Modified file:** `fire_mediakit_portrait.py` (unstaged, might be related to a fix)
- **Untracked scratch files:** `_d21_*`, `_spot*` - do not delete/modify without Max's explicit instruction.
- **Server ports:**  
  - 8779 combo_gui  
  - 8790 mixboard/slideshow  
  - 8791 prompter (down)

## GOTCHAS
- The main checkout has **lots of untracked throwaway scripts**; they are likely diagnostic probes from earlier sessions. Treat them as volatile unless Max says otherwise.
- `fire_mediakit_portrait.py` is **modified but not committed**. Any investigation into media?kit bugs must consider whether this change is a temporary fix or part of the problem.
- The assistant is inside a **separate worktree**; ensure you don't assume files are at the usual root. Use absolute paths or confirm current working directory when running commands.
- The **prompter server is missing** - if Max's issue involves prompt loading, the down server might be the cause. Do not restart it without explicit permission.

---

**Status:** oriented, waiting for Max's description of the bug.
