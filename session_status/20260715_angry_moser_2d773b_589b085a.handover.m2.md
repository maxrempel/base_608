# Scribe handover - milestone 2 (~165K tokens)
# session: 20260715_angry_moser_2d773b_589b085a
# cwd: C:\claude_base\.claude\worktrees\angry-moser-2d773b
# written: 2026-07-15 08:41:50 by deepseek-v4-pro

# HANDOVER - Nadali Project, Anna Meeting Room Presenter Image

---

## GOAL (Max's words)

Making an image for a new MoMA project called **Nadali** (a nickname; no meaning). It's a video presentation on hybrid children. First step: create an image of **Anna in a kitchen room** - "kitchen" is just a nickname; it's actually the meeting room where they have gatherings. Anna is **chest-up portrait, standing** (not visible in frame, but she IS standing - so slight up-angle perspective shift), framed like a **podcast presenter**. Behind her: the beautiful greens and the window with a view of Earth from orbit. Use the proper Anna portrait and the proper room background.

---

## DECISIONS + WHY

### 1. Canonical Anna reference
- Used: `anna_approved_headshot_white_cloak_20260330.png` from `characters/anna/approved portraits/`
- This is the single blessed canonical portrait - redhead, freckles, green eyes, cream linen hooded robe.

### 2. Room reference - CORRECTED mid-session
- **Initial mistake**: Claude grabbed `bfl_v06_room_anna_tight.png` from the `combo_output_stills` folder (raw worker generations, not approved). Max stopped the workflow with: *"There are canonical approved positions of the room and you grab non-approved ones. Do you know what approval means?"*
- **Corrected to**: `station_con_a1_ls.png` from `interiors/station_landscape_remake_20260628_v01/` - this is from the canonical approved set of room angle positions (plate labeled `station_room` in `canonical_status.py`, rated 5, from round `sc11_arr02_v62`). Ornate white walls, large arched window showing Earth from orbit, lush greens, warm floor.
- This room has ~10 approved angle variants in that folder. The straight-front one was used.

### 3. Generation method
- Used `/v1/images/edits` endpoint (not `/generations`) because identity preservation from a reference image is required.
- Combined Anna's face+costume reference and the room reference into a single horizontal composite strip using PIL, then sent that as the reference image.
- Model: `gpt-image-1.5` (critical - wrong model name is the #1 failure mode).
- Called via MCP `run_python_code` on (likely) pine-python.

### 4. Output location
- New project folder created: `C:\Users\maxre\Nextcloud\ai_images\nadali\anna_meetingroom\`
- Three variants generated: `anna_meetingroom_presenter_v1.png`, `_v2.png`, `_v3.png`

### 5. Framing prompt
- "Chest-up" portrait
- "Standing" (slight up-angle perspective, even though legs not visible)
- "Podcast presenter" framing
- Greens + Earth-view window in background
- Standard cinematographic pastel style from the imagegen skill

---

## CURRENT STATE

| What | Status |
|------|--------|
| v1 generated | ? Done - viewed, strong match |
| v2 generated | ? Done - viewed, arguably cleanest presenter framing |
| v3 generated | ? Landed (confirmed via file listing at end) |
| Which variant approved | ? Not yet - Max hasn't reviewed |
| Room angle decision | ? Not yet - Claude asked if front angle is good or if another of the 10 approved positions is preferred |

All three variants saved in `C:\Users\maxre\Nextcloud\ai_images\nadali\anna_meetingroom\`.

---

## EXACT NEXT STEP

1. **Max reviews v1 / v2 / v3** and picks one (or asks for refinements).
2. **Decide on room angle** - the `station_landscape_remake_20260628_v01` folder has ~10 approved positions. Front view used. If a different angle is wanted, regenerate with that plate.
3. **If refinements needed**: feed the chosen variant back through `/edits` with `LOCK` prompt ("Keep the same composition, same location, same people, same layout") plus the specific fix instruction.

---

## OPEN QUESTIONS

- **Which room angle does Max want?** Front was the natural starting point, but ~9 other approved angle plates exist in the same remake folder. Max may prefer a specific one.
- **Does Anna's framing/size/pose match what Max envisioned?** v1 and v2 had slightly different crops - v2 looked cleaner. Max hasn't weighed in.
- **What's next after this image?** Nadali is a "video presentation on hybrid children" - this is the first still. Other shots likely coming.

---

## KEY PATHS / IDS

| What | Path |
|------|------|
| Anna canonical portrait | `C:\Users\maxre\Nextcloud\ai_images\kazarian_episode\characters\anna\approved portraits\anna_approved_headshot_white_cloak_20260330.png` |
| Room canonical front | `C:\Users\maxre\Nextcloud\ai_images\kazarian_episode\interiors\station_landscape_remake_20260628_v01\station_con_a1_ls.png` |
| All approved room angles | `C:\Users\maxre\Nextcloud\ai_images\kazarian_episode\interiors\station_landscape_remake_20260628_v01\` |
| Canonical status tracker | `C:\moma\sc10\combo_runner\code\canonical_status.py` |
| Output folder | `C:\Users\maxre\Nextcloud\ai_images\nadali\anna_meetingroom\` |
| Output files | `anna_meetingroom_presenter_v1.png`, `_v2.png`, `_v3.png` |
| Imagegen skill location | `C:\Users\maxre\AppData\Roaming\Claude\local-agent-mode-sessions\skills-plugin\...\skills\imagegen\` |
| API key | `C:\Users\maxre\Nextcloud\zSyncMain\ssh\openai_api_key_20260216.txt` |
| API model | `gpt-image-1.5` |

---

## GOTCHAS

1. **"Approved" means Max-blessed canonical, not raw worker output.** `combo_output_stills` contains unapproved generations. The real approved plates live in the `canonical_status.py` tracking system and the corresponding `approved station` / remake folders. Do not grab files from archive or worker output folders by filename guessing - always consult `canonical_status.py` first.

2. **Room vs. station exterior**: `earth_orbit_station` (canon0 in canonical_status.py) is the station EXTERIOR. The interior meeting room is a different plate set (`station_room`, canon1 or canon2 area, from `sc11_arr02_v62`). Don't confuse them.

3. **Model name**: `gpt-image-1.5` (not `gpt-image-1`, not `dall-e-3`). Wrong model = identity drift or failure.

4. **/edits vs /generations**: When a character face must match a reference, use `/edits` with multipart form (`files=` and `data=`), NOT `/generations` with JSON body. For multiple references, combine into a horizontal strip first via PIL.

5. **The "kitchen" is just a nickname** - Max said so explicitly. It's the meeting room with the greens and the Earth-view window. Don't go looking for an actual kitchen.

6. **Anna is standing, but chest-up framing**: The fact that she's standing (not seated) changes the perspective slightly - slight up-angle. This was baked into the prompt.

7. **Nadali is a separate MoMA project** from the main Kazarian episode work. It has its own output folder (`nadali/`), not under `kazarian_episode/scenes/`.
