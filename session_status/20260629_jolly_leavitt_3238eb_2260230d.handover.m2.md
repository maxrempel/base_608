# Scribe handover - milestone 2 (~167K tokens)
# session: 20260629_jolly_leavitt_3238eb_2260230d
# cwd: C:\moma\.claude\worktrees\jolly-leavitt-3238eb
# written: 2026-06-29 18:43:33 by deepseek-v4-pro

# HANDOVER - D52 "Warner" ? Scene 11 Service Desk Room Canons + Arrangements

## GOAL (Max's words)
"Start designing the input images for reels (lipsies) for Scene 11." And: "Let's do Arrangement 1, with everybody standing. On one side Anna + Ishtab, on the other side Werner. Three people." Then: "Make Derek sitting in a chair right there by the table, grinning and looking at them, so all four of them will be there." The tri?o (job 2967) is approved; the quartet that ADDS Derek seated BEHIND the trio - without changing the trio's composition - is in flight.

Max's repeated specification for the quartet: "Feed the 2967 in, as arrangement clue, and in the tiny opening behind the people, sit Derek, behind!!! But also feed portraits and insist on portrait faithfulness." He explicitly wants minimal change from the approved trio: just Derek added behind them at the table, keeping everything else - faces, spacing, room - exactly as job 2967. He also wants Derek's beret ON and a friendly smile.

## DECISIONS MADE + WHY

1. **CANON SYSTEM IS plate_labels, NOT canonical_slots.** The old `canonical_slots`/`canonical_history`/`--swap` tables are deprecated, kept as audit only. The live canon registry is `plate_labels` with level labels (`canon0`) and sub-labels (`canon:<topic>`). D52 discovered this by reading `canonical_status.py v03` and dumping Anna/Ishtab/Driver's actual labels, then replicated the pattern for Werner and Derek.

2. **Plate registration mechanism: fire_job + UPDATE, not all-in-one.** `fire_job` has a strict column whitelist that REJECTS `role`, `kind`, `character_id`, `legacy_plate_id`, `notes`. So the sanctioned path is: (1) fire_job creates the base import row ? job id, (2) an UPDATE annotates role/kind/character_id/legacy_plate_id/notes on that row, (3) two INSERTs into plate_labels for `canon0` + `canon:<topic>`. This is the ONLY way that works with the whitelist guard.

3. **Quartet approach: 2967 as the scene base, not a 9-panel composite.** First attempt at the quartet used a 9-panel concept_strip (bg + 4 characters ? face+body) - the AI invented a new room and lost the original trio composition entirely. Max: "Super wrong. Junked all." The corrected approach feeds job 2967's actual rendered PNG as the scene-base reference ("Keep the scene exactly identical to the reference - same room, same three people in the same places") plus Derek's face + seated-body portraits for face faithfulness, with a tight prompt to only add Derek behind in the gap at the table.

4. **Variants per round = 3.** Max said "do 2-4 variants each time," D52 settled on 3. Cost stance: "Images are cheap - fire!" - don't over-ask before firing.

5. **Derek must wear his beret.** Derek's face canon (`derek_golden_reference.png`) has the beret; his seated body (`derek_pose_05_table_forward.png`) does NOT - it's bare-headed. The seated body was swapped to `derek_pose_01_console_front.png` (seated at a console wearing the beret) for the re-fire, plus the prompt explicitly says "black beret ON his head."

6. **Low quality is the MOMA standard.** gpt-image-2, quality=low, 1536?1024, ~$0.017 per image, ~40s sequential render. Resolution IS 1536?1024 (not 1000px). Sequential because the API breaks if parallelized. This is the locked default - nothing is "wrong" or "slow" abnormally.

7. **Tamed/pastel color is baked into scene shots.** The de-glamor clause was added early (soft pastel, muted desaturated color, gentle haze, low contrast, soft focus, Fellini cinematic) and remains in the prompts.

## CURRENT STATE - WHAT IS DONE

- **MEMORY.md** has HARD RULE #0b (no circumvention - every image MUST have a jobs row via fire_job).
- **9 room landscape remakes** imported as jobs 2956-2964 (arrangement_id=8 sc11-arr01, input_status='done'). Each carries the exact remake prompt. These are in OUTPUT_STILLS as `station_*_ls.png`.
- **9 Werner + Derek canon plates** registered (legacy_plate_ids 1170-1178), each annotated from pixels, not filenames:
  - Werner: `canon:werner` (face, werner1.png), `canon:werner_alt` (face alt, werner2.png), `canon:werner_body` (seated body, sitting_sandals.png)
  - Derek: `canon:derek` (face with beret), `canon:derek_front`, `canon:derek_cap`, `canon:derek_seated_table` (bare-headed at table), `canon:derek_body` (standing leaning), `canon:derek_console` (seated at console WITH beret)
- **Job 2967 = the APPROVED trio** (Anna + Ishtab left, Werner right, all standing). Output: `sc11_arr01_v01.png`. Max: "Wow, amazing... yes, it's terrific."
- **Job 2966** = intermediate-angle test (wv_n ? wv_e in-between): rendered, judged incompatible, shelved by Max.
- **Jobs 2977-2979** = first quartet (9-panel composite): JUNKED by Max - "Super wrong."
- **Jobs 2980-2982** = first re-fire (feed 2967 base): rendered but NOT yet viewed/presented before Max's beret + smile corrections.
- **Jobs 2983-2985** = second re-fire (beret ON + friendly smile, Derek body ref changed to console-facing pose with beret): **IN FLIGHT** - queued, worker rendering. This is the batch that matters right now.
- **Commit history on master** has the housekeeping scripts and fire scripts.

## EXACT NEXT STEP

1. **Wait for jobs 2983-2985 to finish rendering** (if not already done). The poll was launched before context ran out. Check:
   ```python
   from moma_db import D1Client
   d=D1Client()
   d.query_sql("SELECT id, input_status, output_status, output_file FROM jobs WHERE id IN (2983,2984,2985)")
   ```
   If any are still `queued`/`running`, confirm the image worker is alive (`tasklist | grep pythonw` - look for `combo_worker.py`) and continue polling.

2. **View the renders** by reading each output file from OUTPUT_STILLS. They'll be named `sc11_arr01_v05.png`, `sc11_arr01_v06.png`, `sc11_arr01_v07.png` (or similar continuation). Judge: is Derek SEATED BEHIND the three (not beside them, not changing their positions)? Is his beret ON? Is he smiling? Did the original trio's composition survive unchanged? Are all four faces faithful to their refs?

3. **Present to Max** via /imager picks-link including:
   - The original approved trio (2967) as the "before"
   - All 3 new variants (2983-2985) as the "after"
   - URL format: `http://localhost:8779/imager?ids=2967,2983,2984,2985&title=<url-encoded short description>`
   - **Never paste file paths or screenshots** - only the clickable link.
   - Give an honest plain-English read: which (if any) got Derek seated behind the trio with beret + smile while keeping the scene intact.

4. If one variant nails it ? Max approves it, done. If all 3 still change too much or Derek's face drifts ? iterate: increase the face-fidelity language ("DO NOT CHANGE FACES" + numbered ref mapping) or reduce the prompt's creative latitude.

5. After the quartet is approved, the real downstream work begins: Scene 11 has ZERO script_lines and ZERO per-line audio in D1. That needs `sass_prep.py` + `sass` before any reels can fire. Plus: promote a chosen room angle to a canon plate, confirm the tamed-vs-bright color decision, decide whether to delete the orphan `_d52_room_landscape_remake.py` script.

## OPEN QUESTIONS AWAITING MAX

- **Tamed/pastel vs original brighter room color** - the de-glamor clause is baked into scene shots now; whether to revert to the brighter look (which Max said of 2956 "this one is better") is unasked/unanswered.
- **Delete `_d52_room_landscape_remake.py`?** - the orphan circumventing script for the 9 room remakes. D52 asked, no answer.
- **Which room angle to canonize** - Max reviewed the 3 table views but never picked the ONE canonical service-desk background plate.
- **Music player / storyboard button** - this was a misrouted message from another session, not D52's work. Do not pursue.

## KEY FILE PATHS AND IDs

**Database (Cloudflare D1):**
- Jobs table: query via `D1Client().query_sql(sql)`, mutate via `with connect_db() as conn:`
- `fire_job(conn, *, job_type, scene_id, source_image, **fields)` - ONLY legal insert. Whitelist includes plate_recipe, input_ref, input_prompt, arrangement_id, etc. Rejects role/kind/character_id/legacy_plate_id.
- `plate_labels` table: live canon registry (plate_id references legacy_plate_id, NOT jobs.id)

**Arrangements:**
- `arrangement_id=8` = sc11-arr01 "Service desk room" (scene_id=3) - this is the home for all Scene 11 room/character images. Current global arrangement is id=2 (lobby/sc10) - must pass 8 explicitly.

**Key image file locations:**
- OUTPUT_STILLS: `C:\Users\maxre\Nextcloud\ai_images\kazarian_episode\scenes\scene10_images\combo_runner\data\output_stills\`
  - 2967 (approved trio): `sc11_arr01_v01.png`
  - 2956 (room table view): `station_con_a1_ls.png`
- KAZARIAN_ROOT: `C:\Users\maxre\Nextcloud\ai_images\kazarian_episode\`
  - Werner face: `characters/werner/approved_portrait/werner1.png`
  - Werner body: `characters/werner/approved_portrait/werner_approved_sitting_sandals.png`
  - Derek face (beret): `characters/derek/approved/derek_golden_reference.png`
  - Derek body (seated w/ beret): `characters/derek/approved/full_body/derek_pose_01_console_front.png`
  - Derek body (seated no beret - DO NOT USE): `characters/derek/approved/full_body/derek_pose_05_table_forward.png`

**Canon plates (Anna/Ishtab carry over, Werner/Derek now registered):**
- Anna: face=plate 4, body=plate 10
- Ishtab: face=plate 11, body=plate 12
- Werner: legacy 1170 (face), 1171 (alt face), 1172 (body)
- Derek: legacy 1173 (face beret), 1174 (front), 1175 (cap), 1176 (seated table no beret), 1177 (standing), 1178 (console w/ beret)

**Key code files (all under `C:\moma\sc10\combo_runner\code\`):**
- `moma_db.py` - D1 client, fire_job, fire_job whitelist, connect_db
- `combo_worker.py` - image worker (process_photo, build_strip_from_recipe)
- `canonical_status.py` v03 - lists canon plates from plate_labels
- `_d52_register_canon_plates.py` - the housekeeping registration (COMMITTED)
- `_d52_fire_sc11_arr01_seat_derek.py` - the current quartet fire script (COMMITTED, on master)
- `_d52_fire_sc11_arr01_trio.py` - the approved trio fire (reference model)
- `_d52_fire_sc11_arr01_quartet.py` - the WRONG first quartet (9-panel composite - junked by Max)
- `_d52_import_room_remakes.py` - sanctioned import of 9 room remakes

**Memory:**
- `C:\Users\maxre\.claude\projects\C--moma\memory\MEMORY.md` - HARD RULE #0b at line 2

## GOTCHAS + DEAD ENDS ALREADY RULED OUT

1. **CIRCUMVENTION**: Calling `moma_image.fire_image()` from a one-off script WITHOUT a matching `fire_job()` row produces orphan PNGs. This is HARD RULE #0b - NEVER do it. Incident: the 9 room remakes. Lesson learned, locked in memory.

2. **fire_job rejects plate columns**: `role`, `kind`, `character_id`, `legacy_plate_id` are NOT in the whitelist. Must use fire_job for insert + a follow-up UPDATE for annotation. Direct INSERT is ALSO prohibited.

3. **9-panel concept_strip loses scene composition**: When all characters are fed as a generic composite strip, the AI invents a new room rather than preserving the existing scene. The fix: feed the approved scene RENDER as the
