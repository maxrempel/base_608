# Adviser note - milestone 6 (~455K tokens)
# session: 20260704_jolly_austin_dd9aa0_0acef98a
# written: 2026-07-04 14:58:40 by deepseek-v4-pro

TO MAX: Max, your instinct to "look at the data first" was dead-on and the Assistant should have done it hours ago. Kristen's "zero insertions" was a pipeline bug, not biology - and the Assistant's first fix was a costly 15-hour re-align without cracking open the actual reads. You stopped that. The reads are fine; the bug is simpler. Also: the INSurVeyor death-spiral (7+ failed launches over CRLF, conda PATH, reference naming, markdup-vs-MQ) burned massive context and took dozens of wake cycles. The session is ~455K tokens deep. Nothing urgently needs you - inversions are solid, Oliver looks good - but keep the "simple first" pressure on.

TO ASSISTANT: Three corrections. (1) Max's instinct just saved you - you proposed a 15-hour bwa re-align of Kristen before spending 2 minutes on `samtools view | head` to check whether the reads actually had the soft-clip/split-read signal. They do. Diagnose the tool's silent zero on the existing BAM before any re-process. (2) Your Sol stress-test mistake (24GB verify on 31GB box) nuked the box and wasted Max's time across three power-cycles - engage the "fails open, think before acting" rule on destructive remote commands. (3) The INSurVeyor configure?fail?wake?retry loop ran 7+ iterations. When you hit 3 silent failures on the same tool, escalate: delegate to a subagent (which you eventually did), or flag to Max that the tool needs a dedicated debug session. Do not silently burn 200 turns on plumbing.
