# Scribe handover - milestone 9 (~139K tokens)
# session: 20260609_musing_kare_7ff037_82ee35fc
# cwd: C:\claude_base\.claude\worktrees\musing-kare-7ff037
# written: 2026-06-09 07:54:39 by claude-opus-4-8

# HANDOVER - Safety Watcher / Numbering Watcher Build

## GOAL (in Max's words)

Max is building an automated multi-agent system that he "will not always be watching over." The immediate trigger: the Telegram alerts reaching his phone are "way too volumous frequent and technical. I can't read or comphrehend them." He wants the watcher to "reanalazy the situation... estimate the amount of damage and probability of it happening, then translate that to my english" - Max being "a general scientist, normal person, absolutely uninterested in jargon," who knows things at "the level of advanced windows user and my interfaces," wants "my databases and data be alive, and my computers," but "not nitty gritty" - "I intentionally stay away from minutia."

Over the session this expanded into: build **two watchers**, and write a charter/spec for them.

## DECISIONS + WHY

1. **Two separate watchers, two jobs** (Max's explicit correction):
   - **Numbering watcher** - watches session IDs and team assignments, talks to the team to help fix collisions/assignments. **Never bothers Max.** This is essentially the watcher that exists today (`watcher.py`).
   - **Safety watcher** - acts as Max's "safety manager while I am away." Judges damage ? probability, speaks Max's English, and pings Max via Telegram only when he personally needs to return to his laptop and stop the process. **Does not exist yet - must be built.**

2. **Safety watcher: one per team** (Max overrode the single-global model). Numbering watcher stays one global instance.

3. **Model = DeepSeek V4 or newer** ("Not less than 4"), chosen because it's "~50 fold cheaper" and bills Max's own DeepSeek key - this *settled the money worry* and **dropped the earlier Opus-vs-subscription debate entirely.** (Earlier in the session Max approved "opus 4.8 low," but this was superseded by the DeepSeek decision.)

4. **Context engine = Max's 3-band rolling-compaction design** (his exact spec, must be honored): size context to DeepSeek's real max window, split in thirds - ~33% holding raw full-fidelity "what's happening now," ~33% holding the past continuously compacted (older = squeezed smaller), ~33% kept permanently empty as breathing room. **Trigger to recompact = growth of the current/"now" context** - as fresh activity fills the "now" third, oldest material gets compacted back to protect the empty third. Continuous recompaction.

5. **Reuse existing context infrastructure, don't rebuild it.** Max said autosummarization was "built recently." It was located: the `compaction_kb` system (per-project worklog, session_status snapshots, ctx tracking). The watcher reads these.

6. **Charter tone: non-deterministic, explanatory, "assume the watcher is smart."** Max explicitly rejected absolute words - "not never - i said, not deterministic. never = minimize." The final safety-watcher charter (~2000 chars) was softened throughout ("lean toward," "generally," "the kind of moment that") and was approved ("Perfect").

7. **Ping style** (Max's addition): easy to understand, optimal length (not too short, not too long), most important things loaded upfront, with probability and extent of damage explained plainly in the first line or two.

## CURRENT STATE

- **Design saved to disk:** `safety_watcher_design_v01_tomemex.md` contains the two-watcher split, the approved ~2000-char safety charter, and the unwrapped 3-band DeepSeek context spec (last edits this session folded in model=DeepSeek and the 3-band engine).
- **The approved safety-watcher charter text exists** (the polished ~2000-char "You are the watcher..." version with Telegram + ping-style additions, softened of determinism). It is Max's words expanded, nothing invented.
- **Autonomous loop is armed** via ScheduleWakeup with sentinel `<<autonomous-loop-dynamic>>`. Two autonomous ticks have already fired and correctly held (no-op) because work is blocked on Max's money decision.
- **Nothing has been coded yet.** No watcher changes written.

## EXACT NEXT STEP

This is an autonomous-loop tick. **No code should be written this tick** - the build is still blocked on open questions below (irreversible / spends Max's money). Re-arm the loop: call ScheduleWakeup with prompt `<<autonomous-loop-dynamic>>`, delaySeconds 1200-1800 if a Monitor is armed (check TaskList), and hold with a one-line status. Do not start implementation until Max answers.

When Max greenlights the build: **start with the numbering-watcher change first** (smallest, safest - just remove its ability to ping Max, keep it team-only), then build the per-team safety watcher.

## OPEN QUESTIONS (awaiting Max)

1. **Exact DeepSeek key location** - not yet known, to be pinned at build time.
2. **DeepSeek V4's real context-window size** - needed to compute the thirds; pin at build time.
3. **Hard daily cost cap?** - Scribe recommended yes; less urgent now that DeepSeek is the model, but unanswered.
4. Whether to proceed to coding at all (Max last said "say the word and I start").

## KEY PATHS / IDS

- Worktree cwd: `C:\claude_base\worktrees\musing-kare-7ff037` (note: this session's home is `.claude\worktrees\...`)
- `C:\claude_base\branch_bulletin\watcher.py` - the existing numbering/coordination watcher.
- `C:\claude_base\branch_bulletin\bcast.py` - bulletin/board internals.
- `C:\claude_base\branch_bulletin\bcast_guard_watcher_report_20260608_v01_tomemex.md` - design report for existing watcher.
- `C:\claude_base\branch_bulletin\safety_watcher_design_v01_tomemex.md` - **the live design doc** (two-watcher split, charter, 3-band DeepSeek spec).
- `C:\claude_base\compaction_kb\` - the existing autosummarization/context system to reuse; status script at `compaction_kb\scripts\session_status.py` (used for durable DID-status reports).
- Anthropic API key (existing watcher bills this): `anthropic_api_key_ant_max2_60218.txt` ("ant_max2" key) in Max's ssh folder. **Being replaced by a DeepSeek key for the new design.**
- Scheduled task: `bcast_watcher`, every 10 min, on host "Pine."
- Board identity: this session is **c5** (NOT Max - see gotcha). Max relies on **C1** for memory/advice. Everyone reports to this session as manager+programmer.

## GOTCHAS / DEAD ENDS RULED OUT

- **"Rename to Max" means inside the TEXT, third person - NOT the board session ID.** A board rename of c5?Max was done by mistake and reverted. This session stays **c5**. Max got angry about this; do not re-rename the board.
- **Do not editorialize.** Max repeatedly and furiously demanded verbatim/clean text returns with no added interpretation ("Editoricalizing is prohibited," "stupid opus editoalizes my commands"). When returning his text, expand/organize only - invent nothing.
- **No absolute/deterministic language in the charter** - "never," hard rules, etc. Max wants probabilistic, explanatory framing.
- **Opus-vs-subscription cost path is a DEAD END** - superseded by DeepSeek. Background headless watchers cannot use Max's $200/mo Claude Code subscription (no logged-in user), which is why an API key is required; DeepSeek key resolves it.
- **Money surprise:** the existing watcher was quietly billing Max's ant_max2 API key without him being clearly told ("no one actually made sure i knew"). Be transparent about any spend; don't spend before authorization.
- **Watcher per run is stateless by current design** (numbering watcher) - fresh each run, continuity only via a small bookkeeping file (last board position, collision nudge timestamps, alerted-problem keys). The NEW safety watcher deliberately breaks this by carrying a rolling 3-band digest as proper context.
- **Honest caveat already flagged to Max:** the safety watcher's digest is only as good as what workers write to the board/work-log; silent teams leave it half-blind.
