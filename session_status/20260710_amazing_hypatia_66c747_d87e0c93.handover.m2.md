# Scribe handover - milestone 2 (~176K tokens)
# session: 20260710_amazing_hypatia_66c747_d87e0c93
# cwd: C:\claude_base\.claude\worktrees\amazing-hypatia-66c747
# written: 2026-07-10 08:37:52 by deepseek-v4-pro

**GOAL (Max's words):**  
Download 23andMe raw genetic data for Lottie Bowater (and her parents, Julie and Roger). Log in as needed, kick off any delayed requests, grab whatever files are ready now. The father (Roger) may still be processing; we'll come back later for the rest.

**DECISIONS + WHY**  
1. **All Bowater kits are under `max@tamza.com`** - not a separate account. Lottie is just a profile inside Max's main 23andMe account. This avoided a wild goose chase for a "Lottie" login.  
2. **Used Playwright browser for login** - the session already held the browser lock. Bypassed the Bitwarden extension autofill because the extension was logged out in this browser profile (shared persistent profile held by another Claude session).  
3. **Used Bitwarden CLI (`bw get totp`) to obtain the 2FA code** - TOTP seed is stored in the Bitwarden vault item `UK bowater lottie 23andme.com`. This is the permanent solution to get 2FA codes without relying on the extension being logged in.  
4. **Submitted Lottie and Julie as "requests"** - 23andMe generates the file offline and emails when ready; these aren't instant downloads. Roger's was already processed and could be downloaded immediately.  
5. **Extracted Roger's presigned S3 download URL via in?browser fetch** - avoids needing httpOnly cookies; the URL is self-contained.

**CURRENT STATE**  
- Logged into 23andMe (Playwright session open, two?factor already cleared).  
- **Lottie** raw data request **submitted** - status: "in progress" (expect email in a few days).  
- **Julie** raw data request **submitted** - same "in progress" status.  
- **Roger** raw data **ready** - presigned S3 link captured but **file not yet saved**.  
- The `C:\claude_base\projects\XG1\bowater` folder does not yet exist (needs creation, or we save to the parent project folder).  
- No data has actually been written to disk.

**EXACT NEXT STEP**  
1. Create the save directory: `C:\claude_base\projects\XG1\bowater\`  
2. In the still?open Playwright browser (same page context), use `browser_evaluate` with a `fetch` call to the captured presigned URL, pipe the response into a file via a small script (e.g., Python or a PowerShell download) or directly save it using the Playwright `browser_run_code` / `browser_evaluate` plus a download path.  
3. Verify the file is a valid text?based raw data file (should contain genotype lines).  
4. Close the browser and release the Playwright lock (`browser_close`) so other sessions can access the shared profile.

*If the presigned URL has expired or the page context is lost (user restarted Claude), you can simply re?login (TOTP via CLI as before) and re?extract the link from Roger's download page.*

**OPEN QUESTIONS (waiting on Max)**  
- None outstanding; Max's intent was to start the downloads. The only pending action is actually saving Roger's file. Lottie/Julie are out of our hands until 23andMe emails.

**KEY PATHS / IDs / COMMANDS**  
- **23andMe account:** `max@tamza.com`  
- **Bitwarden vault item:** `UK bowater lottie 23andme.com` (ID `7772765a-6e05-44ab-9955-b3fa0142a736`) - contains password + TOTP seed.  
- **Bitwarden CLI session:** `bw_session.txt` at `C:\Users\maxre\Nextcloud\zSyncMain\ssh\bw_session.txt` - already unlocked; fetch 6?digit code with:  
  `bw get totp 7772765a-6e05-44ab-9955-b3fa0142a736 --session (cat ...\bw_session.txt)`  
- **Raw data download page:** `https://you.23andme.com/tools/data/download/`  
- **Profile switcher** - use JavaScript to click dropdown elements; exact selectors depend on the page DOM, but the core pattern is to evaluate clicks on the "Switch Profile" button then on the profile name.  
- **Project save location:** `C:\claude_base\projects\XG1\bowater\` (create if missing).  
- **Playwright lock release:** always call `mcp__playwright__browser_close` when finished.

**GOTCHAS**  
- **Bitwarden extension logged out:** other concurrent Claude sessions may hold the shared persistent browser profile. The extension will be installed but vault?locked. Always fall back to CLI for TOTP; never rely on the extension autofill during peak concurrency.  
- **TOTP expiration:** 30?second window; generate code immediately before using.  
- **23andMe request flow:** for "in progress" kits you must submit via checkbox and wait for email. Roger's was already done; others follow the same pattern when email arrives.  
- **Presigned S3 URL** - if expired, re?navigate to Roger's download page; the site will generate a fresh one.  
- **No bowater folder exists yet** - the assistant must create it. If the folder already exists from a previous attempt, check for partial files before overwriting.
