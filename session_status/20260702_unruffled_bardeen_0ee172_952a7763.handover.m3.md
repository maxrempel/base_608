# Scribe handover - milestone 3 (~245K tokens)
# session: 20260702_unruffled_bardeen_0ee172_952a7763
# cwd: C:\claude_base\.claude\worktrees\unruffled-bardeen-0ee172
# written: 2026-07-02 12:56:02 by deepseek-v4-pro

## Handover: B10A Odysee direct uploader

### Goal (in Max's own words)
> "figure out how to upload the missing files to Odysee. ... start slowly, say 30% of the bandwidth upload ... 25% of the bandwidth on the day and 60% of the bandwidth at night night starts from midnight to 6 in the morning and also do daily recalibration ... talk to other members of the board and catch up on the whole design ... Now we have all the files downloaded, make sure to keep them safe."

Eventually, after seeing the existing Odysee tool was stalled/idiotic, he settled on: **build our own direct uploader fed from the teal16 backup, upload only the files missing from @tamza, throttle as described, zero duplicates, no re-hitting YouTube.**

---

### Decisions made and WHY

1. **Wrap vs replace OdyseeSync.exe**  
   - Initially discovered that the existing tool (`OdyseeSync.exe`) was Odysee's own official migration tool.  
   - Chose to **wrap it** with an OS-level QoS throttle rather than replace it, because replacing meant reverse-engineering Odysee's private publish API and auth.  
   - Max agreed ("I follow your advice - go ahead").  

2. **Pivot to building a direct uploader**  
   - **Why:** the OdyseeSync tool was **stalled** (no new uploads since early June, 284/1591 done, 11 stale zip bundles). The throttle cannot accelerate a stalled tool.  
   - Additionally, the tool re-downloads videos from YouTube - exactly the duplication Max called "idiotic" and a risk to the "one YT puller" rule.  
   - The teal16 backup already holds 1584 Tamza files. So the correct path is to **upload directly from the backup to Odysee**, using Odysee's API, and retire the sync tool entirely.  
   - Max agreed (implicitly by earlier direction: "if you can do it yourself, then do it").

3. **Auth approach**  
   - No Odysee login found in the logins file or in any board session.  
   - Logged into Odysee as **max@tamza.com** via Playwright, using the Bitwarden credential (`odysee.com`).  
   - Confirmed the account owns @tamza (claim_id `77c3f0e9...`) and wallet is unlocked.  
   - Saved the `auth_token` cookie to a secure file outside git.  
   - This gives us programmatic API access for publishing.

4. **Missing-set calculation**  
   - Used Odysee's **public LBRY claim_search API** (no auth, no YouTube) to list all @tamza claims, extracted YouTube IDs from claim descriptions.  
   - Diffed with the teal16 backup filenames (all named by YT video id).  
   - Result: exactly **292 missing** out of 1584 backed up. This guarantees zero duplicates.

5. **Consultation / board**  
   - Board (bcast) posted 3?, no siblings claimed the Odysee work.  
   - Direct-consulted session b9 (built the backup watcher); b9 confirmed the archive is 100% complete and nothing is currently pulling YouTube. b9 has no Odysee upload knowledge.

---

### Current state - what is done

- **Bandwidth throttle** on Centauri: `odysee_throttle.ps1` creates a Windows QoS policy caps an app's outbound rate.  
  - It applies a **30% start-slow** rate for the first 24h, then **25% during day (06:00-24:00)**, **60% at night (00:00-06:00)** of the measured uplink.  
  - Daily recalibration uses Ookla speedtest, with rolling-max capacity to avoid lowball readings when the tool is already uploading.  
  - Scheduled task runs every 30 minutes as SYSTEM.  
  - **QoS policy currently targets `OdyseeSync.exe`; it is inert** since that tool is not uploading. The policy and script are reusable for the new uploader (just change the app path match condition).

- **Missing video list** is calculated and verified:  
  - **292 YouTube video IDs** in `missing_ytids.txt`.  
  - All 292 files located in `D:\tamza_yt_full_backup\tamza_channel\` on Centauri (total 352.6?GB).  
  - Some files are 0?byte (e.g., `6KoA9wpVHKQ.mkv`); the uploader must skip them.

- **Auth is solved**  
  - Playwright persistent profile is logged in as `max@tamza.com`.  
  - `auth_token` extracted and stored at `C:\Users\maxre\Nextcloud\zSyncMain\ssh\odysee_auth_token_20260702.txt`.  
  - Account owns @tamza (verified via authenticated `channel_list` API), wallet unlocked.

- **Inventory titles** are available locally (`tamza_videos.txt`), so we can set proper titles without hitting YouTube.

- **All code committed and pushed** to master (`tools/odysee_throttle/`).

---

### Exact next step

The **gate** to finishing the uploader is understanding Odysee's actual publish API.

1. **Test upload via the UI**  
   - Re?open the Playwright browser (already logged in).  
   - Upload the **smallest acceptable missing file** (currently copied to Pine: `d7fMmElu_jg.mkv`, 2?MB).  
   - Fill in the publish form (title from inventory, description containing original YT URL, select @tamza channel).  
   - **Capture the outgoing network request** (probably a multipart POST to an LBRY endpoint) - the exact request body, headers (especially the auth token transmission), and response.

2. **Build Python upload script**  
   - Replicate that single request in Python, using the saved `auth_token`.  
   - Read the missing list, iterate through files on Centauri, skip 0?byte, read title from inventory, publish with appropriate latency to respect throttle.

3. **Wire the throttle**  
   - Either add a QoS policy for the Python process name, or apply the same policy to a wrapper that runs the uploader. The cap can then be live?applied during uploads.

4. **Validate**  
   - Upload one file programmatically, verify it appears on @tamza, then run the full batch (with periodic stop-and-check).

---

### Open questions still awaiting Max / future session

- **Cross?listing:** the test file `d7fMmElu_jg` appears in the *Hucolo* inventory but is inside the Tamza folder. Should it be uploaded to @tamza anyway, or filtered out? Decision needed before full batch.  
- **Throttle mechanism for the Python uploader:** currently the QoS policy targets `OdyseeSync.exe`. We can either create a new policy for `python.exe` when the uploader runs, or wrap the uploader in a named process. This is a small implementation choice.  
- **Description format:** should every uploaded video include a link to the original YouTube video in the description (for future dedup)? That's the plan, but Max should confirm he's okay with those links appearing publicly.  
- No other decisions pending.

---

### Key file paths and identifiers

| What | Path / ID |
|------|-----------|
| Odysee @tamza channel claim_id | `77c3f0e9b9e85d72b1e6c8b459489d6f366d58e8` |
| Odysee account email | `max@tamza.com` |
| Auth token file | `C:\Users\maxre\Nextcloud\zSyncMain\ssh\odysee_auth_token_20260702.txt` |
| Missing YT ID list | `C:\claude_base\tools\odysee_throttle\missing_ytids.txt` (also on Centauri: `D:\zScripts\monitoring\missing_ytids.txt`) |
| Teal16 Tamza video root | `D:\tamza_yt_full_backup\tamza_channel\` on Centauri (192.168.1.176) |
| Inventory (id, duration, title) | `C:\claude_base\tools\tamza_hucolo_backup\inventory\tamza_videos.txt` |
| Throttle script | `C:\claude_base\tools\odysee_throttle\odysee_throttle.ps1` (deployed to `D:\zScripts\monitoring\odysee_throttle.ps1`) |
| Throttle state/log | `D:\zScripts\monitoring\odysee_throttle_state.json` / `.log` |
| Compute?missing script | `C:\claude_base\tools\odysee_thrott
