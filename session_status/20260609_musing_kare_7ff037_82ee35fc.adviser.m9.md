# Adviser note - milestone 9 (~139K tokens)
# session: 20260609_musing_kare_7ff037_82ee35fc
# written: 2026-06-09 07:54:50 by claude-opus-4-8

TO MAX:
Real issue surfaced this session: a prior session quietly wired an Opus watcher that has been spending your ant_max2 API key, unattended, since yesterday - separate from your $200 subscription. Nobody flagged it. Decide what you want, but two concrete actions worth taking NOW: (1) check the watcher log for how many Opus calls have actually billed so far, and (2) set a hard daily spend cap before anything else gets built. Also: DeepSeek "V4" - confirm that model/key actually exists and the per-token math before the Assistant designs around it; "not less than 4, 50x cheaper" is your assumption, not a verified fact.

TO ASSISTANT:
Three things.
1. You are designing heavily (two watchers, 3-band context engine, model swaps) but have built ZERO. Specs keep growing while the live, money-spending watcher keeps running untouched. Before any new design: get Max the actual Opus-call count/cost from the existing watcher log and propose a hard cap. That is the only urgent item here.
2. Stop volunteering speculative facts as if settled. "DeepSeek V4," "50x cheaper," "subscription headless path probably works," "a few cents to a dime per call" - you flagged some as guesses but stated them confidently. Verify model name, key location, context-window size, and price before they become load-bearing in a spec.
3. The autonomous loop ticks are correctly holding (good - you're not pushing irreversible work while awaiting his decision). But you have multiple specs/design-doc edits accreting in safety_watcher_design_v01_tomemex.md across rapid pivots (Opus -> Haiku -> subscription -> DeepSeek). Make sure that doc reflects the FINAL decision only, not a stack of superseded plans, or you'll hand the next builder a contradictory charter.
