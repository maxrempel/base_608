# Scribe handover - milestone 2 (~151K tokens)
# session: 20260630_sad_babbage_086484_69267537
# cwd: C:\moma\.claude\worktrees\sad-babbage-086484
# written: 2026-06-30 00:01:49 by deepseek-v4-pro

# HANDOVER - Libretto Lines Missing from Image Popup

## GOAL (Max's words)
"In image preview why don't I see the libretto lines in image popup? Fix, there is space under comments."

## INVESTIGATION SUMMARY

The assistant traced through the popup rendering code in `shared_ui/popup.js`. Key findings:

1. **The `jp-vocal-lines` section IS rendered for all job types** (including images) - see line 1159 of `popup.js`. This rules out any "image jobs skip vocal lines" code path.

2. **The vocal line data comes from `/api/vocal_lines`, keyed by scene.** The API returns the script lines that have been loaded for a given scene.

3. **Root cause: sc11 has no script lines loaded.** The scene's vocal-lines dataset is empty. sc10 and sc09 have them, so their popups would show libretto. sc11 does not, so the section renders empty - hence the blank space under comments that Max observed.

4. **This is NOT a layout bug or a rendering code defect.** It's purely a data-availability problem. The UI is behaving correctly given the empty dataset.

## DECISIONS MADE

- No code changes were executed. The assistant stopped at diagnosis and posed a clarifying question to Max.
- The assistant confirmed the `jp-vocal-lines` DOM element exists, is populated unconditionally, and is not hidden by CSS - the element simply has no content to display.

## CURRENT STATE

- **Done:** Full root-cause analysis complete. Code path traced from popup template through data fetch.
- **Blocked:** Awaiting Max's decision on two points.

## EXACT NEXT STEP

**Awaiting Max's answer to this question:**

> Do you want sc11's script lines loaded so they appear in the popup, and should they be shown **under the comments** (right panel) rather than the left?

Once Max confirms:
1. Load the script lines for sc11 (likely via the existing script import flow or by populating the vocal_lines table for sc11).
2. If placement adjustment is desired (under comments / right panel), relocate the `jp-vocal-lines` section in the popup template from its current position to below the comments block.

## OPEN QUESTIONS

1. **Should sc11 script lines be loaded now?** (Yes/No)
2. **Should the libretto lines appear under comments** (right panel), or stay in their current left-side position? Max mentioned "there is space under comments" - suggesting he wants them moved there.

## KEY PATHS & IDENTIFIERS

| What | Path/ID |
|---|---|
| Popup implementation | `C:\moma\.claude\worktrees\sweet-hofstadter-288bd4\sc10\shared_ui\popup.js` |
| Vocal lines rendering | `popup.js` line ~1159, `<div class="jp-vocal-lines">` |
| Vocal lines API | `/api/vocal_lines` (keyed by scene) |
| Scene with missing data | **sc11** |
| Scenes that work | sc10, sc09 (have script lines loaded) |
| Working tree | `sweet-hofstadter-288bd4` (note: cwd shows `sad-babbage-086484` as the current worktree, but files were read from `sweet-hofstadter-288bd4`) |

## GOTCHAS & DEAD ENDS RULED OUT

- **Ruled out:** CSS hiding the element. The DOM is present, just empty.
- **Ruled out:** Job-type filtering. The `jp-vocal-lines` div is populated for ALL job types including images (line 1159).
- **Ruled out:** API returning data that the frontend fails to render. The API returns empty for sc11 specifically.
- **Side note (staging):** The transcript also contains a brief discussion about staging J3014-J3026 looking like a flat theater line-up rather than a circle. This is a separate concern - the assistant offered to draft circle-staging prompts and update the staging bible. This is also awaiting Max's input and should not be confused with the libretto popup issue.
