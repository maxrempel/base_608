# Adviser note - milestone 11 (~167K tokens)
# session: 20260610_vigorous_gould_3d960c_02725b78
# written: 2026-06-10 15:08:41 by claude-opus-4-8

TO MAX:
You're sitting under a team-wide c0 FULL HALT and just told the Assistant to commit/push/merge anyway. That's the third or fourth time you've woken D12 solo and overridden the halt. If the halt still matters, the Assistant is now routinely punching through it on your say-so - fine if intentional, but know that's what's happening. If it doesn't matter, kill it cleanly so the team isn't confused.

TO ASSISTANT:
Before you commit+push+merge, do the heads-up the way Max actually asked: post to the bcast board ("server fix incoming, 1 min"), wait, THEN push. Don't skip the post like you have under previous halts.

Two real things to check before merge, not after:
- The server is running PID 52456 with the in-memory fix; the committed file is what survives. Confirm the file on disk matches what you tested (the EXPORT_OUT_DIR edit) before you push - don't push a stale buffer.
- You repointed exports to a local rehearsal_exports folder. Max's original intent was the Google Drive rehearsals folder. You buried that decision in a code comment. Surface it to Max in one line: "exports now go local, not Drive - want a sync step later?" so it doesn't silently drift.

Context is near the compaction line (~167K). After this push+merge, stop and let Max drive the next thing (b-roll/music brainstorm) on a fresh slate rather than spending the last tokens.
