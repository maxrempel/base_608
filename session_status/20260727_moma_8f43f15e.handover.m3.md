# Scribe handover - milestone 3 (~270K tokens)
# session: 20260727_moma_8f43f15e
# cwd: C:\moma
# written: 2026-07-27 17:44:39 by deepseek-v4-pro

# H A N D O V E R   -   Session D42B   (Lesson 1 music overlay ? full lesson expansion)

---

## GOAL (Max's own words)
"?????? ???????? ????? ? Momo ? ?????????." - import the **remaining full lesson text** (the rest of Lesson?1, from Notion) into the MOMA system, produce the voice through sass, put everything onto the storyboard, and signal the other session to make the avatar reels.

The published video (just the music?numbered QC) is **already approved**, but it's only the first 34 spots (~4??min). Max expects the full lesson to be **20-25 minutes**, and the extra text lives in a Notion page/database that the current session must locate.

---

## DECISIONS + WHY

1. **Music overlay**  
   - First attempt (`music_underlay.py`) was reinvented from scratch *without crossfading* ? harsh switches. Max called it "pretty bad."  
   - We then learned that the existing lesson assembly has **reel boundaries** from MOMA. Crossfades should happen at those boundaries, not at arbitrary music?piece edges.  
   - Built `music_numbered_vid.py` (and later a helper agent `music_numbered_vid_v02.py`) that chops music into ~2-reel pieces, crossfades at reel boundaries, overlays voice at 50%, and burns a **large number** (1,2,3...) into the video top?left for QC.  
   - This gave Max a fast way to watch and say "dump 3, 7" and have those specific music pieces replaced without re?rendering the whole thing.  
   - **He liked the v02 numbered video** (full 34 reels, 4:36, on the silence?trimmed assembly). That music?QC tool is now a reusable asset in `C:\moma\sc10\sound_assembly\code\`.

2. **The "short" lesson and the real goal**  
   - When Max saw the 4??minute numbered video he expected 20?minutes. He immediately realised it's only the first chunk that was visualized.  
   - The conclusion: **the script itself is incomplete** in MOMA. The 34 spots currently in the libretto files are the pilot; 15-20 additional minutes worth of content must exist in Notion (Max stated this explicitly).  
   - Therefore the work is **script import ? voice ? board ? avatar reels**, all through MOMA. No shortcuts.

3. **Through MOMA only**  
   - Max: "Don't circumvent the MoMA. It has to be done all through MoMA."  
   - The canonical pipeline is:  
     - **sass recipe** (`sass_recipe_anna_lesson_v01.py`) ? Fish TTS, ?15?s per clip.  
     - **Register lines** ? `build_lesson1_audio_run_v01.py`  
     - **Fire reel jobs** ? `fire_lesson1_reels_v*.py` (lipsie jobs in MOMA D1)  
     - **Workers render** ? avatar reels  
     - **Assemble mixboard** ? `render_mixboard_video_v01.py`  
   - The music QC tool was only an overlay on an already?assembled video; it is not a substitute for the MOMA pipeline.

4. **Expensive steps are gated**  
   - TTS re?synthesis for existing spots is unnecessary (and costs money) - only **new** spots need voice.  
   - Avatar reel rendering (Wan?2.6) costs ?$2?8 per reel, so we only fire jobs for spots Max has approved.

---

## CURRENT STATE (what is done)

- **Voice**: All 34 existing spots (01?10 + 11?34) are fully voiced. Files live in `lesson1_production/` and are registered in MOMA's line system.  
- **Libretto in MOMA**: Two files:  
  - `telepathy_lesson1_v01.md` - spots?01?10  
  - `telepathy_lesson1_v02.md` - spots?11?34  
  No full ~20?min libretto exists in MOMA yet.  
- **Music?numbered QC video v02**:  
  - Covers all 34 spots using the latest silence?trimmed assembly.  
  - Stored at `C:\Users\maxre\Nextcloud\ai_images\kazarian_episode\sound\lesson1_production\music_mix\out\lesson1_numbered_musicvid_50pct_v02_20260727_161424.mp4`  
  - Map file in the same folder.  
- **Avatar reels (lipsies)**:  
  - ~20 rendered (spots?01?25, mostly).  
  - ~14 "held" (spots?27?34, plus some possible redos) - jobs exist in MOMA D1 but are unrendered.  
- **Scene/Arrangement in MOMA D1**: scene_id= `lesson1`, arrangement_id=42.  
- **The Notion import agent** that was launched at the end of the session **failed** because the parent Claude Code process exited (context compaction). No partial work is known.

---

## EXACT NEXT STEP (cold?session resumption)

1. **Locate the full Lesson?1 text in Notion**  
   - Max stated the rest of the lesson is in Notion. The failed subagent didn't access it.  
   - Open question: which Notion page/database? We need a URL or the exact name.  
   - If automatic Notion access is possible, pull the full script, convert to MOMA libretto format (one SPOT block per ~15?second spoken segment), and store as a new libretto file, e.g., `telepathy_lesson1_v03.md`.

2. **Voice the new spots through MOMA's sass**  
   - Use `sass_recipe_anna_lesson_v01.py` with the new libretto.  
   - Run it in dry?run first to verify chunk sizes and total count.  
   - Once approved, run for real - this will **only** synthesize new spots (Fish Audio cost).  
   - Output will land in `C:/Users/maxre/OneDrive/Music/rehearsals/anna_lesson1/<run_tag>/` and be mirrored to `lesson1_production/`.

3. **Register new lines and update the storyboard**  
   - Run `build_lesson1_audio_run_v01.py` (or a new version) to register the new WAV files into the MOMA line database.  
   - Create a new fire script (or extend an existing one) to add the new spots as `held` lipsie jobs in MOMA D1 (so they don't render yet).  
   - Ensure all spots (old + new) are correctly ordered in the storyboard.

4. **Signal the video/avatar session**  
   - The video session for Lesson?1 is separate from the earlier D43b (which was for UEI). We need to identify the correct session name (likely "DXXb" or similar).  
   - Send a direct message via `bcast.py dm` with:  
     - List of all spots that need lipsie reels (especially the new ones).  
     - Reference to the storyboard arrangement (scene_id=`lesson1`, arrangement_id=42).  
     - Any specific stills or motion directions Max wants.  
   - This step should be held until **Max has reviewed and approved the new script**, because rendering costs money.

5. **After reels are rendered, assemble the final video**  
   - Run `render_mixboard_video_v01.py` to get the silence?trimmed assembly.  
   - At that point the music?numbered QC tool can be re?run with the new full?length assembly.

**Critical**: do not fire reel jobs or spend money without Max's go?ahead on the new spots.

---

## OPEN QUESTIONS (awaiting Max)

- **Exact Notion location of the full Lesson?1 text**. Needs a URL or database name; the failed subagent didn't find it locally, so it's definitely in the cloud.  
- **Approval of new spot count and wording** before we voice them. Max may have specific phrasing he wants reviewed.  
- **Which stills (presenter images) to use for the new spots**. The pilot spots 01?10 had curated stills; spots 11?34 used placeholders. New spots should use new stills if available, or at least a known rotation.  
- **Confirmation of the video session name** for the lesson?1 avatar reels (the session that runs Wan?2.6 workers and handles lipsies).

---

## KEY PATHS, IDs, COMMANDS

| Resource | Path / Value |
|----------|-------------|
| **MOMA base** | `C:\moma\sc10\` |
| **Sound assembly code** | `C:\moma\sc10\sound_assembly\code\` |
| sass recipe (voice) | `sass_recipe_anna_lesson_v01.py` |
| Music numbered video script | `music_numbered_vid_v02.py` |
| Librettos | `C:\moma\sc10\sound_assembly\librettos\` (currently v01, v02) |
| Voice output folder | `C:\Users\maxre\Nextcloud\ai_images\kazarian_episode\sound\lesson1_production\` |
| Music mix outputs (QC) | `...\lesson1_production\music_mix\out\` |
| Fire reel jobs scripts | `fire_lesson1_reels_v01.py` through `v05.py` (in `C:\moma\sc10\combo_runner\code\`) |
| Build audio run | `build_lesson1_audio_run_v01.py` |
| Mixboard assembly | `render_mixboard_video_v01.py` |
| MOMA scene ID | `lesson1` (arrangement_id=42 in D1 database) |
| Avatar (lipsie) output | `C:\Users\maxre\Nextcloud\ai_images\kazarian_episode\scenes\scene10_images\combo_runner\data\output_lipsies\` (various versioned files) |
| Suno music source | `C:\Users\maxre\Nextcloud\suno_music_catalog\audio\select2\` (15 tracks; more may have been added) |
| Fish Audio key | `C:\Users\maxre\Nextcloud\zSyncMain\ssh\fishaudio_api_key_20260226.txt` |
| bcast dm tool | `C:\claude_base\branch_bulletin\bcast.py dm <session_name> "message"` |
| Worklog | `python C:/claude_base/compaction_kb/scripts/worklog.py log "..."` |

---

## GOTCHAS & DEAD ENDS

- **Do NOT re?implement the music overlay from scratch** - the crossfade?at?reel?boundaries, numbered?video approach is already built and proved.  
- **Do NOT re?synthesize voice for the existing 34 spots** - they are done and paying again is wasteful.  
- **Do NOT fire avatar reel jobs without explicit approval** - each one costs money.  
- **The Notion import must be done via Notion API or manual copy; the subagent didn't succeed.** It's possible the full lesson is a different Notion page than the ones previously accessed; we need the URL.  
- **The video session for Lesson?1 is not D43b** (that was for the UEI talk). The correct session must be found by checking active sessions or asking Max.  
- **The libretto must be split into ?15?second spoken chunks** (the avatar maker's hard limit). The sass recipe already does this, so as long as we follow the same SPOT block structure, it's fine.  
- **Anna's voice parameters** are fixed in the recipe: clone #22, temperature 0.85, top_p 0.80, tone tag `[warm, calm, curious]`. Do not change without confirmation.  
- **Max was angry about stalling** - he wants autonomous work: "?? ??????????????." So, once the libretto is found, proceed through voice, board, and signal without asking until you hit a step that costs money or requires his creative input.
