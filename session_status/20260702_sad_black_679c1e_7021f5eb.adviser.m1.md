# Adviser note - milestone 1 (~99K tokens)
# session: 20260702_sad_black_679c1e_7021f5eb
# written: 2026-07-02 14:28:00 by deepseek-v4-pro

TO MAX: The Assistant just burned three stabilization proofs in a row (all identically broken), and now instead of searching for the real cause of a 1-second periodic twitch, they're probing OBS keyframe intervals -- which is the wrong rabbit hole. You already told them to search. They started but veered into file-format forensics instead of the specific symptom. If you want this done, you may need to intervene: tell them to search for "vidstab periodic twitch every 30 frames" or switch tools entirely.

TO ASSISTANT: Stop probing keyframes. GOP spacing has zero effect on vidstab's per-frame transform application. The ~1s twitch at 30fps = a 30-frame period, which is the exact default smoothing window (`smoothing=30`). That's the prime suspect. Search for "vidstab periodic jump every 30 frames" or "vidstab smoothing window boundary artifact." Also: three identical failures means the parameter-tweaking loop is dead -- pivot. Try `deshake` with a different ffmpeg build, or skip vidstab entirely and try a simple `tripod` + `hqdn3d` denoise instead of stabilization. If Max's camera is truly on a tripod, maybe the "shake" is just compression artifacts that don't need stabilizing at all.
