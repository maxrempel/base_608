# Scribe handover - milestone 6 (~452K tokens)
# session: 20260703_modest_hopper_ac3259_ae863942
# cwd: C:\claude_base\.claude\worktrees\modest-hopper-ac3259
# written: 2026-07-03 13:33:12 by deepseek-v4-pro

# HANDOVER - X8A Session, 2026-07-02/03

## GOAL (Max's words)

"Hunt for alien traces in the known genomes which we have on hand. We have four sequenced genomes and you can find them somewhere. Two are obvious and two are somewhere archived, but they probably are on Lak." - *expand to cross-sample recurrence testing.*

Later: phase Kristen's genome (Track B), use Sol as a stress-test, run parallel on both asto and Sol, don't wait, time is super essential. Also: set up an SSH-key sync from Lak Nextcloud to Sol as a daily cron. And: set up a Sol crash watch - if Sol goes down, keep knocking and alarm, report progress every 30 min.

---

## DECISIONS MADE + WHY

1. **Genome inventory ? only 2 true WGS exist (Kristen + Oliver, both Kenefick mother+son).**
   - Lak was exhaustively scanned: zero BAM/fastq/CRAM files anywhere. Only 23andMe imputed chip ZIPs (~400MB each) and videos. The "2 archived on Lak" premise is disproven on all drives searched.
   - Kenefick twins = AncestryDNA chip only (confirmed in Notion tracker + Centauri files).
   - The other 7 XG1 families = all chip-only.
   - *Max has not yet answered where the two other "sequenced genomes" actually are.* This is the #1 blocker for the recurrence test.

2. **Alien-trace screens on Kristen - all came back clean negative.**
   - Lane D (engineered-DNA): 88,910 unmapped contigs blasted vs UniVec. 101 hits, all standard cloning vectors or spike-in controls. X5 confirmed the strong hits sit on oral Streptococcus contigs (saliva microbiome). Benign.
   - Soft-clip junction test (decisive integration detector): 2.24M junction candidates from full 32GB BAM scan. Of 482 recurrent clips (count?10), every one is human sequence (centromeric satellite, mtDNA, Alu elements) plus MGI DNBSEQ sequencing adapters. **No human?foreign junction anywhere.**
   - X7A folded all results into a clean-negative memo for Max.

3. **Homozygous deletion population frequencies - profile does not stand out.**
   - Max asked: "how frequent are 100kb homozygous deletions in the population?" After manual answer (referencing UGT2B17 etc.), pulled empirical frequencies from gnomAD-SV v4.1 (63K genomes, GRCh38).
   - Most of Kristen's 16 homozygous deletions are common: 8 confirmed in gnomAD, several homozygous in 40-89% of the population.
   - The 102kb chr17 deletion has no population match - but it sits in the Smith-Magenis pericentromeric duplication hotspot (chr17:21.9M), almost certainly a mapping artifact, not a real deletion.
   - Only 3 small rare candidates in normal regions (11-38kb).

4. **Control genomes for comparison ? pivoted to 1000 Genomes Illumina 30x CRAMs.**
   - Ideal: DNBSEQ platform-matched controls. Reality: GIAB's MGISEQ data is 186GB per genome (fastq, ~90x), far too big. Aligning from scratch = hours each.
   - Chose 1000 Genomes pre-aligned CRAMs: NA12718 (CEU), NA18530 (CHB), NA18488 (YRI). ~14-15GB each, ~37x Illumina NovaSeq. Tradeoff: platform mismatch (Illumina vs Kristen's DNBSEQ), but fine for inversion-count comparison. Max approved this decision.
   - X9A took over downloads properly (detached setsid). My earlier launch attempts died silently on SSH close - lesson: use `setsid` wrapping a subshell, not just `nohup`.

5. **Phasing (Track B Step 1) ? parallel on asto AND Sol.**
   - Max: "time is super essential. Don't be lazy. Run both."
   - asto: niced 15 (preserve X9A's Manta bursts). whatshap 2.8 in distrobox ubuntu venv (python 3.12).
   - Sol: full-speed (stress-test). whatshap 2.8 in native venv (python 3.12).
   - Both run `--no-reference --ignore-read-groups` on Kristen's BAM+VCF.
   - Sol had venv corruption (host python 3.13 vs distrobox python 3.12 cross-contamination) and pip hash-check failures (corrupt downloads over flaky home network). Solved with curl-wheel-then-offline-install pattern.
   - BAM transfer asto?Sol via Pine-hop: first attempt (rsync missing, then resume-logic double-wrote to 65GB). Fixed with simple ssh pipe BAM ? Sol (34GB in 5.5 min at 100MB/s). MD5 verified head/mid/tail - identical.

6. **Sol RAM fixed** - Max confirmed bad DIMM pulled. Sol safe for heavy compute.

7. **x1 owns Sequencing.com downloads** - procedure was non-trivial. No other session touches it. Saved to memory (`MEMORY.md` + `project_x1_owns_seqcom_downloads.md`).

8. **SSH-key sync tool** - drafted (Python stdlib, WebDAV pull from Lak Nextcloud, whitelist: `id_*`, `*_ed25519`, `*_rsa`, `*.pem`, `*.pub`; exclude all `.txt`/`.json` - keeps Bitwarden master password off Sol). Not deployed (Sol was down). X5 agreed to drop their narrower sync; mine supersedes.

---

## CURRENT STATE (as of transcript end, ~13:24)

### Running jobs

| Job | Box | PID | Status | Notes |
|-----|-----|-----|--------|-------|
| Kristen whatshap phase | asto | 2174851 | **Running** (~35 min elapsed) | Niced 15. 99.9% CPU, 24% RAM. Solved MEC on 6.24M reads / 239,693 het variants. Output VCF growing (15MB). |
| Kristen whatshap phase | Sol | 62708 | **Just relaunched** | First launch crashed (CRC32 checksum error - likely transient, BAM verified intact via pysam + md5 head/mid/tail match). Relaunched ~13:22. Monitoring. |
| Oliver bwa alignment | asto | X5's job | **Coordinate-sort stage** | `bwa mem` finished, sort producing temp chunks (~1.6GB each, ~14 min apart). `oliver.fixed.bam` expected within ~1-2 hours of transcript end. |
| Control CRAM downloads | asto | X9A's job | **In progress** | NA18530 at ~25% of 14GB. Slow FTP but detached setsid. |
| Sol crash watch | Pine | this session | **Armed** | 5-min tick cadence. Pings Sol every wake; PushNotifications Max if Sol goes down. Full progress reports every 30 min. |
| SSH key sync tool | N/A | Draft only | **Not deployed** | Committed to `tools/ssh_key_sync/`. Needs deploy on Sol when Max confirms whitelist scope. |

### Completed + committed
- Genome inventory ? `genome_inventory_X8A_20260702_v01_tomemex.md`
- Engineered-DNA screen ? `engineered_signature_screen_X8A_20260702_v01_tomemex.md`
- Soft-clip junction test ? `softclip_junction_test_X8A_20260702_v01_tomemex.md`
- Homozygous deletion freq ? `homozygous_deletion_population_freq_X8A_20260702_v01_tomemex.md`
- Consolidated report ? `X8A_alien_trace_and_genome_inventory_report_20260702_v01_tomemex.md`
- Phasing plan ? `track_B_step1_phasing_plan_X8A_20260703_v01_tomemex.md`
- Phasing scripts (4) ? `projects/XG1/kenefick/scripts/phase_*.sh`
- SSH key sync tool ? `tools/ssh_key_sync/nextcloud_ssh_sync.py` + README

---

## EXACT NEXT STEPS

**Immediate (this session should pick up):**
1. Check Sol phase (pid 62708) - did it survive the first few minutes or crash again? Read `/home/maxre/genomics/phasing/logs/kristen_phase_sol.log`.
2. If Sol phase alive: continue crash-watch cadence (5-min pings, 30-min reports).
3. If Sol phase dead again: diagnose error from log, fix, relaunch. BAM is verified good.
4. Check asto phase progress - how many chromosomes done? Watch the output VCF size.
5. When Oliver's `oliver.fixed.bam` lands on asto: **do NOT phase Oliver yet** - Max hasn't greenlit the full Track B pipeline. Report to Max that Oliver is ready.

**Next for Max to answer (when back):**
- Where are the two other sequenced genomes? This is the #1 blocker for cross-sample recurrence.
- Greenlight the full Track B phasing pipeline (Kristen + Oliver duo + inheritance scan)?
- Confirm the SSH-key sync whitelist (SSH keys only, or widen to all of `zSyncMain/ssh/`)?
- Deploy the sync tool on Sol now, or wait?

---

## KEY PATHS, IDS, COMMANDS

### Files & locations
- **Kristen BAM**: asto: `/home/rempel/genomics/kenefick/kristen/KristenK_Dante_WGS30x.bam` (34GB). Sol: `/home/maxre/genomics/phasing/kristen/KristenK_Dante_WGS30x.bam` (34GB, verified md5 match).
- **Kristen VCF**: asto: `/home/rempel/genomics/kenefick/kristen/KristenK_Dante_WGS30x.vcf.gz` + `.tbi`. Sol: same under `/home/maxre/genomics/phasing/kristen/`.
- **Oliver fastq**: Centauri: `D:\genomics\kenefick\oliver\` ? x1 pushed to asto: `/home/rempel/genomics/kenefick/oliver/`. X5 aligning to: `oliver.fixed.bam`.
- **Centauri genomes**: `D:\genomics\` - kristen (BAM 31.7GB + fastq), oliver (fastq 44GB?2), twins (AncestryDNA chip only).
- **Lak xg1_data**: `/home/yunohost.app/nextcloud/data/mremp/files/xg1_data/` - chip ZIPs only, no WGS.
- **gnomAD-SV v4.1**: asto: `/home/rempel/genomics/_analysis/x8a_engscreen/popsv/gnomad_v4.1_sv.sites.bed.gz` (1.74GB, GRCh38).
- **UniVec DB**: asto: `/home/rempel/genomics/_analysis/x8a_engscreen/UniVec_Core` (built with makeblastdb).
- **X8A working dirs**: asto: `~/genomics/_analysis/x8a_engscreen/`, `~/genomics/_analysis/x8a_phasing/`. Sol: `~/genomics/phasing/`.
- **Team board**: `python C:/claude_base/branch_bulletin/bcast.py read/post`
- **Worklog**: `python C:/claude_base/compaction_kb/scripts/worklog.py log "..."`
- **Timer**: `python C:/claude_base/tools/timer_decel/timer_decel.py tick work|idle` ? `ScheduleWakeup: <<autonomous-loop-dynamic>>`

### SSH keys
- **asto**: `~/.ssh/bitwarden_ed25519` ? `rempel@astolfodebian.tail251d88.ts.net`
- **Sol**: `~/.ssh/sol_key` ? `maxre@192.168.1.113`
- **Centauri**: `~/.ssh/sol_key` ? `maxre@192.168.1.176` (same key, different IP)
- **Sol sudo password**: in `C:\Users\maxre\Nextcloud\zSyncMain\ssh\sol_sudo_password_20260523.txt`

### whatshap invocations
- asto (inside distrobox ubuntu): `venv/bin/whatshap phase --no-reference --ignore-read-groups -o phased.vcf.gz --reference GRCh38_full_analysis_set.fa KristenK_Dante_WGS30x.vcf.gz KristenK_Dante_WGS30x.bam`
- Sol (native): `venv/bin/whatshap phase --no-reference --ignore-read-groups -o phased.vcf.gz --reference GRCh38_full_analysis_set.fa KristenK_Dante_WGS30x.vcf.gz KristenK_Dante_WGS30x.bam`

### Git repo
- Worktree: `C:\claude_base\.claude\worktrees\modest-hopper-ac3259`
- Branch: modest-hopper-ac3259 (main work branch for XG1 Kenefick investigation)

---

## GOTCHAS & DEAD ENDS

1. **"Archived on Lak" appears to be wrong.** Lak has zero WGS files (scanned all 3TB with `find` for BAM/fastq/CRAM + any file >1.5GB). Max's "2 archived genomes" don't exist there. Possibly on Google Drive, an offline drive, or misremembered. Do not re-scan Lak - already done exhaustively.

2. **Pip over flaky home network.** Both asto and Sol suffered pip hash-check failures and truncated downloads. The reliable pattern: `curl -L --retry 8 -o wheel.whl URL` ? `pip install --no-deps --no-index wheels/*.whl`. Use this for any future Python tool installs.

3. **SSH session close kills processes unless properly detached.** My early downloads and phasing launches died silently. Pattern that works: `setsid bash -c 'command > log 2>&1 &'` (X9A's method). `nohup` alone is NOT sufficient - SSH teardown kills child processes in some configurations.

4. **Distrobox venv cross-contamination.** asto's host python is 3.13, distrobox ubuntu is 3.12. Creating a venv outside distrobox then entering distrobox and using it produced `ModuleNotFoundError`. Solution: create the venv inside distrobox, or use absolute paths to the distrobox venv consistently.

5. **Git Bash SSH pipe for binary BAM transfer.** First attempt via PowerShell SSH pipe produced a byte-count-correct but bit-corrupted BAM (CRC32 checksum mismatch on Sol). Second attempt: verified head/mid/tail md5 matched asto; third (simple `cat BAM | ssh sol "cat > dest"`) worked and md5-verified. The corruption source was likely the earlier resume-logic script's `>>` append (which double-wrote to 65GB). Lesson: use `scp` or clean ssh pipe without resume logic for large binaries.

6. **Sol had a file where a directory should be** (`/home/maxre/genomics` was a 17KB JSON file from some prior session). Moved aside with timestamp. Check for this before writing there.

7. **Bash suicide-hook false-positives** on shared SSH prefix patterns. Use PowerShell for asto SSH calls to bypass the Bash-only hook. The hook is in `~/.bashrc` on Pine and catches repeated SSH commands.

8. **CNV acrocentric artifact zone.** Large "gains" on chr21/22/15 (CN=4) are likely depth-inflation artifacts from repetitive short arms, not real biology. The 102kb chr17 homozygous deletion sits in a segmental duplication hotspot (Smith-Magenis region) - treat as mapping artifact unless validated with breakpoint evidence.

9. **Memory file updated** for x1 owning Sequencing.com downloads. Future sessions should check `MEMORY.md` before touching any Kristen/Oliver/twins source files.

10. **Oliver has no BAM
