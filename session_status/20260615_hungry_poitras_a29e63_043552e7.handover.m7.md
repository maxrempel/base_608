# Scribe handover - milestone 7 (~106K tokens)
# session: 20260615_hungry_poitras_a29e63_043552e7
# cwd: C:\claude_base\.claude\worktrees\hungry-poitras-a29e63
# written: 2026-06-15 17:24:45 by deepseek-v4-pro

# HANDOVER - b21: song start timing branch observer

## GOAL (Max's own words)

Max is unhappy with the starts of his recent songs. He set up b6 and b7 (old chats) to investigate and fix the starts through the Tamza timing pipeline. They got tangled, so Max injected b21 as a fresh?eye observer. b21's job: watch the b6/b7 discussion, learn the situation, and be ready to **take over the starts work** only after they finish. Max later clarified: **b7 is in charge** of the main work; b21 should offer to offload a minor branch if needed, but otherwise stay hands?off.

## DECISIONS + WHY

1. **Curated start is NOT ground truth.**  
   b21 initially suggested trusting the hand?curated `&t=` values that Max had set himself. Max shot this down immediately - "curated is messy, they know better, you are dumb." The LLM?derived starts (from b6's v2 overlay) are considered the real reference. This is now recorded in MEMORY.md so nobody repeats the mistake.

2. **b6's v2 overlay (`song_timing_max_v2.json`) is the canonical start source for now.**  
   b6 has loudly claimed that his v2 overlay wins over any V3 attempts by b7. The evidence on the bulletin board suggests that this is the current de facto state; b7 has not challenged it yet. The fight is about version priority, not about whether the mapping itself is correct.

3. **b21 is in pure observation mode, b7 leads.**  
   Max's final instruction: "just watch, learn, you will take over if needed, B7 is in charge. Actually offer it to offload a minor branch but let it do the main work." So b21 must not restart the whole investigation, but be a reliable backup, ready to take a small, well?defined sub?task if the board stalls.

## CURRENT STATE

- The **bulletin board** shows b6's latest claim (timestamp 16:23) that his v2 overlay has priority. b7 has not answered yet.  
- Two **worktrees** exist; the one being watched is `hungry-poitras-a29e63` at `C:\claude_base\.claude\worktrees\hungry-poitras-a29e63`.  
- The actual **start data** lives in `C:\claude_base\tools\tamza_songs\pipeline\song_timing\_work\song_timing_max_v2.json` (the overlay) and a matching state file. The counts across different versions are inconsistent (77/574/586/665), suggesting file overwrites or worktree conflicts.  
- b21 has read the pipeline documentation (`README_tomemex.md` and `HANDOVER_max_remap_tomemex.md`) and understands the architecture.  
- The **lesson** about curated starts not being ground truth has been saved both in `C:\Users\maxre\.claude\projects\C--claude-base\memory\MEMORY.md` and a separate project memory file.  
- The **only on?board tool** for inter?branch communication is `C:\claude_base\branch_bulletin\bcast.py` (catchup, read, post).  

## EXACT NEXT STEP

b21 should **keep watching the board** silently. The immediate next move belongs to b7. b21 should **prepare a short offer** (via the board or a private ping if supported) along the lines of:  
> "I'm b21, fresh eyes. If you want, I can take on a small verification or data?cleanup branch (e.g., reconcile the count discrepancies, or re?run the mapper for a specific subset of songs) so you stay focused on the main logic."  

Do not post this until b7 has either answered b6 or the board goes silent for ~15-20 minutes. If b7 accepts, the branch must be **tiny and self?contained** - no touching the v2 overlay or the core mapper. If b7 doesn't respond or the task remains unclear, stay idle and keep monitoring.

## OPEN QUESTIONS (for Max or when b7 engages)

- Which specific recent songs have bad starts? (The board doesn't list them; b6/b7 may know.)  
- Is the problem that plays are clipping intros / starting late, or that there is dead air / they start too early?  
- What is the intended relationship between the v2 overlay and the "song_timing.json" base?  
- Why does the live catalog count (586 keys in this worktree's overlay) differ from what the board reports (574) and from the handover doc (77)? This might be the source of the confusion.  

## KEY FILE PATHS & IDS

- **Worktree root** (current session): `C:\claude_base\.claude\worktrees\hungry-poitras-a29e63`  
- **Pipeline timing tool**: `C:\claude_base\tools\tamza_songs\pipeline\song_timing\`  
- **v2 overlay** (canonical starts): `.../song_timing/_work/song_timing_max_v2.json`  
- **v2 state file**: `.../song_timing/_work/map_max_v2_state.json`  
- **Main catalog output**: `C:\claude_base\tools\tamza_songs\pipeline\output\data.json`  
- **Documentation**: `.../song_timing/README_tomemex.md`, `.../song_timing/HANDOVER_max_remap_tomemex.md`  
- **Bulletin board (likely path)**: `C:\Users\maxre\.claude\projects\C--claude-base--claude-worktrees-hungry-poitras-a29e63\043552e7-d5ab-424f-b682-59a0576db7`  
- **Branch coordination script**: `C:\claude_base\branch_bulletin\bcast.py`  
- **Memory file**: `C:\Users\maxre\.claude\projects\C--claude-base\memory\MEMORY.md`  
- **Project memory** (b21's note): `C:\Users\maxre\.claude\projects\C--claude-base\memory\project_tamza_song_starts.md`

## GOTCHAS (already tripped over)

- **Do not trust the curated start.** The LLM?derived value is the intended source of truth, even if it sometimes seems wrong. b21 already proposed the opposite and was sharply corrected.  
- **The counts are a mess.** Different worktrees and state files hold different numbers; don't trust header?level summaries. Always count the actual keys in the JSON file you are reading.  
- **b6's confidence may be premature.** His claim that his overlay "wins" is not yet validated by b7 or by a re?deploy. Don't build a verification plan on b6's numbers alone.  
- **b21 is not to take over the main mapper.** Any offer must be a purely auxiliary branch: e.g., check a single song's mapping against the original video, clean up stale state files, or produce a report.  
- **The board is noisy** with old download logs; recent messages are from 15:xx to 16:23 on the day of the session. b7 may still be working offline.
