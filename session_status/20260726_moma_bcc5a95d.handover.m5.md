# Scribe handover - milestone 5 (~399K tokens)
# session: 20260726_moma_bcc5a95d
# cwd: C:\moma
# written: 2026-07-26 14:01:38 by deepseek-v4-pro

## HANDOVER

### GOAL (in Max's words)
"Proceed with better implementation of the video making. Produce a document with instructions how to make the video and then let's migrate to MoMA and read the whole MoMA thing, so integrate it with MoMA."

The overall mission: Taygeta (the new RTX 5060 Ti box) does **local, free** image+sound+prompt ? lip-synced video renders inside Max's existing MoMA movie pipeline. He wants a proper MoMA integration, not one-off scripts. Everything should happen autonomously - "you work independently."

---

### DECISIONS MADE AND WHY

1. **Local video model = Wan 2.2 S2V (Speech-to-Video)**
   - This is the only downloadable model that accepts image + audio + prompt and outputs a talking video with lip sync. Verified via official ComfyUI docs. The cloud-only Wan 2.6-flash is the current MoMA engine; Wan 2.2 S2V is the local equivalent.
   - Fits in 16 GB VRAM using FP8 weights. First test render succeeded (3 seconds, 480?480, lip-synced audio).

2. **ComfyUI headless runs on Taygeta, listening on 127.0.0.1:8188**
   - Stable PyTorch 2.11.0+cu128 (not nightly - nightly caused dependency hell).
   - Custom nodes: WanVideoWrapper, KJNodes, VideoHelperSuite, ComfyUI-Manager.
   - Models living in `~/ComfyUI/models/{diffusion_models,text_encoders,audio_encoders,vae}`.

3. **MoMA integration design: worker lives on Pine, Taygeta is a dumb renderer**
   - The new worker (`combo_s2v_local_worker.py`) will run on Pine alongside all other MoMA workers, fetch its own job type from D1, push the still + voice clip to Taygeta over SSH, trigger the ComfyUI API, and pull the finished mp4 back into MoMA's standard `OUTPUT_LIPSIES` folder.
   - Why not run the worker on Taygeta? Taygeta isn't synced to Nextcloud and doesn't have the rest of the MoMA plumbing (paths, DB access, restart watchdog). Pine already has everything. This keeps MoMA whole and unchanged.

4. **Resource access: SSH tunnel from Pine to ComfyUI on Taygeta**
   - Taygeta's ComfyUI only listens on localhost. The worker will establish an SSH tunnel to forward a local port on Pine to Taygeta's 127.0.0.1:8188. Nothing exposed on the LAN; uses the existing `sol_key` for SSH.
   - This avoids opening any ports on Taygeta's firewall.

5. **Worker follows existing cloud lip-sync pattern**
   - Will use the same `lipsync_tool`-based job claiming (`lipsync_tool='s2vlocal'`), so it doesn't collide with the cloud worker (`wan26flau`). Jobs will be enqueued via `fire_job`, never ad?hoc scripts.
   - Output file naming and D1 status updates will mirror `combo_wan26au_worker.py`.

6. **Docker containers for Mike & Liz: design locked but not built**
   - Two separate GPU?enabled LXC/Docker containers, each with `--cpus`/`--memory` ceilings (~33% of host, release when idle), SSH key?only access, reachable via Tailscale when Max authorises the pending auth link.
   - Because the GPU can't be hard?partitioned, they will share it cooperatively (Max was told). No VRAM carve?out.

7. **24 TB external drive: green24, exFAT**
   - Reformatted from factory exFAT (no data lost) to exFAT for Windows/Unix portability. Mounted at `/mnt/green24`, auto?mounts at boot, 22 TB free. Registered in all fleet registries.

8. **Sol's health?check monitors killed**
   - Repurposed Sol's `sol-host` Heartbeats.io check into `taygeta-host` (pings every 5 min, Telegram+email alarms). Deleted `sol-cpu-temp` and `sol-notion-task-sync`. Free tier cap lifted from 20 to 18.

---

### CURRENT STATE (what is done, what is in flight)

**Done:**
- Taygeta installed Ubuntu 24.04, secured (key?only SSH, LAN?only firewall, reboot?safe).
- RTX 5060 Ti recognised, NVIDIA driver 595, CUDA 13.2.
- Genomics: Python venv with pysam, whatshap, samtools, bcftools, bwa - verified working.
- Wan 2.2 S2V pipeline **end?to?end proven** - first render (s2v_test_00001_.mp4) saved to `/mnt/green24/s2v_tests/` and sent to Max.
- Video how?to doc written: `C:\claude_base\tools\taygeta_s2v\s2v_video_method_v01_tomemex.md`.
- MoMA orientation read and exploration agent (a252ca432809ea8b4) produced a full integration blueprint.
- Drive green24 (24TB, exFAT) mounted and registered.
- Heartbeat monitor live for taygeta.
- Terminology: `tay` = `taygeta` in user dictionary.

**In flight:**
- **Building `combo_s2v_local_worker.py`** on Pine, using the blueprint. This is where the session left off. The agent's output was read, design decision made, implementation started but not written to disk yet.

**Pending (blocked/awaiting):**
- **Docker containers for Mike & Liz** - blocked on Max clicking the Tailscale auth link (https://login.tailscale.com/a/16b7e2f018827). Tailscale is installed on Taygeta but not yet joined to his tailnet.
- Optional: render a longer/higher?res S2V test using 3 chained sections for 15 seconds.

---

### EXACT NEXT STEP
Implement the new MoMA worker `combo_s2v_local_worker.py` on Pine. This worker must:
- Read D1 for jobs with `lipsync_tool='s2vlocal'` and status needs?render.
- Establish an SSH tunnel to Taygeta's ComfyUI (port 8188).
- Download the source still image and generated voice clip from MoMA's working directories to a temp folder on Taygeta.
- POST the proven S2V API graph (UNETLoader?CLIPLoader?...?SaveVideo) to the forwarded ComfyUI endpoint.
- Poll the render progress until done, then copy the output mp4 back to the correct `OUTPUT_LIPSIES` path.
- Update D1 with the output path and mark the job complete, using the same status conventions as `combo_wan26au_worker.py`.
- Include the standard singleton PID lock, heartbeat pings, and job?claim guard to avoid colliding with the cloud lipsync worker.

**File to create:** `C:\moma\sc10\combo_runner\code\combo_s2v_local_worker.py`

**Start by:** reading the cloud cloud?worker template (`combo_wan26au_worker.py`) in full, then copying its structure and replacing the cloud API call with the ComfyUI SSH?driven logic.

---

### OPEN QUESTIONS (no answer from Max needed to proceed)
- Tailscale auth: Max must eventually click the login link. The Docker containers cannot be built until then, but that is a separate task. The S2V integration does not depend on Tailscale.
- Mike & Liz: Do they have existing SSH public keys, or should the worker generate keypairs and hand them to Max? (Earlier, the plan was to generate them if Max doesn't answer.)

---

### KEY PATHS, IDS, COMMANDS

| What | Path / ID |
|------|-----------|
| MoMA code root | `C:\moma\sc10\combo_runner\code\` |
| Existing cloud lip-sync worker | `combo_wan26au_worker.py` (tag `wan26flau`) |
| New local worker to create | `combo_s2v_local_worker.py` (tag `s2vlocal`) |
| MoMA DB | Cloudflare D1, accessed via `moma_db.py` using `D1Client`. API token at `{NEXTCLOUD_ROOT}\zSyncMain\ssh\moma_d1_auth_*.txt`. Never use the stale `combo_db.sqlite`. |
| Taygeta SSH | `ssh -i ~/.ssh/sol_key maxre@192.168.1.142` |
| Taygeta ComfyUI (internal) | `http://127.0.0.1:8188` |
| S2V how?to doc | `C:\claude_base\tools\taygeta_s2v\s2v_video_method_v01_tomemex.md` |
| Output folder on Taygeta for finished renders | `/mnt/green24/s2v_tests/` (will be read back by the worker) |
| Taygeta machine memory | `C:\Users\maxre\.claude\projects\C--claude-base\memory\project_tageta_machine.md` |
| Fleet registries | drives_registry.md, user_dictionary_tomemex.md, global2.md - all synced in Nextcloud |
| Heartbeats.io check for taygeta | UUID `023cf3f6-186a-4af3-...` (repurposed from sol-host) |
| Tailscale auth URL (pending) | `https://login.tailscale.com/a/16b7e2f018827` |
| Worklog | `C:/claude_base/compaction_kb/scripts/worklog.py log "..."` |

---

### GOTCHAS AND DEAD ENDS ALREADY RULED OUT
- **DO NOT** treat `combo_db.sqlite` as the live DB. It is stale. Everything goes through `moma_db.py` ? Cloudflare D1.
- **DO NOT** write one?off fire scripts. Enqueue jobs via `fire_job` with the correct `lipsync_tool='s2vlocal'`.
- **DO NOT** open ComfyUI to the network. Keep it on 127.0.0.1 and use an SSH tunnel from the worker.
- The first test render used hardcoded `format="mp4"` and `codec="h264"` because the API enum picker misread a combo field. The production worker must use the same hardcoded values (they are the correct choices).
- Wan 2.2 S2V requires frame counts of `4n+1` at 16 fps (e.g., 49 frames for ~3s). For 15s, the doc describes chaining 3 sections with `WanSoundImageToVideoExtend`. The worker should start with short 3?second renders first; 15s chaining later.
- The worker **must not** collide with the cloud lipsync worker. Use a distinct `lipsync_tool` value and a job?claim filter that excludes `wan26flau` jobs.
- Max expects autonomy. After building the worker, test it by firing a sample job from the DB and confirming the pipeline from enqueue to finished clip in `OUTPUT_LIPSIES`. Report back only with results (or if truly stuck).
- The genomics venv and Docker containers are separate workstreams, not to be tangled into the S2V integration.## HANDOVER - Taygeta S2V ? MoMA Integration

### GOAL (in Max's words)
From final message and earlier notes:

> "Produce a document with instructions how to make the video and then let's migrate to MoMA and read the whole MoMA thing, so integrate it with MoMA."

The objective is to add a **free, local lip-sync lane** to the MoMA movie pipeline, running on taygeta's RTX 5060 Ti, so that Max can fire "image + audio + prompt ? talking video" jobs just like he does with the cloud (Wan 2.6?flash) but with **no per?clip cost**. The implementation must be a proper MoMA DB?driven worker, not a one?off script.

All other setup (OS, GPU, genomics, heartbeat, green24 drive) is complete. The only open thread is this integration.

---

### DECISIONS MADE AND WHY

1. **Local video engine: Wan 2.2 S2V**  
   *Why:* Research confirmed it's the only downloadable model that accepts image + audio + prompt and produces lip?synced video. The cloud?only Wan 2.6?flash is already MoMA's primary engine; S2V is the local equivalent. FP8 build fits taygeta's 16 GB VRAM (proven in test render).

2. **Worker placement: on Pine, not on taygeta**  
   *Why:* Pine holds the whole MoMA codebase, Nextcloud sync, DB access, and the worker?watchdog infrastructure. Taygeta is a headless render appliance. The new worker (`combo_s2v_local_worker.py`) will live on Pine, reach taygeta over SSH, push inputs, drive ComfyUI, and pull the finished clip back into the normal `OUTPUT_LIPSIES` folder. This keeps MoMA unchanged and avoids duplicating plumbing.

3. **ComfyUI stays 127.0.0.1?only; worker uses SSH tunnel**  
   *Why:* No open ports on taygeta's LAN. The worker will create a temporary SSH tunnel forwarding a local Pine port to taygeta's `127.0.0.1:8188`. Safe, transparent, and nothing to leave exposed.

4. **Worker follows existing cloud pattern**  
   *Why:* The cloud worker (`combo_wan26au_worker.py`, tag `wan26flau`) already implements job claiming from D1, status lifecycle, heartbeat, and output placement. The new worker will mirror it exactly but use its own `lipsync_tool='s2vlocal'` to avoid collisions. Jobs will be enqueued via `fire_job`, never ad?hoc.

5. **Test render parameters**  
   *Why:* The first render proved the pipeline with short clips (49 frames ? 3s, mp4/h264). For 15s, the doc describes chaining 3 sections with `WanSoundImageToVideoExtend` - that will be built later. Start with a solid 3s baseline.

---

### CURRENT STATE

**Done and verified:**
- Taygeta OS, firewall, SSH key?only, reboots fine.
- GPU driver, CUDA 13.2, all genomics tools installed.
- **Wan 2.2 S2V pipeline** - end?to?end proven. First render saved to `/mnt/green24/s2v_tests/s2v_test_00001_.mp4` and sent to Max.
- Video how?to doc written: `C:\claude_base\tools\taygeta_s2v\s2v_video_method_v01_tomemex.md`.
- MoMA orientation read (`C:\moma\memos\moma_orientation_tomemex.md`).
- Exploration agent returned a full blueprint mapping job claiming, DB interaction, and output paths.
- Heartbeat monitor (Healthchecks.io) repurposed from dead Sol check.
- Green24 24TB exFAT drive mounted and registered.

**In flight (where the session left off):**
- Building `combo_s2v_local_worker.py` on Pine. The structure and exact API calls are understood from the cloud worker template; implementation had just started when the context ran out. **The next cold session must finish writing that worker and test it.**

**Blocked (not part of this integration, separate track):**
- Docker containers for Mike & Liz - waiting on Max to click the Tailscale auth link: `https://login.tailscale.com/a/16b7e2f018827`. Do not build them until that's done.

---

### EXACT NEXT STEP (for the cold session)

**Create the worker file:** `C:\
