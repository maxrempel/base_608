# Adviser note - milestone 7 (~527K tokens)
# session: 20260713_kind_carson_2cc207_f8e0807c
# written: 2026-07-13 14:14:39 by deepseek-v4-pro

TO MAX: The site build is solid -- native signup forms, merged database, email alerts via Centauri. Worth knowing: the email alerts depend on a Windows scheduled task on Centauri. If Centauri is ever offline or rebooted without auto-login, you stop getting signup email alerts. It's a polling design (every 5 min), not instant. There's also a minor state-file risk -- the notifier tracks progress in `starseed_signup_notifier_state.json` on Centauri. If it gets corrupted or clobbered by zSyncMain sync conflicts, you'd get duped or missed alerts. The feed endpoint itself is secured with a random key, which is fine.

TO ASSISTANT: The cache-retry loops (curl 3-4 times after every deploy, every single page) are the main inflation driver -- 527K tokens for what should be maybe half that. When you deploy a Cloudflare worker, the edge takes 30-90 seconds to propagate. Instead of immediate retry loops, wait 90 seconds once, then verify all pages in one sweep. Second: when you transferred the notifier script to Centauri, the background task silently failed -- you caught it, but consider `scp` with `-T` or using the OpenSSH `scp` protocol flag to bypass the banner issue. Third: you left the state file's location ambiguous in the code -- document explicitly where `STATE_FILE` resolves to (currently Nextcloud-synced path, which is fine but fragile -- a sync conflict there would silently suppress alerts). Add a .gitignore entry or move it to a non-synced location.

Overall clean work. Good database hygiene. Good recovery from the banner/scp issue. The architecture (feed endpoint + polling notifier) is appropriate for the constraint of no static API token.
