# Scribe handover - milestone 2 (~167K tokens)
# session: 20260726_awesome_swirles_97603c_44272c02
# cwd: C:\moma\.claude\worktrees\awesome-swirles-97603c
# written: 2026-07-26 14:55:36 by deepseek-v4-pro

# Handover: Storyboard Arrangement Selection Fix

## GOAL (Max's words)
"Make sure to use cheap models to do hard work. So the task for you is to fix the arrangement selection in storyboard. We just created a new arrangement and a new scene, but selection of the scene doesn't actually update the storyboard. Test it end to end and fix it. The selection of the scene should propagate through the whole MoMA and update all the tabs and storyboard should also be updated to the new scene. So far we have only one clip on the storyboard and everything else, I mean one clip is produced, everything else belongs to the other scene. So, it's just the wrong scene is shown. Please fix. Again, I remind you to use cheap models for hard work, for dirty work."

## DECISIONS + WHY
1. **Investigation focused on `storyboard_editor_v2.html` (the live storyboard) rather than v3.** The server (`slideshow_server_v01.py`) serves v2 on `/storyboard`, `/storyboard2`, etc. v3 is only at `/storyboard3`. Confirmed via direct file reads of server routes.
2. **Root cause narrowed to two candidates, possibly both in play:**
   - **Cause A: `ST.sceneRank` stays pinned to hardcoded default 10.** Handler fallback `d.scene_rank ? +d.scene_rank : ST.sceneRank` never advances if picker event detail lacks `scene_rank`. Then the board shows scene 10 instead of scene 11.
   - **Cause B: `ST.arrIds` includes ALL arrangements for the selected scene, so arrangement-specific filtering is absent.** Even if `sceneRank` is correct (11), `imgInScene()` passes any arrangement belonging to scene 11, including arrangement 20's 120 images, not just arrangement 42's single clip.
3. **The pile source endpoint `/api/approved_images` returns EVERY non-error job unfiltered.** Client-side filtering via `renderPile()` applies `imgInScene()`, but that only scopes to scene, not arrangement.
4. **A scoped replacement endpoint `/api/sb_pool` already exists** (built for v3) and accepts an optional `arrangement_id` param. This is the "elegant" fix path per project rules.
5. **Fix strategy (tentative):** first resolve the live state (which cause is active), then implement a fix that both ensures `ST.sceneRank` updates correctly and narrows the pile to the chosen arrangement (or scene, depending on `current_view`). Migrate to `/api/sb_pool` for server-side scoping.
6. **Cheap models:** Previous subagents (Explore for code map, grunt for D1 queries) were used; future dirty work can be delegated similarly.

## CURRENT STATE
- **Live servers:** `combo_gui.py` on 8779 (up), `slideshow_server_v01.py` on 8790 (up). `prompter_server.py` on 8791 not running.
- **Live D1 state:** `current_arrangement_id` = 42 (arrangement "lesson1", scene 11), `current_view` = 'scene'. Arrangement 42 has 22 images + 10 lipsies; scene 11 also has arrangement 20 with 120 images. Scene 10 has arrangement 41 with 43 images.
- **Last action:** curl to `http://localhost:8790/api/app_state/current_arrangement` - the output was not shown due to context thrashing (session aborted after compact). Need to re-fetch and examine the payload for `scene_rank` presence.
- **Relevant code files (all read, no edits):**
  - `sc10/sound_assembly/code/storyboard_editor_v2.html` - live UI. `ST.sceneRank` default 10, handler fallback, `imgInScene()` uses `ST.arrIds` (all scene arrangements), `loadAll()` fetches `/api/approved_images` unscoped.
  - `sc10/sound_assembly/code/slideshow_server_v01.py` - server. `/api/approved_images` unscoped SQL; `/api/sb_pool` scoped replacement (line ~1200); route mapping at lines 666-673 confirms v2 is live.
  - `sc10/combo_runner/code/arrangement_picker.js` - dispatches `arrangementchanged` with detail `{id, ids, is_scene, scene_id, scene_name, scene_rank}`.
  - `sc10/combo_runner/code/app_state.py` - reads/writes `current_arrangement_id`/`current_view`.
- **Working tree clean,** on branch `claude1/sad-dhawan-8a49fc` at commit `e480e83`.

## EXACT NEXT STEP
1. **Probe live state** to disambiguate the active cause:
   - `curl -s "http://localhost:8790/api/app_state/current_arrangement"` - verify that `scene_rank` is present (it should be, from `arrangement_picker.js`). Also note `current_view`.
   - If possible, check what the storyboard's JS state thinks: either inject a quick probe or examine the browser console (if accessible).
2. **If `scene_rank` is missing from payload:** fix the picker or the handler; eliminate hardcoded default `sceneRank:10`; ensure `arrangementchanged` updates `ST.sceneRank` reliably.
3. **If `scene_rank` is present and correct, then `ST.arrIds` is the issue:** modify `ST.arrIds` builder to respect `current_view` - if 'arr', restrict to the single chosen arrangement id; if 'scene', keep all scene arrangements.
4. **Meanwhile, migrate pile fetch from `/api/approved_images` to `/api/sb_pool`** (the already-built scoped endpoint) in both `loadAll()` and `quietPoll()`, passing `scene` and potentially `arrangement_id`. Compare response shapes to ensure compatibility.
5. **Harden the `arrangementchanged` handler** to avoid silent pinning: remove the fallback `ST.sceneRank` and directly set `ST.sceneRank = newScene` from the detail, defaulting only as a last resort.
6. **Restart `slideshow_server_v01.py`** after any server-side changes.
7. **Test end-to-end:** with arrangement 42 selected, the storyboard should show only the one clip, not scene 10 or arrangement 20 media.
8. **Commit and push to master** (per rule) before asking Max to verify.

## OPEN QUESTIONS
- Is the `arrangement_picker.js` detail always including `scene_rank`? Need to confirm by live API probe.
- Does the storyboard currently show scene 10 (stale rank) or all of scene 11's arrangements (too wide)? That determines the primary fix.
- Should the pile be scoped by arrangement or scene? Max said "arrangement selection" but also "only one clip... everything else belongs to the other scene" - implies arrangement-level scoping. However, `current_view` 'scene' might indicate scoping to the whole scene is desired in that mode. Need to clarify or decide based on rule "LONG-TERM ELEGANCE OVER SHORT-TERM SHORTCUTS" and the existing `sb_pool` design, which accepts arrangement_id.

## KEY PATHS/IDS
- **Repo root:** `C:\moma\.claude\worktrees\awesome-swirles-97603c`
- **Live storyboard HTML:** `sc10/sound_assembly/code/storyboard_editor_v2.html`
- **Storyboard server:** `sc10/sound_assembly/code/slideshow_server_v01.py` (port 8790)
- **Global state:** `app_state` table in D1, key `current_arrangement_id` = 42, `current_view` = 'scene'
- **Arrangement 42** = "lesson1", scene 11, created 2026-07-25
- **Scene 11** rank 11, service desk; also contains arrangement 20 (sc11_arr02)
- **Alternative scoped endpoint:** `/api/sb_pool` (line ~1200 in server) accepts `scene` and optional `arrangement_id`
- **Branch:** `claude1/sad-dhawan-8a49fc` (based on master); commit `e480e83`

## GOTCHAS / DEAD ENDS ALREADY RULED OUT
- **Do NOT query local `combo_db.sqlite`** - it's stale; D1 is live.
- **Do NOT analyze v3 storyboard** - the live page is v2 (confirmed via server routes).
- **The global selection plumbing is NOT broken** - `arrangementchanged` event is dispatched and all tabs subscribe. The defect is downstream consumption in the storyboard.
- **My earlier claim of "no scene gate at all" was wrong.** `imgInScene()` (v2.57 D75) exists and filters, but it only scopes to the scene, not the arrangement, and it relies on `ST.arrIds` which is wide. The gate exists but doesn't narrow enough.
- **`/api/sb_pool` shape vs. `/api/approved_images` shape**: before switching, verify fields match what the consumer expects (`job_id`, `path`, `job_type`, `status`, `arrangement_id`, `line_hash`, `role`, etc.).
- **Stale `storyboard_pool_order` for scene 10** - if `binOrder` is loaded from that, old ordering data could also affect display, but the primary issue is scope.
- **Autocompact thrashing** occurred due to large file reads; next session should read in smaller chunks or avoid huge transcripts.
- **Pile gate hard rule:** must be role + status + used-set + SCENE BOUNDARY only. No content/filename heuristics. Our fix must preserve that.
