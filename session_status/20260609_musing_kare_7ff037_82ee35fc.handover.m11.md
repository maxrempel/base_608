# Scribe handover - milestone 11 (~167K tokens)
# session: 20260609_musing_kare_7ff037_82ee35fc
# cwd: C:\claude_base\.claude\worktrees\musing-kare-7ff037
# written: 2026-06-09 08:25:44 by claude-opus-4-8

# HANDOVER

## GOAL (in Max's words)

Max set out to fix his alert system: *"The problem is that the alerts are way too voluminous frequent and technical. I can't read or comprehend them."* From there it grew into a **two-watcher system** he wants built in `C:\claude_base\branch_bulletin\`:

- A **numbering watcher** that *"should watch the numbering and talk to team to help them fix numbering and team assignments. The numbering watcher should not bother me."*
- A **safety watcher** that *"watches as a safety manager while I am away"* - estimates *"the amount of damage and probability of it happening, then translate that to my english"* (Max = general scientist, advanced-Windows-user level, *"absolutely uninterested in jargon"*, wants his *"databases and data be alive, and my computers. But not nitty gritty"*). It must fix problems itself by talking to workers, and ping Max on Telegram *"only when I need to return to my laptop and stop the process."*

His final marching order before leaving: *"i go away, see you in 100 min. YOur team is capable and you are in charge. Safety first. I hope to see the work done. Your team is qualified. you are c6."*

## DECISIONS + WHY

- **Two separate watchers, not one.** Max realized today's single `watcher.py` was doing coordination/numbering, while his alert instructions were really meant for a *safety* manager that doesn't exist yet. So: split the roles.
- **Numbering watcher never pages Max.** It posts to the team board instead. Done because Max wants zero phone noise from coordination issues.
- **Model = DeepSeek v4-pro** (never below V4). Chosen because it's ~50x cheaper than Opus and bills Max's own DeepSeek key. This *superseded* an earlier "Opus 4.8 low" idea and a subscription-vs-API-key debate. Reason the $200 Claude Code subscription can't cover it: a headless watcher (nobody logged in) can't use the interactive subscription - it must use an API key.
- **One safety watcher pass per team** (not one global). Max's explicit instruction.
- **3-band self-compacting context** (Max's spec, verbatim intent): size to DeepSeek's max window, keep ~33% raw/full present state, ~33% gradually-compacted past (older = more compressed, "constantly recompacting"), ~33% kept empty as headroom. Trigger to recompact = growth of the present. Read the already-built `compaction_kb` files (worklog, session_status) rather than rebuilding.
- **Style rules Max enforced hard:** return his words cleaned, NOT editorialized/inflated unless asked. "Not deterministic" - he scolded the word "never": *"never = minimize"* - so the safety charter was softened to "lean toward / generally / most / by default."
- **Hard $2/day cost cap** added as a backstop even with cheap DeepSeek (adviser-recommended; Max was surprised a prior session had been quietly spending his API key unattended).

## CURRENT STATE - THE BUILD IS DONE AND SHIPPED

Both watchers are live and pushed to master. Specifically:

- **Numbering watcher** (`watcher.py`): de-paged - high-severity issues now post to the joint board via `bcast._append_rec`, never Telegram. Committed earlier as **a49218f2**.
- **Safety watcher** (`safety_watcher.py`): NEW, built this session. Per-team pass, DeepSeek v4-pro, 3-band self-compacting context off board + worklog, per-team `safety_digest_<team>.json` state, Telegram only for "come stop the process," global $2/day cap.
- **Tests:** `tests/test_safety_watcher.py` - 13/13 pass (sandboxed, mocks DeepSeek + Telegram, temp dirs).
- **Live verification done:** fake `rm -rf C:\moma` ? correctly flagged severe/high/needs_max with a clean plain-English ping; benign refactor ? judged safe/silent; dry run over real live teams (b/c/d, 9 sessions) ? all clear, zero false pages, ~$0.018 total sweep, $0.0012/call.
- **Scheduled task** `bcast_safety_watcher` registered (every 10 min, mirrors `bcast_watcher`, runs `pythonw.exe`).
- **Commits pushed to master:** `2a91439e` (safety watcher + tests + design doc update), `0118ad57` (infra_map: added safety watcher entry, marked bcast_watcher as de-paged/board-only).
- Worklog logged, team updated on the joint board, registered on board as **c6** (no collision - c6 was free).

The current autonomous tick has nothing new to advance - the work Max asked for is complete and verified.

## EXACT NEXT STEP

This is a repeated autonomous-loop tick with **no remaining actionable work**. Per the loop rules: state in one line that things are quiet, re-arm the ScheduleWakeup with sentinel `<<autonomous-loop-dynamic>>` (delaySeconds 1200-1800 if a Monitor is armed; check TaskList), and stop. Do NOT invent new work, do NOT re-narrate the completed build. Stay reachable as team lead c6 until Max returns (~100-min window). If three consecutive ticks find nothing, scale back to a single-line quiet check.

## OPEN QUESTIONS (awaiting Max)

None blocking. Everything Max asked was decided or built. Things he may want to weigh in on when back: whether the $2/day cap value is right; whether `deepseek-v4-pro` vs `-flash` is the right tier (he said "use pro"). Nothing requires action before he returns.

## KEY PATHS / IDS

- Work dir: `C:\claude_base\branch_bulletin\`
- `safety_watcher.py` (NEW), `watcher.py` (numbering, de-paged), `bcast.py` (585-line board system), `tests/test_safety_watcher.py`
- Spec: `safety_watcher_design_v01_tomemex.md` (status updated to reflect shipped)
- `infra_map_tomemex.md` (updated)
- DeepSeek key: `C:\Users\maxre\Nextcloud\zSyncMain\ssh\deepseek_api_key_20260226.txt` (`sk-de9a8719072f47ce8362016d0301e7c9`)
- Telegram: `telegram_critical_alarms_bot_token_20260604.txt`, chat_id `1395850773`
- DeepSeek V4 = 1M token window (1,048,576); v4-pro is a reasoning model (needs generous output budget - small token caps starved JSON content)
- compaction_kb scripts: `worklog.py`, `session_status.py`, `ctx_track.py` (durable state across the ~169K compaction cliff)
- Scheduled tasks: `bcast_watcher` (PT10M) and new `bcast_safety_watcher` (PT10M)
- Board identity: this session is **c6**. Advisers c1 + c5 and a session c0 were live on the board.
- Commits: a49218f2, 2a91439e, 0118ad57 - all on master, pushed.

## GOTCHAS / DEAD ENDS RULED OUT

- **Editorializing is forbidden.** Max repeatedly scolded paraphrasing his instructions. Return his words cleaned only.
- **"Never" / absolutes are wrong tone.** Max wants "not deterministic" - soften absolutes.
- **The c5 collision** (two live sessions claiming c5 - the exact two-manager bug) was resolved by Max renaming this session to c6. Don't re-register as c5.
- **The dirty git tree:** `C:\claude_base` main checkout on master contains *other teams' active runtime work*. Stage ONLY your own source files (safety_watcher.py, its test, the design doc, infra_map) - never `git add -A`. Runtime files (digests, boards) stay untracked by convention.
- **Git Bash mangles `schtasks` slashes** ? use PowerShell for scheduled-task work.
- **`MaxValue` repetition duration is out of range** for New-ScheduledTask ? use a finite (e.g. 10-year) duration.
- **The old b0 collision** that paged Max ~10 times over ~17 hours is a *numbering* issue, correctly NOT a safety event - the safety watcher must not page on it.
- **Subscription cannot fund a headless watcher** - that path is closed; DeepSeek API key is the funding source.
- Watcher spend so far was tiny (~$1, ~15 Opus calls) - money was never the real danger; the noise was.
