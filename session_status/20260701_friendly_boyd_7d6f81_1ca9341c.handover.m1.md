# Scribe handover - milestone 1 (~104K tokens)
# session: 20260701_friendly_boyd_7d6f81_1ca9341c
# cwd: C:\claude_base\.claude\worktrees\friendly-boyd-7d6f81
# written: 2026-07-01 09:39:12 by deepseek-v4-pro

## HANDOVER - 2025-07-14

### GOAL (in Max's words)
The auto-loaded instruction files are too huge-about a third of the context window. Max wants to identify what can be safely offloaded and loaded on-demand instead, particularly from a large history file where some content may be outdated. The goal is to summarize and offload details, not delete them.

### DECISIONS + WHY

**Diagnosis:** Claude measured all auto-loaded instruction files. Total is ~184 KB (~46K tokens). The distribution:

| File | Size | Decision |
|---|---|---|
| global2.md | 110 KB | **THE TARGET** - 60% of the load, needs shrinking |
| CLAUDE.md (global) | 30 KB | Keep mostly intact (core rules) |
| infra_map | 27 KB | Keep (cron/backup registry) |
| MEMORY.md | 9 KB | Keep (index, already tiny) |
| user_dictionary | 4 KB | Keep |
| max_profile | 4 KB | Keep |

**Agreed approach:** Don't move content to new files (sessions won't know to look there). Instead, **shrink each block in global2.md to a trigger stub** - just the trigger word, a one-line description of what the tool/capability does, and the path to the full details. The full "how-it-works," honest-limits, flags, history, and rationale already live in `_tomemex` documents. Every block already ends with "full method: <path>".

**Why stubs work:** A 1-3 line stub is enough for a session to know the capability exists and where to retrieve full details. Rough estimate: ~30-40 tool blocks at ~2 KB each could shrink to ~150 bytes each, cutting global2.md from 110 KB to ~30-40 KB.

### CURRENT STATE
- Measurement completed. The problem is precisely identified.
- Proposal made but **not yet executed**.
- Claude asked to demonstrate the pattern on ONE fat block (e.g., the `attention` tool at ~2 KB) before applying it everywhere.
- **Awaiting Max's approval** to run the sample.

### EXACT NEXT STEP
Max needs to say "yes, show me the sample" (or equivalent). Then Claude:
1. Shrinks one block (e.g., `attention`) to its stub form.
2. Shows before/after side-by-side.
3. Max approves the pattern or gives feedback.
4. If approved, Claude applies the same treatment to all tool blocks in global2.md.

### OPEN QUESTIONS (awaiting Max)
- None explicitly asked yet. The pending question is implicit: "Show me a sample of the stub pattern?"
- Eventually Max may have opinions on: how aggressively to shrink (just the fat tool blocks, or other sections too), and whether certain entries deserve more than a one-liner.

### KEY PATHS
- **The whale:** `C:\Users\maxre\Nextcloud\claude_md_synced\global2.md` (~110 KB)
- **Global CLAUDE.md:** `C:\Users\maxre\.claude\CLAUDE.md` (~30 KB)
- **Infra map:** `C:\Users\maxre\Nextcloud\claude_md_synced\infra_map.md` (~27 KB)
- **Other auto-loads:** `MEMORY.md`, `user_dictionary`, `max_profile` (all in the Nextcloud synced directory)
- **Full details live in:** `_tomemex` documents (referenced at the end of each block)

### GOTCHAS / DEAD ENDS
- **Don't just move content to a separate file** - cold sessions won't go hunting for an obscure doc they don't know exists. The stub approach keeps discovery working.
- The full detail is NOT being deleted - it already exists in `_tomemex` docs. This is purely about what gets auto-loaded into context.
- No changes have been made yet. The session ended with a proposed plan awaiting approval.
