# Scribe handover - milestone 6 (~460K tokens)
# session: 20260714_nice_maxwell_b61025_3f62f3fe
# cwd: C:\claude_base\.claude\worktrees\nice-maxwell-b61025
# written: 2026-07-14 08:33:57 by deepseek-v4-pro

# HANDOVER - Session B60 / C60: Monitor fixes, Tamza kartoteka, board janitor

---

## GOAL (in Max's words)

**Session-opening request:** Max asked to investigate monitor errors, fix broken monitors (Read AI, Memex feed, Odysee), address Telegram error messages - specifically a Tamza kartoteka data-wipe alarm and a spam problem on the bulletin boards - and then build a general "janitor" to auto-sweep board mess. The broader theme is Max asking: "How do sessions report breakdowns? Who watches for mess on boards and disks? This needs to be reliable, not just instructions."

---

## DECISIONS MADE + WHY

### 1. Transcript-only secret leaks: IGNORE
- Max's words: "If something is linked to the transcript, just ignore it. Don't bother me."
- Saved as a **standing rule** in `C:\Users\maxre\.claude\projects\C--claude-base\memory\MEMORY.md` and a separate file `feedback_transcript_leak_ignore.md`.
- Rotation only matters if a secret escapes to a real external surface (public repo, third-party service, email).

### 2. Tamza kartoteka rebuild cadence: 3 hours ? weekly
- Max said: "rebuilding it once a week is just fine."
- Changed scheduled task trigger to Sundays 4:15 AM, next run July 19.
- Reduces churn and clobber exposure.

### 3. Session archiving rule: 7 days of SILENCE (from last response/touch), not from session start
- Max corrected: sessions live ~7 days, some run continuously for weeks. Archive only if a session has been completely silent (no response, no action) for 7 days from its LAST touch, not from creation.
- This rule is encoded in the board janitor.

### 4. Janitor delivery: email, not Telegram
- Janitor is non-urgent housekeeping - daily digest goes to email, not the critical Telegram bot.
- Safety watcher's 6.4MB memory bloat is **reported** (not trimmed) to avoid breaking the watcher's judgment.

### 5. DNA Vibe Microsoft password change
- Password was force-expired by Microsoft during Read AI re-auth.
- New password: **Sunny-Otter-Lake-92** - saved in Bitwarden item "dnavibe at microsoft.com 202603".
- Max's other devices (Outlook/Teams) will need this new password.

---

## CURRENT STATE - WHAT IS DONE

### ? Fixed monitors (all 3 green)
1. **centauri-odysee-sync** - resumed. Real cause was the modem (Max restarted it).
2. **dax-memex-feed** - circuit breaker had silently disabled the feed on July 2 (file flood to ~3200). Queue drained days ago but guard never auto-restored. Manually restored with `--restore`. **Also deployed watchdog v02.1 with auto-restore logic** to Dax so this can't recur - watchdog now re-enables crons when queue drops below 2000 files.
3. **Read AI weekly transcript downloader** - OAuth token was fully revoked. Re-authed via:
   - Built scripted DCR+PKCE auth-code flow (`readai_authcode.py`)
   - Drove browser through Microsoft login
   - DNA Vibe M365 password had expired - reset it
   - Bitwarden provided both password and TOTP
   - Exchange completed, token written to `readai_oauth_token.json`
   - Downloader ran successfully, pulled 3 new transcripts, pinged green
   - Re-auth is now self-service: `readai_authcode.py` can be re-run by any future session

### ? Tamza kartoteka clobber risk CLOSED
- **Live catalog is intact**: 34,062 rows / 33,937 timed ends at tamza.com. Verified independently by b61.
- **Hardened `publish_catalog.py`**: (a) sanity gate floors raised to just under live counts (so a regressed build is rejected), (b) build crash now ABORTS instead of silently shipping stale data, (c) syntax-checked.
- **Rebuild cadence**: now weekly (Sundays 4:15 AM), was every 3 hours.
- Committed as `7627c3df` (pushed? NO - blocked, see gotchas).

### ? Board watcher spam - already resolved
- The echo-loop was old watcher processes lagging the committed fix `ea2505c6` (Max's Jul 2 commit - excludes watchers' own posts + adds 30/60-min cooldowns). Tasks caught up; duplicate sessions renamed. Verified on Pine: log shows "0 collisions" on every sweep since ~1:22 PM.
- Posted finding to C board. No task-kill needed.

### ? Board janitor built, tested, and scheduled
- **Script**: `C:\claude_base\tools\board_janitor\board_janitor.py`
- **Behavior**: daily at 8 AM:
  - Archives session state-files (`.json` in `branch_bulletin/sessions/`) that have been silent >7 days (from last `mtime` touch) ? moved to `sessions/archive/` (NOT deleted)
  - Archives room files idle >7 days ? moved to `rooms/archive/`
  - Reports safety_watcher memory bloat (does NOT trim it - safer to report for the watcher's owner)
  - Emails a short digest to Max
- **First run**: manually tested, archived 7 genuinely stale room files (8-11 days idle), no session cards were old enough to archive (oldest ~1.2 days silent).
- **Scheduled**: Windows Task Scheduler task `board_janitor`, daily 8:00 AM, next run July 15.
- **Documented**: README at `C:\claude_base\tools\board_janitor\README_tomemex.md`
- Updated infra_map to reflect the janitor and the weekly kartoteka cadence.
- Committed locally as `0dbf9c20`? (PUSH BLOCKED - see gotchas).

### ? Memory files written
- `C:\Users\maxre\.claude\projects\C--claude-base\memory\feedback_transcript_leak_ignore.md` - standing rule: ignore transcript-only leaks
- `C:\Users\maxre\.claude\projects\C--claude-base\memory\recurrence_bcast_watcher_spam.md` - the watcher spam is a known trap; verify checkout + no stuck process before rewriting anything; the real fix is already in master (ea2505c6)
- Updated `MEMORY.md` with both entries

---

## EXACT NEXT STEP (for a cold session picking this up)

**There are two open items; the push blocker is the only one that needs action now.**

### Immediate: Push blocker on master - huge genomics files in git history
Two commits added 500+ MB of TSV data files to the repo:
- `denovo_gw.tsv` = 321 MB
- `compress_per_change` = 211 MB

These were added in commit `5a868a71` (genomics session, project OMEGA/Kenefic). A follow-up commit removed them from the working tree and added them to `.gitignore`, but **the blobs remain in git history**. GitHub rejects blobs >100 MB, so **no one can push to master**. All sessions are accumulating local unpushed commits.

**The fix requires a coordinated history rewrite** (e.g., `git filter-repo` to purge those blobs). This is dangerous: it rewrites shared history, and ALL active sessions must re-sync afterward. It should be done by one person with coordination. Max was asked whether to proceed - awaiting answer.

### Non-urgent: Tamza build is frozen - `normalize_performers.py` lost
The catalog builder (`build_catalog.py`) imports a module that was never committed to git and is gone from disk. Builds crash, so no NEW songs can be added. The catalog is frozen-but-safe (live 34,062 rows intact). This was handed to b15merger (the session with performer context). Not urgent - the publish gate now prevents regressions regardless.

---

## OPEN QUESTIONS STILL AWAITING MAX

1. **Push blocker resolution:** Does Max want B60/C60 to run the git history rewrite (`filter-repo`) to purge the 500MB+ genomics blobs from master and unblock all pushes? This requires coordination with all active sessions.

---

## KEY FILE PATHS, IDs, NAMES

### Credentials / secrets
- **Healthchecks API key** (leaked in transcript, Max said ignore): `hcw_FURiOSiC9Vszzf2OWydsJumrkNj9`
- **Bitwarden item** for DNA Vibe M365: `"dnavibe at microsoft.com 202603"` - now contains new password
- **Telegram user API session**: `C:/Users/maxre/Nextcloud/zSyncMain/ssh/telegram_userapi_maxasst_20260612.txt`
- **Read AI OAuth token store**: `C:\Users\maxre\Nextcloud\zSyncMain\ssh\readai_oauth_token.json`

### Tamza kartoteka
- **Live catalog**: `https://tamza.com/wp-content/kartoteka/data.json` - 34,062 rows / 33,937 timed ends
- **Publisher script** (hardened): `C:\claude_base\tools\tamza_songs\pipeline\scripts\publish_catalog.py`
- **Handover**: `C:\claude_base\tools\tamza_songs\pipeline\TAMZA_HANDOVER_START_HERE_v01_tomemex.md`
- **Scheduled task**: `kartoteka_publish` - now weekly, Sundays 4:15 AM
- **Lost module**: `C:\claude_base\tools\tamza_songs\ingest_v2\site\staging_performers\normalize_performers.py` - never committed, gone from disk
- **Key commits**: `75d7d9c7` + `14e14274` (b15merger's overlay-loss fix), `7627c3df` (gate hardening + cadence change)

### Board watcher / janitor
- **Bulletin board code**: `C:\claude_base\branch_bulletin\`
- **Watcher spam fix**: commit `ea2505c6` (July 2, Max's own fix - already in master + working copy)
- **Janitor**: `C:\claude_base\tools\board_janitor\board_janitor.py`
- **Scheduled task**: `board_janitor` - daily 8 AM, next run July 15
- **Session state-files**: `C:\claude_base\branch_bulletin\sessions\` (JSON, one per session)
- **Room files**: `C:\claude_base\branch_bulletin\rooms\`
- **Archive dirs**: `sessions/archive/`, `rooms/archive/`

### Memex / Dax
- **Dax host**: `bitnami@35.80.203.42`
- **SSH key**: `C:\Users\maxre\Nextcloud\zSyncMain\ssh\dax_lightsail_max_id_rsa.pem`
- **Watchdog** (now auto-restores): `/home/bitnami/memex_watchdog_v02.py`
- **Watchdog state**: `/home/bitnami/memex_watchdog_state.txt`

### Read AI re-auth tooling
- **Re-auth script**: `C:\claude_base\tools\readai_transcripts\readai_authcode.py`
- **Device-code fallback**: `C:\claude_base\tools\readai_transcripts\readai_reauth.py`
- **Downloader**: `C:\claude_base\tools\readai_transcripts\readai_weekly_download.py`

### Telegram reader
- **Tool**: `C:\claude_base\tools\tg_alerts\tg_alerts.py` (shipped by b61)

### Infra / memory
- **Infra map**: `C:\claude_base\infra_map_tomemex.md` (updated with janitor + weekly kartoteka)
- **Memory file**: `C:\Users\maxre\.claude\projects\C--claude-base\memory\MEMORY.md`
- **Recurrence ledger (watcher spam)**: `C:\Users\maxre\.claude\projects\C--claude-base\memory\recurrence_bcast_watcher_spam.md`

---

## GOTCHAS AND DEAD ENDS

1. **Git push to master is BLOCKED** - 500+ MB genomics blobs in history. No one can push. The janitor commit (`0dbf9c20` or similar) and the kartoteka gate fix (`7627c3df`) are committed locally but NOT on GitHub. The remote (`origin/master`) is 13 commits behind local.

2. **Read AI token used to be on Nextcloud sync** - multi-machine sync conflicts likely caused the revocations. The scripted re-auth flow (in `readai_authcode.py`) now exists as a self-service recovery, but the **permanent fix** (pinning the token file off Nextcloud to Pine only) was discussed but not yet executed. Max wasn't asked explicitly about it.

3. **Read AI device-code flow is blocked** by the auth server - the registered client doesn't support the device grant. Only authorization-code (browser) flow works. Microsoft SSO is the only login method for Max's Read AI account.

4. **The Dax Memex watchdog's `--restore` is manual** - the v02.1 auto-restore fix was deployed but needs confirmation it actually re-enables crons automatically. The manual `--restore` worked and the feed is green.

5. **DNA Vibe M365 password was force-expired** - Max's other devices (Outlook, Teams, phone) will prompt for the new password `Sunny-Otter-Lake-92`. Bitwarden item "dnavibe at microsoft.com 202603" has the updated password and TOTP generator.

6. **`normalize_performers.py` is truly lost** - not in any git branch, not on disk. Its three functions (extract_names, canon_key, pick_display) are unrecoverable from git. Only `canon_key` survives in a different file for the author variant. Reconstruction was handed to b
