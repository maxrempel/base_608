# Scribe handover - milestone 4 (~353K tokens)
# session: 20260713_nice_maxwell_b61025_3f62f3fe
# cwd: C:\claude_base\.claude\worktrees\nice-maxwell-b61025
# written: 2026-07-13 10:50:10 by deepseek-v4-pro

# HANDOVER - Monitor Fixes & Tamza Kartoteka Data-Wipe Closure

## GOAL (Max's words)
"Investigate my monitors messages and address them. Fix everything. Then check Telegram messages - the main one is troubled on Tamza."

---

## DECISIONS MADE + WHY

### 1. Odysee Sync (centauri-odysee-sync)
- **Decision:** Just resumed via Healthchecks API.
- **Why:** Max confirmed the real problem was the network modem (he restarted it). Odysee itself was fine - it was paused in Healthchecks after the modem issue. No code changes needed.

### 2. Memex Memory Feed (dax-memex-feed) - DOWN since July 2
- **Decision:** Manually ran `--restore` on the watchdog, then **modified the watchdog to auto-restore** when queue clears.
- **Why:** The watchdog tripped a runaway-guard on July 2 (ingest queue spiked to ~3200 files, threshold was 3000). It disabled all memex crons but **never re-enabled them**, even after the queue drained to 1472 files (healthy). Root cause: the watchdog's KILL state had no auto-recovery path - it set its own state back to "ok" while leaving crons disabled. Flaw design, now fixed.
- **The fix:** Edited `/home/bitnami/memex_watchdog_v02.py` on Dax so that when the queue falls back below the threshold, it automatically re-enables the crons. Verified syntax and dry-run on Dax.

### 3. Read AI (weekly meeting transcript grabber) - DOWN, login fully revoked
- **Decision:** Full re-authentication via scripted OAuth authorization-code + PKCE flow, driving browser through Microsoft login.
- **Why:** The old refresh token was completely revoked (`invalid_grant` - "refresh token can not be found"). Root cause: Max's **DNA Vibe Microsoft 365 password had expired**, which killed the entire token chain. Device-code flow wasn't available for the registered client. DCR with authorization_code grant worked - registered a new client with localhost callback.
- **Credentials used:** Bitwarden item "dnavibe at microsoft.com 202603" for password + TOTP.
- **New password set:** `Sunny-Otter-Lake-92` (forced password reset during flow). Saved back to Bitwarden.
- **Token captured, downloader tested:** Pulled 3 new transcripts, Healthchecks ping went green.
- **Long-term re-auth tooling built:** `readai_authcode.py` and `readai_reauth.py` in `C:\claude_base\tools\readai_transcripts\` - self-service from now on.

### 4. Tamza Kartoteka Data-Wipe (the Telegram alarm)
- **Decision:** Harden the publish gate instead of restoring from backup.
- **Why:** Critical discovery by helper session b61 - **the live catalog at tamza.com is intact at 34,062 rows with ~33,900 timed ends.** No data was actually lost; the alarm was warning about *risk* of future clobber. The real problem was two bugs in `publish_catalog.py`:
  1. **Silent stale-data fallback:** When `build_catalog.py` crashed (missing `normalize_performers.py`), the publisher silently shipped an old, smaller `data.json` instead of aborting.
  2. **Safety thresholds too low:** The gate floors (26000 rows / 20000 timed-ends) were far below live (34062/33937), so a regressed build would sail through and clobber.
- **The fix (both in publish_catalog.py, committed as 7627c3df):**
  - Publisher now **ABORTS** if the build step fails - no more silent stale deploys.
  - Raised floors to just below live: ~33000 rows, ~32000 timed-ends. Any regressed build is rejected.
- **Leftover:** The builder itself is still broken because `normalize_performers.py` is lost (never committed, only existed in a recycled worktree). No new song additions can deploy until reconstructed. Handed to b15merger who has performer-domain context.

### 5. Standing Rule Established
- **Decision:** Transcript-only credential leaks are to be ignored - no rotation, no nagging.
- **Why:** Max said "If something is linked to the transcript, just ignore it. Don't bother me." Recorded in `MEMORY.md`.

---

## CURRENT STATE

| Monitor | Status | Details |
|---------|--------|---------|
| centauri-odysee-sync | GREEN | Resumed, modem was the real cause |
| dax-memex-feed | GREEN | Watchdog auto-restore now live on Dax |
| Read AI | GREEN | Re-authed, pulled 3 transcripts, token fresh |
| Tamza kartoteka | PROTECTED | Live intact (34062 rows), gate hardened, clobber risk closed |

**Tamza builder is still broken** - `normalize_performers.py` is lost, so no new songs can be deployed until reconstructed. This is safe (can't accidentally clobber) but frozen.

---

## EXACT NEXT STEP

**For Tamza:** b15merger needs to reconstruct `normalize_performers.py` (three functions: `extract_names`, `canon_key`, `pick_display` - all lost from a recycled worktree) so the builder can produce fresh catalogs again. Once built, the hardened publisher will safely deploy it.

**For the Memex feed:** No action needed - watchdog auto-restore is live. Will self-heal next time the queue spikes.

**For Read AI:** No action needed - re-auth is scripted. If token ever expires again, run `readai_authcode.py`.

---

## OPEN QUESTIONS AWAITING MAX

None. All actions completed.

---

## KEY PATHS, IDs, COMMANDS

### SSH / Servers
- **Dax (AWS Lightsail):** `bitnami@35.80.203.42`
- **Dax SSH key:** `C:\Users\maxre\Nextcloud\zSyncMain\ssh\dax_lightsail_max_id_rsa.pem`
- **Memex watchdog on Dax:** `/home/bitnami/memex_watchdog_v02.py`
- **Memex state file on Dax:** `/home/bitnami/memex_watchdog_state.txt`
- **Memex crons (3):** sync_memories, push to Memex, ingest queue processor

### Healthchecks
- **API key:** `hcw_FURiOSiC9Vszzf2OWydsJumrkNj9` (exposed in transcript; Max said ignore)
- **Odysee check ID:** `6dcbc67d-b557-4d41-81a4-b0186873bd76`
- **Resume API:** `POST /api/v3/checks/{id}/resume`

### Read AI
- **Token store:** `C:\Users\maxre\Nextcloud\zSyncMain\ssh\readai_oauth_token.json`
- **OAuth client (registered):** `6eef81e3-fa4f-468a-8b2b-e3ad90aeea27` (localhost:8765 callback)
- **Re-auth script:** `C:\claude_base\tools\readai_transcripts\readai_authcode.py`
- **Downloader:** `C:\claude_base\tools\readai_transcripts\readai_weekly_download.py`
- **Transcript output:** `C:\Users\maxre\Nextcloud\dnavibe\meeting_transcripts\`
- **DNA Vibe M365 account:** `max@dnavibe.com`
- **Bitwarden item:** "dnavibe at microsoft.com 202603" (password + TOTP)
- **New M365 password:** `Sunny-Otter-Lake-92`

### Tamza Kartoteka
- **Pipeline root:** `C:\claude_base\tools\tamza_songs\pipeline\`
- **Publisher (fixed):** `scripts/publish_catalog.py`
- **Builder (broken):** `scripts/build_catalog.py` (imports lost `normalize_performers.py`)
- **Live catalog:** `https://tamza.com/wp-content/kartoteka/data.json`
- **Pre-deploy backups:** `output/backups/` (Jun 26 and Jul 6 snapshots)
- **Fix commit:** `7627c3df` on master - hardens gate + aborts on build crash
- **Live stats:** 34,062 rows, ~33,937 timed-ends (verified by b61)

### Telegram
- **Monitor channel:** "MMMMonitor global of Max's systems" (read via Telethon)
- **User API session:** `C:\Users\maxre\Nextcloud\zSyncMain\ssh\telegram_userapi_maxasst_20260612.txt`
- **Tamza alerts:** Fired Jul 11 11:59 and Jul 12 11:09 - now addressed
- **Tamza-Zumy channel:** Fine, posting weekly Zoom links normally

### Git
- **Worktree:** `C:\claude_base\.claude\worktrees\nice-maxwell-b61025` (fresh, old one recycled)
- **Branch:** `claude1/epic-snyder-8f86b4`
- **Bulletin board:** `python C:/claude_base/branch_bulletin/bcast.py`

---

## GOTCHAS / DEAD ENDS

1. **Watchdog KILL is not auto-reversible** (was, now fixed on Dax). The old code set state back to "ok" but left crons disabled - silent 8-day outage. Now auto-restores.

2. **Read AI device-code flow unavailable** - the DCR-registered client only got `refresh_token` grant. Had to use authorization_code + PKCE with localhost callback instead.

3. **Read AI token store is in Nextcloud** - multi-machine sync likely causes token conflicts (old copies revoke the chain). Pinning it to Pine-only was suggested but not done (Max didn't confirm).

4. **`normalize_performers.py` is unrecoverable** - never committed to git, only existed in a recycled worktree. Not on disk anywhere across all worktrees. The three functions (`extract_names`, `canon_key`, `pick_display`) must be reconstructed from scratch. `canon_key` has an author-variant survivor in another file but the other two are gone.

5. **Worktree was recycled mid-session** - agitated-fermat-7ec333 ? nice-maxwell-b61025. Any absolute paths pointing to the old worktree no longer exist.

6. **Max's standing rule on transcript leaks:** ignore them. If a secret only shows up in the session transcript (local disk + Anthropic + his own DeepSeek adviser), don't rotate, don't flag, don't nag.

7. **b61 was the hero on Tamza** - independently verified live catalog was intact at 34062 rows, preventing an unnecessary restore that could have made things worse.
