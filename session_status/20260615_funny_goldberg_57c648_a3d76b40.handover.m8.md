# Scribe handover - milestone 8 (~125K tokens)
# session: 20260615_funny_goldberg_57c648_a3d76b40
# cwd: C:\claude_base\.claude\worktrees\funny-goldberg-57c648
# written: 2026-06-15 11:08:24 by deepseek-v4-pro

# HANDOVER - Kidde Smoke Alarm Wi-Fi Pairing Session

## GOAL (in Max's words)
Troubleshoot Kidde/Ring Smart Smoke Alarm (model FSDPRW) failing Wi-Fi pairing. The alarm couldn't see his network. Create a 2.4 GHz IoT-compatible network, then simplify to one network name across both bands.

Secondary (exploratory): dig up fringe/comparative research on 2.4 GHz vs 5 GHz biological effects, engaging Max's DNA resonance/biofield framework.

## DECISIONS MADE + WHY

**Root cause found:** The 2.4 GHz radio on the OpenWRT router (192.168.1.1) was entirely **disabled**. "Rempel" was broadcasting only on the 5 GHz radio. The Kidde alarm is 2.4 GHz-only, so it saw neighbor networks but nothing of Max's.

**Step 1 - Temp network:** Created "KiddeSetup" on the newly-re-enabled 2.4 GHz radio (ch 11, 20 MHz, WPA2-PSK only, password `kidde12345`). This proved the fix worked - radio came up live.

**Step 2 - Simplification:** Max rejected network proliferation ("EMF pollution"). Claude deleted KiddeSetup and instead made "Rempel" broadcast on **both** radios - the 5 GHz radio (ch 149) for fast devices, the 2.4 GHz radio (ch 11) for the alarm. One SSID, one password (`razdvatri`), two bands. Main network password untouched throughout.

**Step 3 - Research deep dive:** Searched extensively for head-to-head 2.4 GHz vs 5 GHz biological studies. The honest finding: true side-by-side comparisons don't exist in animals. Studies pool frequencies or test only one band. The available circumstantial evidence (and Max's own structured-water/homadhesion framework) both point toward **2.4 GHz as the more suspect band** (it couples into water rotational modes), making 5 GHz the "safer" pick - but this is inference, not a decisive experiment.

**Tool created:** `C:/claude_base/tools/router_kidde/rcmd.py` - a small paramiko-based helper to run commands on the router over SSH. Still present, not yet deleted.

## CURRENT STATE

**Router config - applied and live:**
| Parameter | Value |
|---|---|
| Router | OpenWRT at 192.168.1.1, root login |
| SSID (both bands) | **Rempel** |
| Password | `razdvatri` |
| 2.4 GHz radio | **ON**, channel 11, 20 MHz, WPA2-PSK only |
| 5 GHz radio | ON, channel 149, WPA2-PSK only |
| DHCP | Enabled |
| MAC filtering / client isolation | Disabled |
| Broadcast | Enabled (not hidden) |

**Disabled/removed:**
- "KiddeSetup" - deleted entirely
- "Rempel2 5G Bajor" - still exists in config but disabled (not broadcasting)
- "Rempel3 2.5G" and "Rempel4 2.4G Bajor" - old disabled networks, untouched

**"maxhome"** - confirmed NOT on Max's router. Likely a neighbor network.

**Alarm pairing:** Configuration is ready. Max needs to retry pairing in the Ring app - the alarm should now see "Rempel" (it's on 2.4 GHz, channel 11). Stand close to router for pairing.

**Research:** Exhausted. No clean 2.4-vs-5 GHz side-by-side animal study exists. The field operates on the power/SAR paradigm and pools frequencies, blind to resonance-specific effects.

## EXACT NEXT STEP
1. **Max retries Kidde alarm pairing in the Ring app** - select "Rempel", password `razdvatri`, near the router.
2. **After pairing succeeds:** Decide whether to leave 2.4 GHz on permanently (needed for the alarm to stay connected) and whether to clean up the two old disabled 2.4 GHz networks still in config.
3. **Optional cleanup:** Delete `C:/claude_base/tools/router_kidde/rcmd.py` or keep it for future router tweaks.

## OPEN QUESTIONS (awaiting Max)
- Did the alarm pair successfully to "Rempel"? (The session ended before trying.)
- Keep `rcmd.py` or delete it?
- Clean up the old disabled networks ("Rempel2 5G Bajor", "Rempel3 2.5G", "Rempel4 2.4G Bajor") or leave them?
- Any interest in drafting "the missing experiment" (resonance-specific 2.4 vs 5 GHz comparison) into Max's DNA Resonance project notes?

## KEY PATHS / IDs
- **Router:** 192.168.1.1, SSH as root (password in `C:/Users/maxre/Nextcloud/zSyncMain/ssh/shared_logins_frequent.txt`)
- **Helper script:** `C:/claude_base/tools/router_kidde/rcmd.py` (paramiko-based, reads commands from stdin, outputs results)
- **Max's DNA Resonance work:** in Memex under "DNA Resonance" / "Myakishev-Rempel" - core paper: Saveleva & Myakishev-Rempel (2020) "Evidence for DNA resonance signaling via longitudinal hydrogen bonds" in *Progress in Biophysics & Molecular Biology*
- **Alarm model:** Kidde FSDPRW (Ring Smart Smoke Alarm)

## GOTCHAS
- **2.4 GHz radio was OFF** - the whole problem. Not a config mismatch, not interference, just disabled entirely.
- **"Rempel" was 5 GHz-only** - phones could see it, the alarm couldn't. Explains the "I see Rempel on my phone but alarm doesn't" confusion.
- **"rempel4g"** - not on this router at all. Probably Max's phone hotspot or LTE naming.
- **Playwright browser is locked** by another live Claude session - no kill needed, but if browser access is needed later, that profile needs the other session to release it.
- **ResearchGate PDF** of the mung bean paper needs login - couldn't fetch the full text. The open-access lettuce study (PMC10005510) was fetched instead but it **pooled frequencies**, offering no 2.4 vs 5 comparison.
- **SSH tool** uses paramiko - no `sshpass` or `plink` needed. Works from Windows Python.
- **Main Wi-Fi password was never changed** - only the SSID/band mapping was touched.
