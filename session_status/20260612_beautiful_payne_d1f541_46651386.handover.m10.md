# Scribe handover - milestone 10 (~154K tokens)
# session: 20260612_beautiful_payne_d1f541_46651386
# cwd: C:\claude_base\.claude\worktrees\beautiful-payne-d1f541
# written: 2026-06-12 15:27:13 by claude-opus-4-8

# HANDOVER - Tamza + Hucolo Video Backup (session "B9")

## GOAL (in Max's words)
"WE need ALL without Exception videos from Tamza" - and the same for Hucolo. Both channels must be fully backed up, then "upload to Odysee, and do more work on videos." The original pain point was YouTube download blocks ("bot-wall"). Max also wants a simple, humanized system for naming the backup drives that will "float around" between machines.

Most recent steering (the live task right now): "likely many of 57 videos are already available on odysee. Talk to b8. Also test if you can actually download from odysee. Also, I am not sure it is that tolerant to bots. Do proper matching."

## DECISIONS + WHY
- **You are B9** (renamed from D9). You hold **single-writer ownership** of the Tamza+Hucolo full-video BACKUP (inventory + downloads). A watcher flagged overlap with B10, so ownership was claimed explicitly on the joint board.
- **Shorts are excluded** - Max calls them derivative. (Confirm this still holds, but treat as decided.)
- **Source of truth = YouTube** for both channels. Max corrected an earlier assumption: the Odysee copies are themselves incomplete, so YouTube is authoritative.
- **BUT the new winning strategy is to rebuild mostly FROM Odysee**, because Odysee has no (or far weaker) bot-wall and the videos already uploaded there are recoverable fast. Only the YouTube-only gap gets the slow, paced YouTube pull. This is exactly what Max's last message is pushing on - but he is now skeptical Odysee is truly bot-tolerant, so this must be tested, not assumed.
- **Drive naming system: color + size**, e.g. `teal16`. The name belongs to the physical drive forever (survives moving between machines); a single registry file tracks where each drive currently lives. Centauri's 16TB drive = **teal16**.
- **No downloads have started yet** - parked by Max's explicit order while B8 uses the shared home IP for YouTube.

## CURRENT STATE
**Inventory done (metadata only, no media pulled):**
- Tamza YouTube (UCo-O_aBrW8J3hEGEdow71Iw): 936 regular videos + 33 shorts + 622 livestreams = 1591 total (tabs verified non-overlapping), ~2720 hrs. Without shorts: **1558**.
- Hucolo YouTube (UCj5wGWloHE8hKHPd5kqWsJQ): 730 videos + 553 livestreams = **1283** (no shorts).
- Tamza Odysee **@tamza = 1292 videos** (~83% of YT source).
- Hucolo Odysee **@hucolo = 1167 videos** (~91% of YT source).

**The big discovery:** the Centauri odysee-sync tool was a *mirror-and-delete* - it uploaded each video to Odysee then deleted the local .mp4. So the 304 (Tamza) / 121 (Hucolo) ID-named folders on Centauri are mostly **empty sync-bookkeeping shells**. Only ~28 real video files exist locally (21 Tamza / 28.8 GB + 7 Hucolo / 9.6 GB). Three sampled Tamza folders were confirmed completely empty. Effectively **~1% real local backup**.

**Access:** SSH from Pine to Centauri works: `ssh -i ~/.ssh/sol_key maxre@192.168.1.176`. Centauri has Python 3.14 + yt-dlp + ffmpeg. teal16 has 12.8 TB free. Target folder `D:\tamza_yt_full_backup\` exists.

**Drive registry created and filled.**

**B8** is downloading YouTube videos in another session - at 17/57 last checked, on the shared home IP. Max wants you to coordinate with B8 because many of those 57 may already be on Odysee.

## EXACT NEXT STEP
Do three things from Max's last message:
1. **Talk to B8** via the board - find out which of the 57 videos B8 is pulling, so you can check which are already on Odysee (and possibly save B8 the YouTube risk).
2. **Actually test an Odysee download** - pick one real video and download it end-to-end with yt-dlp to prove it works (not just metadata listing). Do NOT assume Odysee is bot-tolerant; do it gently and watch for blocks.
3. **Do proper matching** - match the Odysee inventory against the YouTube inventory to determine the true overlap and the real YouTube-only gap, per channel. Earlier coverage figures (~83% / ~91%) are count-based estimates, not verified matches - Max explicitly doubts them. Match by video identity, not just totals.

## OPEN QUESTIONS AWAITING MAX
- Confirm job scope = full fresh backup of both channels' real videos + livestreams, **shorts excluded** (~2841 YT videos, or far fewer if Odysee covers most).
- Whether to kick off the Odysee bulk download now (pending the bot-tolerance test result).

## KEY PATHS / IDS / COMMANDS
- Centauri SSH: `ssh -i ~/.ssh/sol_key maxre@192.168.1.176` (hostname/IPv6 fails - must use this IPv4).
- Drive registry: `C:\Users\maxre\Nextcloud\zSyncMain\ssh\drives_registry.md` (teal16 = Centauri 16TB).
- Backup target on Centauri: `D:\tamza_yt_full_backup\` ; old sync shells live under `D:\odysee_sync\<channelID>\`.
- Inventory files: `C:\claude_base\tools\tamza_hucolo_backup\inventory\` - includes `hucolo_odysee.txt`, the per-tab Tamza lists, and `INVENTORY_SUMMARY_20260612.md`.
- Channels: Tamza YT `UCo-O_aBrW8J3hEGEdow71Iw` / Odysee `@tamza`. Hucolo YT `UCj5wGWloHE8hKHPd5kqWsJQ` / Odysee `@hucolo:1`.
- Board: `python C:/claude_base/branch_bulletin/bcast.py` (whoami / catchup / post / post --joint / read).
- Worklog: `python C:/claude_base/compaction_kb/scripts/worklog.py log "..."`.
- Inventory technique: `yt-dlp --flat-playlist --print "%(id)s|%(duration)s|%(title)s" <URL>` - listing only, never touches media.

## GOTCHAS / DEAD ENDS
- **Topology:** Sol, Lak, Centauri, Pine are all at Max's San Diego home = ONE residential IP. YouTube blocks by IP, not machine - so B8 + you pulling YouTube simultaneously stacks load on one IP. Pacing > more machines. (Odysee is a different platform/servers, so it doesn't conflict with B8's YouTube load - but verify it's actually bot-tolerant before trusting that.)
- **Bot-wall fix (D8/B8's, proven):** yt-dlp TV-app client mode defeats YouTube's "confirm you're not a bot" wall with zero logins, zero cookies. Google Takeout was tried and ruled out as harder.
- The odysee_sync ID-named entries look like folders but are mostly **empty** - do not count them as backup.
- **Count via `dir | find` was unreliable** over SSH; use remote PowerShell (`Get-ChildItem`) for accurate counts/sizes.
- Piping remote `dir ... | head` runs head on the remote Windows box (no head) - filter remote-side with `findstr`, pipe to `head` locally.
- The repeat-command detector throws false positives when two yt-dlp calls share the same shape with different URLs - restructure so the URL leads.
- Honesty note already given to Max: the earlier "weird, try again" SSH failure was real (IPv6 resolution); IPv4 fixed it.
