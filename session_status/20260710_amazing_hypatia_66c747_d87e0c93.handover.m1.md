# Scribe handover - milestone 1 (~124K tokens)
# session: 20260710_amazing_hypatia_66c747_d87e0c93
# cwd: C:\claude_base\.claude\worktrees\amazing-hypatia-66c747
# written: 2026-07-10 08:29:55 by deepseek-v4-pro

# Handover - Download Bowater Family 23andMe Raw Data

## Goal (Max's words)
"Download the data for Lottie... 23andme... mother and father. So we'll just reclaim the data and then we'll come back and download later."

All Bowater family raw DNA data files (Lottie, mother Julie, father Roger) need to be downloaded from 23andMe. A delay is expected for Roger (still processing). The priority is to get whatever is ready now, move the files to the project, and return later for Roger.

## Decisions made & why
- **Account identification**: Searching through existing notes and stored logins revealed that Lottie Bowater, Julie Bowater, and Roger Bowater are all child/mother/father profiles **under Max's personal 23andMe account** (`max@tamza.com`). There is no separate "Lottie" login - "Lottie" is the profile name inside the account. This was confirmed via `es.exe` and `xg1_starseed_forms` notes.
- **Credentials**: The password for `max@tamza.com` was retrieved from `C:\Users\maxre\Nextcloud\zSyncMain\ssh\shared_logins_frequent.txt` (the line labelled "23andme: max"). This file is the canonical source for shared logins.
- **Navigation**: Claude used Playwright to open the correct sign-in page (`https://you.23andme.com/user/signin/` after a redirect from the main site). The email and password were filled using the stored values.
- **2FA hit**: 23andMe requires a time?based one?time passcode (TOTP) from an authenticator app. No backup code or secret is saved anywhere in the notes or login file. So the session is paused at the 2FA challenge.
- **Alerting Max**: The `attention.py` tool was used to pop up a visual notification on the desktop telling Max that his 6?digit code is needed.

## Current state
- **Browser**: Playwright is holding the browser lock, sitting **logged?in but on the 2?factor authentication page**. The email and password have been accepted; only the 6?digit code is missing.
- **Max has been prompted**: The `attention.py` notification is asking for the TOTP code from his authenticator app.
- **Worktree**: Session `amazing-hypatia-66c747`, checked in as branch `x31b`.

## Exact next step
1. **Max must provide the current 6?digit TOTP** from his authenticator app (labelled "23andMe"). The code expires in ~30 seconds, so it should be sent freshly.
2. Claude will type it into the 2FA field and click Continue to complete login.
3. Once inside the 23andMe dashboard, navigate to the **Raw Data / Browse Raw Data** section. Switch to each family profile in turn:
   - **Lottie Bowater** - if download is still pending (notes say she was done, but verify and re?download if missing).
   - **Julie Bowater** - download raw data zip.
   - **Roger Bowater** - check status. If "processing", note the estimated completion date and move on.
4. Save downloaded files to the **project's working directory** (path to be finalised - possibly under `C:\claude_base\.claude\worktrees\amazing-hypatia-66c747\data\` or a known sync folder). After downloading, kill the browser session to release the Playwright lock.

## Open questions (for Max)
- **Where exactly should the raw data zips be saved?** Do we put them in the current worktree's `data/` directory, or is there a specific Nextcloud/Syncthing location used for DNA data?
- **What to do if Roger's kit is still processing?** Set a reminder, check again later, or just flag it?
- **Confirm priority**: After Lottie and Julie are downloaded, do we also need to download Max's own data (the account owner), or strictly only the three Bowater profiles?

## Key paths & identifiers
- **Login credentials file**: `C:\Users\maxre\Nextcloud\zSyncMain\ssh\shared_logins_frequent.txt`  
  Relevant line: `23andme: max | max@tamza.com | [password]`
- **Search indexes / method notes**:  
  - `es.exe` used for full?text search  
  - `xg1_starseed_forms_v01_tomemex.md` confirmed account structure
- **Attention tool**: `C:/claude_base/tools/attention/attention.py` (used to flash desktop notification)
- **Playwright browser lock**: held by this session - must be released after use.
- **23andMe raw data download path** (once logged in): typically under the profile menu ? "Browse Raw Data" ? "Download".

## Gotchas & dead ends
- **No saved 2FA secret**: The TOTP secret is only on Max's phone. No backup code exists in any notes. Max must be present to supply the code.
- **Code expiry**: The 6?digit code is only valid for a short window; stale codes will be rejected.
- **Roger's kit was "processing"** as of mid?June. If it still shows that status, downloading isn't possible - don't wait, just note it.
- **Profile switching**: 23andMe requires manually selecting the profile (usually via a dropdown under the profile photo) before the "Browse Raw Data" option appears for that person. If you just click the link for the account owner, you might see Max's data instead of Lottie's.
- **Possible download delays**: 23andMe sometimes prepares raw data archives asynchronously - you might need to request a download and refresh a few minutes later to get the link. Handle that patiently if it occurs.

---

**Everything is paused waiting for the 2FA code.** Once Max supplies it, the login can be completed and the downloads started immediately.
