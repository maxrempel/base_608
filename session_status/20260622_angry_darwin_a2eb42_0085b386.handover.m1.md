# Scribe handover - milestone 1 (~89K tokens)
# session: 20260622_angry_darwin_a2eb42_0085b386
# cwd: C:\claude_base\.claude\worktrees\angry-darwin-a2eb42
# written: 2026-06-22 14:41:59 by deepseek-v4-pro

# HANDOVER - Speech Recognition & Retake Removal for Video

---

## GOAL (Max's words)

"Locate the handover or instructions how to do speech recognition and remove retakes in my video."

---

## DECISIONS + WHY

- **Tool chosen:** Retake Cleaner (`retake_cleaner.py`). This is a pre-existing, documented tool built exactly for talk-to-camera video retake/stutter/filler removal.
- **Transcription engine: Deepgram nova-3, in verbatim mode.** The reasoning is explicit: verbatim mode surfaces abandoned takes and false starts that Whisper silently merges away. If you want to *cut* retakes, you need to *see* them first. Whisper is actively ruled out for this task.
- **Two-pass workflow:** First, transcribe and produce an indexed transcript. Second, manually review that transcript and write a per-video `cuts.py` file listing word ranges to drop. This keeps the human in the loop for editing decisions.

---

## CURRENT STATE

- Max has asked where the instructions live. Claude identified the tool and its method doc. No video has been specified yet. No transcription has been run. No cuts have been made. The session is at the very start - information-gathering phase only.
- The tool and its handover document have been located and their paths noted (see below).

---

## EXACT NEXT STEP

1. Ask Max which specific video file he wants to process.
2. Open the method document (`retake_cleaner_v06_tomemex.md`) and walk through the workflow with that concrete file - likely: ensure Deepgram API key is set, run the transcription, review the indexed output, then collaborate on the `cuts.py`.

---

## OPEN QUESTIONS (awaiting Max)

- Which video file should be processed?
- Is a Deepgram API key already available/configured, or does that need setup?
- Any specific constraints (e.g. output format, target length, things to preserve)?

---

## KEY PATHS / IDS

| What | Path |
|---|---|
| Retake Cleaner tool | `C:\claude_base\tools\retake_cleaner\retake_cleaner.py` |
| Full method / handover doc | `C:\claude_base\tools\retake_cleaner\retake_cleaner_v06_tomemex.md` |
| Working directory | `C:\claude_base\.claude\worktrees\angry-darwin-a2eb42` |

---

## GOTCHAS

- **DO NOT use Whisper** for this type of work. It silently merges retakes and abandoned takes, which is the exact opposite of what you want when your goal is to find and remove them. Stick to Deepgram nova-3 verbatim.
- The workflow requires writing a per-video `cuts.py` - it is not a one-click automatic process. Human review of the transcript is a deliberate design choice.
