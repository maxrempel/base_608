# Scribe handover - milestone 3 (~233K tokens)
# session: 20260619_quirky_kare_af0238_ba92a082
# cwd: C:\moma\.claude\worktrees\quirky-kare-af0238
# written: 2026-06-19 11:39:34 by deepseek-v4-pro

# HANDOVER - dnavibe / read.ai transcripts

## GOAL (Max's words)
**"search olnine - it there an API in read ai, so to download all transcripts via api?"**  
Max wants to know if read.ai exposes an API for programmatically pulling full meeting transcripts, not just the summary emails Tony shares. If it does, use that API to retrieve everything and presumably populate the Notion meeting database.

## DECISIONS MADE & WHY
1. **Notion meetings database created** rather than freeform notes - Max was in a meeting and couldn't give detailed instructions ("do something smart"). A structured DB under "DNA Vibe Essentials" now exists, pre-fabbed with date/host/attendees/type/status columns and one row for today's meeting (Perry, ~15 attendees).  
2. **Transcript source is read.ai web app, not email.** Gmail connector revealed that Tony Estrella's "Weekly Team Huddle | Read Meeting Report" emails contain only a summary + "View report" link; the actual transcript lives behind a read.ai login.  
3. **Playwright MCP set up with persistent Bitwarden + Grammarly** - so future logins (like read.ai) can be semi-automated with stored credentials, avoiding the "why ask" friction. Config persists across restarts (profile dir + extension flags).  
4. **Bitwarden not yet active in the running session** - the change requires a Claude Code restart. The read.ai login attempt in the prior session therefore stalled. The new branch must either restart CC or accept that the first login may need to be manual.

## CURRENT STATE
- **Notion:** DB "DNA Vibe Meetings" under DNA Vibe Essentials exists. One row for today's meeting, but no transcript or action items filled in yet.  
- **Playwright MCP:** Now configured to launch with a persistent user-data-dir (`C:\claude_base\playwright_profile`) and the `--load-extension` flags for Bitwarden and Grammarly. Verified both extensions load (service workers logged). Extension IDs are path-derived but stable because the unpacked dir won't change.  
- **Gmail data:** About 19 threads from Tony with read.ai report links, dating back weeks. The links look like `https://app.read.ai/analytics/report/...` (exact pattern unclear but they redirect to a login wall).  
- **Transcripts:** Not downloaded; zero full transcripts in local files or Notion.  
- **Claude configuration:** `C:/Users/maxre/.claude.json` now contains the updated MCP entry (both `C:\moma` and the worktree path). The Playwright launch command includes `--config=...` pointing to `C:\claude_base\playwright_profile\pw_mcp_config.json`.  

## EXACT NEXT STEP
**Search the web for read.ai's developer/API documentation** (public or unofficial).  
- Look for an API endpoint, token/auth scheme, and any export capabilities (list transcripts, get transcript text).  
- If a documented REST API exists, plan to use it with the max@dnavibe.com account or an API key.  
- If no API exists, the fallback is Playwright scraping: log into read.ai, navigate to "My Reports", iterate links, and save the transcript text. Then parse and insert into Notion.  

Also: before any web interaction, **restart Claude Code** so the Bitwarden-armed Playwright MCP activates. Without the restart, the browser will still be a plain temp profile and will require manual login.

## OPEN QUESTIONS (for Max or future resolution)
- **API vs scraping:** Does read.ai offer an official API? If so, does it require a paid tier?  
- **Scope:** All 19 meetings? Only recent ones? Any specific date range?  
- **Notion insertion format:** Should each transcript become a child page under the matching meeting row? Or a plain text block inside the row's notes?  
- **Authentication:** If API, are OAuth tokens or API keys available for the max@dnavibe.com workspace? Could be under "Integrations" or "Settings" inside the read.ai web app.  
- **Bitwarden unlock:** Remember that even after restart, the extension needs to be unlocked by Max once per browser lifetime. That step will still be required before automated login works.

## KEY PATHS & IDENTIFIERS
| What | Path / ID / Note |
|---|---|
| Notion meetings DB | Name: **DNA Vibe Meetings**, under parent page **DNA Vibe Essentials**. Row exists for today's meeting. |
| Playwright profile | `C:\claude_base\playwright_profile` - contains `pw_mcp_config.json`, `extensions/bitwarden`, `extensions/grammarly`. |
| MCP config file | `C:\claude_base\playwright_profile\pw_mcp_config.json` - specifies user-data-dir and extension paths. |
| Claude.json | `C:/Users/maxre/.claude.json` - project keys `C:\moma` and worktree path point to the above config. Backup: `C:/Users/maxre/.claude.json.bak_bw2_<timestamp>`. |
| Gmail search terms | `from:tony@dnavibe.com` + subject contains "Read Meeting Report" / "Weekly Team Huddle" |
| read.ai home / report base | `https://app.read.ai/analytics/home` (login redirects to /signin). Reports likely at `https://app.read.ai/analytics/dashboard/...` or `.../report/...` |
| Attendee list (today) | Perry (host), Max, Troy Reisner, plus 12 others (visible in the screenshot). Stored in the meeting row properties. |

## GOTCHAS & DEAD ENDS
- **Gmail only gives summaries.** The email body ends with a "View report" link; there's no embedded full transcript. Searching the thread files for a direct `.pdf` or file attachment also came up empty.  
- **read.ai login requires interaction.** The sign-in flow uses Google OAuth; simply navigating to a report URL redirects to a login page. Playwright must either use stored cookies (after Bitwarden-assisted login) or the manual login flow.  
- **Bitwarden extension needs a CC restart.** The current branch's Playwright instance will NOT have Bitwarden until CC is restarted. If the new branch restarts CC, the persistent profile will be picked up. If not, it will be a fresh temp profile again - so a manual login would be required.  
- **Extension IDs are path-derived, not Chrome Web Store IDs.** The loaded Bitwarden ID was something like `hcgcgmickjodmmlcbcjmgklhfadjbcec` (may differ each time unpacked, but the directory is stable). This is fine for `--load-extension`, and it should survive restarts.  
- **Grammarly was added** alongside Bitwarden, so the browser now has two extensions. Not a gotcha, just noting for completeness.

---

? **You are the new branch.** Your sole mission: research whether read.ai has an API and, if yes, design a plan to pull all transcripts. If no, confirm and we fall back to Playwright scraping (ideally after a CC restart to leverage Bitwarden). When you have a yes/no, report back.
