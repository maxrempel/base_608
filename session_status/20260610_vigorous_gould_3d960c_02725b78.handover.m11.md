# Scribe handover - milestone 11 (~167K tokens)
# session: 20260610_vigorous_gould_3d960c_02725b78
# cwd: C:\moma\.claude\worktrees\vigorous-gould-3d960c
# written: 2026-06-10 15:08:31 by claude-opus-4-8

# HANDOVER - MOMA / Assemble Video + b-roll work

## GOAL (in Max's words)
- "Broll is inserted as a title slide, hurray, it is 8 sec, but in player (mb player) it plays only for 2-4 seconds... Please fix." ? DONE, confirmed.
- "the video assemble was in mb, which is now hidden. Add the button to the sb." ? button shipped.
- "failed //start failed: HTTP 500/// try fixing, adn if possible test in playwr." ? fixed + tested in Playwright.
- Most recent instruction: **"Ok, alway commit and push, like give the team heads up, 1 min and commit push merge."** This is the live order to act on now.
- Paused/deferred brainstorm Max still wants eventually: how to insert b-rolls into the libretto?spine pipeline, and how to overlay music (he sees music as beyond MOMA's per-line design).

## DECISIONS + WHY
- **B-roll = silent clip line** (no per-line mp3). The player was advancing such lines on a ~2-second no-audio fallback timer while the 8s clip looped muted underneath, so Max only saw the tail. Fix detects "clip + no mp3" (`clipNoAudio`) and drives advance off the video's real `ended` event, with a safety timer derived from true duration. General fix, covers all future clip b-rolls - NOT still-image b-rolls (Max said that's fine, don't cover them).
- **Assemble button ported to storyboard, not mb.** The mixboard went player-only (2026-05-28), so the old assemble button was stripped. Storyboard is now the sole spine assembler, so the button belongs there.
- **HTTP 500 root cause = unmounted G: drive.** `EXPORT_OUT_DIR` pointed at `G:\My Drive\00Main2026\00_rehearsals` (Google Drive, not always mounted), so `os.makedirs()` threw. Repointed to a local always-present folder under the sound data dir. Comment left in code saying repoint here if Max wants Drive again.
- **Stale-renderer fear disproved.** The April-18 renderer was feared to predate b-rolls. Verified end-to-end: it correctly honors the current spine including the BROLL line "earth arrival" and all lipsies.

## CURRENT STATE
- mixboard.html b-roll fix: committed + pushed (**e36b28e**), Max-confirmed "looks good now."
- storyboard Assemble Video button: committed + pushed (**009bb7e**).
- **slideshow_server_v01.py 500 fix: EDITED LOCALLY, NOT YET COMMITTED OR PUSHED.** This is the one outstanding artifact.
- Server already restarted with the fix live (killed old PID 13024, relaunched as PID 52456 on port 8790).
- Verified end-to-end: direct POST returns 200; renderer rc=0, ~18s, 1.87 MB, 50.2s MP4. Playwright clicked the real button in the storyboard UI and watched the status label go rendering ? **DONE 1.87 MB mixboard_assembly_scene9_20260610_142625.mp4**.
- D12 was joined to the bcast team; a c0 FULL HALT has been active across the team. The push was being held pending Max's word because of that halt - Max has now overridden it with "alway commit and push."

## EXACT NEXT STEP
Max just authorized the push and asked you to give the team a heads-up first. So:
1. Post a heads-up to the bcast board (as D12) that you're about to commit+push the server export-dir fix - wait ~1 minute.
2. `git add` slideshow_server_v01.py, commit (describe: repoint EXPORT_OUT_DIR from unmounted G: Drive to local rehearsal_exports, fixes Assemble Video HTTP 500), push to master, and merge as Max said ("commit push merge").
3. Report done in plain English.

## OPEN QUESTIONS
- None blocking - Max cleared the push. The b-roll-into-libretto/spine mechanics and the music overlay layer remain an open brainstorm Max paused, to resume when he wants.

## KEY PATHS / IDS
- Repo: `C:\moma\` (worktree cwd `C:\moma\.claude\worktrees\vigorous-gould-3d960c`), branch master, github.com/maxrempel/moma.
- `C:\moma\sc10\sound_assembly\code\mixboard.html` - mb player (player-only since 2026-05-28).
- `C:\moma\sc10\sound_assembly\code\storyboard_editor.html` - sb, sole spine assembler; vertag now `v39 assemble video button | 2026-06-10`; button id `export-btn`, status span `export-status`, JS `exportVideo()`.
- `C:\moma\sc10\sound_assembly\code\slideshow_server_v01.py` - port 8790 server, **no auto-restart watcher (manual restart needed)**. Line ~212 `EXPORT_OUT_DIR` now `os.path.join(SOUND_DATA_DIR, "rehearsal_exports")`. RENDER_SCRIPT = `render_mixboard_video_v01.py`.
- Endpoints: `POST /api/export_video {scene, quality}` (spawns renderer detached via win_subprocess/wsp); `GET /api/export_video/status?job=`.
- Commits: e36b28e (b-roll player), 009bb7e (sb button).
- bcast: `python C:/claude_base/branch_bulletin/bcast.py whoami/catchup/post`. worklog: `python C:/claude_base/compaction_kb/scripts/worklog.py log`.
- Test scene: arrangement sc9-arr01 ("Taxi cabin"), scene 9, spine L00 BROLL "opening title card" ? L11 BROLL "earth arrival".

## GOTCHAS / DEAD ENDS
- Server has **no file watcher** - any slideshow_server_v01.py change requires a manual kill + relaunch (use windowless/pythonw launch) before testing.
- `G:\My Drive` (Google Drive) is not reliably mounted - never hardcode export/output paths there; use SOUND_DATA_DIR (Nextcloud-synced, always present).
- claude-in-chrome upload is broken on Windows; use **Playwright MCP** for browser/UI testing.
- 2s no-audio fallback timer still intentionally exists for truly audioless non-clip lines (missing mp3, stills) - do not remove it.
- A c0 FULL HALT is/was active team-wide; Max has been waking D12 solo with direct orders that override autonomous-loop ticks. Adviser caution: don't self-authorize irreversible pushes during a halt on "Max woke me solo" reasoning - but Max has now explicitly authorized this push, so it's clear.
- If continuing the autonomous loop: reschedule via ScheduleWakeup with sentinel `<<autonomous-loop-dynamic>>`; to stop, TaskStop the monitor.
