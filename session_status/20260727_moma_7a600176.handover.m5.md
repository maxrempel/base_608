# Scribe handover - milestone 5 (~399K tokens)
# session: 20260727_moma_7a600176
# cwd: C:\moma
# written: 2026-07-27 11:19:50 by deepseek-v4-pro

# HANDOVER: Taygeta S2V local lipsync - pivot to MultiTalk

## GOAL (Max's words)
Produce local, free, lip-synced Anna "lecture" reels (image + audio ? talking video) on Taygeta's RTX 5060 Ti, integrated into MoMA so he can fire jobs and compare local-vs-cloud side by side. "I'm coming here once in a while" - work autonomously, don't ask, just implement. Never self-approve MoMA output; Max approves in the GUI.

After the first batch of local reels came back glitchy/angry, Max's directive: "Search online. There must be some other solutions. The quality parameter is minimized, but we can't really afford long pieces. So maybe there are other models, other engines. Search online. Do a thorough search. There should be a better way."

## DECISIONS MADE + WHY

**1. Local engine = Wan 2.2 S2V FP8 (now REJECTED)**
Chosen because it was the only local image+audio?lipsync model that fit the 16GB card. Free, no per-clip cost. But independent testing rates it "cannot be recommended in its current state" - produces jumpy mouth motion, angry over-expression, and multi-window exposure drift. Max's own review: "still pretty bad... changed a little but still very bad." The earlier good movie reels were made with Wan 2.2 **i2v** (silent animation), not S2V (audio-driven talking). Different models.

**2. Shift to MultiTalk (DECISION, not yet executed)**
After the research confirmed Wan S2V is a dead end, the decision was to switch to **MultiTalk** - a purpose-built talking-head engine that runs on the same WanVideoWrapper node suite already installed on Taygeta. It treats the face as "digital makeup on your existing still" rather than repainting it, which directly addresses the angry-over-expression problem. It handles single-face clips under 15s well and fits 16GB. **InfiniteTalk** is the escalation if MultiTalk feels stiff.

**3. Render-appliance architecture**
The MoMA worker lives on Pine (with all other MoMA workers). Taygeta is a dumb render box reached by SSH/SCP. ComfyUI stays bound to 127.0.0.1:8188 - never exposed. This keeps MoMA whole and unchanged.

**4. Single-window vs multi-window**
Multi-window S2V had both a seam-offset bug AND exposure drift (window 2 comes out darker/harsher). The fix was single-window mode (one long render, no seam). QC-verified clean end-to-end at 161 frames. But the base model quality was still poor, so this fix is academic for now - the engine is being replaced anyway.

**5. Aspect matching + audio quality**
The original worker forced 480?480 square (squashing Anna). Fixed: `dims_from_still()` reads the still's PIL aspect and computes width at target height, rounded to /16, long-side clamped to 832. For 16:9 stills ? 832?480. Original audio was 16kHz/62kbps (phone quality) because the S2V encoder input was muxed as the final soundtrack. Fixed: re-mux 48kHz/192k AAC from the original source audio.

**6. Progress ring + ETA in ReelMaker**
Built and committed: a compact circular SVG progress ring per reel in the /lipser tab, with percent in the center and "~N minutes left" underneath. Self-calibrating EMA (`_expected_secs` / `_record_actual`) learns true render speed from each finished reel. Dashboard auto-refreshes every 3s. Cold-start ETA = 3300s reference.

**7. No-parallel-render guard**
Code-enforced: `_comfy_busy()` queries ComfyUI `/queue`; if a render is already running or pending, the worker refuses to fire a second and requeues/rechecks.

## CURRENT STATE

**What works and is durable:**
- Taygeta (tay) is online at 192.168.1.142, Ubuntu 24.04, SSH key-only (sol_key), LAN-locked firewall, heartbeat monitor active on Healthchecks.io
- green24 drive (24TB exFAT, Windows+Unix universal) mounted at /mnt/green24
- ComfyUI installed on Taygeta with torch 2.11.0+cu128, all WanVideoWrapper/KJNodes/VideoHelper custom nodes
- `combo_s2v_local_worker.py` is built, committed, and registered in moma_restart.py + moma_worker_watch.py - the full MoMA integration plumbing works
- aspect-match, 48kHz audio re-mux, single-window mode, progress ring, no-parallel guard - all committed + pushed to master
- The worker correctly copies cloud prompts and forwards them to the render
- Genomics venv is installed (pysam/whatshap)
- 15 approved Lesson 1 Anna stills live in `tape1select_output/`

**What is broken:**
- **The S2V render engine produces bad quality** (jumpy mouth, angry expression) - Wan 2.2 S2V FP8 is the wrong model for this task
- **The Lesson 1 batch is paused** - jobs 3453-3461 held (output_status='pending'), 3452 interrupted. Nothing is rendering.
- **Taygeta's ComfyUI queue is empty** - GPU idle, ready for the new engine

**What was researched but NOT yet done:**
- The MultiTalk weights download and workflow setup
- A working MultiTalk Anna test render
- Restarting the batch with the new engine

**What the background research agent found before the session died:**
- MultiTalk runs on the already-installed ComfyUI-WanVideoWrapper (Kijai's node suite)
- It needs: MultiTalk model weights (HuggingFace), a specific workflow JSON
- Independent testing: MultiTalk is the champion for single-face <15s clips
- The weights are NOT yet downloaded to Taygeta
- The agent's full findings were lost when the session exited (agent a829771be53de6e6b failed)

**What is blocking but separate:**
- Tailscale auth for Mike & Liz Docker containers - Max must click https://login.tailscale.com/a/16b7e2f018827
- Telepathy Tape 1 per-line audio via sass - not the current focus; needs narration text location

## EXACT NEXT STEP

**Complete the MultiTalk research and install, then test one Anna reel before re-batching.**

1. Research MultiTalk ComfyUI setup (the previous agent died; redo the search):
   - Find the exact HuggingFace model files needed
   - Get a verified working workflow JSON (image + audio ? talking video)
   - Check for any additional dependencies or custom nodes beyond the already-installed WanVideoWrapper

2. Download MultiTalk weights to Taygeta (throttled - protect Max's internet)

3. Build a working MultiTalk render on Taygeta using one real Lesson 1 Anna still + one real per-line audio (reuse spot06 = cam_right.png + anna_005_spot05.wav, the ~8.4s line - cloud job 3407)

4. QC the test render: must be calm expression, smooth mouth, no jitter. If good, rewire `combo_s2v_local_worker.py` to call the MultiTalk workflow instead of S2V, and re-batch the Lesson 1 opening (twins of cloud 3403-3412).

5. If MultiTalk also fails: try InfiniteTalk as escalation.

6. Do NOT self-approve any output - queue for Max to review in the /lipser GUI.

## OPEN QUESTIONS (awaiting Max)
- MultiTalk vs InfiniteTalk - we decided MultiTalk first, but Max may want InfiniteTalk if MultiTalk is stiff
- The Tape 1 narration text location for the deferred audio task
- Whether to proceed with Mike & Liz Docker containers (Tailscale auth pending)

## KEY PATHS, IDs, COMMANDS

**Taygeta access:**
- SSH: `ssh -i ~/.ssh/sol_key maxre@192.168.1.142`
- ComfyUI: `http://127.0.0.1:8188` (on Taygeta, not exposed)
- GPU: RTX 5060 Ti 16GB, driver 595, CUDA 13.2
- Working dirs: `~/ComfyUI/`, `~/ComfyUI/models/`, `~/setup/scripts/`

**MoMA files (on Pine):**
- `C:\moma\sc10\combo_runner\code\combo_s2v_local_worker.py` - the local lane worker (committed, needs engine swap)
- `C:\moma\sc10\combo_runner\code\taygeta_s2v_render.py` - repo mirror of the Taygeta render script (will be replaced)
- `C:\moma\sc10\combo_runner\code\moma_restart.py` - worker launcher (s2vlocal registered)
- `C:\moma\sc10\combo_runner\code\moma_worker_watch.py` - watchdog (s2vlocal registered)
- `C:\moma\sc10\combo_runner\code\runner_core.js` - frontend (progress ring code)
- `C:\moma\sc10\combo_runner\code\runner_core.css` - frontend (progress ring CSS)
- `C:\moma\sc10\combo_runner\code\combo_lipsync_worker.py` - ds/bb worker (edited to exclude s2vlocal jobs)
- `C:\moma\sc10\combo_runner\code\audio_resolver.py` - resolves per-line audio paths
- DB: Cloudflare D1, accessed via `from moma_db import connect_db, fire_job`
- Lesson 1 cloud reels: jobs 3403-3412 (wan26flau, done) - these are the golden reference
- S2V local twins queued: jobs 3452-3461 (3452 interrupted, 3453-3461 held pending)

**Taygeta render scripts:**
- `~/setup/scripts/s2v_render.py` - current S2V graph (with shift node, single-window support)
- `~/setup/scripts/s2v_render_v1_backup_20260727.py` - original (no shift, multi-window)
- `~/setup/scripts/comfy_fix.log` - ComfyUI install log
- `~/setup/scripts/s2v_models.log` - model download log
- `~/setup/logs/comfyui.log` - ComfyUI server log

**Scratchpad (this session):**
- `C:\Users\maxre\AppData\Local\Temp\claude\C--moma\bcc5a95d-de9a-4191-91a5-c73a7734c1a7\scratchpad\` - contains batch scripts, QC frames, refire scripts

**Key DB IDs:**
- Lesson 1 scene: `lesson1`, arrangement_id 42
- Cloud reels: 3403-3412
- S2V test/QC jobs: 3413 (first test, junk), 3414 (720p OOM), 3415 (960x540 OOM), 3416 (timeout loop), 3441 (shift-fix test, junk), 3442 (5s test, junk), 3451 (shift-fix re-test), 3452 (batch start, interrupted)

**Healthchecks monitor:**
- Taygeta heartbeat: uses Sol's old host check (023cf3f6-186a-4af2-9a2f-...) - renamed to taygeta-host
- Sol's other checks (cpu-temp, notion-sync) deleted

## GOTCHAS AND DEAD ENDS

**Already ruled out - do not re-investigate:**
1. **Settings alone won't fix Wan S2V.** We tried: adding ModelSamplingSD3 shift node (8.0), length 81?77, cfg 6.0?5.0, dropping "static" from negative prompt, single-window vs multi-window. Each helped marginally but the base model quality is poor. Independent testers confirm this.
2. **720p and 960?540 both OOM on the 16GB card.** Max safe size is 832?480 (16:9). Higher resolutions need a different model or engine.
3. **The worker's poll ceiling was too low** (80 min ? 160 min) - if a render just disappears from the DB without saving, check this ceiling.
4. **The ds/bb lipsync worker was stealing s2vlocal jobs** - fixed by adding `'s2vlocal'` to its _QUEUE_FILTER exclusion. If future s2vlocal jobs error with "Per-line audio lookup failed: scene 1," it means another worker sniped the job - add the lane tag to that worker's exclusion list.
5. **fire_job does NOT set lipsync_params.** Per-job width/height/steps must be set by a separate UPDATE _after_ fire_job, then flip output_status='queued'.
6. **D1Client has no `.query` method.** Use `from moma_db import connect_db; c=connect_db(); c.execute(...).fetchone()` - returns dict-like rows.
7. **Wan 2.2 S2V ? Wan 2.2 i2v.** Max's good earlier movie was made with i2v (silent animation). S2V is the audio-driven variant and is genuinely inferior. Don't conflate them.
8. **Tailscale on Taygeta installed but NOT authed.** Remote Docker access for Mike/Liz is blocked until Max clicks the auth link.
9. **Never run the local render on placeholder inputs.** Max: "Such a shame you made me wait for half an hour and we ended up with junk." Confirm the real still + real audio BEFORE committing a long render.

**Session identity:** You are ? K2b (auto-renamed from K2 to avoid collision). Lead every reply to Max with that tag. No Unicode, plain ASCII only, bold H1 TLDR blocks, pingpong ~100-200 chars. Never self-approve MoMA output. Never call Opus API / spawn Opus sub-agents. Commit+push to master after every working edit. MoMA images fire via fire_image at quality 'low', sequential only.
