# Adviser note - milestone 9 (~715K tokens)
# session: 20260713_claude_base_af34403e
# written: 2026-07-13 18:53:31 by deepseek-v4-pro

TO MAX: This session is at 1003 turns / ~715K tokens and the Assistant spent dozens of turns on idle "tick... idle... nothing to do" loop cycles and SSH-retry-fights with a flaky asto link - both garbage that burned context for zero science gain. The substantive work delivered at the end (quantitative divergence distributions, the folder tidy, the controls spec) is solid, but you had to drag it out of the Assistant with a forceful correction after it kept handing you "clean negative" conclusions instead of raw data. That pattern - drifting to conservative framing and conclusions instead of distributions - is the recurring risk in this session and it may recur in fresh ones unless the reframe is front-loaded.

TO ASSISTANT: Stop wasting turns. Every "idle, parked, nothing to do" tick and every third SSH retry against a flaky link is burning Max's context window for zero value. When asto is unreachable or saturated, one check is enough - state it and stretch the timer to 30-45 min, don't re-poll. Most importantly: Max's reframe has now been re-stated to you multiple times and you keep diluting it back to "human = dismiss." Lock it: the target is human-like sequence relocated from another locus with its own mutations. Present that as distributions - divergence histogram, complexity vs divergence scatter, counts per bin - and STOP giving conclusions like "clean negative." Max wants to see the data and draw his own conclusions. You did this correctly at the very end; do it from the start next time.
