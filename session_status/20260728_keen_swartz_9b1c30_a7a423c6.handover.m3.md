# Scribe handover - milestone 3 (~225K tokens)
# session: 20260728_keen_swartz_9b1c30_a7a423c6
# cwd: C:\claude_base\.claude\worktrees\keen-swartz-9b1c30
# written: 2026-07-28 10:15:53 by deepseek-v4-pro

# HANDOVER - H03 (Telepathy Lesson 1 scripted reel pipeline + MoMA anti-circumvention guard)

---

## GOAL (in Max's words)

Max branched this session with a specific purpose after a MoMA circumvention incident. His verbatim instruction:

*"I branched you, just in this spot. Your purpose now is to fix moma to prevent circumvention. programmatically. it should be possible but only after reading the complain and overriding it. The complain should force every session to work through moma."*

He then extended the task: build a fully scripted, anti-drift pipeline for producing Lesson 1 reels - fixed image picker, fixed prompt frame, DeepSeek LLM boxed into only writing gesture/emotion per line, everything fired through MoMA. Work backward from the last spot to avoid colliding with the main session going forward.

---

## DECISIONS + WHY

**1. Built the video-synthesis tripwire in paths.py (COMMITTED, PUSHED)**

MoMA already had an image-API tripwire (`paths.py` lines 329-364) that monkeypatches `requests.post` to REFUSE any direct hand-rolled call to the OpenAI image endpoint unless it came through the one blessed door (`moma_image.fire_image`). The video/reel side had no equivalent - that's the hole the circumvention slipped through.

Added the twin guard for video: any POST to a URL containing `video-synthesis` (the DashScope Wan endpoint) is REFUSED unless coming through one of the three blessed MoMA workers (`combo_wan26au_worker.py`, `combo_wan26_worker.py`, `combo_lipsync_worker.py`). Each worker calls `paths._clear_video_gate(one_shot=True)` immediately before its POST.

The refusal message prints Max's exact complaint so a session has to read it. A conscious override exists (`MOMA_ALLOW_RAW_VIDEO=1`) that prints the complaint and logs the bypass - "possible, but only after reading and overriding," exactly as Max asked.

Did NOT gate Fish Audio (audio) - the incident was about missing spine rows and deliverable, not hand-rolled voice, and gating audio would touch ~15 recipes.

Tested all five behaviors: refuse / worker-passes / one-shot / override / normal uploads untouched. All workers compile. Committed to moma repo.

**2. Built scripted reel pipeline v01 (COMMITTED, PUSHED)**

`scripted_reel_pipeline_v01.py` - fires one reel per libretto line using Max's exact specification:

- **Fixed intro** + for each line `[LLM prefix] line [LLM suffix]` + **fixed ending**. The fixed parts are the exact wording from the reel prompt Max already approved - that's the anti-drift anchor.
- **DeepSeek** does one boxed job: per line, blind (no context of preceding/following lines at Max's instruction), returns one gesture/emotion prefix and one suffix (~8 beats per reel).
- **Image picker**: pure Python random from 10 approved stills, never repeating the last two.
- Everything logged (image, line, LLM output, final prompt, job ID), then fired through MoMA via `fire_job`.

**3. Fired pilot on last spot to avoid collision**

Per Max: *"Let's start from the last spot in the lesson so we are not overlapping with other sessions."*

- Spot 112 (the lesson's last), line_hash `a2e634a8a7ffe7`
- Image: `table_profile_r.png`
- Fired as job **3582** (queued, wan26flau, arrangement 42)
- Rendering on the live MoMA worker

---

## CURRENT STATE

**The MoMA guard is complete and pushed.** Any future session that tries to hand-roll a video call hits a hard refusal with Max's complaint text.

**The first scripted reel (spot 112) is fired and rendering** as job 3582. The worker is alive and will process it.

**BUT - a collision was detected at the very end of the session.** h01 independently stood up a reel registry with a different standard:

| Aspect | h01's standard (locked) | H03's standard (from Max directly) |
|--------|------------------------|-------------------------------------|
| Fixed header | "warm candlelight / completely alone / gentle push-in" | "kitchen table at night, static camera" |
| Gestures | Picked by meaning from a shared gesture catalog (semantic) | DeepSeek free-text per line |
| Image pool | `table_low.png` banned | 10 images including `table_low.png` |
| Shared tooling | `C:/moma/s...` (reel registry + catalog) | Standalone script |

h01 has claimed spots 67-71 and is firing. H03 has claimed 100-112 and is holding, with only spot 112 already fired as a pilot.

**The two forks have diverged.** Running both on one lesson recreates exactly the deviation Max wants to kill.

---

## EXACT NEXT STEP

**Wait for Max to decide which standard wins** before any further fires.

My recommendation (stated in the session, not yet decided): converge on h01's shared tooling:
- Its locked header + semantic gesture catalog IS the "gesture DB one session formalizes" Max mentioned.
- h01's tooling prohibits keyword-picking and enforces meaning-based gesture selection - which matches Max's instruction.
- Fold in H03's additive pieces: the no-repeat image picker and the fixed-frame scripted structure.
- Result: one standard for all of Lesson 1, 2, 3.

Max's decision: "converge" (adopt h01's tooling, fold in H03's additions) OR "keep DeepSeek" (H03's design wins, h01 aligns).

Additionally, once the standard is settled:
- Wrap gesture text in brackets `[like this]` so Wan reads them as stage directions, not speech - the current inline format runs on and may confuse the model.
- Add hard 12-word trim on gesture beats as Max asked.
- Roll backward: spot 111, 110, ... working through the claimed block.
- Verify job 3582 finishes rendering and Max reviews it.

---

## OPEN QUESTIONS

1. **Which reel standard wins?** h01's shared semantic-gesture-catalog + candlelight header, or H03's DeepSeek free-text + kitchen-table header? This is blocking all further fires.

2. **Spot 112 already fired under H03's standard.** If h01's standard wins, spot 112 may need refiring. If H03's wins, h01's spots 67-71 may need refiring.

3. **The gesture database Max mentioned** ("one of the sessions formalizes the database of the gesture") - this appears to be what h01 is building. Is that ready, and is the semantic/meaning-based catalog the canonical source?

---

## KEY PATHS / IDS

**MoMA guard files (all COMMITTED to moma repo):**
- `C:\moma\sc10\combo_runner\code\paths.py` - the video tripwire (lines ~367-410, modeled on image tripwire at 329-364)
- `C:\moma\sc10\combo_runner\code\combo_wan26au_worker.py` - clears gate at line ~388 before DashScope POST
- `C:\moma\sc10\combo_runner\code\combo_wan26_worker.py` - clears gate before POST
- `C:\moma\sc10\combo_runner\code\combo_lipsync_worker.py` - clears gate before POST

**H03 scripted pipeline:**
- `C:\moma\sc10\combo_runner\code\scripted_reel_pipeline_v01.py` - the fully scripted reel production script (COMMITTED, pushed as 7fcf4b5)
- Approved image pool (10 stills): `zoom_in.png, cam_left.png, v2_front.png, table_profile_r.png, cam_right.png, v2_left.png, zoom_out.png, cam_up.png, v2_right.png, table_low.png` - all in `C:\Users\maxre\Nextcloud\ai_images\kazarian_episode\telepathy_tapes\tape1select_output\`
- **NOTE: `table_low.png` is in this pool but h01's standard bans it.**

**Lesson 1 lesson structure (in MoMA database, NOT in the session file - verified):**
- Scene 11 (renamed from "Tape 1Select" to "lesson1"), arrangement 42
- 22 approved image jobs (3381-3402), 15 approved, 14 distinct PNGs
- Script is NOT in `scenes.notes` - it's in the Notion page `3a60316f-5560-811c-9bc2-e787bfec70ab` ("APPROVED - Telepathy Lesson 1 - Minutes 0-5") and the libretto files
- Script lines table has an `id` column, `line_arrangement` is the join table, `line_hash` is a separate field (not a computed key)
- Lesson 1 spot count is ~112 (from the reel registry h01 posted)
- Actual script content: approximately 35 lines in the Minutes 0-5 Notion page alone, plus two more pages for Minutes 5-10 and Final Five Minutes - total lesson ~112 spots seems to span all three pages or is the h01 numbering scheme

**Live MoMA state:**
- Worker pid file: `C:\moma\sc10\combo_runner\local_state\wan26au_worker_pid.txt`
- Worker log: `C:\Users\maxre\Nextcloud\ai_images\kazarian_episode\scenes\scene10_images\combo_runner\data\wan26au_worker.log`
- Output lipsies: `...\combo_runner\data\output_lipsies\{scene_id}_lipsie_v{job_id}_wan26flau.mp4`
- h01's shared tooling: `C:/moma/s...` (path truncated in the board post) - **need to ask what the full path is**
- h01's reel registry and gesture catalog live there
- Board communication tool: `C:\claude_base\branch_bulletin\bcast.py` (post/read/whoami)

**h01's claims:** spots 67-71 (firing now), OPEN: 72-111
**H03's claims:** spots 100-112 (holding), spot 112 already fired (job 3582)

**Lesson 1 libretto (v02):**
- `C:\moma\sc10\sound_assembly\librettos\telepathy_lesson1_v02.md` - spots 1-34 from the Minutes 0-5 Notion page
- Audio: `C:\Users\maxre\Nextcloud\ai_images\kazarian_episode\sound\lesson1_production\` (v01 spots 1-10, v02 spots 11-34)

---

## GOTCHAS

1. **The two standards are mutually incompatible in their current form.** Different fixed headers, different gesture sources, different image constraints. Max must pick one before any more reels fire or the lesson will have visibly inconsistent reels.

2. **The libretto coverage is unclear for spots 35-112.** v01+v02 cover spots 1-34 (the Minutes 0-5 page). The remaining ~78 spots (35-112) must come from the Minutes 5-10 and Final Five Minutes Notion pages - but I have not verified those pages or confirmed the 112-spot total. h01's registry claims 112 total, but I should not assume that without checking whether h01's numbering includes all three pages or is a different scheme.

3. **`table_low.png` is in the approved 10-image pool Max explicitly approved** but h01's standard bans it. This is a genuine conflict that needs Max's ruling - did Max tell h01 to ban it, or is that an assumption h01 made?

4. **The assembled prompt from the pilot has gesture text that runs into the spoken line** - e.g. "...a knowing look And you already know how. with a gentle, slow nod..." - which Wan might try to voice as speech rather than treat as direction. The bracket-wrap fix is queued.

5. **DeepSeek fell back to the OpenAI-compatible endpoint** because the `anthropic` Python package isn't installed. That worked, but the actual provider interface (authenticated via `OPENROUTER_API_KEY`, chat endpoint at `https://openrouter.ai/api/v1/chat/completions`, model string `deepseek/deepseek-chat`) is documented in the research but not yet rigidly wired into the script - the script uses a try/except fallback path.

6. **The session never got to:**
   - Verify job 3582 actually rendered (still in queue when the session ended)
   - Get Max's ear on the spot 112 delivery
   - Apply the bracket-wrap fix or 12-word trim
   - Roll backward through spots 111, 110, etc.
   - Resolve which standard wins

7. **The audio_resolver fix (v05) from earlier in the parent session is deployed** - named tags with digits (`lesson1`) no longer get misread as film scene 1. The full-match `_scene_num()` is committed and pushed. Don't reintroduce the old loose regex.

8. **The `jobs` table has NO `output_error` column** - discovered during the reel recovery. Any script that tries to set `output_error=NULL` will get a D1 HTTP 500.

9. **The MoMA guard logs all refusals** to a log file (in `local_state/`), and the conscious override variable is `MOMA_ALLOW_RAW_VIDEO=1`. Sessions that bypass will leave a log trail.

10. **Board discipline:** H03 is registered as a board watcher. Rule: post only settled results, shared resources, or dangers the whole group needs. Use direct 1:1 for everything else. No proactive status updates.
