# Scribe handover - milestone 1 (~103K tokens)
# session: 20260707_nice_maxwell_b61025_1d2a748f
# cwd: C:\claude_base\.claude\worktrees\nice-maxwell-b61025
# written: 2026-07-07 09:21:21 by deepseek-v4-pro

# HANDOVER

## GOAL (Max's words)
Max is frustrated that all sessions produce MD files but never give him a clickable link to open them. He knows HTML files open perfectly via `file://` links in his browser. He wants to try the same with an MD file - take any known MD (e.g., Memory MD) and provide a `file://` link to it to see what happens when clicked.

Broader goal: find a way for Claude sessions to give Max **clickable links** to output files that actually open in the browser without fuss.

## DECISIONS + WHY
- **Used a `file://` link to MEMORY.md** - this is the direct analogue of what works for HTML files. The hypothesis: let's just test whether the browser handles it.
- **Predicted the outcome upfront**: Chrome will either show raw text or prompt a download. This was communicated to Max immediately - no false expectations.
- **Offered a fallback**: an HTML wrapper that renders any MD file in-browser with proper formatting (headings, lists, etc.). This would be reusable across all sessions.

## CURRENT STATE
- A `file://` link to MEMORY.md was provided.
- The link points to: `file:///C:/Users/maxre/.claude/projects/C--claude-base/memory/MEMORY.md`
- **Max has NOT yet reported what happened when he clicked it.** The experiment is in flight.

## EXACT NEXT STEP
Wait for Max to report back on the `file://` MD link test:
- Did Chrome show raw text? Offer a download? Something else?
- Does he want "open somehow" or "open beautifully formatted"?

If he wants beautiful formatting ? build a reusable HTML wrapper that renders arbitrary MD in the browser. If "somehow" is enough ? the raw `file://` link may suffice.

## OPEN QUESTIONS
1. What actually happened when Max clicked the `file://` MD link?
2. Does Max need **pretty formatting** (headings, lists, code blocks rendered) or just **any way to open it**?
3. If the HTML wrapper approach is needed - should it be a single static HTML file that fetches and renders MD, injected into the project root?

## KEY PATHS / IDS
- **Memory MD path**: `C:/Users/maxre/.claude/projects/C--claude-base/memory/MEMORY.md`
- **Project root** (cwd from session): `C:\claude_base\.claude\worktrees\nice-maxwell-b61025` (this is a worktree; actual project is at `C--claude-base`)

## GOTCHAS
- Browsers don't natively render Markdown - they'll show raw text or force a download when given a `file://` link to an `.md` file.
- The `file://` approach for HTML works because browsers are HTML renderers; MD is not HTML. This fundamental asymmetry is the root problem.
- Any solution must be a single click for Max - no "open this, then drag it into that."
