# Scribe handover - milestone 5 (~395K tokens)
# session: 20260619_quirky_kare_af0238_ba92a082
# cwd: C:\moma\.claude\worktrees\quirky-kare-af0238
# written: 2026-06-19 12:54:05 by deepseek-v4-pro

# HANDOVER: DNA Vibe Meeting Transcript Pipeline

## GOAL (in Max's words)
"Collect all info on dnavibe in one place" - specifically, build a system that pulls Read AI meeting transcripts into a canonical Nextcloud dnavibe hub, then make it run automatically every week (deterministic, no AI, idle-time only, not right after wake).

---

## DECISIONS MADE + WHY

**1. Notion meetings DB was built, then bypassed for transcripts**
- Created "DNA Vibe Meetings" database under DNA Vibe Essentials with columns (Date, Host, Attendees, Type, Status, etc.) and today's row (Perry hosting, 15 attendees).
- But Max opted NOT to pollute Notion with full transcripts. Transcripts live on disk in Nextcloud instead. The Notion DB exists and could still be used for meeting metadata/attendee tracking separately.

**2. Transcript source: Read AI API, not Gmail scraping**
- Gmail has ~19 "Weekly Team Huddle | Read Meeting Report" emails from Tony Estrella, but those only contain summaries + links to read.ai. Full transcripts require read.ai login.
- Read AI has a public REST API (OAuth 2.1) + an official MCP server. Endpoint: `https://api.read.ai/mcp` (NO trailing slash - slash 404s).
- Connected the readai MCP to this worktree, authenticated as max@tamza.com via "Sign in with Zoom" (that's the account that owns the meetings, NOT max@dnavibe.com which says "request access" via Microsoft, and NOT max.rempel2@gmail.com even though it lists them - the Zoom login is what ties them together).

**3. Account identity chain**
- Read AI account = created via "Sign in with Zoom" ? Zoom = max@tamza.com ? that account holds the 8 meetings (5 dnavibe, 3 personal).
- max@dnavibe.com is a separate Read workspace (admin: likely Tony Estrella) - access requested but not yet granted. That workspace holds the other ~14 weekly recaps Tony shared. Currently out of reach.

**4. Storage location: Nextcloud dnavibe hub**
- Max explicitly said Nextcloud over OneDrive or Google Drive.
- Created: `C:\Users\maxre\Nextcloud\dnavibe\meeting_transcripts\`
- 5 DNAvibe meetings in root, 3 personal in `_personal_nondnavibe\` subfolder.
- Format: Markdown with summary, topics, action items, key questions, full speaker-labeled transcript.

**5. Weekly downloader: deterministic, self-gating**
- Built as a standalone Python script (`readai_weekly_download.py`) - no LLM calls, no cost.
- Uses Ory OAuth with self-refreshing token (rotates on each run, stored in ssh folder).
- Windows scheduled task runs hourly but self-gates: only fires when ?6.5 days since last run AND ?1 hour uptime AND machine is idle. Uses `pythonw.exe` (no console window).
- Filters for meetings with @dnavibe participants; skips already-downloaded meetings.

**6. Bitwarden + Grammarly in Playwright**
- Side quest during this session: configured Playwright MCP to launch with a persistent profile that auto-loads both Bitwarden (v2026.5.1) and Grammarly (v14.1304.0) extensions.
- Config lives at `C:\claude_base\playwright_profile\pw_mcp_config.json` and is patched into `.claude.json` for the actual worktree key (`quirky-kare-af0238`).
- Verified both extensions load together.
- Note: rotating the Read token for the weekly script means the interactive `readai` MCP may need a one-time `/mcp` re-auth if used again.

---

## CURRENT STATE

**Done:**
- ? Notion "DNA Vibe Meetings" DB created under DNA Vibe Essentials (today's row with attendees ready).
- ? Read AI MCP connected and authenticated to this worktree as max@tamza.com (Zoom login).
- ? 8 transcripts downloaded to `C:\Users\maxre\Nextcloud\dnavibe\meeting_transcripts\` (5 dnavibe, 3 personal separated).
- ? Converter tool built: `C:\claude_base\tools\readai_transcripts\convert_meeting.py` + `extract_from_session.py`.
- ? Weekly downloader built: `C:\claude_base\tools\readai_transcripts\readai_weekly_download.py`.
- ? Windows scheduled task `ReadAI_Weekly_Transcript_Download` created and Ready (hourly trigger, self-gating).
- ? Job registered in `infra_map_tomemex.md` (Pine section) and that registry is `@`-imported into `global2.md`.
- ? Playwright Bitwarden + Grammarly persistence configured and verified.
- ? All tools committed to claude_base and pushed.

**In flight / incomplete:**
- ? **Healthchecks.io monitoring** - Max said "yes" to adding a Healthchecks.io ping so he gets a Telegram notification if the weekly pull fails. This was the very last exchange and was NOT implemented yet.

**Not yet available:**
- Today's June-19 huddle transcript was not yet in the Read API at time of pull (still processing on Read's side). The weekly script will pick it up on its next eligible run (~Saturday or later).
- The ~14 other weekly recaps in Tony's dnavibe Read workspace (requires admin to approve max@dnavibe.com access).

---

## EXACT NEXT STEP

**Add Healthchecks.io monitoring to the weekly downloader script.**

What this means concretely:
1. Create a Healthchecks.io check (or use an existing one if Max has a standard pattern) - the check URL goes in the script or in an env/secrets file.
2. Modify `readai_weekly_download.py` so it:
   - Pings `/start` at the beginning of a run.
   - Pings the success/fail URL based on outcome.
   - This ties into Max's existing Telegram notification pipeline.
3. Commit and push.

The script currently just logs quietly - no alerting if it fails silently.

---

## OPEN QUESTIONS (awaiting Max)

None explicitly stated. The "yes" to Healthchecks.io is the only un-actioned item.

Implicit open question: does Max want to pursue getting access to the dnavibe Read workspace (max@dnavibe.com) to pull the other ~14 weekly recaps, or leave it at the current 5? The session ended with "No, that's good enough" for the immediate pull, but the weekly script only sees max@tamza.com's account.

---

## KEY PATHS, IDS, COMMANDS

**Nextcloud hub:**
- `C:\Users\maxre\Nextcloud\dnavibe\meeting_transcripts\` - 5 dnavibe transcripts
- `C:\Users\maxre\Nextcloud\dnavibe\meeting_transcripts\_personal_nondnavibe\` - 3 personal

**Tools (in claude_base):**
- `C:\claude_base\tools\readai_transcripts\readai_weekly_download.py` - the weekly auto-downloader
- `C:\claude_base\tools\readai_transcripts\convert_meeting.py` - converts Read API JSON to Markdown
- `C:\claude_base\tools\readai_transcripts\extract_from_session.py` - extracts meetings from session JSONL

**Credentials:**
- Read OAuth token (self-refreshing) stored in ssh folder: `C:\Users\maxre\Nextcloud\zSyncMain\ssh\readai_token.json`
- Playwright persistent profile: `C:\claude_base\playwright_profile\`
- Playwright MCP config: `C:\claude_base\playwright_profile\pw_mcp_config.json`

**Scheduled task:**
- Name: `ReadAI_Weekly_Transcript_Download`
- Trigger: hourly, but script self-gates
- Command: `C:\Users\maxre\AppData\Local\Python\pythoncore-3.14-64\pythonw.exe C:\claude_base\tools\readai_transcripts\readai_weekly_download.py`
- Visibility: hidden (pythonw.exe = no console)

**Notion:**
- DNA Vibe Meetings DB (under DNA Vibe Essentials) - has today's row with 15 attendees, Perry hosting, ready for metadata/notes

**Read AI:**
- MCP endpoint: `https://api.read.ai/mcp` (no trailing slash!)
- REST endpoint: `https://api.read.ai/v1/meetings`
- Auth server: `authn.read.ai` (Ory OAuth)
- Account: max@tamza.com (Zoom login)

**Registries:**
- `C:\claude_base\infra_map_tomemex.md` - Pine section has the weekly downloader job
- `C:\Users\maxre\Nextcloud\claude_md_synced\global2.md` - `@`-imports the infra map

---

## GOTCHAS

1. **Read token rotation**: The weekly script rotates the refresh token on every run. The interactive `readai` MCP shares the same credential store, so the MCP may break after the script runs - a one-time `/mcp` re-auth fixes it. The script itself is unaffected (it owns its own token cycle).

2. **Trailing slash on Read API**: `https://api.read.ai/mcp/` 404s. Must be `https://api.read.ai/mcp` (no slash). Already fixed in config.

3. **Worktree key matters**: The MCP and Playwright config were patched into `.claude.json` under the specific worktree key `C:\moma\.claude\worktrees\quirky-kare-af0238`. Other worktrees won't see readai or the Bitwarden Playwright profile unless separately configured.

4. **Today's meeting not yet downloaded**: June-19 huddle wasn't in the Read API at pull time. The weekly script will catch it on its first eligible run (?6.5 days later + idle + ?1h uptime). If needed sooner, manually run the script or use the interactive MCP.

5. **dnavibe workspace access**: The bulk of Tony's shared recaps (~14) are in a separate Read workspace accessed via max@dnavibe.com, which currently says "request access." max@tamza.com only sees meetings where the Zoom host/participant ties to that account directly.

6. **The Notion meetings DB exists but has no transcripts in it** - by Max's choice. It has today's meeting row with attendee metadata. Future sessions could still use it for meeting tracking separate from transcript storage.
