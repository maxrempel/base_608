# Scribe handover - milestone 13 (~197K tokens)
# session: 20260617_hungry_mayer_737e63_0588553c
# cwd: C:\claude_base\.claude\worktrees\hungry-mayer-737e63
# written: 2026-06-17 13:10:36 by deepseek-v4-pro

# HANDOVER - Mike DC Calendar Update Monitor (branch G2monitor)

## GOAL (Max's own words)
> "Develop a monitor as a part of monitors, it should panic if scheduled Mike's calendar update is missed by 1.5 days. The update should run daily at 9am or any time that day if computer was asleep."

## DECISIONS MADE + WHY
- **The daily autopilot** that fills Mike's calendar is already running via a self?wake (id `44823c93`). It fires at 09:00 every day. If the computer was asleep at 09:00, the wake fires when the computer wakes up later that day - which satisfies "any time that day if computer was asleep".
- **The monitor must check whether a calendar update has actually occurred**, not just whether the wake fired. The safest approach is to check the last fire time of wake `44823c93` (the wake system records when it last triggered). However, a better signal is a **marker file or healthcheck ping** written by the autopilot itself after a successful fill. That marker does *not* exist yet; the monitor will need to create one or rely on the wake's last?fire time.
- **"Panic"** likely means an alert to Max (email or healthchecks fail). The exact channel is not yet chosen - this is an open question.
- All the existing calendar?refill infrastructure and instructions live in:
  - Wake system: `C:\claude_base\tools\wake_listener\wakeup.py`
  - Wake id: `44823c93` (daily, 09:00, self?terminates after July 31)
  - Method doc: `C:\claude_base\tools\mike_dc_calendar\mike_dc_calendar_method_v01_tomemex.md`

## CURRENT STATE
The session just received the command to create the monitor. **No work has been done yet on the monitor itself.** The wake + autopilot are already functional; they were validated in the preceding conversation.

## EXACT NEXT STEP (for the cold session)
1. **Investigate the wake system's ability to report last?fire time** - run `python C:\claude_base\tools\wake_listener\wakeup.py status 44823c93` (or whatever subcommand reveals last fire). Confirm that the wake's last?fire timestamp is reliable and updated even when the computer wakes up late.
2. **Look for existing "monitors" infrastructure** - check if a directory like `C:\claude_base\tools\monitors\` already exists, and whether a monitoring scheduler is already running (a cron, another wake, or a service). If not, decide whether the monitor itself will be a standalone script invoked by a wake.
3. **Write the monitor script.** The script should:
   - Read the last fire time of wake `44823c93` (or the timestamp of a success marker that the autopilot will write - if you add that marker, you must also modify the wake's autopilot instructions to write it).
   - If the last fire time is older than 1.5 days (36 hours) ? **panic** (send an alert). The alert mechanism must be chosen (see open questions).
   - If the last fire time is within tolerance, do nothing.
4. **Integrate the marker into the autopilot wake message if needed.** The current wake message (command passed to `wakeup.py add`) is a long prompt that instructs the session to fill the calendar. You might add a step at the end: "after a successful fill, write a timestamp file to `C:\claude_base\tools\mike_dc_calendar\last_fill.txt`" or ping a healthchecks URL. Then the monitor checks that marker instead of the wake last?fire time, which is more robust.
5. **Decide how to deploy the monitor** - it must run periodically. A natural choice is another wake (e.g., every hour), or a cron if available.

## OPEN QUESTIONS (awaiting Max)
- **"Panic" how?** Email Max? A healthchecks fail ping? Slack message? Max must specify the alert channel.
- **Does a "monitors" framework already exist?** We haven't searched yet. If a standard pattern for monitors exists (e.g., a script in a specific directory called by a supervisor), we should follow it. This will also determine the file name and return?code expectations.
- **Should the monitor use the wake's last?fire time or a dedicated success marker?** The wake time is simpler but might be inaccurate if the wake fired but the autopilot crashed. A dedicated marker (written inside the autopilot instructions) is safer but requires modifying an already?armed wake. We can start with the wake's last?fire time and later improve to a success marker.
- **Is the monitor only for Mike's calendar, or should it be a generic "wake?liveness" monitor?** For now, the task is specific to Mike's update.

## KEY PATHS / IDS / COMMANDS
- Wake system script: `C:\claude_base\tools\wake_listener\wakeup.py`
- Target wake ID: **`44823c93`** (daily 09:00, self?terminates Jul 31)
- Add/update a wake: `python wakeup.py add --at "..." --every daily --msg "..."`  
- List wakes: `python wakeup.py list`  
- Cancel a wake: `python wakeup.py cancel ID`  
- (You will need to find `status` subcommand; it may be `python wakeup.py status ID` or `python wakeup.py list --verbose`.)
- Mike's calendar method doc: `C:\claude_base\tools\mike_dc_calendar\mike_dc_calendar_method_v01_tomemex.md`
- Mail poll tool (unrelated but still running): `C:\claude_base\tools\mxmail\mass_inbox_poll.py`

## GOTCHAS & DEAD ENDS ALREADY RULED OUT
- **Broken cron lesson**: The previous autopilot used a long?term cron/ScheduleWakeup that *silently never fired*. That's why the calendar froze for 10 days. The daily self?wake (`44823c93`) is the reliable replacement. Never rely on the old cron method again.
- **Fake?reply email bug**: The assistant sent Mike an email that falsely implied he'd written to us. That caused suspicion and is now documented as a permanent never?repeat rule (see `C:\Users\maxre\.claude\projects\C--claude-base\memory\feedback_no_fake_received_message.md`). The monitor task is not affected, but subsequent interactions with Mike must be extremely careful.
- **Congress.gov blocking Playwright**: When filling hearings, the system sometimes gets blocked. Fallback order is: 1) Playwright first (fast), 2) your Chrome instance if Playwright fails. This is relevant only to the fill agent, not the monitor, but documented here so future fills don't get stuck.

## SUMMARY FOR THE COLD SESSION
You are the `G2monitor` branch. Your immediate job: build a small monitor that checks the last fire time of wake `44823c93` (or a success marker) and panics if it's older than 36 hours. Start by poking the wake system for its `status` command, then design the check, choose the alert channel (get Max's decision on that), and write the monitor script. If you add a success marker, you must also update the wake's autopilot instructions so that each fill writes the marker. No code exists yet - you are at square one.
