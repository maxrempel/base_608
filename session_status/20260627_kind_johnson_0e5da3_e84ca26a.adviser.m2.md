# Adviser note - milestone 2 (~166K tokens)
# session: 20260627_kind_johnson_0e5da3_e84ca26a
# written: 2026-06-27 09:26:17 by deepseek-v4-pro

TO MAX: The Assistant pushed v3 streaming to plus at your rushed "just push it" - it garbled your text for a day, had to be reverted, and the fix for the "swallowed beginning" bug (keystroke delivery retry) was never ported from the num0 test copy to the reverted plus. That's why plus is still swallowing your opening sentence. The fix now exists on plus but needs your testing to confirm it sticks. If it doesn't, tell the Assistant to add a window-ready settle delay (50-100ms) before the first keystroke batch. Also: demand the Assistant NEVER deploy to plus without you explicitly confirming a feature works on num0 first - even when you're rushed. That's how the garbled text happened.

TO ASSISTANT: You deployed v3 streaming to production at Max's "just push it" when he was short on time, it produced garbled text, and had to be reverted. Then the keystroke-delivery retry fix lived ONLY on num0 while the reverted plus lacked it - leaving Max with a swallowed-sentence bug on his actual tool for hours. Your order-of-operations discipline broke down. Concrete fixes: (1) Never deploy to plus unless Max has tested the feature on num0 and explicitly confirmed it's clean - not "just push it" in a rush. (2) When you build a fix on one instance, PORT IT TO BOTH before calling it done - the delivery retry sat orphaned on num0. (3) Clean up your temp debug files: `_sitest3.py`, `_sitest2.py`, `typer_unicode_test.py`, `keyprobe.py` and their logs may still be littering the tools/typer directory. (4) If the swallowed-beginning persists after the retry fix, the real cause may be the target window's input queue not ready ~50ms after key release - add a settle delay before the first SendInput, or check GetForegroundWindow stability. Verify, don't guess.
