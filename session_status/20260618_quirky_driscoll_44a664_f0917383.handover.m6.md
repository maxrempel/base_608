# Scribe handover - milestone 6 (~463K tokens)
# session: 20260618_quirky_driscoll_44a664_f0917383
# cwd: C:\moma\.claude\worktrees\quirky-driscoll-44a664
# written: 2026-06-18 08:06:17 by deepseek-v4-pro

# D23 HANDOVER - Update MOMA System for sc10 Merged Multiline Lipsies

## GOAL (Max's words)
*"Update the system, so the new line combinations / arrangements would match the new lipsies and populate the SB with new approved and done lipsies. Save a backup of the old spine, maybe outside of DB - specifically the sc10 spine - and update the system to place new lipsies, so I can watch the assembly video."*

## WHAT HAPPENED IN D21

D21 completely rebuilt sc10 (a 33-line two-hander dialogue scene between Anna and Ishtab) from **per-line clips** into **11 merged multiline lipsies** covering the whole scene. Every arrangement is approved and rendered. But the system (MOMA's DB spine + arrangement views) doesn't know about them - their line hashes are synthetic (merged audio from multiple lines), so they don't attach to any single script line and the arrangement view shows empty slots.

The **content work is done**. D23 is a **system/admin task**: make the database and UI recognize these new merged lipsies so they appear in the assembly video.

## DECISIONS MADE (D21) + WHY

| Decision | Reasoning |
|---|---|
| Merge per-arrangement into ONE lipsie each (multiline, ~4 lines ideal) | User wanted fewer clips; arrangement = a dramatic beat |
| 15s hard cap per merged clip (wan2.6 limit) | Two long monologues (line 8 ?14s, line 23 ?13s) forced singles |
| **Speak-ORDER prompt fix** ("left speaks first, then right answers") | wan2.6 ignored bare "Left/Right" labels; order-of-mention cracked speaker assignment |
| **Describe-both-first** (character names + positions before quoted lines) | Further fix for swapped speakers in merged dialogue |
| **Neutral hand phrasing** (no direction at all) | Prompting "still" froze hands mid-air; prompting "arms moving" made robotic periodic motion - best lever is to say nothing about hands |
| **No smile/grin words** in prompts | Even "minimal grins" triggered excessive smiling/laughing |
| **Walking beats on corridor-walk still** (`B1_corridor_walk_warm_v01`), not greeting still | Post-greeting they walk and talk through the facility |
| **Window beats on `sc05_window_twoshot`** | Lines 10-23 are the "they think they're alone / looking at the sky" dialogue in front of the Earth-view window |
| **Alcove/doorway beats use frames extracted from approved spine lipsies** | No standalone two-shot still existed for those locations; pulled mid-frames from the original per-line clips |
| **1s silence pads** (via `silence_buffer=1.0`) where they fit within the 15s cap | User's request to avoid trimmed-too-tight starts/ends |
| **Distinct still per lipsie** is the #1 refinement for next content round | Reusing one still per location (e.g. B1 for all walking) was wrong - each beat needs a different starting image from its own part of the corridor |

## FINAL APPROVED SCENE (11 ARRANGEMENTS)

| Arr | Lines | Job ID | Status | Location |
|---|---|---|---|---|
| arr01 | 0-3 | 2774 | approved | Greeting hall (`sc01_meet_twoshot`) |
| arr02 | 4-5 | 2775 | approved | Walking corridor (`B1_corridor_walk_warm_v01`) |
| arr03 | 6-7 | 2810 | approved ("excellent") | Walking corridor |
| arr04 | 8 (Anna mono) | 2812 | approved ("perfect") | Walking corridor |
| arr05 | 9 (Ishtab) | 2811 | approved ("great") | Walking corridor |
| arr06 | 10-16 | 2805 | approved | Window (`sc05_window_twoshot`) |
| arr07 | 17-22 | 2806 | approved | Window |
| arr08 | 23 (Ishtab mono) | 2807 | approved ("good") | Window |
| arr09 | 24-27 | 2808 | approved ("good") | Alcove (frame-extracted) |
| arr10 | 28-29 | 2794 | approved | Doorway (frame-extracted) |
| arr11 | 30-32 | 2795 | approved | Room (frame-extracted) |

**All 33 lines covered. No overlaps. No gaps.**

Fine-print: arr07 (2806) has a note - Anna's delivery should be "more relaxed, analytical, warm" - but it's approved as-is for now.

## THE DB PROBLEM

The MOMA DB has:
- `script_lines` - 33 rows for sc10, each with a `line_hash`
- `arrangements` - containers mapping line ranges: arr01(id2)=0-3, arr02(id3)=4-9, arr03(id4)=10-21, arr04(id5)=22-29, arr05(id7)=30-32 (these are the **old** beat boundaries; D21 changed them!)
- `jobs` - each lipsie job carries `script_line_hash` (a single hash) and `arrangement_id`

**Root cause of "arrangements show nothing":** the 11 merged lipsies have **synthetic line hashes** (they merge audio from 2-7 lines), so their `script_line_hash` matches NO real script line. The arrangement view filters by hash ? zero matches ? empty slots.

Additionally, the old arrangement boundaries (4-9, 10-21, 22-29, 30-32) don't match the new 11-chunk breakup. The DB never got updated when D21 split beats differently.

## CURRENT STATE

- All 11 lipsies are rendered and approved in the `jobs` table
- MOMA GUI is running on port 8779
- The `/lipser` view works (you've been reviewing clips via `?ids=` links)
- The arrangement view / assembly video does NOT show the clips because of the hash mismatch
- Old per-line spine (the pre-merge version) is intact in the DB

## EXACT NEXT STEPS (D23)

1. **Back up the old sc10 spine** - export the current `script_lines` table and the `arrangements` table for sc10 to a CSV/JSON outside the DB (e.g. `C:\moma\sc10\combo_runner\data\spine_backup_sc10_<date>.json`). The backup must include: script_lines (id, line_number, line_text, line_hash, arrangement_id, scene_id) and the arrangements table rows for sc10.

2. **Update arrangement boundaries** in the DB to match the new 11-chunk breakup (not the old 5-arrangement one). This may mean updating existing arrangement rows or creating new ones.

3. **Wire the 11 merged lipsies into the spine** - each merged lipsie needs to be linked to its arrangement so the assembly video can find and play them in order. Since each covers multiple lines, the spine representation needs to handle multiline coverage (a single job covering lines X through Y, not just one line).

4. **Verify the assembly video plays** the 11 clips in scene order, with the correct location transitions.

The key insight: the D1Client API (`moma_db.py`) has `query_sql()` and the schema has `jobs`, `script_lines`, `arrangements` tables. Use the existing code patterns from the `_d21_*.py` scripts as reference for DB operations. The GUI is in `combo_gui.py`.

## KEY PATHS

| Item | Path |
|---|---|
| Worktree (cwd) | `C:\moma\.claude\worktrees\quirky-driscoll-44a664` |
| Main code | `C:\moma\sc10\combo_runner\code\` |
| DB client | `C:\moma\sc10\combo_runner\code\moma_db.py` |
| GUI server | `C:\moma\sc10\combo_runner\code\combo_gui.py` (port 8779) |
| Worker | `C:\moma\sc10\combo_runner\code\combo_wan26au_worker.py` |
| Audio resolver | `C:\moma\sc10\combo_runner\code\audio_resolver.py` |
| Output stills | `C:\Users\maxre\Nextcloud\ai_images\kazarian_episode\scenes\scene10_images\combo_runner\data\output_stills\` |
| Composites | `C:\Users\maxre\Nextcloud\ai_images\kazarian_episode\scenes\scene10_images\composites\` |
| Approved two-shot (greeting) | `.../approved_stills/sc01_meet_twoshot_var01.png` |
| Corridor walk still | `.../output_stills/B1_corridor_walk_warm_v01.png` |
| Window two-shot | `.../output_stills/sc05_window_twoshot.png` |
| Frame-extract dir | `.../output_stills/_d21frames/` (frame_alcove_24.png, frame_alcove_27.png, frame_door_29.png, frame_door_32.png) |
| Worklog | `C:\claude_base\compaction_kb\scripts\worklog.py` |
| Memory (lessons) | `C:\Users\maxre\.claude\projects\C--moma\memory\` |
| MOMA launch | `C:\moma\sc10\start_moma.bat` |
| D1 DB | Cloudflare D1, accessed via `moma_db.py` D1Client |

## CRITICAL GOTCHAS

- **Only Max approves** - the worker writes `'done'` when rendering finishes, never `'approved'`. D21 accidentally reset approvals once (and reverted). Do NOT set `output_status='approved'` in any script; only Max's manual action in the UI sets that.
- **Synthetic line hashes** - merged audio produces a hash that doesn't match any single script line. The spine wiring must handle this, likely by linking via arrangement_id rather than line_hash.
- **The old arrangement boundaries are wrong** - the DB still has the old 5-beat structure; D23 must update to the 11-beat one.
- **Worker must be running** for any new fires (MOMA stack starts via `start_moma.bat`).
- **$4-5 budget** was authorized for finishing touches; adjust as needed for DB work (DB updates cost no rendering $).
- **Anna is LEFT in all stills EXCEPT the window still** (`sc05_window_twoshot`) where Ishtab is left and Anna is right. Any prompt generation for window beats must swap the description order.

## PROMPT RECIPE (for any refires)

```
Describe both characters by name + position first.
Then the quoted dialogue lines, labeled by speaker.
Add: "in profile, eyes on each other, never looking at the camera."
For monologues: "only [speaker]'s lips move; [listener] listens in attentive stillness."
For walking: DO NOT mention hands/arms at all (neutral).
Tone: "calm, formal, serious, composed throughout."
Add the antiglamour boilerplate: "real skin, no makeup, film grain, documentary."
NEVER include the words "smile", "grin", "laugh", or "warm."
```

`silence_buffer=1.0` on the wan26au worker call.

## OPEN QUESTIONS (for Max)

1. For the spine update: should each merged lipsie replace ALL the per-line clips for its covered lines in the assembly, or append as an alternative?
2. Should the old per-line spine be kept as a fallback branch in the DB, or fully replaced?
3. Do you want the arrangement boundaries in the DB updated to match the new 11-chunk breakup, or keep the old 5 and just wire clips differently?
4. The `distinct still per lipsie` refinement (using a different corridor frame, different window framing per beat) is saved for the next content round - confirm you want that as a separate future D-branch, not part of D23.
