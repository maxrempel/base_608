# Adviser note - milestone 2 (~163K tokens)
# session: 20260625_crazy_ptolemy_294abf_c644d998
# written: 2026-06-25 16:30:55 by deepseek-v4-pro

TO ASSISTANT: Good diagnosis and the manual DB fixes were correct. But you're patching symptoms. The worker has no deadlock protection - it will loop forever re-selecting the same empty-prompt job with no backoff, no quarantine, no skip-counter. That's the bug that turned one bad job into a complete pipeline freeze. Fix the root cause: add a retry/deadlimit in the worker's job selection loop so any job that gets "SKIP" N times gets auto-marked done or quarantined. Otherwise this will happen again next time any job is malformed.

TO MAX: No action needed right now - images are rendering after two bugs were cleared. But the Assistant should fix the worker loop itself (not just the fire script) so a single bad job can't freeze your whole queue again. Let them do both fixes.
