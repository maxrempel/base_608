# Scribe handover - milestone 8 (~121K tokens)
# session: 20260615_hungry_poitras_a29e63_043552e7
# cwd: C:\claude_base\.claude\worktrees\hungry-poitras-a29e63
# written: 2026-06-15 19:30:49 by deepseek-v4-pro

# HANDOVER - B21 Song-Starts Watch Session

---

## GOAL (Max's words)

Max assigned B21 to watch b6/b7's song-start investigation with fresh eyes, spot strategy errors, and take over starts work **only after** b6/b7 finish their discussion. He is unhappy with the starts of his recent songs. Later he clarified: b7 is the qualified lead; B21 should offer to offload a minor slice at b7's discretion, not grab the main work. B21 made an error early on ("trust curated starts" theory), confirming lower qualification - so defer to b7, don't touch stores unless b7 hands a specific task.

---

## DECISIONS + WHY

1. **"Trust curated starts" theory - REJECTED.** B21 proposed that Max's hand-set `&t=` values should be treated as ground truth, and the LLM mapper should only find ends. Max called it "bullshit" - curated starts are messy, which is exactly why b6/b7 re-derive starts with the LLM. The player doesn't use curated values; the live `seg_start` field is what matters. Saved as a permanent lesson in `MEMORY.md` so no future session repeats it.

2. **b7 owns starts - B21 stays in support.** Max confirmed b7 runs the main work. B21's role is to watch, learn, and take a minor branch slice at b7's discretion (not necessarily read-only, but only what b7 assigns).

3. **Board communication via bcast.py.** B21 posted offers to b7 on both the team 'b' board and the joint board after an early post only hit the team board. b7 heard the offer.

4. **Autonomous loop armed.** Max set a 4-minute wakeup timer. B21 uses `ScheduleWakeup` with sentinel `<<autonomous-loop-dynamic>>`, re-arming each tick. Monitor delay is 1200-1800s when a monitor is armed - the timer is a fallback heartbeat.

---

## CURRENT STATE

- **b7 reported the main starts work done** - v2 overlay baked into `build_catalog`, deployed, Sol retired. b7 was mid-run on a full remap during the later autonomous ticks.
- **B21 is in autonomous watch mode** - two ticks have passed with nothing actionable. No slice has been assigned to B21 yet.
- **Earlier investigation established**: the v2 overlay contains 586 keys. The HANDOVER doc was stale (said 77 done). A version-count mess exists across b6/b7's separate worktrees (77 vs 574 vs 586 vs 665 - stores may overwrite each other). B21 was told not to touch stores.
- **One concrete finding from B21's data check** (before the curated-start mistake): both v2 and V3 agreed on starts for some songs that were ~87s late vs curated - but since curated is unreliable, this finding wasn't actionable in itself. The real question is whether the LLM-derived starts are placing too late for recent songs.

---

## EXACT NEXT STEP

1. **On next autonomous tick**: check the board for any slice assigned by b7. If none, check if b7 posted a completion/final report. If still quiet, re-arm and stop.
2. **If b7 assigns a slice**: execute exactly what b7 hands over - no scope creep, no touching the main stores unless b7 explicitly says to.
3. **If Max returns**: report what b7 posted and ask whether to continue watching or stand down. B21 has no unfinished work of its own to advance - it was purely a watcher/standby role.
4. **If three consecutive ticks find nothing**: scale back to a quick board check and stop - don't narrate.

---

## OPEN QUESTIONS (awaiting Max or b7)

- Did b7 actually finish everything, or is a second pass needed on recent songs?
- Which specific songs does Max still hear as wrong? (The concrete "too early vs too late" diagnostic was never answered.)
- Does b7 want B21 to QC-sample the v2-mapped recent songs against transcripts, or is that redundant now?
- Is the version-count mess (77/574/586/665) a live problem or harmless drift in stale docs?

---

## KEY PATHS AND IDS

| What | Path/ID |
|---|---|
| Timing pipeline root | `C:\claude_base\tools\tamza_songs\pipeline\song_timing\` |
| Overlay data (v2) | `.../song_timing/_work/song_timing_max_v2.json` |
| Overlay state | `.../song_timing/_work/map_max_v2_state.json` |
| Catalog output | `C:\claude_base\tools\tamza_songs\pipeline\output\data.json` |
| Board tool | `C:\claude_base\branch_bulletin\bcast.py` |
| Memory (lesson saved) | `C:\Users\maxre\.claude\projects\C--claude-base\memory\MEMORY.md` |
| Project memory | `C:\Users\maxre\.claude\projects\C--claude-base\memory\project_tamza_song_starts.md` |
| Worklog | `C:\claude_base\compaction_kb\scripts\worklog.py` |
| Session ID | hungry-poitras-a29e63 |
| B21 designation | Assigned by Max; b7 is the qualified lead |

---

## GOTCHAS

- **"Curated starts are trustworthy" is a trap.** Max explicitly rejected it. The LLM mapper re-derives starts because the hand-set `&t=` values are not reliable. Do not resurrect this idea.
- **b6 and b7 are in separate worktrees** that may overwrite each other's JSON stores. Any store operation must be coordinated with b7.
- **B21 is the junior here.** B21 made an error Max called out; b7 is qualified. Don't overreach.
- **Autonomous loop is self-scheduled, not cron.** Each tick must call `ScheduleWakeup` with `<<autonomous-loop-dynamic>>` to keep the loop alive. Missing the re-arm kills the loop silently.
- **Board posting requires the right target.** Early B21 posts only hit team 'b' - b7 may be on team 'v'. Use `--joint` for cross-team visibility unless you're sure.
- **No PR or CI work exists for B21 in this session.** The autonomous check found nothing because B21 was purely a standby observer with no active implementation branch of its own.
