# Scribe handover - milestone 3 (~252K tokens)
# session: 20260705_nice_maxwell_b61025_66b4058f
# cwd: C:\claude_base\.claude\worktrees\nice-maxwell-b61025
# written: 2026-07-05 22:43:09 by deepseek-v4-pro

# Handover - D06B (nice-maxwell-b61025)

## GOAL (Max's words, as stated)
> "the filters in the image maker is messed up ... look at it with a fresh eye ... the imager is polluted with old, ancient images from other scenes and from no scenes ... filters keep failing."

Later expanded to also cover the Storyboard (port 8790) when he saw a "huge leakage" after a MoMA restart.

## DECISIONS MADE AND WHY
1. **Imager plate-scope fix applied.**  
   *Finding:* `/api/plates` on the imager GUI (port 8779) returned *all* plates from every scene, not just the current scene. The client-side arrangement filter hid them, but a brief blink (2?second heartbeat gap, text?search, load) exposed the full 1,159 off?scene plates.  
   *Fix:* Edited `combo_gui.py` to parse the `?arr=` param in `/api/plates` and filter server?side. Edited `runner_core.js` to pass the `arr` param when fetching plates and canons.  
   *Result:* Server?side scoping verified: before = 1,184 plates, after (scene 11) = 25. Browser cache dropped from 1,457 ? 298, all scene?11. Push to `moma/master` as `b9c5aa3`. Only the 8779 GUI process was restarted (worker/Chrome untouched).

2. **Storyboard leak diagnosis - no code edit yet.**  
   Multiple live probes of the storyboard (8790) showed the actual right?rail pile is **119 images, all Scene?11, zero pre?June, zero foreign?scene**. The label "675 images" at the top is a misleading *global* count, not scene?scoped.  
   The only candidate for "ancient unassigned images" entering Scene?11 was a null?arrangement fallback in `storyboard_editor_v2.html` line 1006, but data proved only one pre?June shot (J282, apr) could leak through and it is legitimately scene?11 by its `scene_id`.  
   No filter code broke recently. The only filter?relevant commit in 2 days (67ad744, shared picker re?assert every 2s) was verified safe.  
   **Max sees "tons" of images that he insists were not there yesterday and are not scene?11.**  
   The plausible explanation (from sibling sessions) is 137 newly generated scene?11 experiments today (arr02, by D02A/D52 etc.) that populated the pile because the curated `bin_order` is empty and the fallback shows all approved scene?11 images. Max may interpret these as "ancient" because many are throwaway.

3. **No storyboard edit, no D1 reassignment touched.**  
   To avoid collisions with the stuck sibling session, I did *not* modify the storyboard or any assignment data.

## CURRENT STATE
- **Imager (8779)** - fixed, pushed, live and verified clean.
- **Storyboard (8790)** - investigation in progress; code unchanged. Current live pile is clean by all measurements. The "leak" as described by Max has not been reproduced in a fresh browser load.
- **Open dialogue** - Max is certain it's a filter problem ("I'm pretty sure it is a filtering problem, not reassignment"), despite the data showing otherwise. The session ended asking Max to provide a specific J?number from the pile so we can trace it definitively.

## EXACT NEXT STEP
1. **Wait for Max to click ONE image he considers ancient/foreign and give its J?number.**  
2. With that J?number, run a single DB query to check its `created_at`, `updated_at`, `arrangement_id`, and `scene_id`.  
   - If it is genuinely old (pre?June 29) or belongs to a non?scene?11 arrangement ? hunt the real filter leak (likely in the storyboard's pile fallback when `bin_order` is empty, or in the shared arrangement picker's heartbeat interacting badly).  
   - If it is new and scene?11 ? explain that these are today's experiments from other sessions, confirm the hard?refresh behaviour, and offer to restore a curated pile or hide them.
3. **No other code changes until the diagnosis is settled.** Only D06B to touch the storyboard if needed (all other sessions are advised via board to stand down).

## OPEN QUESTIONS (awaiting Max)
- **Which specific image(s) look ancient?** (J?number required)
- **Did a hard refresh (Ctrl+Shift+R) of the storyboard tab change what is seen?**
- **Should the "675 images" counter be scoped to the scene to avoid confusion?** (trivial change, proposed but not yet done)

## KEY FILES, PATHS, AND IDS
- Imager server: `C:\moma\sc10\combo_runner\code\combo_gui.py`
- Imager client: `C:\moma\sc10\combo_runner\code\runner_core.js`
- Storyboard (combined client+server endpoints): `C:\moma\sc10\sound_assembly\code\storyboard_editor_v2.html`
- Shared arrangement picker: `C:\moma\sc10\combo_runner\code\arrangement_picker.js`
- MoMA root: `C:\moma\sc10`
- Git repo: `C:\moma` (master, pushed fix at `b9c5aa3`)
- Inter?session board (d?board): `C:\claude_base\branch_bulletin\bulletin_d.jsonl` (used for coordination)
- Imager: `http://localhost:8779/imager`
- Storyboard: `http://localhost:8790/storyboard`
- API used by storyboard: `GET /api/approved_images` on port 8790
- Pile control: curated D1 `bin_order` (currently empty for scene?11 ? fallback shows all approved images filtered by scene).

## GOTCHAS AND DEAD ENDS
- **Do not restart the whole MoMA** (moma_restart.py) - it bounces the worker and kills in?flight jobs by other sessions. Only the GUI server can be safely bounced.
- **The "675 images" label is global** - it counts all images loaded across all scenes. Not an indication of a leak; just misleading.
- **arrangement picker heartbeat** (every 2s) was already audited and the storyboard handler correctly no?ops when `arrIds` hasn't changed. It did not cause a regression.
- **bin_order being empty** means the pile is auto?populated with everything that passes the scene filter. Today's flood of new renders (137 shots) instantly appeared, which may be the entire "leak."
- **The imager fix cannot affect the storyboard** - they run on different ports with different server scripts. My changes were confined to combo_runner files.
- **No ancient (pre?June) images passed the scene?11 arrangement filter in any live check.** If Max can name one, we have a real filter bug; otherwise it's mis?reading the new renders.
