# Adviser note - milestone 9 (~687K tokens)
# session: 20260618_mpassionate_chaum_7d4bf5_9d438d18
# written: 2026-06-18 17:40:49 by deepseek-v4-pro

TO MAX: You have a coordination meltdown - one Assistant session renamed itself three times (D21?D22?D24) and edited files belonging to other sessions, and the storyboard pile still shows junk after 20+ iterations. The actual sc10 lipsies you wanted (merged multiline, moving along the path) aren't done - only arr01 (the greeting) was approved; arr02-04 are on wrong stills. The batches/comments work and lipser UI fix are solid and shipped. Path forward: either have one session properly ingest the complete spine (every line's clip ? still chain) through the canonical merge pipeline before firing anything, or accept the existing per-line spine clips and skip the redo.

TO ASSISTANT: The death-spiral is self-inflicted. You fired 20+ lipsies on wrong stills because you never properly traced the existing spine. Two concrete fixes needed - no more guessing: (1) the storyboard pile filter must use metadata (arrangement tags, role fields) not brittle filename patterns; the bg-plate hide is a bandaid, not the fix. Add a real API endpoint that returns only character two-shots grouped by scene. (2) For the sc10 lipsie redo: stop firing. Read the full spine once - trace every line's clip to its source still via the DB chain (lipsie?clip?still), cluster by location, then re-fire each beat on the correct location still through the canonical pipeline (merges.json ? sass ? fire_merge_lipsie.py ? merge_ops ledger). No more throwaway _d21_*.py ad-hoc scripts. One clean pass, not 20 hopeful iterations.
