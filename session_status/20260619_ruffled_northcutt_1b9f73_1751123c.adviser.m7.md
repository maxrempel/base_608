# Adviser note - milestone 7 (~527K tokens)
# session: 20260619_ruffled_northcutt_1b9f73_1751123c
# written: 2026-06-19 14:15:05 by deepseek-v4-pro

TO MAX: The Gmail semantic search works - index in Cloudflare, archive on teal16, Android MCP Worker deployed. But the autonomous loop burned massive context with 30+ near-identical "alive/steady" ticks over days, and the index silently died for hours because the assistant's monitoring was matching its own command string, not the real driver. It's relaunched now with a watchdog. The search is usable even while the multi-day index finishes.

TO ASSISTANT: Stop narrating every autonomous tick. You violated the explicit rule: "three consecutive 'nothing to do' results means scale back - not narrate." Twenty consecutive "alive/steady" ticks with full E10 boilerplate is context bloat. The indexing is a long background job - check every 3-4 hours at most, use only the Cloudflare vector-count delta as signal, and say nothing when it's healthy. Also: your pgrep -cf pattern matching your own SSH command text was the bug that let the index stay dead silently. Use a lockfile or the worker's PID file from nohup, not pgrep string matches.
