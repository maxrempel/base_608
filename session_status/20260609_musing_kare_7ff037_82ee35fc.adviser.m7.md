# Adviser note - milestone 7 (~113K tokens)
# session: 20260609_musing_kare_7ff037_82ee35fc
# written: 2026-06-09 07:42:47 by claude-opus-4-8

TO MAX:
You are about to greenlight a real build (two watcher types, one safety watcher PER TEAM, Opus 4.8, rolling digests, Telegram) but nothing has been written to disk yet and no spec doc exists. Before the Assistant starts coding, make it write the charter + design to a file and confirm three things you have NOT actually pinned down: (1) "opus 4.8 low" - the Assistant guessed this means "cheap/minimal", confirm that is what you meant; (2) the numbering watcher losing ALL ability to ping you - the Assistant decided this unilaterally; (3) one safety watcher per team could mean many parallel Opus calls every cycle - cost/load is unaddressed. Also: the per-project digest only works if workers narrate to the board; the Assistant flagged this honestly - it is a real dependency, not a detail.

TO ASSISTANT:
Stop before coding. You have a multi-part build (two watchers, per-team instances, new model wiring, rolling-digest memory, Telegram routing) and zero of it is on disk. Do this first:
- Write the safety-watcher charter (the ~2000-char text Max approved) to a file. That is the one finished artifact and it is currently only in chat.
- Write a short design doc: numbering watcher changes, safety watcher architecture, the digest mechanism, the three assumptions you made (opus 4.8 = cheap, numbering watcher de-paged, one-per-team), and the worker-narration dependency. Get Max to confirm the assumptions before you touch watcher.py.
- Build and verify the numbering watcher change (it already exists - smallest, safest) BEFORE spinning up the brand-new per-team safety watcher.
Do not let "you are now manager and programmer, team is woken" turn into parallel half-built agents with no spec. One artifact at a time, archive nothing live until the new path is verified.
